# 中心主張v3 作業リスト（0823）

親: [[中心主張v3草案_0823]] / 状態: **作業用**（各項目が何をする作業なのかを、参照先つきで書き下したもの）/ 作成 2026-08-23

> **前提**: v3 差し替えは **2026-08-23 に先生承認済み**。作業1・2まで実施済み。
> **起票**: 未起票。着手が決まった項目から `add_task` する。

---

## 作業1: `p̂ < 0.05` 定義の裁定 【完了 2026-08-23】

**参照**: [[マージン恒等式と消灯点_0822]] §8(a) ／ [[用語と記号]] §1 ／ [[フレーム前の穴]] §6 冒頭の注記

**何が問題か**

「dead」の作業定義が `p̂ < 0.05` になっている。ところが condA は 1 タスクのサポートが 32 パターンなので `p̂ ∈ {0, 1/32, 2/32, …}`。**`1/32 = 0.03125 < 0.05`** なので、**32 通りのうち 1 つでゲートが開くユニットが「dead」に分類される**。

そのユニットは勾配が恒等ゼロでは**ない**。実測で `t_death` 以降も `median |Δb| = 0.43` 動いている。真に凍るのは `p̂ ≡ 0` 以降で、そこからの `|Δb|` は厳密に 0。

**なぜ柱2 の前提なのか**

柱2 の核が「下端は**不可逆**（勾配恒等ゼロ、16,475,293 ペアで復活 0）」だから。この不可逆性は **`p̂ ≡ 0` でのみ成立する**。`p̂ < 0.05` のまま書くと、柱の一番強い部分が定義のズレで崩れる。

**やること**

定義を `p̂ ≡ 0` に直すか、`p̂ ≡ 0`（凍結）と `p̂ < 0.05`（ほぼ消灯）を**別の語**にするかを決める。

**裁定（8/23）**

- **凍結（`strict_dead`）**: `p̂ = 0`。不可逆性・吸収・勾配恒等ゼロを述べる対象
- **ほぼ消灯（`near_off`）**: `0 < p̂ < 0.05`。condA では `p̂ = 1/32` で、まだ勾配がありうる
- **旧作業指標（`dead_0.05` / `dead_frac_0.05`）**: `p̂ < 0.05`。過去のログ・数値との互換用で、不可逆性は含意しない
- 波及先 6 ファイルへ反映済み。修飾なしの `dead` を新しい機構主張では使わない

**波及先（定義を直す場合に書き換えが要るファイル）**

[[用語と記号]] §1（定義の本体・現在は注記のみ追加済）／[[命題リスト]] Q3 主張文／[[整流モデル]] 部品2 の 3 区分／[[死の二経路]] §5／[[停止条件]] の dead_frac 定義／[[移動度勾配モデル]] §1 表

**無傷**（既に `p̂ = 0` を明示している）: [[xr分解と生存機構_0822]] §14 ／ [[不偏ウォーク仮説]] §1(3) ／ [[自己項の恒等式]] §2

**コスト**: 書き物のみ。走なし・解析なし。

---

## 作業2: 柱3 の spec 化・回し直し 【完了 2026-08-23】

**参照**: [[中心主張v3草案_0823]] 柱3 ／ 正本: [[機能盲目性_0823]] ／ repo: `specs/spec_function_blind_0823.md`, `specs/spec_function_blind_0823_addendum.md`, `results/function_blind_0823/`

**なぜ必須か**

柱3（死は機能を見ない）が v3 の**売り**。ところが根拠 2 本はどちらもチャット内の使い捨てスクリプトで、`analysis/` に無く、spec も無い。運用規律（数値は commit 済みコードの出力からのみ転記）に真正面から抵触するので、**このままでは論文に 1 行も書けない**。

**やること (a): 同時刻ハザード層別**

- 問い: 「r が大きいユニットは死ににくいか」
- 方法: 起点 t₀ ∈ {200k…600k} で alive な個体を r 三分位に分け、その後 300k 以内の死亡率を比較。`p̂` と `x` で統制した 3×3 も
- データ: `results/ratchet_log_0819/logs/seed*.npz`（`cos_u_mu` / `w_norm` / `p_hat`）。**新規走は不要**
- チャットでの結果: 周辺死亡率 0.837 / 0.839 / 0.850（**平坦**）、統制セル内も勾配なし
- 要注意: 同一個体が複数起点に重複して入るので SE が過小。転帰が「一時死」を含む（境界復活 11.5%）

**やること (b): オラクル bias 修復 + 対照 4 本**

- 問い: 「死んだユニットの重みは壊れているのか、スイッチが切れているだけか」
- 方法: `results/posreset_0819/snapshots/A_w100_cont_step500000.pt` を読み、現タスクの 32 パターン厳密損失で、**dead を閾値上へ蹴り上げてから全 bias + c だけ**を最適化。重みは触らない
- 対照: ①学習済 W ②新規 He 初期化 ③行列シャッフル ④rnd ブロックのみランダム化
- チャットでの結果: unfit 現状 0.143 → 修復後 0.009。対照の中央値は ① 0.0001 ／ ③ 0.070 ／ ④ 0.081 ／ ② 0.196
- 要注意: **オラクルであって手法ではない**。蹴り上げ幅 0.5 は恣意（感度未確認）。単一スナップショット
- 関連: [[台帳（実効バイアスの縮退）]]（b と w の勾配が同じ因子を共有する構造）／`cbp_harm_0815`（CBP は重みを取り替えるが eval を改善しない ＝ この結果と整合）

**動的版（やるなら別 spec）**: 蹴り上げた状態から学習を再開し、(i) 何 step で再死するか (ii) その間 `eval_loss` が実際に改善するか。**(ii) がゼロなら (b) の主張は畳む**（dead_frac は機能の代理にならないという既知の罠と同じ）。

**コスト**: 解析のみ（既存ログ・既存スナップショット）。走なし。

**実施結果（8/23）**

- spec を解析前に commit `01c91b3`、初回結果後の定義不一致を受けた追補を `85f7974` で固定。実装・修正・結果は `b6b6410`〜`4493a14`。既存ログと既存スナップショットだけを用い、**再学習なし**
- **H-any**: 5 起点から300k以内に一度でも消灯する率は `dead_0.05` で 1.000 / 0.997 / 0.995、high−low = −0.0045 [−0.0136, 0.0000] で EQUIV。ただし明白な**天井効果**があり、旧チャット値 0.837 / 0.839 / 0.850 は再現しなかった
- **H-end（事後追補）**: 300k後の `strict_dead` 占有率は 0.860 / 0.864 / 0.833、high−low = −0.0265 [−0.0894, +0.0378] で **INCONCLUSIVE**。`p̂×x` 調整後も +0.0089 [−0.0443, +0.0582]。したがって「選抜が r と無相関」「統制後も勾配なし」は確定結果へ昇格しない
- **O（旧見出しの集約を再現）**: seed 平均 NMSE は **0.143367 → 0.009148、93.619% 回復**。学習済 W は新しく定義した3破壊対照（fresh He・行シャッフル・rnd列ランダム化）よりすべて良く、paired seed bootstrap の CI も全て0未満
- ただし旧チャットの対照値 0.0001 / 0.070 / 0.081 / 0.196 は、元の操作定義を回収できず**未再現**。今回の対照は固定済みの新しい定義の結果としてのみ使う
- CSV 15本は同一 commit・同一コマンドの再実行で byte 一致。詳細・禁止事項・provenance は [[機能盲目性_0823]]

**作業2の判定**: spec 化・回し直し自体は完了。柱3のうち「スイッチを開けば利用可能な学習済み W が残る」は支持されたが、「選抜は機能代理 r を見ない」は非天井の解析では未決着。よって柱3全体を PASS とせず、草案の断定を分解して扱う。

---

## 作業3: `β ≤ −r` の命題化 ＋ step-0 MC の昇格 【走なしで格が上がる】

**参照**: [[マージン恒等式と消灯点_0822]] §1.3・§2・§9-1・§9-2 ／ [[用語と記号]] §5 の `PB-5` ／ [[自己項の恒等式]] §2

**(a) `β ≤ −r` の命題化**

消灯条件が正規化形で `β ≤ −r`（`r := ‖w_free‖₁/‖w_free‖₂ ∈ [1, √5]`）と書ける、という**解析的に閉じた定理**。走も事前登録も要らず、書けばそのまま命題になる。

価値: [[用語と記号]] §5 の `PB-5`「ガウス入力では `Φ(β) > 0` なので絶対吸収は起きない」に対する、**有界入力版の厳密な対応物**。「condA では文字通りの吸収が存在する」という定性的な言い方を、初めて式にする。

おまけ: Q8（Feller 分類、task_080）に予言が立つ。**µ → 0 の極限で吸収境界でなくなるはず**。ならなければ理論と `ratchet_centered_0822` の間に矛盾が立つ。

**(b) step-0 MC の事前登録昇格**

「ゲート曲線の崖の位置 −0.15 は、初期化則（`w_j ~ U(±√(6/20))`, `b=0`）と入力統計（flip ~ Bern(1/2)¹⁵）だけから予言できる」。**フィットゼロ・パラメータ 0 個**の MC で θ_med = −0.15 が的中（θ_all は予測 −0.50 / 観測 −0.55 で 1 ビン差）。

現状は**事後計算**なので、事前登録して回し直せば正式な結果になる。柱1 の「壁の位置まで説明できる」を支える。

**コスト**: (a) 手計算＋書き物。(b) 事前登録＋解析。走なし。

---

## 作業4: 8/22 系の柱の spec 化・回し直し

**参照**: [[xr分解と生存機構_0822]]（冒頭に「すべて事後計算・未事前登録・spec 未起票・コード未コミットなので現時点では引用できない」と明記されている）

同ファイルの主要結果は全部この格。v3 の柱1・柱2 が寄りかかっているので、**引用したいものだけ** spec 化する。候補:

| 内容                                                   | 使う柱                      | 参照  |
| ---------------------------------------------------- | ------------------------ | --- |
| 片側吸収の厳密性（16,475,293 ペアで復活 0 / `max\|Δcos\| = 0`）     | 柱2 の核                    | §14 |
| ほぼ対称な撹拌（符号 49.7 : 50.1、94.9% が +µ 側を訪問、終端 0）         | 柱2                       | §19 |
| 天井の残差依存（`\|self\|/\|rest\|` が 1 を跨ぐ点が平衡。残差が無いと天井が無い） | 柱2                       | §20 |
| 生存者の整列不在（符号付きペア cos +0.034）                          | **旧主張を殺した根拠**なので v3 でも要る | §18 |

**コスト**: 解析（既存ログ）＋ 事前登録。走なし。

---

## 作業5: µ タイトレーション 【柱1 の本丸・新規走】

**参照**: [[マージン恒等式と消灯点_0822]] §1.5・§3・§9-6

**問い**

`cos_crit = −(b+M)/(‖w‖·‖µ‖)` の分母に ‖µ‖ がいる。では **壁の強さは ‖µ‖ でどうスケールするのか**。

**現状の何が足りないか**

いま持っているのは `std`（‖µ‖ = 3.041）と `centered`（0.117）の **2 点だけ**。しかも比が 26 倍あるので、centered では崖が cos の定義域外へ飛んでしまい（88.1% のユニットで `|cos_crit| > 1`）、**「動いた」ではなく「盤から落ちた」**としか言えない。中間点が無い。

**やること**

`center_alpha`（部分センタリングの強さ）を振って ‖µ‖ を連続的に変え、θ(‖µ‖) のスケーリングを直接測る。

**それで何が言えるようになるか**

1. 「µ が壁を作る」が **2 点比較 → 用量反応曲線**になる。柱1 が一段強くなる
2. §1.5 の **2 経路の分離**ができる。現在「(i) 壁の位置を手前に引く」と「(ii) b の逃走自由度 `1/(1+‖µ‖²)` を奪う」の寄与を分けられていない
3. 「µ は制御ノブである」と言えるようになる ＝ v3 の一行（「µ が作った壁」）が主張から**測定**になる

**コスト**: **新規の学習走**。

---

## 作業6: 直接機能量 `ΔL` による選抜盲目性の確認 【作業2 H の直接確認・着手 2026-08-23】

**参照**: [[機能盲目性_0823]] H ／ [[中心主張v3草案_0823]] 柱3

**なぜ必要か**

作業2では、機能代理 `r` と将来の消灯を比較した。H-any はほぼ全員が転帰に到達する天井効果、非天井の H-end は seed ブロック CI が等価幅に収まらず **INCONCLUSIVE** だった。さらに `r` は入力応答重みの大きさであり、そのユニットが現在の予測に実際に寄与している量そのものではない。既存ログにはユニット別の直接機能量を復元できる W・v・全出力が保存されていないため、再解析だけではこの穴を閉じられない。

**主たる問い**

同じ時刻・同じ生存状態・同じ壁までの距離にいるユニットでも、現在の予測に強く寄与するユニットほど、その後 `strict_dead (p̂=0)` になりにくいか。

**直接機能量**

condA の現タスク32パターンを全列挙し、各ユニットを一つだけ消音したときの厳密な損失増分

`ΔL_i := L(unit i を消音) − L(元のネット)`

を主指標とする。`ΔL_i>0` は現在のネットワーク出力に対する正の限界寄与、`≈0` は冗長、`<0` は現時点では有害であることを表す。これは教師への普遍的価値や将来タスクでの価値ではなく、**その時点・その現タスクでの操作的な機能**である。

**やること**

1. condA・w100 の確認走を新規に計装し、ランドマークごとに `ΔL_i`, `p̂`, `x`, `r`, bias, v と将来の `strict_dead` 転帰を保存する
2. パイロットで天井効果を避ける追跡期間と必要 seed 数を決め、確認データを見る前に spec・等価幅・判定規則を commit する
3. 同一 seed・時刻内で `p̂` と壁までの距離を揃え、`ΔL` 上位群と下位群の将来死亡率差を比較する。依存は seed ブロックで扱う
4. 主判定は「有意差がない」ではなく、死亡率差の95% CI全体が事前固定した等価幅に入るかで決める
5. 同一 commit・同一コマンドで集計出力が再現することを確認する

**判定**

- 有用群が明確に死ににくい → 機能盲目性を否定
- 死亡率差の CI 全体が等価幅内 → 上記の操作的定義・スコープに限り機能盲目性を支持
- CI が等価幅をまたぐ → seed不足または効果不確定として INCONCLUSIVE

**禁止事項**

- `ΔL_i` を unit の普遍的な価値と呼ばない。冗長性・相互作用・将来タスク価値は別
- H-any の天井効果を確認結果として再利用しない
- パイロットと確認データを混ぜて主判定しない
- condB・他幅・他教師へ外挿しない
- 観察的等価性だけから「選抜は機能を一切見ない」という普遍命題へ上げない

**コスト・依存**: **新規学習走あり**。作業2の直接後継。`ΔL` 計装は作業5の走にも流用できるが、µ を動かす作業5とは主判定を分離する。まず本項の登録 commit、その後に解析 repo の spec commit、実装・パイロット・確認走の順で進める。

---

## 順序と依存

```
承認前にやってよい
  └ 作業1（p̂ 定義の裁定）……… 柱2 の前提。走なし・書き物のみ

承認後
  ├ 作業2（柱3 の spec 化）…… 売りなので最優先。走なし
  │   └ 作業6（直接 ΔL 確認）… 作業2 H の未決着を閉じる新規走
  ├ 作業3（β≤−r と MC）……… 走なし。並行可
  ├ 作業4（8/22 系の昇格）…… 走なし。並行可
  └ 作業5（µ タイトレーション）… 柱1 の新規走。1〜4 の後
```

**作業1 → 作業4 の依存**: 片側吸収の主張文は `p̂ ≡ 0` で書かないと成立しないので、定義の裁定が先。

**作業2 → 作業6 の依存**: 作業2で H の天井効果と `r` 代理の限界が確定したため、作業6は転帰と機能指標を置き換える。O のオラクル容量診断は再実行しない。

## 既存タスクとの関係

- **task_105**（中心主張v3 の草案作成・先生提案）… 本リストの親
- **task_097**（フレーム選定）… v3 差し替えに吸収される
- **task_095**（次回 MTG 調整）… 提案の場
- **task_104**（Q3 の「閉じる作業」列の書き換え）… 作業1・作業3(b) と重なる
- **task_080**（Q8 / Feller 分類・期限超過）… 作業3(a) が予言を供給する
- **task_089**（Wang・Ziyin の論文読み・期限超過）… Wang「ランク指標は非予測的」は v3 のランクの扱いに直接触る
