# 機能盲目性（0823・作業2再現解析＋作業6独立確認）

親: [[中心主張v3草案_0823]] 柱3 / 作業: [[中心主張v3作業リスト_0823]] 作業2・6 / 詳細: [[直接機能量による選抜確認_0823]] / 状態: **作業2再現・作業6独立確認・保存完了 2026-08-23**

> **結論**: オラクル修復 O は「スイッチを開けば利用可能な学習済み W が残る」を支持した。機能代理 r の H は未決着だったが、作業6の直接 `ΔL` 独立確認は **PROTECTIVE** で、セル内高 `ΔL` 群ほど凍結しにくかった。したがって柱3を「機能盲目性」とは書かない。

## 1. 位置づけ

8/23 チャットの使い捨て事後計算を、既存ログ・既存スナップショットだけで再現可能な解析へ固定した。既知の概数を見た後の再現解析であり、盲検事前登録・独立確認ではない。新規の学習走は行っていない。

- H: `results/ratchet_log_0819/logs/seed*.npz`
- O: `results/posreset_0819/snapshots/A_w100_cont_step500000.pt`
- 対象: condA・w100・10 seed

死の用語は8/23裁定に従い、`strict_dead := p̂=0`、`near_off := 0<p̂<0.05`、旧互換指標 `dead_0.05 := p̂<0.05` を分離した。

## 2. H: r 三分位と消灯

### H-any（初回 spec）

起点 `t₀ ∈ {200k,300k,400k,500k,600k}` で `p̂≥0.05` の個体を、各 t₀ 内の r 三分位へ分け、保存記録点上で300k以内に一度でも消灯したかを測った。CI は同一個体の反復曝露をばらさず、seed ブロック bootstrap で計算した。

| 転帰 | low / mid / high（seed等重み） | RD high−low [95% CI] | 判定 |
|---|---:|---:|---|
| `dead_0.05` any-hit | 1.000 / 0.997 / 0.995 | −0.0045 [−0.0136, 0.0000] | EQUIV |
| `strict_dead` any-hit | 0.997 / 0.997 / 0.995 | −0.0015 [−0.0136, +0.0091] | EQUIV |

95% CI 全体が事前に固定した等価幅 ±0.05 に入る。しかし率がほぼ1で、**天井効果の下での等価**である。旧チャット値 0.837 / 0.839 / 0.850 は再現しなかった。

### H-end（初回結果後の追補）

天井効果を切り分けるため、bulk の401起点それぞれについて「300k後の一点で消灯状態か」を別 spec で追加した。これは初回結果を見た後の事後追補であり、H-any の主判定を置換しない。

| 転帰 | low / mid / high（seed等重み） | RD high−low [95% CI] | `p̂×x` 調整 RD [95% CI] | 判定 |
|---|---:|---:|---:|---|
| `strict_dead` endpoint | 0.860 / 0.864 / 0.833 | −0.0265 [−0.0894, +0.0378] | +0.0089 [−0.0443, +0.0582] | INCONCLUSIVE |
| `dead_0.05` endpoint | 0.905 / 0.919 / 0.898 | −0.0068 [−0.0564, +0.0386] | +0.0132 [−0.0388, +0.0580] | INCONCLUSIVE |

点推定は小さいが、CI 全体が ±0.05 に収まらない。よって「r 三分位で平坦」「`p̂×x` 統制後も勾配なし」は確定結果として使わない。r 自体も入力応答重みの大きさの代理で、教師への因果的寄与そのものではない。

## 3. O: オラクル bias 修復

32入力を全列挙し、スナップショットの W・v を固定した。`dead_0.05` の bias を最良入力で preactivation 0.5 となる位置へ蹴り上げた後、全 hidden bias と出力 offset c だけを最適化した。

- 旧見出しと同じ seed 平均 NMSE: **0.143367 → 0.009148**
- 回復率: **93.619%**、seed bootstrap 95% CI [89.706%, 97.672%]
- spec の点推定90%基準は PASS。ただし CI 下端は90%をわずかに下回る

固定済みの対照定義による結果:

| arm | mean NMSE | median unfit |
|---|---:|---:|
| current | 0.143367 | 0.109259 |
| repair `dead_0.05`, k=0.5 | 0.009148 | 0.000000 |
| learned W | 0.112614 | 0.068269 |
| fresh He | 0.235382 | 0.171076 |
| row shuffle | 0.382892 | 0.340128 |
| rnd 5列 randomize | 0.779437 | 0.695399 |

学習済み W と各破壊対照の paired seed bootstrap 差（learned−control）は、fresh He −0.1112 [−0.1748, −0.0308]、row shuffle −0.2168 [−0.4765, −0.0792]、rnd randomize −0.5731 [−0.9135, −0.4437]。すべて CI 上端が0未満で、学習済み W に利用可能な構造が残るという容量診断を支持する。

ただし元の使い捨てコードが残っておらず、旧チャットの対照値 0.0001 / 0.070 / 0.081 / 0.196 は操作定義を回収できないため未再現。上表は今回新しく固定した操作の結果である。

## 4. 作業6: 直接 `ΔL` の独立確認

作業2の代理指標と天井効果を避けるため、新規20系列（`generator_offset=20260830`）で現在タスク32入力の単独消音損失を直接計測した。t0の `p_count` を完全層別し、`pre_max` 五分位セル内で `utility_nmse` を三分位化した。

- 主調整RD（high−low）: **−0.2353 [−0.2812, −0.2325]**
- low/high pooled凍結率: 0.4018 / 0.1668
- 判定: **PROTECTIVE**（CI上端 < −0.05）
- C-S1〜C-S8: 全PASS。CLI再実行byte一致、別実装の主解析再計算も完全一致

この結果は「壁の式に機能量がない」と「凍結結果が機能と無関係」を分離する。前者から後者は導けず、後者は作業6の操作的定義で否定された。詳細・制限・provenanceは [[直接機能量による選抜確認_0823]]。

## 5. 言ってよいこと・言わないこと

**言ってよい**

- W・v を固定したオラクル容量診断では、スイッチと bias を開くと損失の大半を回復できる
- 学習済み W は3種類の破壊対照より有意に良い
- H-any は固定済み定義で EQUIV だが天井効果がある
- 非天井の H-end では r 勾配の有無・等価性は未決着
- 直接 `utility_nmse` の独立確認では、セル内高 `ΔL` 群の同一タスク末尾凍結率が明確に低い

**言わない**

- dead unit 単体が教師に有用
- 選抜は機能を一切見ない、と H が確定した
- 作業6の観察的関連を、機能が凍結を防ぐ因果効果と呼ぶ
- オラクル修復が学習手法として有効、または再開後も効果が持続する
- condB・他幅・他時刻へ一般化する
- 新しい対照値が旧使い捨て解析の再現である

## 6. Provenance

解析 repo: `lop_analysis`（作業ディレクトリ `proj_004_drift`）

- `01c91b3`: 初回 spec 固定
- `b6b6410`: 初回実装
- `85f7974`: 天井効果・集約法不一致を受けた追補 spec 固定
- `b8c1c4c`, `a72fa80`, `2179d12`: 追補実装・修正・meta整備
- `4493a14`: 正本結果 `results/function_blind_0823/` 保存
- 同一 commit・同一コマンドで2回実行し、CSV 15本が byte 一致（`determinism_check.md`）
- 作業6: Obsidian先行登録 `66c4f3f`、confirmation spec `cc50ed0`、実装 `9663f52`、実行・解析時HEAD `647316c`、正本結果 `ac29d87`
- 作業6正本: `results/function_blind_direct_0823_confirm/`。7 CSVはCLI再実行でbyte一致

作業2正本は repo の `results/function_blind_0823/`、作業6正本は `results/function_blind_direct_0823_confirm/`。数値を再転記するときはこの note ではなく各正本を照合する。


---

## 7. 動的修復走（`dynrepair_0826`）— O は畳まない【追記 2026-08-27】

転記チャット `claude-復習-0824-0827`。spec [[動的修復spec_0826]]（事前登録 `9f86ee8`、実装 `4c95949` → 実行前 provenance 訂正 `1183d3a`、S8 訂正 `8fb87b0`）／正本結果 `results/dynrepair_0826/`（`1e9dabd`・push 済み）。数値は `verdict.csv` / `summary.md` からのみ転記。判定の全体像は [[論点マップ]] §17。

**§5「言わない」に自分で書いた穴 —「オラクル修復が学習手法として有効、または再開後も効果が持続する」— のうち後半を測った走。** 起点は §3 と同一スナップショット（`A_w100_cont_step500000.pt`）で、そこから **200,000 step（20 タスク）継続**。等価幅は静的 O の効果量 0.1343 の 10% で **δ = 0.0134**。**新規の学習走あり。**

### 7.1 O-1（主判定）— PERSISTENT

| 窓 | `D_A1 = U_A1 − U_A0` | 判定 | 精度 |
|---|---:|---|---|
| 短（介入直後 → 当該タスク終端） | **−0.1375 [−0.2599, −0.0479]** | **PERSISTENT** | INSUFFICIENT（G2） |
| 長（+200,000 step） | **−0.1152 [−0.2089, −0.0308]** | **PERSISTENT** | OK |

**O は畳まない。** §5 の「言わない」から「再開後も効果が持続する」を外せる。ただし **A1 はオラクル**（現タスクの 32 パターン厳密損失を使う）なので、「手法として有効」は依然として言わない。短窓は精度不足なので、数値を引くときは長窓を主にする。

### 7.2 C-1（裁定）— 決まらなかった

`D_A1 − D_A2`（bias を開くか、W を取り替えるか）: 短窓 **+0.0124 [+0.0003, +0.0335]**、長窓 **+0.0155 [−0.0646, +0.0688]**。どちらも **INCONCLUSIVE**。

「スイッチを開けるだけで CBP 型 reset と同等」も「便益は W の取り替えから来る」も、本走からは言えない。A2 は one-shot であり CBP の継続適用ではない。S7 で `cbp_harm_0815` のアンカーとは同一土俵でないことを明記した（A2 直後の mean U = 0.2341）。

### 7.3 F-1（`ΔL` 因果の半歩）— 記述扱い・点推定は作業6 と逆符号

A1 で実用復活（`p̂ ≥ 0.05`）したユニット内で、介入直後 `ΔL` 三分位の high−low 調整 RD は **+0.2762 [+0.1370, +0.4000]**。**G3（実用復活率 0.390 < 0.5）が立ったため、事前規則により DESCRIPTIVE_ONLY**（科学判定 INCONCLUSIVE）。

**この符号反転を、作業6 の PROTECTIVE（−0.2353）への反証として引かない。** 母集団は介入で作り直した 357 本、`ΔL` はランダム化されておらず、介入したのは `p̂`（スイッチ）であって `ΔL` ではない。

### 7.4 蹴り上げの効き（S5・恒真チェック）

蹴り上げ対象 `dead_0.05` は 10 seed 合計 **915 本**。オラクル最適化後に `p̂ > 0` になったのは **0.906**、実用復活（`p̂ ≥ 0.05`）は **0.390**。S1 は §3 の full precision 値 `0.009148272885137731` と `abs_err = 1.7e-18` で一致（PASS）。

### 7.5 ガードと留保

- **G1 CLEAR。G2・G3・G4 の 3 本が発火した**
- **G2**（CI 半幅 > 0.10）: 短窓 O-1 と F-1
- **G3**（A1 の実用復活率 < 0.5）: F-1 と Ch-a を記述扱いへ
- **G4**（A3 起因の致死率が 1 seed でも 0.5 超）: **10 seed 中 6 seed で 0.5 以上**（最大は seed 5 の 0.75）。**A3 は「同大の無方向摂動では改善しない」の対照として使えない** — 破壊対照としてのみ読む。A3 の per-unit 変位は A1 の **2.45〜8.93 倍**
- 記述専用アーム **A1_hi（k=1.0）は非有限値 30,900 件で発散**。判定アーム（A0/A1/A2/A3）に非有限値なし
- **S2 は「元軌道との state hash 一致」ではない。** 元連続軌道の step 700,000 state が保存されていないため、保存済み posreset S2・A0 probe 有無の replay hash・既存 none-resume ログ照合の**合成確認**に置換してある（3 本とも PASS）
- Ch-1（合唱団予言）は [[停止条件]] 側に記録

### 7.6 §5 の更新

- 「言わない」から**外す**: 「オラクル修復が…**再開後も効果が持続する**」（O-1 = PERSISTENT）
- 「言わない」に**残す**: 「オラクル修復が**学習手法として有効**」（A1 はオラクルのまま）
- **新しい穴**（spec §7.2 の事前分岐）: O が動的にも効くなら、**なぜ通常の学習はそれを見つけないのか**。索引の運用ルールに従い、穴の起票までを本走の成果とし、追跡実験は自動起案しない
