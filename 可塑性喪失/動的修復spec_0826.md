# 動的修復走 spec（0826）— O 畳み判定 / CBP 裁定 / ΔL 因果の半歩 / 合唱団予言

親: [[中心主張v3作業リスト_0823]] 作業2 の「動的版（やるなら別 spec）」 / 関連: [[機能盲目性_0823]] §3 O ／ [[直接機能量による選抜確認_0823]] ／ [[停止条件]] ／ [[論点マップ]] §8・§5 ／ [[命題リスト]] Q10・Q11
状態: **事前登録（実行可・Issa 確認済み 2026-08-26）** / 作成 2026-08-26 / 改訂2 2026-08-26（A3 修正＋実装前監査。§13 参照）

> **格**: 判定基準を凍結するための事前登録文書。作成時点で継続走のデータは**存在しない**（静的 O の既知値のみ参照）。実行後に本 note を書き換えず、判定は結果節または [[論点マップ]] に記録する。
> **分類**: **防衛タスク**。[[機能盲目性_0823]] §5「言わない」に明記した穴 —「オラクル修復が学習手法として有効、または**再開後も効果が持続する**」— を、査読で突かれる前に自分で閉じる。
> **コスト**: **新規の学習走あり**（既存スナップショットからの継続。フル走ではない）。8/21b 教訓⑨に従い「安いから」ではなく「反論欄の穴を閉じるから」で採否する。

---

## 1. 何を一度に測るのか（そして、何が独立していないのか）

4 つの目的を 1 本の走に束ねる。ただし**独立な 4 つではない**。先に依存関係を明示しておく。

| # | 目的 | 判定ID | 独立性 |
|---|---|---|---|
| 1 | **O 畳み判定** — 蹴り上げ後に `eval_loss` が実際に改善するか。ゼロなら [[機能盲目性_0823]] §3 の O を畳む | **O-1** | — |
| 2 | **condA CBP との裁定** — 便益は「スイッチを開ける」ことから来るのか、「W を取り替える」ことから来るのか | **C-1** | O-1 と別の量（アーム間差） |
| 3 | **ΔL 因果の半歩** — 作業6 の観察的 PROTECTIVE が、介入で作り直した母集団でも再現するか | **F-1** | O-1 と別の母集団（復活ユニット内） |
| 4 | **合唱団予言** — 復活ユニットは再凍結し、`dead_frac` は介入前水準へ戻る | **Ch-1** | **O-1 とほぼ同じ石**（下記） |

**O-1 と Ch-1 は独立でない。** 「歌い手が足りていないのではなく、合唱団は既に歌い切っている」が正しいなら、復活ユニットには稼ぐ残差がなく、`eval_loss` は改善せず（O-1 = FOLD）、稼がないので保護もされず再凍結する（Ch-1 = PASS）。逆も同様。したがって**一石四鳥ではなく、実質は「二石＋二確認」**である。§7.5 に 2×2 の解釈表を先に凍結し、独立な 4 票のように扱わない。

---

## 2. 起点と前提（既記の引用）

- 静的 O（[[機能盲目性_0823]] §3、`results/function_blind_0823/`）: seed 平均 NMSE **0.143367 → 0.009148**、回復率 **93.619% [89.706%, 97.672%]**。W・v を固定し、`dead_0.05` の bias を最良入力で preactivation 0.5 になる位置へ蹴り上げてから、全 hidden bias と出力 offset `c` のみを最適化した**オラクル**である
- 作業6（[[直接機能量による選抜確認_0823]]）: `ΔL` high−low の調整 RD **−0.2353 [−0.2812, −0.2325]**、凍結率 low/high = **0.4018 / 0.1668**、判定 **PROTECTIVE**。ただし**観察的**
- CBP（[[論点マップ]] §8、`cbp_harm_0815`）: reset は残差の有無に関わらない固定コスト、便益は実容量損失に比例、**`dead_frac` は治療の要否を決めない**（+0.349 [+0.216, +0.503]）。アンカーとして「条件 A では CBP 有効のまま（dead 0.968→0.034、eval 0.937→0.005）」が記録されている。**この 2 数値は本 spec の判定には使わない**（走の設定・幅・seed 数を本 note では確認していない。§9 S7 で同一土俵に載せ直す）
- 一過性の前例（[[論点マップ]] §2、`rank_int_0814`）: **状態への介入は場に再輸送され、~100k step で再崩壊**した。本走はその構図が bias 経路でも起きるかを見る
- `dead_frac_0.05` の実測水準は 0.87–0.99（[[停止条件]]）
- **起点スナップショットの実測構成（0826 に確認）**: 各 seed で `dead_0.05` が **76–98 個**、alive は **2–24 個**。この非対称が A3 の設計を規定する（§3.2.1・§13）

**用語**（作業1 の裁定に従う）:

- `strict_dead := p̂ = 0`（凍結）／`near_off := 0 < p̂ < 0.05`／`dead_0.05 := p̂ < 0.05`（旧互換）
- **復活 := p̂ > 0**、**実用復活 := p̂ ≥ 0.05**。蹴り上げ対象は `dead_0.05` なので `near_off` を含む。**「復活した」を `p̂ ≥ 1/32` で数えない**（1 パターンだけ開いた個体を復活と呼ぶことになる）
- **再凍結 := 介入後に一度 `p̂ > 0` になった個体が、再び `p̂ = 0` に入ること**

---

## 3. データ源とアーム

### 3.1 起点

- スナップショット: `results/posreset_0819/snapshots/A_w100_cont_step500000.pt`（作業2 O と**同一**）
- seed: 作業2 O と同一集合（10 seed。実行前に実在 seed 数を確認し、`manifest.csv` に記録する）
- 条件: condA・w100・T=10,000・batch=1・std。**config の `center_alpha` は起点走と同じ 0.01** だが、`enc=std` なので `centered=False`、学習器入力への中心化は無効（`cmask=0`）。`running_mean` は更新・resume されるが forward には引かれない。**`center_alpha=0` に変更すると起点 config と不一致になるため禁止**

### 3.2 アーム（介入は step 500,000 の一度きり）

| arm | 介入 | 目的 |
|---|---|---|
| **A0** | なし（そのまま継続） | 基準線 |
| **A1** | bias 蹴り上げ（`dead_0.05`、k=0.5）＋ 全 hidden bias と `c` のオラクル最適化 | O の状態から再開 |
| **A2** | 同一ユニット集合を CBP 型 reset（入力重み `w_i` と `b_i` を再初期化、`v_i ← 0`） | C-1 の裁定相手 |
| **A3** | **全 alive ユニット**の bias にランダム符号の摂動を加える。摂動ベクトルの L2 ノルムを **seed ごとに A1 と一致**させる | 「同じだけパラメータを動かせば学習が再開する」を排除 |
| **A1-lo / A1-hi** | A1 の k = 0.25 / 1.0 | 蹴り上げ幅の感度（**記述のみ・判定に使わない**）。[[機能盲目性_0823]] が「k=0.5 は恣意・感度未確認」と留保した点 |

- **A1 は A0 に対する介入であって手法ではない**（オラクルなので現タスクの厳密損失を使う）。この留保は判定文にも書く
- **A2 は one-shot であり CBP の継続適用ではない**（[[論点マップ]] §8 の既記「one-shot ≠ CBP 継続適用（継続型は未検証）」）。C-1 の結論はこの範囲に限る
- **A2 の乱数規約**: snapshot に保存された `gens["method"]` の状態を各アーム共通の起点から clone し、既存 `kaiming_mlp_params` を **1 回だけ**呼ぶ。得た `W_new` の対象行だけを採用し、`b_i ← 0`, `v_i ← 0` とする（既存 CBP reset と同じ）。A2 専用の未登録 seed を新設しない。使用前後の method-generator state hash を `manifest.csv` に残す

#### 3.2.1 A3 の定義（0826 改訂・実行可能形）

初版は「A1 と同数の alive ユニット」を要求していたが、**実スナップショットでは alive が 2–24 個しかなく、`dead_0.05` の 76–98 個と重複なしで同数を取れない**（§13）。以下を登録形とする。

- 対象: **その seed の全 alive ユニット**（`p̂ ≥ 0.05`）
- 摂動: `Δb_i = ε_i · c_s`、`ε_i ∈ {+1, −1}` を等確率・独立に引く（`placebo_seed = 20260826`、**事前固定**）
- 符号の割当順: seed 昇順、その seed 内では unit index 昇順。`placebo_seed` から作る **CPU NumPy `Generator(PCG64)`** の単一ストリームをこの順に消費する。学習用 torch generator は消費しない
- 大きさ: `c_s` は **`‖Δb^A3‖₂ = κ_s`** となるよう seed ごとに決める。`c_s = κ_s / √(n_alive,s)`
- **`κ_s` の定義（曖昧さを潰す）**: `κ_s := ‖b^A1 − b^A0‖₂`、すなわち**介入直後の全 hidden bias の総変位**（蹴り上げ成分 ＋ オラクル最適化成分、100 成分すべて）。**蹴り上げ成分のみではない**。A1 のオラクル最適化は alive の bias も動かすので、総変位で揃えないと「同じだけ動かした」にならない
- `c` と `v`、`W` は触らない

**この設計が何を統制し、何を統制しないか（判定前に固定）**

- 統制する: 「**bias 空間で同じ大きさだけ動かせば、方向に関係なく学習が再開する**」という説明
- 統制しない: 「同じ**ユニット数**を触る」「同じ **per-unit の大きさ**で触る」。alive が痩せているため、**A3 の per-unit 変位は A1 の per-unit 変位より 2–7 倍大きくなる**（`√(n_dead/n_alive)` 倍）
- したがって **A3 は無害なプラセボではなく、破壊寄りの対照になりうる**。alive の margin 中央値は +1.0758（[[論点マップ]] 8/22 節）なので、`c_s` がこれを超える seed では**プラセボ自体が生存者を殺す**
- **読み方の制約（先に凍結）**: A3 で `U` が悪化しても、それを「摂動は無効」の証拠として引かない。S5-A3 の実測（§9）で **A3 起因の致死が 0 に近い seed に限って**、A3 は「同大の無方向摂動では改善しない」の対照として使える。致死が出た seed では **A3 は破壊対照**であり、その読みだけを書く
- **`n_alive,s` と `c_s` は seed ごとに 3.5 倍程度ばらつく**（alive 2–24）。しかも alive が多い seed ほど健全なので、**処置強度が seed の健全さと逆相関する**。A3 は seed プールでの平均を主にせず、**seed 別に併記**する

### 3.3 継続の長さと記録

- 継続 **ΔT = 200,000 step（= 20 タスク）**
- 記録: 介入直後 1 タスク（10,000 step）は **100 step ごと**、以降は **1,000 step ごと**
- 各記録点で保存: `p̂`, `p_count`, `pre_max`, `x`, `r`, `w_norm`, `b`, `v`, 未フィット率、`strict_dead`
- `ΔL_i`（32 パターン全列挙・単独消音の厳密損失増分）は **介入直後（t=500,000⁺）と各タスク先頭**でのみ計算
- unit 単位の時系列は集計 CSV に押し込まず、`logs/unit_traj_<arm>.npz` に固定 shape で保存する（§10）。Git には入れず、SHA-256 と shape を `raw_sha256.csv` に残す

### 3.4 二つの窓（事前登録）

作業2 H の失敗（追跡窓 300k で全群が天井に張り付いた）を繰り返さないため、窓を先に分ける。

- **短窓**: 介入直後 → 当該タスク終端（≤ 10,000 step）。**O-1 と F-1 の土俵**（per-task ハザード）
- **長窓**: 介入直後 → +200,000 step。**Ch-1 の土俵**（復帰の有無）
- **C-1 は両窓で出す**（短窓＝即効性、長窓＝持続性）

---

## 4. 指標

- **未フィット率** `U(t) := E[(ŷ-y)²] / Var(y)`（無次元 NMSE）。condA の現在タスクの **32 パターン全列挙**で `E` と `Var` を厳密計算し、固定 MC eval batch は使わない。8/20 教訓⑤に従い、生の `eval_loss` をアーム間で比較しない
- **アーム差** `D_X(t) := U_X(t) − U_A0(t)`（負なら介入が効いている）。同一 step・同一 seed で対応させる
- **再凍結時間** `τ_i`: 復活ユニット `i` が再び `p̂ = 0` に入るまでの step 数。長窓終端で未再凍結なら右打ち切り
- **`dead_frac_0.05`** と **`frozen_frac := mean(p̂ = 0)`** の軌跡
- **`ΔL_i`**: 作業6 と同一定義（`utility_nmse`）

---

## 5. ブートストラップ

- **seed クラスタ**（seed ラベルを復元抽出し、選ばれた seed の全 unit・全 step をブロックとして複製）
- `B = 10000`、`bootstrap_seed = 20260826`（**事前固定**）
- `OMP_NUM_THREADS = 1`（全アーム共通）
- 非有限値は個数を報告し、除外しない（作業6・r 差し替えと同規約）

---

## 6. 判定基準（**結果を見る前に凍結**）

等価幅は**静的 O の効果量 0.1343 の 10%** を基準に `δ = 0.0134` と置く。以下すべて seed-paired。

### 6.1 O-1（主判定）— O を畳むか

短窓終端の `D_A1` に対して:

| 条件 | 判定 | 含意 |
|---|---|---|
| CI 上端 < **−δ** | **PERSISTENT** | 修復は再開後も効く。O は動的にも生き、柱3 の容量残存が強くなる |
| CI 全体が **[−δ, +δ]** に収まる | **FOLD** | 改善ゼロ。**既記の基準どおり O の主張を畳む**（`dead_frac` が機能の代理にならないのと同じ罠） |
| CI 下端 > **+δ** | **HARMFUL** | 介入は有害。別論点（perturb-only の毒と同型）へ |
| 上記以外 | **INCONCLUSIVE** | 精度不足として記録し、seed を足す判断は別途 |

長窓終端の `D_A1` は同じ表で**副次**として出す。**短窓が主**。

### 6.2 C-1（裁定）— 便益はスイッチか、W の取り替えか

`D_A1 − D_A2`（= `U_A1 − U_A2`）に対して、短窓・長窓それぞれで:

| 条件 | 判定 | 含意 |
|---|---|---|
| CI 全体が **[−δ, +δ]** | **SWITCH_SUFFICIENT** | bias を開くだけで CBP 型と同等。**学習済み W に利用可能な容量が残っていた**を動的にも支持 |
| CI 下端 > **+δ**（A2 が良い） | **REPLACEMENT_NEEDED** | 便益は W の取り替えから来る。O の容量主張は静的オラクルの範囲を出ない |
| CI 上端 < **−δ**（A1 が良い） | **SWITCH_SUPERIOR** | 取り替えは過剰な破壊。CBP の適用条件の未明示という既記の指摘が強くなる |
| 上記以外 | **INCONCLUSIVE** | — |

**スコープ明記**: one-shot・同一ユニット集合・condA 限定。CBP の継続適用への外挿は禁止。

### 6.3 F-1（ΔL 因果の半歩）

- 母集団: **A1 で実用復活（`p̂ ≥ 0.05`）したユニットのみ**
- 群: 介入直後の `ΔL_i` 三分位（各 seed × **介入直後の `pre_max_post`** 五分位セル内で切る。介入前値は `pre_max_pre` として別列に残す。介入後は `p_count` がほぼ揃うので層別は `pre_max_post` のみ）
- 転帰: 短窓終端の `strict_dead`
- 有効セル規則: 比較する 2 群の**両方**に 1 曝露以上あるセルのみ（作業6 の `primary_cell_valid` と同型）

| 条件 | 判定 | 含意 |
|---|---|---|
| 調整 RD（high−low）の CI 上端 < **−0.05** | **PROTECTIVE** | 作業6 の関連が、介入で作り直した母集団でも再現。**因果の半歩** |
| CI 全体が **[−0.05, +0.05]** | **NULL** | 介入後は機能が守らない。作業6 の関連に選択効果が混じっていた可能性が上がる |
| 上記以外 | **INCONCLUSIVE** | — |

**「半歩」と呼ぶ理由を判定文に必ず併記する**: 介入したのは `p̂`（スイッチ）であって `ΔL` ではない。`ΔL` はランダム化されておらず、依然として観察量である。**PROTECTIVE でも「機能が凍結を防ぐ因果効果」とは書かない**（[[機能盲目性_0823]] §5 の禁止事項をそのまま継承）。

### 6.4 Ch-1（合唱団予言）

**仮説（合唱団説）**: 生存者集団は既に現タスクを歌い切っている。復活ユニットには稼ぐ残差がないので、保護されず、元の水準まで再凍結する。

事前予言 2 本。**両方満たしたときのみ PASS**。

| # | 予言 | 判定条件 |
|---|---|---|
| **Ch-a** | 復活ユニットは再凍結する | 長窓終端の再凍結率が **≥ 0.80**、かつ `τ` の中央値 **< 50,000 step（5 タスク）** |
| **Ch-b** | `dead_frac` は元の水準へ戻る | 長窓終端の `dead_frac_0.05`(A1) − (A0) の CI 全体が **[−0.05, +0.05]** |

- 片方のみ成立 → **PARTIAL**（どちらが成立したかを明記）
- Ch-b の **CI 上端 < −0.05**（A1 の `dead_frac` が等価域より低い側に残る）→ **REJECTED_LOW**。「復活ユニットは仕事を見つけた」ことになり、[[停止条件]] の残る候補 (a) 罠幾何説／(b) 低次成分の稼ぎ説の判別に直接効く。**CI 下端 < −0.05 だけでは REJECTED にしない**（CI が等価域を跨ぐなら INCONCLUSIVE）
- Ch-b の **CI 下端 > +0.05**（A1 の `dead_frac` が A0 より高い）→ **REJECTED_HIGH**。修復が凍結を促進した別機序として記録する
- `frozen_frac` 版も**副次**として同じ表で出す（不可逆性を語るのはこちら）
- Kaplan–Meier 曲線（A1 の復活ユニット、右打ち切りあり）は**記述**として必ず出す

### 6.5 検出力ガード（事前登録）

- **G1**: F-1 の有効セル保持率が復活ユニット総数の **40% 未満**なら、F-1 は**記述扱い**とし判定に用いない
- **G2**: いずれかの CI 半幅が **0.10 を超えたら**、判定を上書きせず「精度不足」として別ラベルで記録する
- **G3**: A1 の実用復活率が **0.5 未満**なら、F-1 と Ch-a の母集団が痩せているので**両方を記述扱い**にする
- **G4（0826 追加）**: A3 起因の致死（介入直後に alive → `p̂ < 0.05` へ落ちた個体）が **1 seed でも 50% を超えたら**、A3 はプラセボではなく**破壊対照**としてのみ記録し、「同大の無方向摂動は改善しない」という読みには使わない
- どのガードが立っても、**seed を足したり層別を緩めたりしない**（結果を見てからの設計変更を禁じる）

---

## 7. 事前に決めておく分岐

### 7.1 O-1 = FOLD のとき

- [[機能盲目性_0823]] §3 の O を「**静的オラクルの容量診断であり、学習の再開には移らない**」に格下げする
- [[中心主張v3草案_0823]] 柱3 の残り半分（「スイッチを開けば利用可能な学習済み W が残る」）に、**動的には移らない**という但し書きを足す
- 静的 O の数値（0.143367 → 0.009148）は**取り消さない**。測っているものが違う

### 7.2 O-1 = PERSISTENT のとき

柱3 が強くなる代わりに、**「なぜ通常の学習はそれを見つけないのか」という新しい穴が開く**。この場合は穴を [[命題リスト]] に起票するところまでを本走の成果とし、追跡実験を自動起案しない（索引の運用ルール）。

### 7.3 C-1 = REPLACEMENT_NEEDED のとき

[[論点マップ]] §8 の「posreset が reset の内訳を初めて分解」の読みを、**動的には座標復元側が本体**へ寄せる。ただし one-shot 限定のスコープを外さない。

### 7.4 Ch-1 = REJECTED のとき

[[停止条件]] の Next action（`spec_surv_hist_0820.md` の T1×T2）と結論が衝突しうる。**その場合は本走を優先せず、両方を併記して [[停止条件]] に判別の再設計を起票する**（介入走と観察走で母集団が違うため）。

### 7.5 O-1 × Ch-1 の 2×2 解釈表（**先に凍結**）

| | Ch-1 = PASS（再凍結・復帰） | Ch-1 = REJECTED |
|---|---|---|
| **O-1 = FOLD** | **合唱団説の完全形**。容量は残っているが仕事がない。柱3 は「容量残存」までで止め、機能主張へ伸ばさない | 起きにくい組み合わせ。実装疑い → S 系を全て再確認 |
| **O-1 = PERSISTENT** | 改善したのに元へ戻る＝**一過性**。`rank_int_0814` の ~100k 再崩壊と同型として書く | 修復が定着。**この場合のみ「手法」の語を検討してよい**（それでもオラクルであることは変わらない） |

**この表を結果に合わせて書き換えない。**

---

## 8. 実装上の罠（先に潰す）

- **乱数の独立化**: 作業6 で見つかった「seed 番号だけでは乱数系列が変わらない」実装上の罠を踏まない。本走は新規初期化ではなく既存 snapshot の完全 resume なので、`generator_offset` は起点走と同じ **0** に固定し、snapshot 内の各 generator state を真の起点とする。config 値・全 state hash を `manifest.csv` に記録する
- **アーム間のビット同一性**: 入力ストリーム・タスク境界・教師は全アームで**ビット同一**にする（`teachw_0820` の S2 と同型）。介入以外の差をゼロにする。**A3 の符号引きは学習用 RNG と別ストリーム**（`placebo_seed`）から取り、A0 の軌道に影響を与えない
- **境界のオフバイワン**: flip は記録点 B と B+1 の**間**で起きる。境界ペアの定義は `(s % T == 0) & (gap == 1) & (s > 0)`。**`s // T` ではタスクが分かれない**（8/22c の初回計算がこれで汚染された）
- **µ̂ が動く点を跨がない**: `cos` の増分を境界跨ぎで比較しない。生の `x = w·µ̂` で測る（8/21b 教訓⑦、8/22c の正規化注意）
- **恒真の回避**: 「復活ユニットの再凍結率」は蹴り上げ直後に `p̂ > 0` であることを実測で確認してから数える（S5）。P4 の事前登録指標がトートロジーだった前例に倣う
- **オラクル最適化の再現**: A1 の bias 最適化は作業2 O と**同一実装・同一コマンド**を呼ぶ。書き直さない
- **`n_alive` の実測**: `κ_s` と `c_s` は実行時に seed ごと計算し、`manifest.csv` に `n_dead,s` / `n_alive,s` / `κ_s` / `c_s` を全部残す。**本 note の 76–98 / 2–24 を定数として実装に埋めない**

---

## 9. サニティ（実行時に出力）

- **S1**: 介入直後（t = 500,000⁺、学習再開前）の A1 の seed 平均 NMSE が、作業2 O の full precision 値 **0.009148272885137731** と `abs_err < 1e-12` で一致（同一スナップショット・同一 k・同一 float64 oracle 実装なので一致すべき）。学習再開用 float32 へ cast した後の値も別列で報告するが、中止判定は cast 前で行う。**落ちたら中止**
- **S2（保存済み証拠＋本走 replay の合成サニティ）**: 元の連続軌道の step 700,000 state は保存されておらず、今回それとの state hash 比較を要求するのは実行不能なので、次の 3 本をすべて要求する
  1. `results/posreset_0819/meta.json` の condA `S2 == PASS` と source snapshot の登録 SHA-256 を確認する（既存 none-resume と元連続軌道が step 500,000 後に全ログ列一致した証拠）
  2. 本走 A0 の重複 replay を **probe あり／probe なし**で snapshot から +200,000 step 回し、終了時の net・env・teacher・running_mean・全 generator state hash が bit 一致
  3. 本走 A0 の source run と重なる記録点を `results/posreset_0819/lop_metrics_A_w100_none.csv`（なければ同等の既存 none-resume ログ名を manifest で解決）と全共通列で一致確認
  いずれかが落ちたら中止。**「元軌道との state hash 一致」とは書かず、保存済み S2 証拠と replay hash の合成確認と明記する**
- **S3**: `x² + r² = w_norm²` の最大相対誤差 `< 1e-10`
- **S4**: `p̂ = 0 ⇔ pre_max ≤ 0` が全記録点で成立
- **S5**: 蹴り上げ直後に、A1 の対象ユニットの `p̂ > 0` 率を報告（恒真性チェック）
- **S5-A3（0826 改訂）**: A3 は **`p̂` の不変を要求しない**。代わりに seed 別で以下を報告する
  - `n_alive,s`、`κ_s`、`c_s`、`c_s / median(margin_alive,s)`
  - 介入直前後の `p̂` の変化量分布（対象ユニットごとの `Δp̂`）
  - **A3 起因の致死数と致死率**（alive → `p̂ < 0.05`）、および **A3 起因の飽和数**（`p̂` が 1.0 へ張り付いた個体）
  - A1 の per-unit 変位中央値と A3 の `c_s` の比
  - これらは**判定ではなく、A3 の読み方（§3.2.1）と G4 を決めるための必須出力**
- **S6**: `ΔL_i` の 32 パターン厳密列挙が、別実装（消音前後の直接差分）と最大相対誤差 `< 1e-10` で一致
- **S7**: A2（CBP 型 reset）の直後の `dead_frac` と `U` を報告し、`cbp_harm_0815` のアンカー値と**同一土俵か否か**を明記する。一致を要求しない（設定が違う可能性がある）。**不一致でも中止しない。差の原因を summary に書く**
- **S8**: 同一 commit・同一コマンドの再実行で、時刻・elapsed・絶対 path を含む provenance 列を除いた全科学 CSV が byte 一致。NPZ は key・dtype・shape・配列 byte の SHA-256 一致を要求する。比較用の 2 出力先を分け、自己上書きで比較しない

---

## 10. 出力

`results/dynrepair_0826/`:

- `verdict.csv` — O-1 / C-1 / F-1 / Ch-1 の判定と CI。列は `spec_function_blind_direct_0823_confirm` と同型に揃える
- `summary.md`
- `traj.csv` — アーム × seed × step の `U`, `dead_frac_0.05`, `frozen_frac`
- `units.csv` — A1 の実用復活ユニット単位の介入直後 `ΔL`, `τ`, 打ち切り、`pre_max_pre`, `pre_max_post`, 五分位セル、F-1 群
- `utility.csv` — アーム × seed × unit × task-head step の `utility_nmse`（介入直後を含む）。F-1 の主曝露は A1・介入直後行だけ
- `km.csv` — Kaplan–Meier（記述）
- `placebo.csv` — S5-A3 の seed 別出力
- `logs/unit_traj_<arm>.npz` — `step[T]`, `seed[R]`, `unit[H]` と、各 `[T,R,H]` の `p_hat`, `p_count`, `pre_max`, `x`, `r`, `w_norm`, `b`, `v`, `strict_dead`。raw trajectory なので Git 対象外
- `raw_sha256.csv` — raw NPZ ごとの相対 path・byte size・SHA-256・各 key の dtype/shape
- `sanity.csv`, `manifest.csv`, `determinism_check.md`

---

## 11. スコープ（外挿禁止）

- condA・w100・T = 10,000・batch = 1・std（config `center_alpha = 0.01`、ただし `enc=std` のため中心化は無効）・起点 step 500,000 限定
- **one-shot 介入**。CBP の継続適用、および任意の学習手法への一般化は禁止
- A1 は**オラクル**（現タスクの 32 パターン厳密損失を使う）。手法として提案しない
- F-1 は**介入後の観察的関連**。`ΔL` はランダム化されていない
- **A3 は「bias 空間の総変位を揃えた無方向摂動」であり、per-unit の大きさも触るユニット数も A1 と揃っていない**。プラセボという語を無条件で使わない
- condB・他幅・他時刻・他教師へ外挿しない

---

## 12. Next action

1. `specs/spec_dynrepair_0826.md` としてリポジトリへコピーし、**実行前にコミット**（事前登録）
2. 実装・スモーク・S1/S2 を通し、実行前コードを commit / push。その commit hash を `manifest.csv` に固定してから本走を実行
3. 結果を [[機能盲目性_0823]] と [[停止条件]] に追記、判定を [[論点マップ]] へ
4. 実験を回すと決めた時点で秘書システムへ起票する

---

## 13. 改訂履歴

**2026-08-26 改訂1（実行前・Issa 指摘）— A3 の実行不能を修正**

- **指摘**: 初版 A3 は「A1 と同数の alive ユニット」を要求していたが、実スナップショットでは各 seed の `dead_0.05` が 76–98 個、alive が 2–24 個しかなく、**重複なしで同数を選べない**。仕様として実行不能だった
- **修正（Issa 指示）**: 「全 alive ユニットへランダム符号の bias 摂動を与え、seed ごとの摂動ベクトルの L2 ノルムを A1 と一致させる」。S5 の A3 は `p̂` 不変を要求せず、変化量を報告する
- **修正に伴い Claude が付した実装確定事項**（§3.2.1・§9 S5-A3・§6.5 G4）:
  - `κ_s` を「介入直後の全 hidden bias の総変位（蹴り上げ＋オラクル最適化、100 成分）」と定義。蹴り上げ成分のみではない
  - `c_s = κ_s / √(n_alive,s)`、符号は `placebo_seed = 20260826` の別ストリーム
  - alive が痩せているため **A3 の per-unit 変位は A1 の 2–7 倍**になり、A3 は破壊対照になりうる。読み方の制約を §3.2.1 に凍結し、G4 を追加
  - 処置強度 `c_s` が seed の健全さ（alive 数）と逆相関するため、A3 は seed プール平均を主にせず seed 別併記
- **初版の A3 定義は本節に記録として残す。復活させない**

**2026-08-26 改訂2（実行前・Codex 実装監査）— 起点忠実性と保存契約を修正**

- **起点 config**: `enc=std` を `center_alpha=0` と誤記していた。実 snapshot の config は `center_alpha=0.01` であり、`cmask=0` により中心化だけが無効。resume state を変えないため 0.01 に訂正
- **A2 再現性**: 未定義だった reset RNG を snapshot の `gens["method"]` からの 1 回の既存 Kaiming draw に固定。A3 は NumPy PCG64 の割当順まで固定
- **S1**: 丸め値 0.009148 では厳密一致を判定できないため、既存 `oracle_per_seed.csv` から full precision の **0.009148272885137731** を登録
- **S2**: 元連続軌道の step 700,000 state が保存されていないため、「元軌道との state hash bit 一致」はそのままでは実行不能だった。保存済み posreset S2、A0 probe 有無の重複 replay hash、既存 none-resume ログ照合の 3 点セットに置換
- **Ch-b**: 「CI 下端 < −0.05」を有意な低下としていた不等号誤りを修正。低下側の棄却は **CI 上端 < −0.05**、増加側は **CI 下端 > +0.05**
- **保存契約**: §3.3 が要求する unit 時系列と task-head `ΔL` を既存出力一覧では保存できなかったため、`logs/unit_traj_<arm>.npz`, `utility.csv`, `raw_sha256.csv` を追加。raw NPZ は Git に入れない
- **S8**: CSV の時刻等を除外し、NPZ は配列 byte hash で比較する再現可能な determinism 規約に具体化


---

# ⚠ 実行済み（2026-08-27）— 冒頭の「事前登録（実行可）」は古い

**本 note の §1–§13 は起草時のまま。結果を見た後に一切書き換えていない**（運用ルール「計画文書は結果を見た後に書き換えない」）。以下は帰趨のポインタのみ。転記チャット `claude-復習-0824-0827`。

| | |
|---|---|
| リポジトリ複製 | `specs/spec_dynrepair_0826.md`（事前登録 **`9f86ee8`**、実行前） |
| 実装 | `4c95949` → 実行前 provenance の dirty 判定訂正 **`1183d3a`**（`summary.md` の source commit） |
| 結果 | `results/dynrepair_0826/`（**`1e9dabd`**、S8 のクロス走比較訂正 `8fb87b0`。push 済み） |
| 判定 | **O-1 = PERSISTENT**（短 −0.1375 [−0.2599, −0.0479] / 長 −0.1152 [−0.2089, −0.0308]）／ **C-1 = INCONCLUSIVE**（短 +0.0124 / 長 +0.0155）／ **F-1 = DESCRIPTIVE_ONLY**（+0.2762、科学判定 INCONCLUSIVE）／ **Ch-1 = PARTIAL**（Ch-a PASS・Ch-b INCONCLUSIVE） |
| ガード | G1 CLEAR、**G2・G3・G4 が発火** |
| 記録先 | [[機能盲目性_0823]] §7（O-1・C-1・F-1）／[[停止条件]]（Ch-1）／[[論点マップ]] §17 |

- **§7.5 の 2×2 は「O-1 = PERSISTENT × Ch-1 = PASS」の枠に半分だけ落ちた**（Ch-1 は PARTIAL）。同枠の読み「改善したのに元へ戻る＝一過性」は、`U` の改善が長窓でも残る点と整合しない部分がある → [[停止条件]] の追記節
- **G4 発火により A3 はプラセボではなく破壊対照**（10 seed 中 6 seed で致死率 0.5 以上）。§3.2.1 で凍結した読み方の制約どおり扱う
- §9 S1・S2・S3・S4・S5・S5-A3・S6・S7・S8 はすべて出力済み（S1 は `abs_err = 1.7e-18` で PASS）
- §12 Next action 1〜3 は完了。**4（実験を回すと決めた時点で秘書システムへ起票）は未実施**
