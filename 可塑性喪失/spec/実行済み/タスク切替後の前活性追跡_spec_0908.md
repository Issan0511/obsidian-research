# タスク切替後の前活性の時系列追跡 0908
親: [[境界の群別回復と出力修正_結果_0908]] / 状態: 未実施
run_id: boundary_trace_0908 / Codex / 2026-09-08
Issa依頼: タスク切替後の前活性を追跡する。

新規学習なしの事後可視化。元CUDAのpmnist_boundary_0908、SNA/LR×none/l2-1e-3、seed0..2を使用。CPU追加軌道は混ぜない。task101..119の19境界で切替直前end、直後jump、毎更新1..300、625終端=end[next task]を追跡。301..624は欠測で線を結ばない。最初の例示は事前に固定したseed0/task101、第1層100unitをすべて描く。例示を代表的と断定しない。

第1層を主対象。全unitのほか、前specと同じD/N排他的4群を境界で固定して追跡。Dはstrip<−1e-6の下降が大きい順最大25、Nは|jump|が小さい順25、両者重複を区別。意味は相対的0近傍でありSnakeの折れ目ではない。第2層は全unit平均の補足時系列とする（設定点は使わない）。

数値は各境界でunit平均→19境界平均→seed3値と中央値/min/max。strip=jump−endと学習変位z(s)−jumpを別記録。steps1,5,10,20,50,100,200,300,625を表にする。unit平均が相殺を隠すため個別軌跡と群別変位も表示する。
「最初の学習でも下がるか」を後付けの谷選択で判断せず、最初の1stepの変位と1..20step窓平均を報告。0..300の最小値と時点は記述値として併記し、因果判定や登録仮説の支持にしない。
625終点のD回復は前回元CUDA集計に一致することを検算。全群の人数加重平均=全unit平均、各軌跡で最終差=strip＋学習差、rawのstep/task対応を検算する。

成果物: 個別unit軌跡、seed集約の群別学習変位、seed別/集約CSV、説明ノート。元npzのsha256を記録。spec→実装→結果の順でcommit/push。主張の採否は変更しない。


実施帰趨: 2026-09-08事後解析完了。[[タスク切替後の前活性追跡_0908]]。本文は変更していない。
