# Q17 駆動対称性 spec（0828）— `F_rest` の陣営別符号と正味力

親: [[命題リスト]] Q17 / 関連: [[論点マップ]] §14・[[中和と境界窓]] §8・[[死の二経路]]・[[天井プログラムT0_spec_0827]]

状態: **実行済み**（`results/q17_symmetry_0828/`・総合判定 `MATERIAL_REST_ASYMMETRY`。[[命題リスト]] Q17 の証拠列に反映済み。**フレームの再判断は先生案件のまま** — task_113 / task_097） / 作成 2026-08-28 / 凍結 2026-08-28d / 実行 2026-08-28 / 状態行更新 2026-09-01（`spec/実行済み/` へ移動。本文は起草時のまま）

> **格**: 本文は実行前に対象・等価域・判定規則を凍結するための文書である。起草・改訂時に確認したのは保存列、shape、記録グリッド、生成コードの代数、既存の commit 済み指標だけで、Q17 の符号割合・陣営差・ゼロ割合・正味比は計算していない。
>
> **改訂1**: 初稿の `Δq=q_+-q_-` は、生の `F_rest` 符号では陣営差に見えるが、生成式へ戻すと共通場 `h` の符号偏りである。これを訂正し、厳密ゼロ割合を必須判定から外し、`F_self` を拘束・減衰側へ帰属させた。
>
> **改訂2**: 大きさ側にも同じ陣営符号反転が残っていた。生の正味比 `N_c` は物理的な x 方向の記述量として残し、`M_c:=-cN_c` を `h` 空間の正味比と定義する。真の陣営差は `M_+-M_-=-(N_++N_-)` であり、旧 `ΔN=N_+-N_-` は共通場側の量へ降格した。
>
> **改訂3・凍結決定**: Q17-N は輸送不能ではなく rest 駆動の大きさ込み対称性だけを判定する。`epsilon_M=0.05` を「総絶対 rest 変動の5%以内の正味コヒーレンス」という実質ゼロ幅として、Q17 値を見る前に凍結した。`d_off_x` を用いる壁到達量の判定は本 spec から分離する。
>
> **位置づけ**: 8/27 に「整流を名乗る」と決めたため Q17 は防衛である。天井 T0 とは入力 npz が同じだけで、測定対象も判定形式も異なる。本 spec は T0 に相乗りさせない。

---

## 0. 生成式から先に固定する

`src/ratchet_log.py` の生成式は

```python
pref   = -2.0 * lr[:, None] * v
F_rest = pref * (d_rest * gate * xdm).mean(dim=0)
```

である。ここで

\[
h_i:=E[\delta_{rest,i}\,gate_i\,(x\cdot\hat\mu)]
\]

とおけば

\[
F_{rest,i}=-2\eta v_i h_i
\]

が恒等的に成立する。`v_i≠0`、`F_rest≠0` では

\[
\operatorname{sign}(h_i)
=-\operatorname{sign}(v_i)\operatorname{sign}(F_{rest,i})
\]

なので、`h` の符号は除算せず保存列 `v,F_rest` だけから厳密に復元できる。

`q_c:=P(F_rest<0 | camp=c,F_rest≠0)`、`p_c:=P(h>0 | camp=c,F_rest≠0)` とすると

\[
q_+=p_+,\qquad q_-=1-p_-.
\]

したがって

\[
q_+-q_-=p_++p_--1
\]

は**両陣営に共通する `h` の符号偏り**であり、

\[
p_+-p_-=q_++q_--1
\]

が**`h` の真の陣営差**である。初稿はこの二つを取り違えていた。

さらに `d_rest=δ-v_i a_i` なので

\[
h_i=E[\delta\,gate_i\,(x\cdot\hat\mu)]
-v_iE[a_i\,gate_i\,(x\cdot\hat\mu)].
\]

condA std では `x·mu_hat>=0`。第二項は camp ごとに逆向きへずれうるため、共通偏りだけでは陣営差を判定できない。二つを別測度にする。

---

## 1. 問いと三つの主測度

### 1.1 問い

condA・w100・T=10,000・batch=1 の `ratchet_log_0819` において、ゲート後ユニット別 `rest` 駆動は

1. 潜在符号 `sign(h)` が各陣営内で 50:50 に近いか
2. `sign(h)` の分布が陣営間で同じか
3. 大きさ込みの `h` 空間正味比が各陣営でゼロに近く、陣営間でも同じか

を判定する。死亡個体・alive 個体には限定せず、全ユニット・全該当記録点を対象にする。

### 1.2 Q17-W — `h` の陣営内方向バランス

時間領域 `D`、seed `s`、陣営 `c` ごとに

\[
p_{s,c,D}
=P(h>0\mid camp=c,F_{rest}\ne0)
\]

を作る。実装は

```text
h_sign = -sign(v_current) * sign(F_rest_current)
```

で行い、`h` を数値除算で復元しない。

各陣営で `p=0.5` が対称点である。

### 1.3 Q17-B — `h` の真の陣営間差

seed 内対応差を

\[
B_{s,D}=p_{s,+,D}-p_{s,-,D}
=q_{s,+,D}+q_{s,-,D}-1
\]

とする。**差を seed 内で先に作る。** 別々の欠測集合から二つの全体平均を引かない。

初稿の

\[
C_{s,D}:=q_{s,+,D}-q_{s,-,D}
=p_{s,+,D}+p_{s,-,D}-1
\]

は「共通 `h` 符号偏り」と改名し、登録副次量として残す。Q17-B とは呼ばない。

### 1.4 Q17-N — 大きさ込み `h` 正味比

まず seed・陣営・領域ごとに、生の物理方向の正味比

\[
N_{s,c,D}
=
\frac{\sum F_{rest}}{\sum |F_{rest}|}
\in[-1,1]
\]

を作る。`N=0` が x 方向の正味ゼロ、負が下向き、正が上向きである。

ただし `F_rest=-2ηv h` なので、生の `N_c` を陣営間でそのまま引いてはならない。`c=sign(v)∈{+1,-1}` として

\[
M_{s,c,D}:=-cN_{s,c,D}
=
\frac{\sum 2\eta |v|h}{\sum 2\eta |v||h|}
\in[-1,1]
\]

を定義する。`M_c` は保存列だけで作れる、`2η|v|` 重み付きの `h` 空間正味比である。符号頻度が 50:50 のまま一方向だけ大きい場合を検出する。

真の陣営差は同じ seed 内で

\[
B^M_{s,D}:=M_{s,+,D}-M_{s,-,D}
=-(N_{s,+,D}+N_{s,-,D})
\]

とする。一方、旧案の

\[
\Delta N_{s,D}:=N_{s,+,D}-N_{s,-,D}
=-2A^M_{s,D},
\qquad
A^M_{s,D}:=\frac{M_{s,+,D}+M_{s,-,D}}{2}
\]

は陣営差ではなく、両陣営に共通する `h` の大きさ込みコヒーレンスである。`N_+`,`N_-`,`ΔN`,`A^M` は登録記述量として残すが、Q17-N の主対象は `M_+`,`M_-`,`B^M` とする。差は seed 内で先に作る。

### 1.5 zero は判定から外す

\[
z_{s,c,D}=P(F_{rest}=0\mid camp=c)
\]

は報告するが、総合判定に使わない。`p_hat=0` では `F_rest=0` が恒等的であり、strict-off 後は `v` も凍結するため、`z_c` は主として陣営別の死の累積を測る。これは非対称な拘束側の予言であって、駆動非対称の反証にしてはならない。

---

## 2. 等価域

### 2.1 符号確率

確率と確率差の意味幅を 5 percentage point に固定する。

- Q17-W: 各陣営の `p` の等価域 `[0.45,0.55]`
- Q17-B: `B=p_+-p_-` の等価域 `[-0.05,+0.05]`
- 副次 `C`: 記述だけ。同じ `[-0.05,+0.05]` を図示するが総合判定には追加しない

初稿の `q_+` / `q_-` に同じ帯を置く方法は採らない。生成式による符号反転を戻した `p_+` / `p_-` を同一尺度で判定する。

### 2.2 大きさ側の等価域（凍結）

Q17-N の実質ゼロ幅を

\[
\epsilon_M=0.05
\]

に固定する。各 seed×陣営×領域で `M_c=Σ(2η|v|h)/Σ(2η|v||h|)` なので、`|M_c|<=0.05` は総絶対 rest 変動100に対する正味 `h` コヒーレンスが5以内、という意味である。真の陣営差 `B^M=M_+-M_-` にも倍幅を認めず、同じ `[-0.05,+0.05]` を置く。

Q17-N の等価域:

- `M_+`: `[-0.05,+0.05]`
- `M_-`: `[-0.05,+0.05]`
- `B^M`: `[-0.05,+0.05]`

この5%は輸送量から導いた壁到達不能幅ではなく、**大きさ込み駆動対称性に対する最小意味幅**である。D2 の変位PE、D3/E1の既知の力空間符号安定率、Q17の予備値は校正に使っていない。`alpha=0.10` と `epsilon_net=0.0375432` は採用しない。

したがって `SYMMETRIC_REST_DRIVE` が出ても、「長時間積分した `F_rest` だけでは壁へ届かない」とは書かない。コヒーレンス比は輸送効率であって輸送量ではなく、小さい偏りも10^6 stepでは蓄積しうる。

#### 壁到達量は別命題へ分離

同次元判定を行う場合は、窓内または高密度時系列の

\[
D_{rest,W}:=\sum_{t\in W}F_{rest}(t)
\]

と、full stateから操作的に定義した strict-off 壁までの `d_off_x` を追加再計測する。現行 npz の `x,p_hat,w_norm,b,flip_state` だけでは full `W`、32パターンの preactivation、または直接計算した `d_off_x` がなく、一意に復元できない。`pre_max` 単独は preactivation 単位であって一般には x 距離ではない。観測された初回 `p_hat=0` の `x` を使う方法も死亡個体を未来結果で選ぶため採らない。

この再計測は Q17 の成否や解釈を変更せず、必要なら別 spec・別命題として事前登録する。

---

## 3. 時間領域と位相ガード

境界は `flip_state` が変わる記録点ペアの左端を `B` とする。実装では

```python
changed = (np.diff(flip_state, axis=0) != 0).any(axis=1)
```

を使う。変化は `B→B+1` で起きる。

| ID | 記録点 | 点数/境界 | 役割 |
|---|---|---:|---|
| **FULL** | `[B-100,B+100]` | 201 | 主対象。元 Q17 の境界前後窓 |
| **PRE** | `[B-100,B-1]` | 100 | 必須層別。同一タスク内・境界遷移なし |
| **POST** | `[B+1,B+100]` | 100 | 必須層別。切替直後の新タスク側 |
| **EDGE** | `{B}` | 1 | FULL にだけ入る境界左端。記述のみ |

FULL だけでは PRE と POST の逆向き偏りが相殺されうる。三主測度を FULL・PRE・POST の全領域で判定する。bulk 点は足さない。既報 `A_within=0.497` はゲート抜き共通場の別量であり、予備値・検出力計算・判定に使わない。

---

## 4. 局所判定と総合判定

領域ごとの各測度を次で分類する。

| 測度 | `EQUIV` | `MATERIAL` | それ以外 |
|---|---|---|---|
| Q17-W | `p_+` と `p_-` の 95% CI がともに `[0.45,0.55]` 内 | いずれかの CI 全体が `<0.45` または `>0.55` | `INCONCLUSIVE` |
| Q17-B | `B` の CI 全体が `[-0.05,+0.05]` 内 | CI 全体が `<-0.05` または `>+0.05` | `INCONCLUSIVE` |
| Q17-N | `M_+`,`M_-`,`B^M` の CI が全て `[-0.05,+0.05]` 内 | いずれかの CI 全体が `<-0.05` または `>+0.05` | `INCONCLUSIVE` |

総合判定の優先順位:

1. sanity・標本ガード落ち → **`INCONCLUSIVE_GUARD`**
2. CI 精度ガード落ち → **`INCONCLUSIVE_PRECISION`**
3. FULL・PRE・POST のどこか一つでも `MATERIAL` → **`MATERIAL_REST_ASYMMETRY`**
4. 3領域×3測度族がすべて `EQUIV` → **`SYMMETRIC_REST_DRIVE`**
5. それ以外 → **`INCONCLUSIVE_REST_SYMMETRY`**

FULL だけが EQUIV で PRE/POST が未解像なら SYMMETRIC にしない。

---

## 5. データ源と行の定義

対象は次の 10 ファイルだけ。

```text
proj_004_drift/results/ratchet_log_0819/logs/seed{0..9}.npz
```

確定構造:

- 10 seed、各20,901記録点、width=100
- grid は bulk 1000刻みと各境界±100の毎step記録の和集合
- gap分布は各seed `{1:19900,900:199,1000:801}`
- 実境界は各seed 99本
- 必須保存列は `v,F_rest,F_self,F_gate,p_hat,flip_state`
- `flip_state` shape は `(20901,15)`、unit列ではない

期待行数/seed:

| 領域 | 行数 |
|---|---:|
| PRE | 990,000 |
| POST | 990,000 |
| EDGE | 9,900 |
| FULL | 1,989,900 |

行キーは

```text
(seed,boundary_step_B,relative_step_r,unit)
```

同じ記録点の `v_current` と `F_rest_current` を使い、1行ずらさない。

```text
camp=+1 if v>0
camp=-1 if v<0
camp=0  if v==0
```

`camp=0` は主測度の分母から外し、`v_zero_rate` として報告する。直前・直後符号へ補完しない。

`F_rest==0` は符号測度の分母から外すが、zero記述へ残す。数値 floor を結果を見て足さない。`+0.0/-0.0` はともに zero。

---

## 6. 集約、bootstrap、精度ガード

### 6.1 集約

- alive/dead、`p_hat>=0.05`、`p_hat>0`、死亡経路、未来生存で条件付けない
- 各 seed 内では該当 unit-time 行を等重み
- Q17-N は seed×camp×領域内で `N` の分子・分母をそれぞれ全行合計してから比を取り、その後 `M_c=-cN_c` を作る
- seed 間では等重み
- camp サイズを subsample して揃えない
- 境界を結果に応じて除外しない

### 6.2 seed cluster bootstrap

- cluster: seed
- 10 seedを復元抽出
- `B=10,000`
- `bootstrap_seed=20260828`
- 全領域・全測度を同じ resample indexで回す
- `OMP_NUM_THREADS=1`
- 重複seedはdrawごとの別clusterとして平均へ入れ、元seed idで再集約しない
- 95% percentile CI

### 6.3 標本ガード

各領域で以下を要求する。

1. 8/10 seed以上で両陣営が存在
2. 有効seed・陣営ごとに camp 行300以上
3. Q17-W/B はさらに非ゼロ行300以上
4. Q17-N は `Σ|F_rest|>0`
5. 陣営差は同じseedで両陣営量が定義できる

行数300は破損・空セルガードであって検出力ガードではない。

### 6.4 CI 精度ガード

主測度ごとに CI 半幅を出す。

- Q17-W/B: 半幅 `<=0.05`
- Q17-N: 半幅 `<=0.05`

一つでも超えたら、その測度は `INCONCLUSIVE_PRECISION`。9個超の同時等価条件は保守的であり、10 seed で精度が足りなければ「対称」とも「非対称」とも書かない。

bootstrap replicate に非有限値が一件でもあれば、有限 replicate だけで CI を作らず `SANITY_FAIL_NONFINITE`。

---

## 7. Sanity gate

以下のいずれかが落ちたら主判定を出さない。

### S1. 入力とグリッド

- 10 seed、各20,901点
- 必須列・shape・dtype・有限性が一致
- gap分布が確定構造と一致

### S2. 境界

- `(diff(flip_state)!=0).any(axis=1)` で各seed 99境界
- 左端 `B` が `(B%10000==0)&(gap==1)&(B>0)` と完全一致
- FULL/PRE/POST/EDGE の行数が§5と一致
- PRE、POST、EDGEが排他的で、その和がFULL

### S3. 行キー

- `(seed,B,r,unit)` が領域内で一意
- `r`集合が定義どおり
- 全unitが `0..99`

### S4. 力の分解

保存列をfloat64へ上げ、全対象行で

```python
rtol = 8 * np.finfo(np.float32).eps
atol = 8 * np.finfo(np.float32).tiny
abs(F_gate-F_self-F_rest) <= (
    atol + rtol*(abs(F_gate)+abs(F_self)+abs(F_rest))
)
```

を要求する。

### S5. 恒等式の保存確認

- campのpositive/negative/zeroが全行を排他的に分割
- `F_rest`のup/down/zeroが全行を排他的に分割
- `v==0 => F_rest==0`
- `p_hat==0 => F_gate==F_self==F_rest==0`

S5 は生成式の恒等式であり、ファイル破損・配列ずれ検出だけに使う。科学的証拠に数えない。

### S6. 代数の実装確認

非ゼロ行で

```text
h_sign == -sign(v)*sign(F_rest)
p_plus == q_plus
p_minus == 1-q_minus
B == q_plus+q_minus-1
C == q_plus-q_minus
M_plus == -N_plus
M_minus == +N_minus
B_M == M_plus-M_minus == -(N_plus+N_minus)
A_M == (M_plus+M_minus)/2 == -(N_plus-N_minus)/2
```

を機械照合する。浮動小数の比については同じ集約済み分子・分母から両辺を作り、S4 と同じ相対・絶対 tolerance で照合する。

### S7. スコープ検査

主判定に `delta_x`、alive/dead、死亡経路、ゲート抜き `G` を含めていないことを列名で検査する。Q17-N の対象は常に `F_rest`。`F_gate/F_self` は provenance、S4、フレーム帳簿の記述にのみ許可する。

---

## 8. 登録副次量

判定を増やさず次を報告する。

- `C=(p_++p_-)-1=q_+-q_-`: 共通`h`符号偏り
- `A^M=(M_++M_-)/2=-ΔN/2`: 共通`h`大きさ込みコヒーレンス
- 生の `N_+`,`N_-`,`ΔN`: 物理的 x 方向の記述。`ΔN` を陣営差とは呼ばない
- seed・領域・陣営ごとの `N_up/N_down/N_zero/N_vzero`
- `z_c` と `Δz`（判定なし）
- zeroに占める `p_hat==0` の割合
- 境界index四分位 `1–25/26–50/51–75/76–99` ごとの `z_c`（死の累積確認、判定なし）
- camp構成割合
- 窓内 `D_rest,W=ΣF_rest` と `mean(F_rest)`（走行全体へ外挿しない）
- EDGE単独の符号割合・`N`・`M`（CI・判定なし）

`|F_rest|` の分位点は Q17-N の補助として報告してよいが、結果を見て新しい閾値や群を作らない。

---

## 9. フレーム上の割り当てと解釈上限

### 9.1 三項の割り当て

\[
F_{gate}=F_{self}+F_{rest}.
\]

本 spec では

- `F_rest`: 撹拌／駆動の候補
- `F_self<=0`: 系統的な自己減衰（税）。**撹拌には入れず、拘束・減衰側の帳簿に置く**
- `p_hat=0` の片側消灯: 非対称な移動度拘束

と割り当てる。

したがって実際のフレームは厳密な二要素ではなく

> ほぼ対称な rest 駆動 ＋ 系統的な自己減衰 × 非対称な移動度拘束

の三項である。短縮して「対称な撹拌×非対称な拘束」と呼ぶ場合も、self税を拘束側へ含めた略記だと明記する。

Q17 は `F_rest` の対称性だけを判定する。self税が輸送に必要か、罠だけが非対称の原因か、という因果的必要性は Q2b/Q3 の証拠と合わせても本 spec 単独では確定しない。

### 9.2 `SYMMETRIC_REST_DRIVE` のとき言えること

このラベルは§2.2で凍結した `epsilon_M=0.05` を使った場合にだけ使用する。

> condA・w100の境界前後窓では、`h` の符号頻度は各陣営で50:50の5pt等価域に入り、陣営差も5pt以内だった。`h` 空間の大きさ込み正味比 `M` も、各陣営と陣営差 `B^M` の全てで登録した意味幅以内だった。

これは符号だけでなく、**凍結後の大きさ基準が支える範囲に限って** rest の正味駆動が小さいことを防衛する。コヒーレンス基準を選んだ場合は輸送量や到達不能性までは主張しない。

### 9.3 `MATERIAL_REST_ASYMMETRY` のとき

「対称なrest駆動」とは書かない。どの領域・どの測度が外れたかを明記し、結果を見て `epsilon_M` や確率帯を広げない。

### 9.4 常に言えないこと

- `F_rest` の全分布・分散が完全に対称
- `F_gate` や `Δx` が対称
- 正味の非対称の原因が罠だけであるという排他的因果帰属
- Q3 と本結果だけで状態分布の非対称が全て導けること
- bulk、condB、centered、別width/T/optimizerへの外挿
- Q17-N と変位PEが同一の物理量であること
- 現行npzだけからユニット別の strict-off 壁までの x 距離が復元できること
- 境界窓の平均力を10^6 stepへ外挿した値が実走行積分と等しいこと

---

## 10. 出力契約

実装:

```text
proj_004_drift/analysis/q17_symmetry/q17_symmetry.py
```

出力:

```text
proj_004_drift/results/q17_symmetry_0828/
```

必須:

- `config.json`: 領域、確率帯、`epsilon_M=0.05`、大きさ基準の種類、bootstrap、ガード
- `provenance.json`: code commit/dirty、10 npzのSHA-256・shape・dtype・列名、Python/NumPy version
- `sanity.csv`: S1–S7
- `per_seed_metrics.csv`: seed×領域×陣営の件数、p、q、z、N、M、B、C、`B_M`、`A_M`、`ΔN`
- `verdict.csv`: 3領域×3主測度族の点推定・CI・局所判定と総合判定
- `summary.md`: sanity、代数照合、主判定、位相別内訳、フレーム帳簿、解釈上限、逸脱
- `figures/fig_sign_composition.png`
- `figures/fig_net_force_ratio.png`
- `figures/fig_seed_metrics.png`

同じcommit・環境の独立再実行でCSVとsummaryの数値部分がbyte一致することを確認する。

---

## 11. 禁止事項と実行順序

- `epsilon_M=0.05` 以外の値で実行しない
- D2の変位PEやD3/E1の既知値を、そのままQ17-Nの許容幅へ流用しない
- 実行前に本specを凍結・commitする
- Q17統計量を計算した後に、等価域、時間領域、camp/zero定義、集約順を変えない
- `F_rest`を`F_gate`、`G`、`Δx`で代用しない
- `sign(v)*F_rest`を無名の符号補正として使わない。許すのは§0の`h_sign`と§1.4で明示した`M_c=-cN_c`だけ
- alive/deadや死亡経路で主判定を層別しない
- zero差を総合判定へ戻さない
- PRE/POSTの片方を結果に応じて落とさない
- T0/M1/M2の結果でQ17判定を調整しない
- vaultの[[命題リスト]]・[[現在地]]は解析結果commit後まで更新しない

実行順序:

1. 改訂草稿レビュー
2. `epsilon_M=0.05` と対称性限定の解釈上限を凍結
3. spec凍結・commit
4. 解析コードとunit testを凍結値へ同期
5. sanity-only実行
6. 本実行
7. 独立再実行による決定性確認
8. 結果commit後にvaultへ反映