# 可識別性 × µ の 2×2（純化版）— 事前登録 spec（起草・**未凍結**）

状態: **実行済み 2026-09-01**（事前登録 repo `0d7a746`（spec 単独 commit）→ 実装 `0333eb4` → 結果 `ac2727c` = [[可識別性2x2結果_0901]]。S-op FAIL により I+ セルは無効・要因計画の主判定は出していない → [[可識別性2x2_spec_0901_追補1]]。**見出しの「未凍結」は起草時の記述で現在は当たらない**。本文は起草時のまま） / 作成: 2026-09-01 / **改稿: 2026-09-01（v1 → v2・純化）** / run id: `ident_mu_2x2_0901`
親: [[論点マップ]] §20（µ を維持する利得 ★可識別性交絡） ／ 詳細の正本: [[µ維持の利得_0829]] §2
関連: [[引用禁止]] B、[[現象2主張v1_0901]] §5 穴1・C4・C5、[[中心主張v4.2草案_0830]] 柱1・柱3、[[命題リスト]] Q14・Q19・Q26、[[台帳（実効バイアスの縮退）]]、[[チャネル遮断2x2_spec_0901]]（**別の 2×2**）、[[9月運用_0901]] §0-2 例外・§3-6

> **このファイルの格。** 本文・設計・判定規則は Claude の起草である。**§8 の事前予測は 2026-09-01 に Issa が記入済み（5 項すべて）。** 事前登録の成立点は repo 側 `specs/spec_ident_mu_2x2_0901.md` の**単独 commit** であり、それ以前に本ファイルの判定基準を根拠として引いてはならない。

> **改稿履歴（v1 → v2・2026-09-01・チャット `可識別性2x2_0901`・凍結前）。** v1（vault commit `1d58f00`〜`a832af8`）は b-WD なしの 4 セル＋`std_anchor` だった。同日、`center_oracle_0831` の committed ログの事後再計算により **`Aexact`（= v1 の `im` セルと同分布）の死が 1 件残らず b の自走（現象2）であること**が判明した（$M\equiv0$ なので $\hat p=0 \iff b \le -\kappa\sigma$ と代数的に強制。全ユニット中央値 $b$ は task 100 で −0.95、初回消灯時の $b$ は seed 中央値 −0.999。生存者側には +b 走行も 18.5%）。これにより v1 の M ダイヤルは「µ の壁（現象1）」と「b への荷重移転（現象2・Q19 C2）」を同時に切り替えており、`iM − im` は**異なる病気の比較**になっていた。v2 は **4 セル全部に b 限定 WD（λ=1e-3・C4 `BIAS_WD_PROTECTS`）を敷いて現象2 を床に押さえた「純化 2×2」**へ改稿した（Issa 裁定・D8）。凍結前の改稿であり事前登録上の問題はないが、経緯を監査可能にするためここに残す。

---

## 0. 裁定

| #      | 裁定事項                       | 状態                                                                                        | 帰結                                                                      |
| ------ | -------------------------- | ----------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| **D0** | **走枠**（[[9月運用_0901]] との整合） | **確定（9/1・Issa）: 運用ノートを改訂し W1 に置く**                                                        | §0.1。[[9月運用_0901]] §0-2 に例外、§3-6 に新規則「長い走ほど早く投入する」、§4 W1 に走枠を追記した       |
| **D1** | ハーネス                       | **確定（9/1・Issa）: Option B（統一・トラッカー無し）**                                                    | µ を EMA でもオラクルでもなく**可視入力の構成**で置く。EMA 由来の副次交絡 4 つが同時に落ちる（§2.2）           |
| **D2** | (I+, M−) の実装               | **Claude 暫定: A2（ゲート迂回バイパス）。上書き可**                                                         | A1（高次モーメント符号化）・A3（移設）との比較は §3.3                                         |
| **D3** | アンカー腕                      | **確定（9/1・Issa・D8 に同梱）: `im_nowd` と `std_anchor` の 2 本**                                   | `im_nowd` は登録副判定 R-ext を担う（§6.4）。`std_anchor` は REPORT_ONLY             |
| **D4** | 主 endpoint                 | **v2 で変更: E-drift ＋ E-level の共主**（`channel_2x2_0901` §6.1 の標準形に復帰）                        | 純化により天井セルが消える見込みのため（§6.1）。E-onset（到達時刻）は REPORT_ONLY と R-ext 用に降格       |
| **D5** | 等価限界 $\Delta$              | **Claude 暫定: $\Delta = 0.15$ dex（E-drift/E-level 共通）、$\Delta_{\rm int} = 0.50$ dex。上書き可** | `channel_2x2_0901` D4 からの継承。**別サンプル・別 endpoint での較正値の移送**であることを明記（§6.3） |
| **D6** | 走の長さ                       | **Claude 暫定: 5M 直行（パイロット無し）**                                                             | 1M の見かけで二度事故っている（[[9月運用_0901]] §3-5）                                    |
| **D7** | 用量軸                        | **Claude 暫定: 足さない**                                                                       | 用量非単調は [[現在地]] 穴12 の担当                                                  |
| **D8** | **純化（全セル b-WD）**           | **確定（2026-09-01・Issa・チャット `可識別性2x2_0901`）: 4 要因セル全部に b 限定 WD λ=1e-3 を敷く**                 | 根拠と代替案の棄却は §3.5。選択肢「完全な 2×2×2」「b-WD なしの v1 のまま」は提示のうえ棄却された              |

### 0.1 D0 の決着（2026-09-01・Issa）

[[9月運用_0901]] §0-2「現象1 の M 級新規走は今月回さない」と衝突していたが、**運用ノートを改訂して W1 に置くことで決着**（理由:「長い実験だからこそ早くに終わらせる必要がある」）。改訂 3 箇所 —— §0-2 に本走限定の例外、§3-6 に新規則「長い走ほど早く投入する」、§4 W1 に走枠（投入前提 3 点つき）。**§20 に限った例外であり、現象1 の M 級走一般の解禁ではない。**

---

## 1. 問い

### 1.1 §20 の交絡

condA（入力 20 bit のうち flip 15 bit がタスク内定数、rnd 5 bit だけが動く設定）では $\boldsymbol\mu \approx [\text{flip}_t,\ 0.5\cdot\mathbf 1]$ であり、**µ ダイヤルとタスク可識別性ダイヤルが同一の物理量**である。既存の中心化アームは「µ を消す介入」ではなく「µ とタスク可識別性を同時に消す介入」であり、これに答える測定点はゼロ（[[µ維持の利得_0829]] §2、[[引用禁止]] B）。既存の腕は 2×2 の**対角線しか埋めていない**。

### 1.2 v1 が抱えていた第二の交絡（v2 の動機）

**M ダイヤルは 1 つのつまみで 2 つの病気を同時に切り替える。** µ を消すと (i) ゲート DC（現象1 の壁）が消えると同時に、(ii) DC 荷重が b へ 100% 移り（Q19 C2: b の分担 $1/(1+\lVert\mu\rVert^2)$）、税（Q14・入力分布に無条件）が b を自走させる —— **現象2 が最大化される**。実際、`Aexact`（v1 `im` と同分布）の死は全数が b 経由だった（改稿履歴の再計算・$M\equiv0$ による代数的強制）。したがって b-WD なしの `iM − im` は**同じ病気の用量反応ではなく、異なる病気の比較**である。

**v2 の解**: 現象2 の登録済み治療（C4 `BIAS_WD_PROTECTS`）を 4 セルに一律に敷き、**現象2 を床に押さえた地面の上で I × M を測る**。主判定の読みはすべて「**b 拘束下での**」という限定句を伴う。

### 1.3 何が決まるか

| 主判定                              | 帰結                                                                                                                  |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| **`MU_DOMINANT`**                | b 拘束下で、µ（静的なゲート DC）があるだけで劣化が駆動される。柱3 の $\mu^\ast$ は「µ の閾値」側。柱1 の µ チャネル記述は維持                                        |
| **`IDENT_DOMINANT`**             | b 拘束下では µ ではなく「教師の変化を追える情報の欠如」が劣化を駆動する。centered 系の挙動の相当部分は問題の変質（固定 32 点回帰化）で説明され、柱1・柱3 の書き直しが要る                     |
| **`BOTH_MATTER`**                | §20 は「分離したが両方効く」で閉じる                                                                                                |
| **`NEITHER_MATTERS`**（タイトな null） | **b を縛れば、µ（静的）も可識別性も 5M では劣化を駆動しない**。現象2 の主張（[[現象2主張v1_0901]] §2「純化」）を強く支持し、柱1 は「µ の壁は b 経路と結合して初めて 5M 尺度の病理になる」へ寄る |

### 1.4 他の 2×2 と混同しない

|            | 因子                              | 状態                                 |
| ---------- | ------------------------------- | ---------------------------------- |
| (A) 積の構造   | µ/Σ の有無 × 片道ゲートの有無              | [[中心主張v4.2草案_0830]]。別配置の 4 セル      |
| **(B) 本走** | **µ ダイヤル × タスク可識別性ダイヤル（b 拘束下）** | [[論点マップ]] §20                      |
| (C) チャネル遮断 | $b$ チャネル遮断 × µ チャネル遮断           | `channel_2x2_0901`（実行済み `bb5fd09`） |

(C) との関係: (C) は「遮断手段」の直交化で、b-WD はそこでは**因子**だった。本走では b-WD は**全セル共通の地面**であり因子ではない。

---

## 2. Option B の中身（v1 から不変）

### 2.1 µ を「推定」から「構成」へ

**教師の計算には常に真の $(\text{flip}_t, \text{rnd})$ を使い、学習器に見せる可視入力だけを差し替える。**

- **M+**: 可視 flip ブロック = $\text{flip}_0$（seed ごとに固定・タスク非依存）、rnd は生
- **M−**: 可視 flip ブロック = $\mathbf 0_{15}$、rnd は $-0.5$

flip ブロックは構成的にゼロ、rnd の平均 0.5 は既知定数なので、**M− では層1入力平均が厳密に 0**。追随が要らない。

### 2.2 設計から消えるもの

EMA の追随ノイズ／遅れ窓／`ABSORPTION_BROKEN_BY_EMA`／step 0 の初期過渡（教訓⑮）。

### 2.3 代償

既存 committed 腕との bit 一致を捨てる。既存腕は外部アンカーに降格（`channel_2x2_0901` D1 と同じ構造）。**例外として `im_nowd` は S0 replay で `center_oracle_0831` の `Aexact` と実装同値性を照合できる**（§7 S0。可視入力 $[\mathbf 0, \text{rnd}-0.5]$ は `Aexact` の厳密支持平均減算と構成的に一致するため）。

---

## 3. 設計

### 3.1 共通条件

- condA: `m=20`, `f=15`, teacher width 100, $\beta=0.7$, $T=10{,}000$, batch 1
- learner: ReLU、hidden **`[100]`（深さ 1）**、plain SGD、`lr=0.01`、Kaiming uniform
- 5,000,000 step（= task 500）、seed 10 本、CPU、`OMP_NUM_THREADS=1`、checkpoint は step 0 / 5M
- **`generator_offset = 20260910`**（新規系列。既用は `0` / `20260830` / `20260831` / `20260905`）
- 未フィット率の床は **`1e-16`**（1 層系の継承元）。再較正しない
- **b-WD の更新式**: $b \leftarrow b - \eta(g_b + \lambda b)$（decoupled ではない素の L2 勾配）。掛けるのは**隠れ層 bias のみ**（$W$・$v$・$c$・$\mathbf u$ には掛けない）。分岐を置かず常に `gb + wd_b*b` を計算する（$\lambda=0$ 腕が WD 経路を通したうえで bit 一致することを検査可能にするため。S1）

深さ 1 の理由: (a) M− は入力層の介入で、多層では ReLU が次層に µ を作り直し（柱1 1c）M ダイヤルが層2で漏れる、(b) 外部アンカー（`center_oracle_0831` の `Aexact`・`L1w100_A1`）が 1 層 w100。

### 3.2 腕（6 本）

出力は $\hat y = v^\top \sigma(W\mathbf x_{\rm vis} + b) + \mathbf u^\top \mathbf{code} + c$。$\mathbf u \in \mathbb R^{15}$ はバイパス（隠れ層を迂回して読み出しへ直結する線形項）・**全腕で零初期化**（乱数を消費しない。`code`$\equiv\mathbf 0$ の腕では勾配恒等ゼロで厳密に 0 のまま）。

| 腕名           | I   | M   | `wd_b`   | 可視入力 $\mathbf x_{\rm vis}$          | `code`          | 役割                                      |
| ------------ | --- | --- | -------- | ----------------------------------- | --------------- | --------------------------------------- |
| `IM`         | +   | +   | **1e-3** | $[\text{flip}_0,\ \text{rnd}]$      | $\text{flip}_t$ | 要因セル                                    |
| `iM`         | −   | +   | **1e-3** | $[\text{flip}_0,\ \text{rnd}]$      | $\mathbf 0$     | 要因セル                                    |
| `Im`         | +   | −   | **1e-3** | $[\mathbf 0_{15},\ \text{rnd}-0.5]$ | $\text{flip}_t$ | 要因セル                                    |
| `im`         | −   | −   | **1e-3** | $[\mathbf 0_{15},\ \text{rnd}-0.5]$ | $\mathbf 0$     | 要因セル                                    |
| `im_nowd`    | −   | −   | 0        | $[\mathbf 0_{15},\ \text{rnd}-0.5]$ | $\mathbf 0$     | **アンカー＋登録副判定 R-ext**（ハーネス内 `Aexact` 双子） |
| `std_anchor` | 縮退  | 縮退  | 0        | $[\text{flip}_t,\ \text{rnd}]$      | $\mathbf 0$     | REPORT_ONLY（引き直しを持つ唯一の腕・既存腕への橋）         |

I ダイヤル＝「`code` が $\text{flip}_t$ か $\mathbf 0$ か」、M ダイヤル＝「可視 flip が $\text{flip}_0$ か零＋rnd 中心化か」の各 1 スイッチで、**要因 4 セルは実装レベルで完全直交**。std は 2 ダイヤルが 1 本に縛られた縮退点として 2×2 の外に置く。

見積もり: 1 腕 ≈ 28 分（`centered_freeze_0901` 実績・w100 1 層）× 6 腕 ≈ **2.8 時間**（逐次）。

### 3.3 (I+, M−) の実装 — 補題と A2 の限界（v1 から不変・要約）

> **補題**: タスク内で定数、かつタスク間で識別情報を持つ入力チャネルは、必然的にタスク依存の平均を持つ。

よって純セル (µ無・可識別有) を**前活性の経路**で作ることは原理的に不可能。A2（出力側バイパス）は層1前活性 DC を厳密 0 に保つが、運べるのは**大域的な加法成分 1 自由度**であり、std が使える**ユニットごとの実効バイアスの分け前（台帳の縮退・100 自由度）**ではない。per-unit の可識別性は前活性 DC そのもので µ と分離できない。したがって I+ は「**ゲートに触れない経路での可識別性**」であり、本走が割るのは §20 の鋭化版 ——「centered の挙動は µ がゲートから消えたからか、出力側で追える情報が消えたからか」。棄却案: A1 は復調が XOR 等価で幅 100 非定常では学習不能リスク、A3 は µ の移設。

### 3.4 4 セルが共通して持たない性質（スコープ条件・v1 から不変）

M+ でも flip は $\text{flip}_0$ 固定なので、**境界ごとの µ の引き直し（＝唯一の脱出路）は要因 4 セルのいずれにも無い**。本走の µ は**静的な壁**の効果。引き直しを持つのは `std_anchor` のみ（縮退のため要因計画外）。

### 3.5 ★ なぜ全セル b-WD が「フェア」か（D8 の根拠）

1. **b-WD は µ チャネルに触らない。** `bias_wd_0901` 系の台帳 REPORT で、$b$ 遮断が $M = w\cdot\mu$ のドリフトに触らないことを確認済み（[[現象2主張v1_0901]] C5）。b-WD は現象2 を**選択的に**切除し、M ダイヤルは純粋な $w\cdot\mu$ の壁のダイヤルになる
2. **静的コストはセル間でほぼ対称**（論証・未登録）: λ=1e-3 はタスク内の損失曲率（O(0.1–1)）より 2 桁以上小さく、損失が b を係留する方向ではほぼ無料（最適点のずれ ~0.4%）。効くのは**損失が平坦な方向**＝現象2 の住処だけ
3. **現象2 の残差こそ現象1 が初めて見えた場所**: C5 で b 成分除去後に初めて dead↔機能劣化の seed 間共変（ρ=+0.73）が出た

**棄却した代替案**: (i) $b\equiv0$ 凍結 —— `FROZEN_WORSE`（機能 57 倍悪化）・閾値表現力ごと奪う・M+ で非対称。(ii) 縮小先 $-w\cdot\mu$ の b-WD（s 縮小）—— 平衡が $s^\ast = -(\tau+R)/\lambda$ に固定され **$w\cdot\mu$ 輸送そのものを規制**、測るべき M 対比を破壊。縮小先 0 の素の b-WD が µ チャネルを生かしたまま b だけ縛る唯一の形。(iii) 完全な 2×2×2 —— 最もフェアだが 1 spec に現象1・2 を再び混ぜる。off-WD 側の情報は committed `Aexact` ＋ `im_nowd` 1 本でほぼ足りる。

### 3.6 seed 単位隔離（継承・v1 から不変）

probe 1,000 step ごと・非有限 seed のみ quarantine（lr=0・乱数消費は継続）・全時点除外・**各腕 2/10 まで**、3 本目で `ARM_INVALID_EXCLUSION_LIMIT`・主比較は**要因 4 セルの完走 seed 共通集合**のみ（8 本未満で `CONTRAST_INVALID_TOO_FEW_PAIRED`）・補充/再開/rescue/winsorize なし。R-ext は `im`・`im_nowd` の 2 腕の共通集合で別途評価する。

---

## 4. 記録

task 末（10,000 step ごと・step 0 含む 501 点）に 32 パターン厳密列挙。列は `spec_bias_wd_0901` §3 の凍結セットを継承（`strict_dead_frac` / `b_median_alive` / `B_median_alive` / `beta_median_alive` / `kappa_median_alive` / `sigma_median_alive` / `margin_median_alive` / `p_hat_median_alive` / `p_hat_thin_frac` / `p_hat_sat_frac` / `eff_rank` / `eff_rank_W` / `w_norm_median` / `wcos_mean` / `unfit` / `eval_loss_exact`）。

**本走固有の追加列**: `mu_norm_visible`・`u_norm`・`bypass_share`（**パワー比**: $\mathbb E[(\mathbf u^\top\mathbf{code})^2]$ の出力二乗平均に対する分け前。**分散比では定義しない** — バイパスはタスク内定数なのでタスク内分散が恒等 0 になり無意味。9/1 Codex 実装と整合）・$M_i$・$B_i$・`submerged_frac`・境界前後スナップ・**全ユニット中央値 $b$**（alive 条件付きと併記。改稿履歴の選択バイアスの教訓）。

**主判定は §6.3 の 1 つだけ**。相乗り列は事後ラベルでしか使わない。

---

## 5. 窓とブロック

50 task 刻み。**B02 = task 51–100**、**B10 = task 451–500**。10 ブロック全部を `block_levels.csv` に出す。実験単位は seed（ブロック内 50 点を seed 内平均）。

---

## 6. 事前登録判定

### 6.1 endpoint（v2 で標準形に復帰）

純化により天井セルは消える見込み（b-WD 下の centered は dead 0.010・`bias_wd_0901`）。よって `channel_2x2_0901` §6.1 の共主 2 本に戻す。

| ID          | 定義                                       |
| ----------- | ---------------------------------------- |
| **E-drift** | `mean(log10 unfit)` の **B10 − B02**（劣化量） |
| **E-level** | `mean(log10 unfit)` の **B10**（到達水準）      |

- **S-ceiling**: 要因 4 セルの B02 水準の最大差が **3 dex 超**なら `CEILING_CONTAMINATED` を立て「E-drift 単独では読まない」と `summary.md` に明記
- E-drift と E-level で結論の向きが食い違えば `LADDER_INVERTS`、**どちらも単独で引かない**
- **単独でフェアな指標は存在しない**（level は各セルの収束深さ＝精度で汚れ、drift は「落ちる余地」で汚れる）ため共主 2 本＋旗の構えを採る。ただし本ハーネスでは level 汚染は構造的に小さい: (i) M± の可視入力は**定数並進**で同一問題（ラベル系列は S-seq で bit 一致）、(ii) タスク内で `code` は定数なので **I+ のバイパスはタスク末の表現力を足さない**（$\mathbf u^\top\text{code}$ は $c$ で代替可能）。残る汚染はタスク内 $10^4$ step の収束速度差のみ
- **`unfit` は DC 盲である**（`residual.var()/y.var()` の分散比なので出力の定数ずれを見ない。committed 実装 `src/bias_wd_common.py` L153–157 で 9/1 に確認・Codex 指摘）。バイパスが運ぶのはタスク内定数（DC）なので、**I ダイヤルは主 endpoint を表現経由では一切動かせず、力学経由（境界での DC 再フィットが $W$・$b$ を揺らす量の増減）でしか動かせない**。I 主効果が E-drift に出た場合、それは表現力のアーティファクトではありえず機構的である — 解釈はむしろ鋭くなる
- **E-onset**（10 task 移動中央値で `log10 unfit` が −1.0 を初超えする task。感度 −0.5/−1.5）は **REPORT_ONLY** に降格し、`onset.csv` に全腕分を出す。R-ext（§6.4）の記述にも使う

### 6.2 対比

CI はすべて seed 水準 paired percentile bootstrap 95%。

| 因子        | 対比（E-drift・E-level それぞれ）                            |
| --------- | --------------------------------------------------- |
| **M 主効果** | (i) `iM − im`、(ii) `IM − Im`                        |
| **I 主効果** | (i) `IM − iM`、(ii) `Im − im`                        |
| **交互作用**  | $(\text{IM} - \text{iM}) - (\text{Im} - \text{im})$ |

### 6.3 主判定（決定木・この順で評価）

等価限界 $\Delta = 0.15$ dex、$\Delta_{\rm int} = 0.50$ dex（D5・`channel_2x2_0901` D4 からの継承値であり、**別サンプル・別 endpoint での較正の移送**。Issa 上書き可）。主判定は **E-drift** で行い E-level を必ず併記する。

1. 交互作用の CI が $\pm\Delta_{\rm int}$ を外れる → **`INTERACTION_DOMINATES`** — 主効果を単独で読まない
2. M 主効果 2 本とも帯の外・I 主効果 2 本とも帯の内 → **`MU_DOMINANT`**
3. I 主効果 2 本とも帯の外・M 主効果 2 本とも帯の内 → **`IDENT_DOMINANT`**
4. 両方とも帯の外 → **`BOTH_MATTER`**
5. 両方とも帯の内（CI が $[-\Delta,+\Delta]$ に**収まる**） → **`NEITHER_MATTERS`**（タイトな null。§1.3 のとおり強い情報）
6. 同一因子の 2 本が食い違う → `EFFECT_LEVEL_DEPENDENT` — 主効果として書かずセル別に書く
7. CI が帯をまたぐ → `INCONCLUSIVE_WIDE` — 「タイトな null」と混ぜない

### 6.4 登録副判定 R-ext（`im` vs `im_nowd`・**死の主張であって機能の主張ではない**）

問い: **b-WD は `Aexact` 型の全滅を止めるか。** endpoint は**全滅到達**（layer1 **`strict_dead_frac` = 1.0** に 5M までに到達したか。seed 単位の二値・Clopper–Pearson）。**`alive = 0`（σ 退化ユニットも非 alive に数える別定義）では判定しない** — 飽和常時発火（C3 型・勾配は生きている）を絶滅と誤分類しうるため。両定義の食い違い件数を併記する（9/1 Codex 裁定・committed `Aexact` の 10/10 は `strict_dead` 定義で読める側）。参照: `Aexact` は 10/10 到達（task 154–454・committed）。

| 条件                                        | ラベル                                       |
| ----------------------------------------- | ----------------------------------------- |
| `im_nowd` 到達 ≥ 8/10 **かつ** `im` 到達 ≤ 1/10 | **`BWD_PREVENTS_EXTINCTION`**             |
| `im` 到達 ≥ 8/10                            | **`EXTINCTION_PERSISTS`**（λ=1e-3 では止まらない） |
| その他                                       | `PARTIAL_RESCUE`（到達数と E-onset を記述）        |

併記（REPORT）: 両腕の終端 `strict_dead_frac` paired 差・E-level・E-onset。**このラベルを `LOP_CURED` と読み替えない**（dead と機能の解離の前例 2 件）。`im_nowd` が想定どおり全滅すれば、`im − im_nowd` の E-level 差が「v1 設計で現象2 が運んでいた量」の見積もりになる（REPORT）。

### 6.5 副判定（その他）

| ID     | 内容                            | ラベル                                                                        |
| ------ | ----------------------------- | -------------------------------------------------------------------------- |
| **D**  | 各腕 `strict_dead_frac` B02→B10 | REPORT_ONLY。主判定に使わない                                                       |
| **R**  | B10 の活性 `eff_rank`            | REPORT_ONLY                                                                |
| **U**  | `u_norm`・`bypass_share`       | REPORT_ONLY。I+ で `bypass_share` ≈ 0 なら S-op と突合                            |
| **A**  | `std_anchor` と要因 4 セルの差       | REPORT_ONLY。引き直し＋動的 µ の大きさの目安                                              |
| **台帳** | $M$・$B$・全ユニット中央値 $b$ の推移      | REPORT_ONLY。b-WD が $M$ に触らないことの再確認、平衡 $\lambda b^\ast = -(\tau+R)$ との整合は事後 |

### 6.6 集計規約

`mean(log10 unfit)`（seed ごとブロック内 50 点の $\log_{10}$ 平均）を主とし `log10(mean unfit)` は出力のみ。床 `1e-16`・`floor_frac` を出力。paired percentile bootstrap `B=20000`・**`bootstrap_seed = 20260911`**（既用 `20260829`/`20260902`/`20260903`/`20260904`/`20260906` を流用しない）。studentized 併算＋退化ガード（`degenerate_se_tol=1e-15` 等）。二値は Clopper–Pearson。

---

## 7. 実行前・実行中サニティ

| ID                                        | 内容                                                                                                                                                                                                                                                                                                                                                                                         | 失敗時                           |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------- |
| **S-pair** ★                              | 6 腕が seed ごとに init（$W,b,v,c,\mathbf u$）・教師・$(\text{flip}_t,\text{rnd})$ 実現で bitwise 一致。可視入力差し替え・バイパス・`wd_b` が乱数を消費しないことを 30k step・1k 格子 replay で確認                                                                                                                                                                                                                                         | 本走禁止                          |
| **S-seq**                                 | 6 腕で教師出力系列が bit 一致                                                                                                                                                                                                                                                                                                                                                                         | 本走禁止                          |
| **S0** ★                                  | **`im_nowd` を `center_oracle_0831` の `Aexact` に対して 30k step replay**（generator 設定は `results/center_oracle_0831/config_used.yaml` から読む）。`unfit`・`eval_loss_exact`・`strict_dead_frac` 軌跡の一致で実装同値性を示す。**本走（新系列）の腕の正しさは保証しない**                                                                                                                                                                 | 本走禁止                          |
| **S1**                                    | $\lambda=0$ 腕（`im_nowd`・`std_anchor`）が WD コード経路を通しても無 WD 実装と bit 一致                                                                                                                                                                                                                                                                                                                        | 本走禁止                          |
| **S2**                                    | 同一状態から $\lambda=0$ と $\lambda>0$ で 1 step 進め、(i) $W,v,c,\mathbf u$ が bitwise 一致、(ii) $b$ の差が厳密に $-\eta\lambda b_{\rm before}$、(iii) `wd_b` 参照が隠れ層 bias の更新行のみ                                                                                                                                                                                                                              | 本走禁止                          |
| **S-bypass**                              | `code`$\equiv\mathbf 0$ の 4 腕で $\mathbf u$ が全 checkpoint で厳密 0・バイパス無し実装と bit 一致                                                                                                                                                                                                                                                                                                            | 本走禁止                          |
| **S-mu**                                  | M− 腕の可視入力平均が厳密 0、M+ 2 腕の $\lVert\mu_{\rm vis}\rVert$ 一致                                                                                                                                                                                                                                                                                                                                    | 本走禁止（**構成上ほぼ恒真・発見として扱わない**）   |
| **S-op** ★（**9/1 改2・切替プローブ / 本走 regime**） | 旧定義（定常単一タスク・`unfit` 比較）は**二重に成立不能** — バイパスはタスク内定数で $c$ と冗長、かつ `unfit` は DC 盲（Codex 指摘・committed 実装で確認）。改定: **$T=10^4$（本走と同一 regime）・150 境界の切替プローブ**で `Im` 構成と `code` ゼロ化対照を同一 seed・同一乱数で走らせ、**各境界直後 100 step 窓の `eval_loss_exact`（DC 感応の素の MSE）**の平均を**最後の 50 境界**で取り、paired 差（`Im` − 対照）の 95% CI 上端 < 0（10 seed・percentile bootstrap）。`u_norm` の成長と**同定診断**（採点境界で「その bit が過去に何回反転済みか」）を併記 | **失敗なら I+ セルを無効と宣言し主判定を出さない** |
| **S-freeze**                              | M− 腕で可視 flip 15 座標が恒等ゼロ→ $W$ 該当列が init のまま。$\kappa = \lVert w_{\rm free}\rVert_1/\lVert w_{\rm free}\rVert_2 \in [1,\sqrt5]$ は rnd 5 座標定義で不変                                                                                                                                                                                                                                               | 本走禁止                          |
| **S3**                                    | 壁恒等式（$\hat p=0 \iff \beta+\kappa\le0$）・1/32 量子化・$\kappa$ 閉形式・独立実装一致（1e-10）                                                                                                                                                                                                                                                                                                                 | 本走禁止                          |
| **S-iso / S-cap**                         | 隔離の bit 一致・除外上限の作動                                                                                                                                                                                                                                                                                                                                                                         | 本走禁止                          |
| **S-floor**                               | 6 腕・B02/B10 とも `floor_frac = 0`（**b-WD で unfit が改善する側の床割れに注意**）                                                                                                                                                                                                                                                                                                                            | フラグ＋ E-level のみで報告            |
| **S-ceiling**                             | 要因 4 セルの B02 最大差 > 3 dex → `CEILING_CONTAMINATED`。`im_nowd` の天井（`log10 unfit ≥ −0.05`）到達 seed 数も記録                                                                                                                                                                                                                                                                                         | フラグ＋読み替え                      |
| **S-count**                               | ブロック内 task 末点 50 個・`flip_state` 変化回数＝境界個数（教訓⑭）                                                                                                                                                                                                                                                                                                                                             | 本走禁止                          |

### 7.1 S-op の regime をなぜ本走に合わせたか（9/1 改2・凍結前）

改定版の初稿は $T=1{,}000$・30 境界・最後の 20 境界で採点だったが、**プローブがバイパスの能力ではなく $u$ の同定の薄さを測ってしまう**ことが実装検討（Codex）で分かった。機構は次のとおり。

> $u_k$ は**現タスクの DC の分け前**を映すだけで、**教師 DC の「変化」の符号は映さない**。bit $k$ が両状態で観測されて $u_k$ が同定されるまで、境界で自動的に起きる跳ね $\pm u_k$ は符号が当てにならず、境界直後の loss をむしろ悪化させうる。

30 境界では 15 bit あたりの事前反転が**平均 1.36 回**、**採点境界の 28% はその走で一度も反転していない bit** だった（学習なしの入力系列掃引で確認）。加えて $T=1{,}000$ は本走（$T=10^4$）の 1/10 で、タスク内に $u$ が DC を合わせる時間そのものが足りない。**$T$ を本走と一致させ境界を 150（採点は後半 50）に増やすと、事前反転は平均 8.49 回・初見 bit 0%** になる。所要は 1.51M step・2 腕で約 5 分。

**したがって S-op の FAIL は「バイパスが情報を運べない」を意味する**（同定不足という対抗仮説を設計で潰した）。同定診断は毎回 `s_op.json` に出し、万一薄い場合に切り分けられるようにする。

ゲート系は `results/_gate_ident_mu_2x2_0901/` に JSON 保存。PASS でなければ本走は起動しない。

---

## 8. 事前予測（**記入済み: Issa・2026-09-01・チャット `可識別性2x2_0901`**）

> 「わからない」は妥当な答えである。

**格**: 本走の数値は一切見ていない。ただし §1.2 の改稿理由（`center_oracle_0831` の committed ログの事後再計算 = `Aexact` の死が全数 b 経由）は**回答前に共有されている**。したがって項目 3 は「独立に立てた予言」ではなく「**その再計算と同じ向きの予言**」である。当たった場合の格はそこまで。

> **改定記録（9/1・凍結前・回答後）**: 項目 4 の回答時点の S-op は旧定義（定常単一タスク・`unfit` 比較）だった。同日、実装検討（Codex）で旧定義が構造的に通らないこと（バイパスが $c$ と冗長＋`unfit` の DC 盲）が判明し、S-op を切替プローブへ改定した（§7）。**問いの中身（バイパスは使える情報を運ぶか）は不変**なので回答「通る」をそのまま充てる。Issa が改定版に対して回答を変える場合はここを上書きする。
> **改2（同日・同じく凍結前）**: 切替プローブの regime を本走に合わせた（$T=10^4$・150 境界・後半 50 で採点。§7.1）。初稿の $T=1{,}000$・30 境界では $u$ の同定が薄く、バイパスの能力ではなく同定不足を測ってしまうため。**問いの中身も判定規則の形も不変**なので回答「通る」は引き続き有効。**むしろ改2 によって「FAIL = バイパスが情報を運べない」と読める設計になった**ので、この回答が負うリスクは改定前より大きい。

1. 主判定はどれになると思うか（`MU_DOMINANT` / `IDENT_DOMINANT` / `BOTH_MATTER` / `NEITHER_MATTERS` / `INTERACTION_DOMINATES`）:
   → **`MU_DOMINANT`**（可識別性の主効果 2 本は等価帯 $\pm\Delta$ に収まる、の意味を含む）
2. **E-drift（劣化量）**の要因 4 セル順位（劣化が大きい順）。**E-level 順位ではなく drift 順位を聞くのは、LoP は劣化であり、水準はセル別の収束深さ（精度）を混ぜ込むため**（9/1 Issa 指摘・`channel_2x2_0901` の `LADDER_INVERTS` の教訓）:
   → **「µ が強い」型** ＝ M+ の 2 セル（`iM`・`IM`）が M− の 2 セル（`im`・`Im`）より大きく劣化する。M+ 内・M− 内の順位は未指定
3. R-ext: b-WD（λ=1e-3）は `im` の全滅を止めるか（`BWD_PREVENTS_EXTINCTION` / `EXTINCTION_PERSISTS` / `PARTIAL_RESCUE`）:
   → **`BWD_PREVENTS_EXTINCTION`**
4. S-op（バイパスの操作チェック）は通るか:
   → **通る**
5. この予測が外れたとき、[[中心主張v4.2草案_0830]]・[[現象2主張v1_0901]] のどこを書き直すことになるか:
   → **わからない**（[[LR_A1_spec_0901]]・[[柱3定用量5M_spec_0830]] §7 と同じく、妥当な回答として記録する）

### 8.1 予測 2 と 3 を合わせると何を賭けているか（**凍結前に明示・Claude の注記**）

予測 2（M− 側は健全）と予測 3（µ≡0 のセルは b さえ縛れば全滅しない）を同時に認めると、次を主張したことになる。

> **µ→0 端の全滅（`Aexact`・登録判定 `ORACLE_INCREASES_DEATH`・非単調用量反応の右端）は µ の話ではなく、丸ごと b の話だった。**

これは [[現在地]] 穴12（µ の用量反応は非単調・最優先）と [[吸収不等式の命題化_0824]] §6 の「床が天井だった」という記述を、**書き直す側に賭ける**予測である。柱1 の非単調性の扱いが変わる。

- **当たれば**: 非単調性という穴が消えて µ の用量反応は単調側へ整理される（少なくとも b 拘束下では）
- **外れれば**（`EXTINCTION_PERSISTS`）: λ=1e-3 の復元力に**上限がある**ことの初の反例となり、[[現象2主張v1_0901]] C4「復元力はタダ」にスコープ条件が付く。`Aexact` は EMA 版と違い残差ジッタ（$\lVert\mu_{\rm res}\rVert\approx0.075$）もタスク内復活（`ABSORPTION_BROKEN_BY_EMA` の 25,723 件）も持たないので、centered 用に較正された λ が足りない可能性は実在する

**どちらに転んでも既存の主張のどこかが動く。** 予測 5 が「わからない」であることと矛盾しない —— 動く先の候補は上の 2 つだが、どちらの文言をどう直すかは結果を見るまで決めない。

---

## 9. 引いてはいけない線

1. **主判定の読みは「b 拘束下（λ=1e-3）」限定。** 拘束なしの世界の I/M 主効果は測っていない（橋は `im_nowd` と committed `Aexact` まで）
2. **I+ を「可識別性あり」と無条件に書かない**（§3.3。「ゲートに触れない経路での可識別性」）。per-unit 可識別性は µ と分離不能（補題）
3. **要因 4 セルは「境界ごとの µ の引き直し」を持たない**（§3.4）。本走の µ は静的な壁
4. **`BWD_PREVENTS_EXTINCTION` を `LOP_CURED` と読み替えない**（R-ext は死の主張。dead と機能の解離の前例 2 件）
5. **`strict_dead` を主判定に使わない**（R-ext の全滅到達は明示的にスコープした例外で、死についてのみ語る）
6. **λ=1e-3 は移送値**（較正元: centered・hidden `[100,100]`・`bias_wd_pilot_0901`）。**結果を見て選び直さない。** std 側の最適 λ 非単調の兆候（[[現象2主張v1_0901]] 穴4）はスコープ条件として書く
7. **S-mu・「`im_nowd` が全滅した」を発見として引かない**（前者は恒真、後者は committed `Aexact` の再現）
8. **`std_anchor` は要因セルではない**（縮退）。4 セルとの差を主効果として読まない
9. **本走は µ の用量を振らない**（[[現在地]] 穴12 は閉じない）
10. **スコープ**: condA・幅100・hidden `[100]`・$T=10^4$・batch=1・lr=0.01・plain SGD・5M・λ=1e-3。多層の µ 再生成は外

---

## 10. 成果物と commit の順

```
specs/spec_ident_mu_2x2_0901.md     ← 単独で先に commit（事前登録の成立点）
configs/ident_mu_2x2_0901.yaml
src/ident_mu_2x2_0901.py
results/_gate_ident_mu_2x2_0901/    ← S-pair / S-seq / S0 / S1 / S2 / S-bypass / S-mu / S-op / S-freeze / S3 / S-iso / S-cap
results/ident_mu_2x2_0901/
    verdict.csv  summary.md  paired_endpoints.csv  onset.csv  exclusions.csv
    task_end_metrics.csv  block_levels.csv  run_sanity.json  provenance.json
    fig_ident_mu_2x2.png
```

commit は **spec 単独 → config＋実装 → 結果** の 3 段。各段で `git ls-remote origin refs/heads/main` により自分の commit hash がリモートに載っていることを確認し、出力を報告に貼る。**実装ノート（config の `implementation_notes` を含む）を spec より先に、または同時に commit しない** — 1 段目は spec 単独である（9/1 時点で repo に `ident_mu` 系ファイルは未 commit・順序は無傷であることを確認済み）。

---

## 11. spec 前チェックリスト（[[9月運用_0901]] §3-1）

| 項目                           | 対応                                         |
| ---------------------------- | ------------------------------------------ |
| 新 seed 群は `generator_offset` | §3.1（`20260910`）                           |
| 総和と率の両方                      | §6.1（E-drift＋E-level）＋ R-ext は二値到達         |
| 等価限界 Δ                       | §6.3（0.15 / 0.50 dex・移送の出所明記）              |
| 二値 endpoint の飽和              | R-ext は参照 10/10 で飽和しない側を検定。主 endpoint は連続量 |
| studentized fallback         | §6.6                                       |
| 窓ラベル                         | §5（B02/B10）                                |
| マスク個数検算                      | S-count                                    |
| step 0 過渡                    | 構成的 µ のため該当なし                              |
| 床の継承元                        | §6.6（1 層系 `1e-16`）                         |
| ロガー相乗り                       | §4                                         |

---

## 12. 本走が閉じるもの／閉じないもの

**閉じる**

- [[論点マップ]] §20 の可識別性交絡を、**b 拘束下・ゲートに触れない経路**という限定つきで割る
- 柱3 の $\mu^\ast$ の読み（µ か可識別性か）に材料を与える
- [[現象2主張v1_0901]] §5 穴1、および **穴 5 系の新材料**（b-WD が `Aexact` 型全滅を止めるか＝ R-ext）
- v1 設計で現象2 が運んでいた量の見積もり（`im − im_nowd`・REPORT）

**閉じない**

- **b 拘束なしの世界での I/M 主効果**（v1 の問いそのもの。橋のみ）
- per-unit 可識別性の寄与（補題により µ と分離不能）
- 境界ごとの µ の引き直し（要因セルは持たない）
- µ の用量非単調性・層2 の µ 再生成・$r$/$\Sigma$ 縮退・外的妥当性・std 最適 λ

---

> **帰趨バナー（2026-09-01 追記・本文は起草時のまま）。** 事前登録は repo `specs/spec_ident_mu_2x2_0901.md` の単独 commit **`0d7a746`** で成立（sha256 `33c05c7a…`）。本走前ゲートで **S-op が FAIL** したため、**I+ セル（`IM`・`Im`）は無効**となり要因計画の主判定は出ていない。縮小走（`iM`・`im`・`im_nowd`・`std_anchor`）の登録と S-op の帰結は [[可識別性2x2_spec_0901_追補1]]、結果は [[可識別性2x2結果_0901]]（repo `ac2727c`）。**追補1 §2 の読み（S-op 行の失敗欄が表直後の一般文に優先する）は Issa 裁定待ち**で、これが覆れば縮小走の結果は破棄になる。
