# 可識別性 2×2 追補2 — M+ 側の λ=0 腕（`iM_nowd`）の追加

状態: **実行済み 2026-09-02**（判定基準の凍結 repo `d60732b` → 事前予測 `2c0a220` → 実装 `216b961` → 結果 `bfaa514` = [[可識別性2x2結果_0901]] §5。本文は起草時のまま） / 作成: 2026-09-02 / 親: [[可識別性2x2_spec_0901]]・[[可識別性2x2_spec_0901_追補1]]
run id: `ident_mu_2x2_0901`（同一系列・`generator_offset = 20260910`）

> **格。** 本ファイルは**縮小走の結果を見たあとに書かれている**（[[可識別性2x2結果_0901]]）。したがって §3 の判定規則は**走る前に凍結**し、repo 側 `specs/spec_ident_mu_2x2_0901_addendum2.md` の**単独 commit** をもって成立点とする。それ以前に §3 を根拠として引いてはならない。

---

## 1. 何を足すか

| 腕名            | I   | M   | `wd_b` | 可視入力                           | `code`      | 役割            |
| ------------- | --- | --- | ------ | ------------------------------ | ----------- | ------------- |
| **`iM_nowd`** | −   | +   | **0**  | $[\text{flip}_0,\ \text{rnd}]$ | $\mathbf 0$ | `iM` の λ=0 対照 |

既存 6 腕は一切変更しない。腕の追加は他腕の乱数消費に影響しない（各腕は同一 generator から独立に立ち上がる。S-pair が init・入力・教師の bit 一致を保証する）ので、**`iM_nowd` は既存腕と seed 対応が取れる**。所要は 1 腕 ≈ 8 分。

## 2. なぜ要るか

[[可識別性2x2結果_0901]] §5 で、終端の全ユニット中央値 $b$ が同じ「全滅」を 2 種類に割った。

- `im_nowd` −0.969 / `std_anchor` −0.948 …… **b の自走**（現象2）で死んでいる
- `iM` **−0.000** …… b は WD で実質ゼロなのに 1.000 全滅した

ここから「**b が抑えられていても µ があれば死ぬ**」までは言えたが、**「b-WD が `iM` を救えなかった」とは言えない** —— M+ 側に λ=0 対照が無いからである。[[現象2主張v1_0901]] C4「復元力はタダ」にスコープ条件が付くかどうかは、この 1 本で決まる。R-ext（M− 側）は `BWD_PREVENTS_EXTINCTION` で完全に止まったので、**M+ でも止まるのか / 何もしないのか / むしろ早めるのか**が問いである。

## 3. 登録する判定 —— R-ext-M+

**endpoint は全滅到達 task**（`strict_dead_frac = 1.0` に初めて到達した task。5M までに到達しない seed は 501 で右打ち切り）。二値の全滅到達は**両腕とも飽和する見込み**（`iM` は既に 10/10・task 22）なので主にしない（[[運用ルール]] §5 教訓⑪）。

- 対比: **`iM` − `iM_nowd`**、seed 対応・percentile bootstrap 95% CI・`B = 20000`・**`bootstrap_seed = 20260913`**（既用を流用しない）
- 判定は**符号ベースのみ**

| 条件                      | ラベル                                         |
| ----------------------- | ------------------------------------------- |
| CI 下端 > 0（`iM` の方が遅く死ぬ） | **`BWD_DELAYS_EXTINCTION_UNDER_MU`**        |
| CI 上端 < 0               | **`BWD_ACCELERATES_EXTINCTION_UNDER_MU`**   |
| CI が 0 を跨ぐ              | **`BWD_EFFECT_NOT_DISTINGUISHED_UNDER_MU`** |

> **★ 等価限界は登録しない。** v2 で $\tau$ 側の $\Delta$ は落としてあり、ここで新たに較正する材料も無い。したがって **3 番目のラベルは「タイトな null」ではなく「区別できなかった」である**。[[引用禁止]] の「タイトな null と `INCONCLUSIVE_WIDE` を混ぜない」をこの副判定にも適用する。**「b-WD は µ 下では効かない」とは書けない。**

**打ち切り**: どちらかの腕で 3 seed 以上が打ち切られたら `ONSET_CENSORED` を併記し、点推定を単独で引かない。

**併記（REPORT_ONLY）**: 二値の全滅到達数と Clopper–Pearson、E-level B10、E-drift、全ユニット中央値 $b$（B10）。

## 4. 引いてはいけない線

1. **`iM_nowd` は要因計画のセルではない。** M+ 側のアンカーであって、2×2 の主判定には入らない（そもそも主判定は S-op FAIL で出ていない）
2. **R-ext（M−）と R-ext-M+ を同じ格で並べない。** 前者は二値・Clopper–Pearson・しきい値つきの登録判定、後者は符号のみの登録判定で**等価限界を持たない**
3. **`BWD_EFFECT_NOT_DISTINGUISHED_UNDER_MU` を「効果なし」と読み替えない**（§3 の★）
4. **死の主張である。** `LOP_CURED` と読み替えない（追補1・本体 §9-4 と同じ）
5. スコープは本体 §9-10 のまま（condA・幅100・hidden [100]・$T=10^4$・batch=1・lr=0.01・5M・λ=1e-3）

## 5. 事前予測（**Issa が書く。空欄のまま走らせてもよいが、その場合この走に予言の格は付かない**）

> [[運用ルール]] §2 が要求するのは**判定基準の事前凍結**であり、それは §3 で満たしている。§8 型の予測は本プロジェクトの強い慣行だが、本追補では空欄でも走らせられる。**空欄のまま回した場合、結果は「登録済み判定基準による測定」であって「予言の的中／外れ」ではない。**

1. R-ext-M+ はどれになると思うか（`BWD_DELAYS_EXTINCTION_UNDER_MU` / `BWD_ACCELERATES_EXTINCTION_UNDER_MU` / `BWD_EFFECT_NOT_DISTINGUISHED_UNDER_MU`）:
   → **`BWD_DELAYS_EXTINCTION_UNDER_MU`**（2026-09-02・Issa・走行前に記入）。µ が立っていても b-WD は死を遅らせる ＝ C4「復元力はタダ」が M+ でも生きる、という側に賭ける。M− 側の完全な救済（10/10 → 0/10）の弱い版が出る想定
2. 外れたとき [[現象2主張v1_0901]] C4 のどこを書き直すことになるか:
   → （未記入）

> **格。** 本追補の判定基準（§3）は repo `d60732b`（spec 単独 commit）で凍結され、**この予測はその後・走行前**に記入された。数値は一切見ていない（`iM` の task 22 と M− 側 R-ext の結果は [[可識別性2x2結果_0901]] で既知であり、それを踏まえた予測である）。

## 6. 閉じるもの／閉じないもの

**閉じる**: [[可識別性2x2結果_0901]] §5 の「言えないこと」＝ M+ 側で b-WD が何をしているか

**閉じない**: I 主効果（S-op FAIL のまま）／per-unit 可識別性／境界ごとの µ の引き直し／λ の最適性（移送値のまま・結果を見て選び直さない）
