# チャネル遮断 2×2 — 事前登録 spec（起草）

状態: **実行済み** / 作成: 2026-09-01 / 更新: 2026-09-01 / run id: `channel_2x2_0901`
親: [[現象2_b経由の死_0831]] §7 ／ [[9月運用_0901]] W1
前段（いずれも事前登録・committed）: `bias_wd_0901`（centered 側・`BIAS_WD_PROTECTS`）、`bias_wd_std_seediso_0901`（std 側・`INCONCLUSIVE_PARTIAL`）
関連: [[kubo_review_v4.2_0831]] §1（吸収域 × 2 チャネル）、[[命題リスト]] Q14・Q19・Q26、[[論点マップ]] §20・§23

> **このファイルの格。** §0 の裁定 4 点と §8 の事前予測 5 項は 2026-09-01 に Issa が確定させた。**残るのは repo 側 `specs/spec_channel_2x2_0901.md` の単独 commit のみで、それが事前登録の成立点である。** commit 前に本ファイルの数値・判定基準を判定の根拠として引いてはならない。

---

## 0. 裁定（2026-09-01・Issa・チャット `2x2_spec_0901`）

| # | 裁定事項 | **決定** | 帰結 |
|---|---|---|---|
| **D1** | seed 群 | **新規系列**（`generator_offset = 20260905`。既用は `0` / `20260830` / `20260831`） | `--seeds` の変更だけでは同一系列になるので不可（T0b の事故）。既存 0–9 との bit 一致は取れないので、S0 は旧 offset での別 replay として回す（§7） |
| **D2** | オラクル中心化のセルを足すか | **足さない**（4 セルのまま） | [[論点マップ]] §20 の可識別性交絡はオラクルでも閉じないため、§9-2 のスコープ条件として書く。`center_oracle_0831` のオラクル腕は 10 seed すべて `strict_dead`・`unfit` とも 1.000 で、両遮断セルが床に張り付く懸念もある |
| **D3** | 走枠 | **即時実行**（Codex へ委譲） | [[9月運用_0901]] W1 の枠に入れる。N1（一行「吸収域 × 到達 2 チャネル」の提出）に対する直接の検証にあたる |
| **D4** | 等価限界 $\Delta$ | **$\Delta = 0.15$ dex**（Issa 指定域 0.15–0.20 の**保守側**を Claude が採った。上書き可） | 継承した 0.10 dex は `bias_wd_0901` §5.1(b) の**静的水準**用で、劣化量には未登録だった。0.15 は既存 seed 群 0–9 の両遮断セルの CI 幅 0.112 から較正した値で、**D1 により較正元と本走は別サンプルになる**（λ グリッドをパイロットから決めたのと同じ構造）。較正の出所は §6.2 に明記する |

---

## 1. 問い

[[kubo_review_v4.2_0831]] §1 の提案は、吸収域を作るのは ReLU のゲートであり、**µ と $b$ はそこへの到達を駆動する 2 本のチャネル**である、という形をしている。消灯条件は 1 本しかない（[[吸収不等式の命題化_0824]] = [[命題リスト]] Q26）:

$$\hat p_i = 0 \iff \beta_i \le -\kappa_i,\qquad
\beta_i = \frac{s_i}{\sigma_i},\quad s_i = w_i\cdot\mu + b_i,\quad
\kappa_i = \frac{\lVert w_{i,\rm free}\rVert_1}{\lVert w_{i,\rm free}\rVert_2}$$

$\mu$ と $b$ は $s_i$ の 2 項として入る。**2 チャネル説が正しければ、両方を止めた系では壁への到達が止まる。** 本走はこれを介入で割る。

- **$b$ チャネルの遮断** = 隠れ層 bias にだけ weight decay を掛ける（`wd_b`。$W$・$v$・出力バイアス $c$ には掛けない）
- **$\mu$ チャネルの遮断** = 層入力の EMA 中心化（`A_layer_input_centering`、`center_alpha=0.01`、両層）

この 2 因子を直交させ、**交互作用込みで 1 走 1 spec で判定する**。

### 1.1 いまの証拠が要因計画になっていない理由（4 点）

4 セルは既に走っているが、要因計画としては使えない。

1. **別 spec・別 run・別 bootstrap seed**（`bias_wd_0901` は `20260902`、`bias_wd_std_seediso_0901` は `20260904`）
2. **seed 集合が揃っていない。** `S_main`（std × $b$ 遮断）は seed 7 が step 3,381,000 で発散し除外の n=9、centered 側は n=10。9/1 のチャットで並べた表の +9.10 は `S_sub` 側の対応集合（seed 2 除外）、+2.98 は `S_main` 側の対応集合（seed 7 除外）で、**分母の seed 集合が違う 2 つの数を同じ表に並べていた**
3. **交互作用が事前登録されていない**
4. **B02 水準がセル間で桁で違う**のに endpoint が劣化量だけになっている（「落ちる余地」の汚染。§6.1）

### 1.2 他の 2×2 と混同しない

この repo には現在 3 つの別々の 2×2 がある。**本走は (C) である。**

| | 因子 | 状態 |
|---|---|---|
| (A) 積の構造 | µ/Σ の有無 × 片道ゲートの有無 | [[中心主張v4.2草案_0830]]。**別配置の 4 セルであって要因計画ではない** |
| (B) §20 の交絡割り | µ ダイヤル × タスク可識別性ダイヤル | [[論点マップ]] §20・未走・未設計 |
| **(C) 本走** | **$b$ チャネル遮断 × µ チャネル遮断** | 事後配置が存在。**これを要因計画に変換する** |

---

## 2. 既知の 4 セル（**すべて私の事後集計。登録判定ではない**）

出所は `results/bias_wd_0901/block_levels.csv` と `results/bias_wd_std_seediso_0901/block_levels.csv` の `mean_log10_unfit` 列、ブロック `2`（= B02・task 51–100）と `10`（= B10・task 451–500）。**4 セル共通の完走 seed [0,1,2,3,4,5,6,8,9]（n=9）に揃えて再集計した。** `verdict.csv` の行ではないので、[[運用ルール]] §2 に従いファイル名と行の識別子をここに明記する。

| セル | 既存腕名 | $b$ 遮断 | µ 遮断 | B02 | B10 | 劣化 B10−B02 |
|---|---|---|---|---:|---:|---:|
| 無介入 | `S_none` | — | — | −11.169 | −1.849 | **+9.320** |
| $b$ のみ遮断 | `S_main` | 1e-3 | — | −7.306 | −4.330 | **+2.977** |
| µ のみ遮断 | `W2_Aall_none` | — | Aall | −2.615 | −2.218 | **+0.397** |
| 両遮断 | `W2_Aall_main` | 1e-3 | Aall | −2.726 | −2.783 | **−0.057** |

4 セルとも hidden `[100,100]`・床 `1e-23`・`floor_frac = 0.0000`（床に触れた点はゼロ）。

**dead と第1層活性 `eff_rank`**（同ファイル・同ブロック）:

| セル | L1 dead B02→B10 | L2 dead B02→B10 | L1 `eff_rank` B02→B10 |
|---|---|---|---|
| 無介入 | 0.638 → 0.780 | 0.562 → 0.891 | 14.03 → 10.21 |
| $b$ のみ遮断 | 0.561 → 0.682 | 0.404 → 0.530 | 15.15 → 13.79 |
| µ のみ遮断 | 0.030 → 0.020 | 0.114 → 0.541 | 11.28 → **3.27** |
| 両遮断 | 0.001 → 0.000 | 0.001 → 0.000 | 14.67 → **15.89** |

### 2.1 ★ 事後の数字は「加法的な分解」を支持していない（9/1 の言い方の訂正）

9/1 のチャットでは「std の 9.1 dex のうち約 2/3 が $b$ 自走、約 1/3 が µ の壁」という**加法的**な言い方をした。上の表で交互作用を取ると、

- 劣化（B10−B02）: $b$ 遮断の効果は std で **−6.343**、centered で **−0.454**。**交互作用 +5.889**
- 到達水準（B10）: $b$ 遮断の効果は std で **−2.481**、centered で **−0.565**。**交互作用 +1.916**

**単独遮断の効果の和（−6.343 と −8.923）が全体（−9.377）を超えている**ので、2 チャネルは加法的ではなく**強く劣加法**である。したがって事前登録すべき仮説は「劣化量が 2 成分の和に分解できる」ではなく、**「両方を止めたときにだけ止まる（両方が必要）」という必要性の形**である。§6.2 はこの形で書く。

**「2/3 : 1/3」は主張に載せない**（§9-6）。

---

## 3. 設計

- condA: `m=20`, `f=15`, teacher width 100, $\beta=0.7$, $T=10{,}000$, batch 1
- learner: ReLU、hidden **`[100,100]`（4 セルとも深さ 2）**、plain SGD、`lr=0.01`、Kaiming uniform
- 5,000,000 step（= task 500）、seed 10 本、CPU、`OMP_NUM_THREADS=1`、checkpoint は step 0 / 5M
- 走の宿主は `mlp2_phase1_0829` の腕実行経路（`setup_arm_p1` / `train_arm_p1` / `forward_centered` / `exact_layer_record_p1`）。`wd_b` は `VecMLPL.set_weight_decay_b` で構築後に差し込む（乱数も状態も消費しない）
- **深さを 4 セルで揃える**のは、対応ペア比較を深さ内に閉じるため（深さをまたぐ paired 比較は禁止）。副産物として床が `1e-23` 一本で済む
- **`generator_offset = 20260905`**（D1・新規系列）。既用は `0` / `20260830` / `20260831`。**`--seeds` を変えるだけでは同一系列になる**（T0b の事故 → [[9月運用_0901]] §3-1）。seed は 0–9 の 10 本で、offset により実現が別になる

### 3.1 腕

$\lambda$ は前段の段階 A（`results/bias_wd_pilot_0901/grid_selection.json`）が凍結した主水準 **1e-3 のみ**。**本 spec では選び直さない**（§9-8）。用量副水準は置かない（本走の目的は用量反応ではなく直交化である）。

| 腕名 | 意味 | hidden | `centered_layers` | `wd_b` |
|---|---|---|---|---:|
| `none` | 無介入 | [100,100] | `[]` | 0 |
| `bwd` | $b$ チャネルのみ遮断 | [100,100] | `[]` | 1e-3 |
| `cen` | µ チャネルのみ遮断 | [100,100] | `[1,2]` | 0 |
| `both` | 両遮断 | [100,100] | `[1,2]` | 1e-3 |

更新式は $b \leftarrow b - \eta(g_b + \lambda b)$（**decoupled ではない素の L2 勾配**）。掛けるのは**両層の隠れ層 bias のみ**。分岐を置かず常に `gb + wd_b*b` を計算する（$\lambda=0$ 腕が WD 経路を通したうえで bit 一致することを検査可能にするため。§7 S1）。

見積もり: 深さ 2・5M・10 seed で 1 腕 ≈ 38 分（`bias_wd_0901` の実績）。4 腕で **約 2.5 時間**（逐次）。腕ごとの独立プロセス並列は可（軌道不変・`OMP_NUM_THREADS=1` 共通）。

### 3.2 seed 単位隔離（`spec_bias_wd_std_seediso_0901` §2 を継承）

`bwd` 腕は前段で seed 7 が step 3,381,000 で発散した実績がある。**同種の発散が再び起きる前提で設計する。**

- 1,000 step ごとの probe で seed ごとに `W1/W2/b1/b2/v/c` の有限性を見る
- 非有限 seed のみを停止・`seed_status/` に記録・**その seed の全時点を解析から除外**（発散前の部分軌道も使わない）
- 乱数対応を崩さないため隔離後も入力側の乱数消費は継続。停止 seed は lr=0 として有限ゼロへ quarantine
- **各腕 2/10 seed まで除外可。** 3 本目で腕を停止し `ARM_INVALID_EXCLUSION_LIMIT`
- **主比較は 4 腕の完走 seed の共通集合のみ**を使う。共通集合が 8 本未満なら `CONTRAST_INVALID_TOO_FEW_PAIRED` として主判定を出さない
- 補充・seed 追加・途中再開・rescue・winsorize は行わない

---

## 4. 記録

task 末（10,000 step ごと・step 0 を含む 501 点）に 32 パターン厳密列挙を行い、層ごとに記録する。列は `spec_bias_wd_0901` §3 の凍結セットをそのまま継承する: `strict_dead_frac` / `b_median_alive` / `B_median_alive`（$b/\sigma$）／`beta_median_alive` / `kappa_median_alive` / `sigma_median_alive` / `margin_median_alive` / `p_hat_median_alive` / `p_hat_thin_frac` / `p_hat_sat_frac` / `eff_rank` / `eff_rank_W` / `w_norm_median` / `wcos_mean` / `unfit` / `eval_loss_exact`。

**ロガー相乗り**（[[9月運用_0901]] §3-2）: 台帳の $M_i = w_i\cdot\mu$ と $B_i = b_i/\sigma_i$、分母 $\sigma_i$、`submerged_frac`、境界前後のスナップを既定で出す。**ただし主判定は §6.2 の 1 つだけ**で、相乗り列は事後ラベルでしか使わない。

---

## 5. 窓とブロック

- ブロックは 50 task 刻み。ブロック $k$ は task $50(k-1)+1 \ldots 50k$
- **B02 = task 51–100**、**B10 = task 451–500**（前段 2 走から不変）
- 10 ブロックすべてを `block_levels.csv` に出す（判定に使うのは B02・B10 の 2 つだけ）
- 実験単位は seed。ブロック内 50 点を seed 内で平均して一値にする

---

## 6. 事前登録判定

### 6.1 共主 endpoint 2 本

吸収・凍結のある系では総和と率の両方を出す（[[運用ルール]] §5 教訓⑰）。ここでは **劣化量と到達水準の両方**を出す。

| ID | 定義 |
|---|---|
| **E-drift** | `mean(log10 unfit)` の **B10 − B02**（劣化量） |
| **E-level** | `mean(log10 unfit)` の **B10**（到達水準） |

**主判定は E-drift で行い、E-level を必ず併記する。** 事後では 2 つの順位が入れ替わっている（劣化量では `both` が最良、到達水準では `bwd` 単独が −4.330 で最良）。この入れ替わりは B02 水準がセル間で桁違いであること（「落ちる余地」の汚染）に由来しうるので、**規則を先に固定する**:

- **S-ceiling（§7）** で B02 水準の 4 セル間最大差を測る。**3 dex を超えたら `CEILING_CONTAMINATED` フラグを立て、`summary.md` に「E-drift 単独では読まない」と明記する**
- E-drift と E-level で結論の向きが食い違ったら `LADDER_INVERTS` を立て、**どちらも単独で引かない**

### 6.2 主判定（3 条件の連言）

等価限界 **$\Delta = 0.15$ dex**（D4）。**出所を明記する**: 継承候補だった 0.10 dex は `spec_bias_wd_0901` §5.1(b) の**静的水準**用であり、劣化量に対して事前登録された値ではなかった。0.15 は既存 seed 群 0–9 における両遮断セルの E-drift の paired bootstrap CI 幅 0.112（Claude の事後計算・§2 と同じファイル）から、**判定可能域に入る最小の値として較正した**。D1 により較正元（offset 0）と本走（offset 20260905）は**別サンプル**である。Issa 指定域は 0.15–0.20 で、保守側（等価と宣言しにくい側）を採った。

CI はすべて seed 水準の paired percentile bootstrap 95%。

| 条件 | 内容 | しきい |
|---|---|---|
| **(a) 両遮断で止まる** | `both` の E-drift の 95% CI | $[-\Delta, +\Delta]$ に**収まる**（等価） |
| **(b) $b$ チャネルが実在** | `bwd − none` の対応差（E-drift）の CI **上端** | $< -\Delta$ |
| **(c) µ チャネルが実在** | (i) `bwd` の E-drift の CI **下端** $> +\Delta$ **かつ** (ii) `both − bwd` の対応差の CI **上端** $< -\Delta$ | 両方 |

**決定木（この順で評価する）**

1. **(a) が偽で、`both` の E-drift の CI 下端 $> +\Delta$** → `RESIDUAL_UNEXPLAINED` — 2 チャネルを両方止めても**劣化が残る**。到達チャネルが他にある
2. **(a) が偽で、`both` の E-drift の CI 上端 $< -\Delta$** → `OVERSHOOT_IMPROVES` — 両遮断は劣化を止めるだけでなく**改善させる**。「壁への到達を止めた」ではなく「別の何かを良くした」可能性を含むので、機構の主張には使わない
3. **(a) が偽で、CI が $\pm\Delta$ をまたぐ** → `INCONCLUSIVE_WIDE` — 判定不能。**「タイトな null」と混ぜない**（[[保持測定spec_0829]] の教訓）
4. **(a) 真・(b) 偽** → `MU_CHANNEL_ONLY` — $b$ 遮断は効かない
5. **(a) 真・(b) 真・(c) 偽** → `B_CHANNEL_ONLY` — $b$ 遮断だけで止まり、µ 側に残りがない
6. **3 条件すべて真** → `TWO_CHANNELS_BOTH_NECESSARY`

**注意**: (a) は等価判定であり、「CI が 0 を含む」ではなく「CI が $\pm\Delta$ に**収まる**」を要求する。1–3 の分岐は (a) の否定を**符号で割る**ためのもので、劣化残存（1）と過剰改善（2）を同じラベルに流さない。

### 6.3 副判定

| ID | 内容 | ラベル |
|---|---|---|
| **I（交互作用）** | `(bwd − none) − (both − cen)` の対応差（E-drift・E-level それぞれ） | CI が $\pm\Delta_{\rm int}$ 内 → `ADDITIVE` ／ CI 下端 $> +\Delta_{\rm int}$ → `SUBADDITIVE` ／ CI 上端 $< -\Delta_{\rm int}$ → `SUPERADDITIVE`。$\Delta_{\rm int} = 0.50$ dex |
| **L（水準の梯子）** | 4 セルの E-level 順位と、B02 水準の最大差 | REPORT_ONLY ＋ §6.1 のフラグ |
| **D（dead）** | 各セル L1/L2 `strict_dead_frac` の B02→B10 | **REPORT_ONLY。判定に使わない** |
| **R（活性 `eff_rank`）** | 第1層活性 `eff_rank` の B10。`cen` の崩壊が `both` で戻るか | 対応差 CI 下端 $>0$ → `SATURATION_PREVENTED` |
| **台帳** | $M = w\cdot\mu$ と $B = b/\sigma$ の alive 中央値・B02→B10 変化 | REPORT_ONLY。$b$ 遮断が $M$ に触らないことの再確認 |

**I は事前に劣加法が見込まれている**（§2.1 の事後値 +5.889）。したがって `SUBADDITIVE` が出ても**予測の確認であって独立の証拠ではない**旨を `summary.md` に明記する。

### 6.4 集計規約

- 主 endpoint は **`mean(log10 unfit)`**（seed ごとにブロック内 50 点の $\log_{10}$ を平均）。`log10(mean unfit)` も出力するが**判定には使わない**
- 床は **`1e-23`**。`mlp2_phase1_0829` の S6 較正を継承し、**本走では再較正しない**。各腕の `floor_frac` を `block_levels.csv` に出す
- CI は paired percentile bootstrap、`B = 20000`、**`bootstrap_seed = 20260906`**（既用: `20260829` / `20260902` / `20260903` / `20260904`。流用しない）
- studentized も併算し、退化検出（`degenerate_se_tol=1e-15`、`degenerate_frac_max=0.01`、`degenerate_width_ratio_max=100`）を `ci_degenerate` 列に出す。**percentile が主**
- 二値割合の CI は Clopper–Pearson

---

## 7. 実行前・実行中サニティ

| ID | 内容 | 失敗時 |
|---|---|---|
| **S-pair** ★新規 | **要因計画の前提そのもの。** 4 腕が seed ごとに init（$W$・$b$・$v$・$c$）・教師・入力実現で **bitwise 一致**すること。centering も `wd_b` も乱数を消費しないことを 30k step・1k 格子の replay で確認する | **本走禁止** |
| **S0** | `none` と `cen` を既存の committed 腕（`L2_none` / `L2_Aall`）に対して 30k step replay し、`unfit`・`eval_loss_exact`・各層 `strict_dead_frac` が一致。**D1 で新規系列を採ったので、本走の腕とは一致しない。** `generator_offset=0` に切り替えた別 replay として回し、実装が既存 harness と同値であることだけを示す | 本走禁止 |
| **S1** | $\lambda=0$ が WD コード経路を通しても無 WD 実装と bit 一致 | 本走禁止 |
| **S2** | 同一状態から $\lambda=0$ と $\lambda>0$ で 1 step 進め、(i) $W$・$v$・$c$ が bitwise 一致、(ii) $b$ の差が厳密に $-\eta\lambda b_{\rm before}$、(iii) `wd_b` を参照する更新行が `self.bs[i]` の行だけ | 本走禁止 |
| **S3** | 壁恒等式（$\hat p=0 \iff \beta+\kappa\le0$）、$\hat p$ の 1/32 量子化、第1層 $\kappa$ の閉形式一致、凍結済み `exact_layer_record_p1` との独立実装一致（許容 `1e-10`）。**$\beta\le-\sqrt5$ の十分条件は第1層限定で、第2層には適用しない**（教訓⑯） | 本走禁止 |
| **S-iso** | seed 1 に人工的に非有限を注入し、隔離後 100 step で他 9 seed の全状態が対照 runner と bitwise 一致 | 本走禁止 |
| **S-cap** | 人工的に 3 seed を非有限化し、3 本目で `ARM_INVALID_EXCLUSION_LIMIT` になる | 本走禁止 |
| **S-floor** ★新規 | 4 腕・B02/B10 とも `floor_frac = 0` を確認する。**床に触れた点があれば E-drift は無効**とし、E-level のみで報告する | フラグ＋読み替え |
| **S-ceiling** ★新規 | B02 水準の 4 セル間最大差を記録。**> 3 dex なら `CEILING_CONTAMINATED`**（§6.1） | フラグ＋読み替え |
| **S4** | 非有限ガード。`probe_every=1000`。§3.2 の隔離規則で処理 | seed 単位で隔離 |
| **S-count** | ブロック内 task 末点が 50 個ちょうどであること、`flip_state` の変化回数と境界個数が一致すること（教訓⑭） | 本走禁止 |

S-pair / S0 / S1 / S2 / S3 / S-iso / S-cap は本走前のゲートとして実行し、`results/_gate_channel_2x2_0901/` に JSON で保存する。**保存済みゲートが PASS でなければ本走は起動しない。**

---

## 8. 事前予測（**Issa が書く。空欄のまま commit しない**）

> 「わからない」は妥当な答えである。特に 3 は柱1 と柱2 で予測が割れうる。

**記入: Issa（2026-09-01・チャット `2x2_spec_0901`）。** 5 項すべて記入済み。3 は初回 `ADDITIVE` から改訂（§8.1 に経緯と格の限定）。

1. 主判定はどれになると思うか（`TWO_CHANNELS_BOTH_NECESSARY` / `RESIDUAL_UNEXPLAINED` / `MU_CHANNEL_ONLY` / `B_CHANNEL_ONLY`）:
   → **`TWO_CHANNELS_BOTH_NECESSARY`**
2. `both` の E-drift は等価限界 $\pm\Delta$ に収まるか（$\Delta$ は D4 で 0.15 dex に確定。回答時点の案文は 0.10 dex だったが、**より緩い側への変更なので回答の向きは変わらない**）:
   → **収まる（yes）**
3. 交互作用 I は `ADDITIVE` か `SUBADDITIVE` か:
   → **`SUBADDITIVE`（劣加法）**（8.1 の経緯つき）
4. E-level の 4 セル順位（最良から）:
   → **最良は `bwd`**（2 位以下は未指定）
5. この予測が外れたとき、[[中心主張v4.2草案_0830]] のどこを書き直すことになるか:
   → **わからない**（[[柱3定用量5M_spec_0830]] §7 と同じく、これは妥当な回答として記録する）

### 8.1 予測 3 の変更履歴と、その格（**登録前の改訂・監査記録**）

**経緯（2026-09-01・チャット `2x2_spec_0901`）。** Issa の初回回答は `ADDITIVE` だった。Claude が、予測 1（`TWO_CHANNELS_BOTH_NECESSARY` = $D_{\rm both}\approx0$）と組むと

$$D_{\rm bwd} + D_{\rm cen} \;=\; D_{\rm none} + D_{\rm both}$$

（$D$ = E-drift）により**単独遮断 2 セルの劣化の和が無介入セルの劣化に等しい**ことを意味し、既存の事後値（左辺 $2.98+0.40=3.37$、右辺 $9.32-0.06=9.26$）から見て「µ のみ遮断セルが 0.40 → 6 dex 級へ動く」級の強い予言になる、と指摘した。Issa はこれを受けて `SUBADDITIVE` に変更した。

**格の限定 3 点。**

1. **本項の改訂は §2 の事後値を見たうえで行われている。** repo 側の単独 commit 前なので事前登録としては成立するが、**「独立に立てた予言が当たった」とは書けない**。当たった場合の格は「事後配置と同じ向きの予言が再現した」までである
2. **`SUBADDITIVE` が出ても機構の証拠にならない**（§6.3 I の注記と同じ）。centered 系のセルは B02 水準が −2.6 で、std の −11.2 と桁が違う。**落ちる余地がないことだけでも劣加法は生じる**
3. **本走はこの 2 つ（真の劣加法 / 天井汚染）を分離しない。** E-drift は同じ指標で、seed 群を替えても B02 水準の構造は変わらない。分離するには汚染を補正した尺度を別に事前登録する必要があり、**本 spec はそれを行わない**（§12「閉じないもの」に該当）

したがって交互作用 I は**副判定のまま**であり、主判定（§6.2）には一切入らない。

---

## 9. 引いてはいけない線

1. **§1.2 の (A)・(B) と混同しない。** 本走は「$b$ チャネル遮断 × µ チャネル遮断」であって、「µ/Σ × 片道ゲート」でも「µ ダイヤル × 可識別性ダイヤル」でもない
2. **本走の「µ チャネル遮断」は µ の除去ではない。** EMA 中心化は µ とタスク可識別性を同時に消す（[[論点マップ]] §20）。オラクル中心化に替えてもこの交絡は閉じない（[[引用禁止]] B）。**「µ を消した」と書かず「µ チャネルを EMA 中心化で遮断した」と書く**
3. **`strict_dead` を判定に使わない**（§6.3 D は REPORT_ONLY）。前段で dead と機能が逆向きに動いた実例が 2 つある（`centered_freeze_0901` の `FROZEN_WORSE`、`bias_margin_0814` の PB-7）
4. **本走は新規乱数系列（`generator_offset = 20260905`）で回すので、前段 4 セルとは別サンプルである。** 逆に言えば、前段の committed 腕との bit 一致は取れない。S0 は「実装が既存 harness と同値であること」を示すだけで、**本走の腕の正しさを保証しない**
5. **「$b$ だけの weight decay で LoP が治る」と一般に書かない。** スコープは condA・幅100・hidden `[100,100]`・$T=10^4$・batch=1・`lr=0.01`・plain SGD・5M step・$\lambda=$1e-3
6. **「2/3 : 1/3」の比を主張に載せない。** $\lambda$ 依存・床効果・E-drift/E-level の選択に依存する（§2.1）。載せてよいのは**分解の存在**であって比の数値ではない
7. **前段のパイロット（`results/bias_wd_pilot_0901/`）の数値を結果として引かない。** $\lambda$ の由来としてのみ参照する
8. **$\lambda$ を結果を見て選び直さない**
9. **§2 の表は私の事後集計である。** 本走の結果と並べるときは格を分ける

---

## 10. 成果物と commit の順

```
specs/spec_channel_2x2_0901.md      ← 単独で先に commit（事前登録の成立点）
configs/channel_2x2_0901.yaml
src/channel_2x2_0901.py
results/_gate_channel_2x2_0901/     ← S-pair / S0 / S1 / S2 / S3 / S-iso / S-cap
results/channel_2x2_0901/
    verdict.csv  summary.md  paired_endpoints.csv  exclusions.csv
    task_end_metrics.csv  block_levels.csv  run_sanity.json  provenance.json
    fig_channel_2x2.png
```

commit は **spec 単独 → config＋実装 → 結果** の 3 段（`gate_dose_0830` 型）。各段で `git ls-remote origin refs/heads/main` により自分の commit hash がリモートに載っていることを確認し、その出力を報告に貼る（`centered_freeze_0901` の push 漏れの前例）。

---

## 11. spec 前チェックリスト（[[9月運用_0901]] §3-1 の全通し）

| 項目 | 本 spec での対応 |
|---|---|
| 新 seed 群は `generator_offset` で作る | §3（`20260905`・D1 で確定） |
| 吸収・凍結のある系は総和と率の両方 | §6.1（E-drift ＋ E-level の共主） |
| 等価限界 $\Delta$ を事前登録 | §6.2（$\Delta=0.15$ dex・較正の出所を明記、$\Delta_{\rm int}=0.50$ dex） |
| 二値 endpoint の飽和を事前検討 | 主 endpoint は連続量。dead は REPORT_ONLY（§6.3 D） |
| studentized fallback と退化ガード | §6.4 |
| 窓ラベル | §5・§2（全数値に B02/B10 を付す） |
| マスク個数の検算 | §7 S-count |
| step 0 過渡 | 判定窓は B02（task 51–100）起点で、EMA 初期過渡を含まない |
| 未フィット率の床の継承元を明示 | §6.4（`mlp2_phase1_0829` から `1e-23`） |
| ロガー相乗りの検討 | §4（台帳・`submerged`・境界スナップ） |

---

## 12. 本走が閉じるもの／閉じないもの

**閉じる**

- [[現象2_b経由の死_0831]] §1 の「現象1 と現象2 は同一吸収域への 2 チャネル」という**未登録の整理**を、登録された介入判定に変換する
- [[中心主張v5計画_0831]] 未決 2（現象2 を柱1 の書き直しに畳めるか）に、賛否どちらでも材料を与える
- 9/1 チャットの事後 2×2 を、seed 集合・bootstrap seed・交互作用・等価限界のすべてが揃った 1 走に置き換える

**閉じない**

- [[論点マップ]] §20 の可識別性交絡（§9-2）
- 非単調用量反応（[[現在地]] 穴12）。本走は µ の**用量**を振らない
- 外的妥当性。Dohare の Permuted MNIST で同じ分解が出るかは未知
- 「$W$ への weight decay は不要か有害か」（full-L2 腕・$W$-only-WD 腕が要る。**本走には入れない**）

---

> **帰趨（2026-09-01）:** 実行済み。主判定 `TWO_CHANNELS_BOTH_NECESSARY`、`CEILING_CONTAMINATED`、`LADDER_INVERTS`。repo 結果 commit `bb5fd09`。数値と解釈上の制限は [[チャネル遮断2x2結果_0901]] を正とする。
