# 前活性オフセットの格子 — 「井戸の底は同じ線形領域にあるときだけ着座点になる」を a・v・雑音・c の 4 軸で試す（夜間・27 腕）

親: [[前活性オフセット結果_0906]]（ラダー B `OFFSET_SIGNED_B`・spec [[前活性オフセット_spec_0906]]）／[[命題1-5_上端則結果_0905]] §9（full-batch 500 タスク・命題 3 の上から） / 状態: **事前登録（2026-09-06 夜・走らせる前に commit・診断の格＝判定ラベルはブロック 1・2 だけ）** / 出典チャット: lab の引き継ぎセッション（Issa「計算実験いっぱい回しときたい」）

> **run id: `offset_grid_0906`**。`edge_law_0905` runner に `--config configs/offset_grid_0906.yaml`。**新しい機構は 1 つも入れない**——活性化は `leaky_off_*`（c=±1・±0.25 の 4 名を `LEAKY_OFFSET` に足すだけ・式は同一）と `elu`、フックは既存の `lr` / `v_freeze` / `full_batch` / `b_offset`。
> 主判定を持つのは**ブロック 1・2 だけ**。残りは「見る量」と予測を登録する診断。数値の正本は `results/offset_grid_0906/summary.md`（朝に解析を書く）。

## 0. 一行

`act_offset_0906` の事後の読み「**井戸の底（φ+c=0 の点）が、そのユニットの支持と同じ線形領域の中にあるときだけ、ユニットはそこに正確に座る。無ければ折れ目へ引かれ、ラチェットが押し返して釣り合う**」を、(1) 傾き a を変えて底 −c/a を動かし、(2) v を凍らせて経路を切り、(3) 雑音を消し、(4) c を細かく振って、事前に予測を書いてから試す。あわせて (5) 幅（‖w_free‖ の成長）が a・v・雑音・c にどう追随するかを同じログから読む。

## 1. 腕（27 本・幅 100・深さ 1・用量 12.16・10 seed・正本は config）

| ブロック | 腕 | 何を変える | step | 見る量 |
|---|---|---|---|---|
| S-null | `LRoff0_1216` | act_offset と同一設定 | 5M | `results/act_offset_0906/logs/LRoff0_1216_seed*.npz` と**バイト一致**（同じマシン・S-limit と同じ規則） |
| **1. a × c** | `LRa{0.2,0.3,0.5,0.7}_off{0,m0p5,p0p5}` 12 本 | 傾き a と c | 5M | 各 (a, c) の z̄・zmax・3 分割（上/跨/下）の割合と群の中央値・対 c=0 のユニット対応 Δ・幅 ‖w_free‖ |
| **2. v 凍結 × c** | `LRvf1`・`LRoffm0p5_vf1`・`LRoffp0p5_vf1` | v を初期値で凍結 | 5M | 凍結下の Δ(c)（対 `LRvf1`） |
| 3. ラダー B を埋める | `LRoff{m1,m0p5,p0p5,p1}_lr0p00125` 4 本 | c（lr 0.00125・40M） | 40M | 既存 {0, ±2} と合わせた 7 点の z̄(c)・上に抜ける割合 |
| 4. full-batch | `FBLRoff0`・`FBLRoffp0p5` | 雑音ゼロ（32 パターン厳密勾配） | 5M | c=0 の z̄・zmax・幅／c=+0.5 の着座 |
| 5. 小さい c | `LRoff{m0p25,p0p25}` | c=±0.25（lr 0.01） | 5M | Δz̄・3 分割 |
| 6. ELU 参照 | `Eoff0_1216` | `elu` c=0 | 5M | act_offset の `Eoffm1`/`Eoffp1` を Δ で読む |
| 7. 命題 3 の上から | `LRbp2`・`Ebp2` | b +2 | 15M | **走らない**（30k 前検査で 10/10 seed が step 1,000 に発散・Log 参照）。config には残し `NUMERIC_DIVERGENCE` を記録する |

窓: 5M 腕 タスク 451–500（settle 301–350 / 376–425 / 451–500）、15M 腕 1451–1500、40M 腕 3951–4000（settle 2751–2800 / 3351–3400 / 3951–4000）。ALIVE = `layer1_denom` > 0.25。CI = seed 対応 bootstrap 2000・`rng(20260906)`。**3 分割は閾値ではなく作動条件**: 上 = zmin > 0（φ = z + c の線形）／跨ぐ = zmin ≤ 0 ≤ zmax（ラチェットが働ける唯一の状態）／下 = zmax < 0（φ = az + c の線形）。井戸の底: c>0 → −c/a（下の枝）、c<0 → +|c|（上の枝）。

## 2. 判定（ブロック 1・2 のみ）

**ブロック 1（`SEAT_TRACKS_WELL`）**: c=+0.5 の 4 腕について、「下」群の z̄ 中央値と −c/a（−2.5 / −1.67 / −1.0 / −0.71）の差の CI が **±0.3 に内包**される腕の数 k。
- `SEAT_TRACKS_WELL`: k = 4
- `SEAT_TRACKS_WELL_SHALLOW_ONLY`: k ≥ 2 で、外れるのが a ≥ 0.5 の腕だけ（底が折れ目に近く、上の枝の底 −c=−0.5 と融合する）
- `SEAT_OFF_WELL`: k ≤ 1
- `NOT_DETERMINED`: 発散 3/10 超・未定着・「下」群が 20% 未満の腕が 2 本以上

**ブロック 2（`C_VIA_V_ONLY`）**: v 凍結下の Δz̄(±0.5) = 腕 − `LRvf1`（ユニット対応・両腕 ALIVE）。
- `C_VIA_V_ONLY`: ±0.5 の両方で Δz̄ の CI ⊂ [−0.5, +0.5] かつ Δzmax の CI ⊂ [−0.3, +0.3]
- `C_NOT_ONLY_VIA_V`: どちらかで CI が 0 を外す（→ c は v 以外の経路でも z̄ を動かす＝私の勾配の帳簿が間違い）
- `NOT_DETERMINED`: 発散・未定着・`LRvf1` の unfit が act_offset の `LRoff0` の 80 倍超（edge_law 5-c と同じ足切り）

## 3. 事前予測

### Claude（走らせる前・2026-09-06 夜）

| ブロック | 予測 |
|---|---|
| S-null | バイト一致 99%（外れたら環境が変わった） |
| **1** | `SEAT_TRACKS_WELL_SHALLOW_ONLY` 50% / `SEAT_TRACKS_WELL` 35% / `SEAT_OFF_WELL` 15%。a=0.2・0.3 は −2.5・−1.67 に ±0.3 で乗る。a=0.7 は底 −0.71 と上の枝の底 −0.5 が融合して −0.6 ± 0.2。c=−0.5 の「上に抜ける割合」は a とともに増える（0.2: 15% / 0.3: 25% / 0.5: 50% / 0.7: 80%）——ラチェットの非対称 1 対 a が a→1 で消えるから |
| **2** | `C_VIA_V_ONLY` 80%。c は ∂L/∂v にしか入らず δ のずれは c_out が吸収するので、v が凍れば c は何もしない。外れたら勾配の帳簿が間違い |
| 3 | c=+1 は −10 ± 0.3（80%）、c=+0.5 は −5 ± 0.3（85%）。c=−1 の上に抜ける割合は −0.5（10%）と −2（51%）の間で ≈ 30% |
| 4 | 雑音ゼロでも c=0 は −3.6 ± 0.5・zmax 0.1 ± 0.3 に着く（60%。edge_law の 50 タスクでは −1.34 で未定着だった）。**幅は雑音なしでも育つ**（65%）。c=+0.5 は −5.0 ± 0.3（70%） |
| **5** | **非単調を予測する**: c=+0.25 の底 −2.5 は c=0 の着座 −3.6 より**上**なので、着座点は上がって −2.5 ± 0.4（Δz̄ ≈ +1.1）——50%。線形補間（Δz̄ ≈ −0.7）30%。c=−0.25 は上の枝の底 +0.25 に抜ける群 ≈ 5% と、跨ぐ群 ≈ −2.5 |
| 6 | ELU の Δz̄(+1) < 0・Δz̄(−1) > 0（leaky と同じ向き）75% |
| 7 | ~~leaky・ELU とも上から戻る（保持率 < 0.15）85% / 80%~~ → **前検査で発散・予測は無効**（走らせる前に判明） |
| 幅（事後で読む） | ‖w_free‖ の成長率は c で減り（act_offset で ×5.05 → ×1.58）、v 凍結で**消えない**（55%）、雑音ゼロで**残る**（65%）、a とともに減って a≈0.5 で 1 を切る（scale_attractor と整合・80%） |

### Issa（走らせる前に記入）

- **全ブロック: Claude と同じ**（2026-09-06 22:5x・投入後・結果は誰も見ていない段階で記入。「事前予測は全部同じでいいや」）。
  すなわち ブロック 1 `SEAT_TRACKS_WELL_SHALLOW_ONLY`／2 `C_VIA_V_ONLY`／5 c=+0.25 で着座点は**上がる**／4 雑音ゼロでも幅は育つ。
- 一言: c=−2 の「上」群（出力を消す位置 +2 へ抜けて線形化する動き）が、GELU が 0 に引き込まれる話（Chen の機構）に似て見える、との指摘（投入後）。

## 4. 検査（本走前）

- 新しい 4 名は `LEAKY_OFFSET` の追加だけなので、`src/test_act_offset_0906.py` の S-fd／S-shift／S-guard／S-fallthrough がそのまま掛かる（辞書を舐める）。S-const は本 config の `offset` と突き合わせる。
- 短縮走行 27 腕 × 30k: 有限・`lr_used` がフックどおり・`freeze_v`／`batch_mode` 列がフックどおり。
- S-null は本走で（`LRoff0_1216` の全列バイト一致・`results/act_offset_0906/logs` 対照）。
- 安定性の見積り: λ ≈ 2hc² は c=±1 で 200（lr 0.00125 で λη = 0.25）、c=±0.25 で 12.5、c=±0.5 は a に依らず 50。全部 λη < 2。

## 5. コスト

5M 腕 21 本 × 14 分、15M 腕 2 本 × 42 分、40M 腕 4 本 × 105 分、full-batch 2 本 × ≈56 分。長い腕から出して並列 20（RSS ≈ 26 GiB・空き 50 GiB）→ **壁時計 ≈ 2 時間**。生ログ ≈ 4 GB＋40M 腕 4 本 6.4 GB。

## 6. 引用上の注意

1. 判定ラベルはブロック 1・2 だけ。3–7 と幅の読みは診断（予測つきだがラベルなし）。
2. lr 0.00125 の腕は lr 0.01 の腕と水準比較しない（act_offset 追補 1 §9）。
3. 「上に抜ける割合」は ALIVE 規則を通さない ALL ユニットの量（潰れたユニットこそ見たいので）。ALIVE 版は併記。
4. 幅（‖w_free‖）の読みはすべて事後。幅を主題にした登録判定はまだ無い。

## Log
- 2026-09-06 夜: act_offset_0906 の決着（`OFFSET_SIGNED_B`）と事後の 3 分割の読みから起票。Issa の予測を待ってから投入。
- 2026-09-06 夜（走らせる前）: 30k 前検査で **ブロック 7（b +2）が leaky・ELU とも step 1,000 に 10/10 seed 発散**。機構は c=±2 と同じ: b を全ユニットに一様に +2 足すと正側の枝で出力が一様に +2 ずれ、v 空間の一様方向に λ ≈ 2h·2² = 800 の曲率が立つ（`edge_law_0905` の ±5/±4 の即発散も同じ機構だったと分かる）。**ブロック 7 は NOT_RUN**。「上から」を取るなら lr 0.0025 以下か、一様成分の無いオフセット（ユニットごとに ±2 を乱数で配る）が要る。残り 25 腕は 30k で有限・フック列も期待どおり。
- 2026-09-06 夜（投入時）: 前検査 PASS（25 腕有限・登録どおり b+2 の 2 腕が発散）。Issa の予測欄は**投入時点で未記入**（就寝前・返答待ち）。**結果を見る前なら追記可**とし、解析は Issa の記入か「記入なし」の確定を待ってから回す。投入は長い腕から並列 20。
- 2026-09-06 23:0x（投入後・結果を見る前・追補）: Issa の指摘——c=−2 の「上」群（出力を消す位置 +2 へ抜けて支持を潰し線形化する動き）は **Chen（Ziyin ら）の確率的崩壊**（勾配ノイズの Itô ドリフトが「出力 0」の不変集合へ引き込む）に似て見える。この群は自己ポテンシャル E[o²] = v²E[(φ+c)²] の最小集合（φ+c ≡ 0 まで支持を潰す ∪ v→0）に座っており、Chen の到達先と**同じ集合**。違いは駆動が雑音か一次か。ブロック 4 は c=0・+0.5 しか無いので **`FBLRoffm0p5_1216`（full-batch・c=−0.5・5M）を 1 腕追加**する。予測（Claude・Issa 同じ）: 雑音ゼロでも「上」群が +0.5 に現れる（一次＝井戸）65%／消える（Chen 型）35%。現れる割合は online の 10%（act_offset）と同程度なら一次、大きく減れば雑音が寄与。
- 2026-09-06 23:2x（結果を見る前・読みの登録）: 井戸の駆動が雑音か一次かは **2 本で読む**——(i) `FBLRoffm0p5_1216`（雑音ゼロ）と (ii) `LRoffm0p5_lr0p00125_1216`（雑音 1/8）の「上」群の割合を、act_offset の `LRoffm0p5_1216`（lr 0.01・10%）と比べる。**一次なら両方 ≈10%、Chen 型（Itô ドリフト ∝ η）なら (i) ≈0・(ii) は数 %**。Chen 型なら「ラチェット＝一次・井戸＝雑音誘起」と駆動が分かれ、R の第 1 項だけが雑音誘起だったことになる。留保: Ziyin の対称性理論では素の GD は崩壊しないはずなので、full-batch で「上」群が出れば Chen の枠外の一次機構。
