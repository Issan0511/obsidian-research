# ゲート条件付き用量（用量を固定したまま片道の壁だけを外す: ReLU / ELU / leaky ReLU × 3 用量 × 5M）

親: [[中心主張v4草案_0829]] 柱2・柱3 / 状態: **事前登録（§7 記入済・vault push 待ち・repo 側 commit 未了・未実行）** / 作成 2026-08-30 / 出典チャット: `elu_swamp_0830`

前段: [[柱3定用量5M_spec_0830]]（実行済み・`results/dose_const_5m_0830/`）／[[ELU沼_spec_0830]]（実行済み・[[ELU沼結果_0830]]）
関連: [[LoP防止アーキテクチャ_0829]] / [[多層Phase1結果_0829]] / [[論点マップ]] §3・§8・§14 / [[命題リスト]] Q0・Q13 / [[運用ルール]] §2・§5 / [[引用禁止]]

> **本走は前段 2 走の交差点に置く 1 本。** 用量ダイヤルは [[柱3定用量5M_spec_0830]] のオラクル固定をそのまま使い、活性化関数だけを ReLU / ELU / leaky ReLU に振る。**隠れ 1 層**なので、[[ELU沼結果_0830]] で混ざった「第2層の自己生成 µ」と「上流の学習の混入」がどちらも構造的に消える。
>
> **repo 側は spec と config を実装より先に単独 commit する**（[[ELU沼結果_0830]] §6 の反省。運用ルール §2）。
>
> **命名**: 作成日 0830。`20260830` は `generator_offset` として既用なので seed・offset に使わない。bootstrap seed は `20260829` を継承。

---

## 0. 一行

中心主張「LoP は µ が作った片道の壁に、意味のない揺れで落ちていく現象」は、µ（結合）・揺れ（駆動）・片道（吸収）の 3 部品から成る。**これまでの実験は µ と揺れを動かしてきたが、片道だけを外したことがない。** [[ELU沼結果_0830]] は帯の下（dose 5.6）でそれを偶然やり、µ が ReLU より大きいのに落ちなかった。本走は **µ を帯の中に固定したまま**片道だけを外し、用量反応がゲートに条件付けられているかを測る。

## 1. なぜ回すか

| 材料 | 出所 | 格 |
|---|---|---|
| 5M では帯内の全用量で $n_{\rm onset}=10/10$、跳び $J$ は 1M 0.457 → 5M 0.029。**閾値型の柱3 は 5M で消える**。1M の発症数は用量に単調（9.33 で 3、10.04 で 8、10.75 以上で 10）なので、消えたのは閾値であって用量依存ではない | `results/dose_const_5m_0830/summary.md` | **commit 済み出力** |
| `E_A1`（ELU・第1層中心化）は第2層入力の ‖µ‖ が ReLU より大きい（2.00 対 1.45）のに、沈下 1.5%・未フィット率 0.0028（ReLU 0.0219、Δlog10 −0.927 [−1.098, −0.696]） | [[ELU沼結果_0830]] §2・§4 | **commit 済み出力**（`layer_stats.csv` の分解は summary 未掲載） |
| 沈下領域のドリフトは正（ρ = +0.044 [+0.020, +0.078]）。µ 方向に押し込む力は測れなかった | 同 §2 | commit 済み出力（記述の格） |
| ELU の LoP が軽いこと自体は先行文献にある | Dohare et al. 2024 付録・Clevert et al. 2015 | **付録の順序は原典未確認。記憶で引かない** |

**動機は柱3 の書き換え。** 改訂案は

> 柱3（改訂案）: **用量は閾値ではなく壁への到達の率を決める。その率が意味を持つのはゲートが片道である場合だけである。**

前半は `dose_const_5m_0830` が担い、後半を本走が担う。**本走が外れれば後半は書けない。**

## 2. 何を分けるか

### 2.1 片道と滑らかさ

可動度 $\phi'(z)$ で 3 つの活性化関数を並べる。

| | $z>0$ | $z\le0$ | 片道か | 滑らかか |
|---|---|---|---|---|
| ReLU | 1 | **0** | 片道（厳密な吸収） | 折れ目 |
| ELU（$\alpha=1$） | 1 | $e^{z}$ | 片道でない（深いほど遅い） | 滑らか |
| leaky ReLU（$a=0.1$） | 1 | **0.1**（定数） | 片道でない | 折れ目 |

ELU だけでは「片道でないこと」と「滑らかであること」を分けられない。leaky ReLU は折れ目を残したまま片道だけを外すので、**両方を並べて初めてゲートの性質を切り出せる**。$a=0.1$ を選ぶ理由: 負側の可動度が 10 倍遅いだけで減衰しない（ELU の $e^{z}$ と対照的）。$a=0.01$ は 100 倍遅く、5M の地平では ReLU との差が曖昧になりうるので採らない。

### 2.2 前段で消える 2 つの混入

- **第2層の自己生成 µ**（[[ELU沼_spec_0830]] §2.1–2.2）: 隠れ 1 層なので存在しない。入力の µ はオラクル固定で厳密に一定
- **上流の学習の混入**（[[ELU沼結果_0830]] §5）: 隠れ層の入力が固定なので、壁座標の増分 $\Delta\bar z_i$ は自前の勾配だけになる。**Q2（可動度スケーリング・駆動対雑音比）を汚染なしで測れる初めての配置**（§5.4）

### 2.3 3 つの帰結（どれが出ても情報になる）

| ELU（帯内） | leaky（帯内） | 読み |
|---|---|---|
| 発症せず | 発症せず | **ゲートの片道性が load-bearing**。用量はゲート条件付きの率 |
| 発症 | 発症せず | ELU の $e^{z}$ 減衰は高用量では軟らかい壁として働く。片道性でなく**可動度の減衰**が効く |
| 発症 | 発症 | 用量はゲートを経ずに効く（ノルム成長・マージン枯渇）。**柱2 の書き方を疑う** |
| 発症せず | 発症 | 滑らかさ（復元力）が効いており、片道性の除去だけでは足りない |

### 2.4 本走が答えないこと

- **無限時間の主張**。$n_{\rm onset}=0/10$ で言えるのは「5M までに観測しなかった」まで（[[柱3定用量5M_spec_0830]] §5.2 の強さの制限を継承。0/10 の片側 95% 上限 $p\le0.2589$）
- Adam・幅・多層・他教師（rsl_rl への接続は予測のまま）
- $\alpha$・$a$ の掃引。単一値
- 過剰パラメータ化比のスコープ条件（[[過剰パラメータ化とスコープ条件_0829]]）: w100 × LTU 幅 100 で測った結果。引用時に付す

## 3. スコープと前提

condA・幅 100・**隠れ 1 層**・$T=10^4$・batch=1・素の SGD・lr 0.01・seed 0–9・`total_steps=5e6`（500 タスク）・device=cpu。`dose_const_5m_0830` と同一宿主（`src/mlp2_phase1.py` の機構・オラクル固定モード・`eval_loss_exact` 併記・1M `state_hash`）。

## 4. 設計

### 4.1 腕（9 本）

| 腕 | 活性化 | 用量 | 目標 ‖µ‖ | 位置づけ |
|---|---|---|---|---|
| `R_off` | ReLU | 固定なし（中央 12.17） | — | `dose_const_5m_0830` `dose_off` を bit 再現（S0'） |
| `R_933` | ReLU | 9.33 | 2.333 | 同 `dose933` を bit 再現（S0'） |
| `R_1216` | ReLU | 12.16 | 3.041 | 同 `dose1216` を bit 再現（S0'） |
| `E_off` / `E_933` / `E_1216` | ELU $\alpha=1$ | 同上 | 同上 | **主対象** |
| `LR_off` / `LR_933` / `LR_1216` | leaky $a=0.1$ | 同上 | 同上 | **片道性の対照** |

用量 3 点で足りる理由: 5M では ReLU が 3 点とも 10/10 発症しており（commit 済み）、帯内の中間点を刻む意味が無い。`R_off` を残すのは無介入（$\gamma\equiv0$）の基準として。

**ReLU 3 腕を本走に含める理由**: commit 済みの値は引けるが、**全 paired 対比を同一走の腕で取る**ため。コストは §6。

### 4.2 対応づけ

活性化の変更もオラクル固定も乱数を消費しないので、9 腕は init・教師・入力列・flip 軌道が bit 一致する（S-pair）。**ただし活性化の異なる腕は step 1 以降で軌道が分岐する**（[[ELU沼_spec_0830]] §4.2）。ペアリングが落とすのは init・教師・入力実現の分散だけ。引用時にこの一行を落とさない。

### 4.3 実装

`VecMLPL` は `elu_swamp_0830` で `act ∈ {relu, elu}` を持つ。追加は leaky のみ。

- 前向き: `where(pre>0, pre, a*pre)`
- 後ろ向き: `where(pre>0, 1, a)`
- ELU の後ろ向きは `alpha*exp(pre)`（[[ELU沼_spec_0830]] §11.9。`phi+alpha` は float32 で深い負側が 0 になる）
- **ReLU 経路は 1 行も触らない**（S0' が守る）。閉形式のまま、autograd に切り替えない
- 用量固定は `dose_const_5m_0830` のオラクルモードをそのまま使う。**活性化に依存しない**（入力側の変換）

### 4.4 用語

- ReLU 腕: `strict_dead`（$\hat p_i\equiv0$）
- ELU / leaky 腕: **沈下** $\max_{x\in32}z_i(x)\le0$、`submerged_frac`（[[用語と記号]] に登録済みの定義）
- **どちらも verdict に使わない。** 沈下は死ではない（leaky では可動度 0.1、ELU では $e^{z}$ が残る）。§5.3 で「予測の照合」にだけ使う

## 5. 集計（事前登録）

窓・床・CI は [[柱3定用量5M_spec_0830]] §5 を逐語継承する。

- **5M 末尾窓** = タスク 491–500、**1M 窓** = タスク 91–100、早期窓 = タスク 2–11
- 各 seed の窓平均 未フィット率 $U_k$（床 **1e-16**・`dose_const_5m_0830` で較正済み・再較正しない）
- **発症 = $U_k\ge0.05$**（Phase 0 から継承。変更しない）
- CI: $n_{\rm onset}$ は Clopper–Pearson。差は seed クラスタ paired bootstrap、B=10000、`rng=np.random.default_rng(20260829)`、percentile を主、studentized は退化検出つきの副

### 5.1 主 endpoint — 発症数（活性化 × 用量の 9 セル）

**P1**: 各腕の $n_{\rm onset}(5\mathrm{M})$。**P2**: 各腕の $n_{\rm onset}(1\mathrm{M})$。

**主 verdict**（ELU と leaky の帯内 2 腕、`*_933` と `*_1216` で判定。`*_off` は参考）

| ELU 帯内 2 腕 | leaky 帯内 2 腕 | verdict |
|---|---|---|
| ともに $n_{\rm onset}(5\mathrm{M})=0$ | ともに 0 | **`GATE_LOAD_BEARING`** |
| いずれかで $\ge5$ | ともに 0 | **`SOFT_WALL_AT_HIGH_DOSE`** |
| いずれかで $\ge5$ | いずれかで $\ge5$ | **`DOSE_NOT_GATE_MEDIATED`** |
| ともに 0 | いずれかで $\ge5$ | **`SMOOTHNESS_REQUIRED`** |
| 上記以外（1–4 が混じる） | | **`PARTIAL`** — §5.2 の水準側で読む |

ReLU 3 腕は S0' で bit 再現されるので $n_{\rm onset}(5\mathrm{M})=10/10$ が**既知**。ReLU 側が 10/10 でなければ S0' 不合格であり結果を読まない。

**強さの制限**: `GATE_LOAD_BEARING` は「5M までに発症を観測しなかった」まで。**「用量はゲートが無ければ無限時間でも効かない」とは書かない。**

**飽和ガード**（教訓⑪）: 二値 endpoint が予測どおり 0 に揃った場合、**程度**の情報は持たない。§5.2 の水準側を同格で報告する。

### 5.2 水準（主判定と同格）

- 各腕の seed 中央値 $\log_{10}U(5\mathrm{M})$、$\log_{10}U(1\mathrm{M})$
- **P3（活性化の効果）**: 同一用量での paired Δ $\log_{10}U(5\mathrm{M})$、ELU − ReLU および leaky − ReLU（3 用量それぞれ）
- **P4（ゲート無しでの用量の効果）**: ELU 内・leaky 内で $\log_{10}U(5\mathrm{M})$ の paired Δ、`*_1216` − `*_933`。**ゲートが無くても用量が水準に効くか**を見る

| P4 | 読み |
|---|---|
| CI が 0 を含む | 用量はゲートを通してのみ効く |
| CI が 0 を上に外す | ゲートが無くても用量は学習を遅らせる（Clevert の効率経路） |

- 跳び $J$（隣接用量間の $\log_{10}U$ 差の最大）を活性化ごとに報告。verdict には使わない

### 5.3 予測の照合（沈下率・REPORT_ONLY）

§7 の予測「strict_dead 相当は 0 近傍」を照合するための凍結閾値: **5M 末尾窓の `submerged_frac` seed 中央値 < 0.05 を「0 近傍」とする。** ReLU の参照値は `dose_const_5m_0830` の 5M `strict_dead` 98–99（REPORT_ONLY）。**verdict に使わない**（§4.4）。

### 5.4 副次 — 汚染なしの Q2（REPORT_ONLY・事前登録）

[[ELU沼_spec_0830]] §5.2 の P2a（可動度スケーリング $\beta$）・P2b（駆動対雑音比 $\rho$）を、**隠れ 1 層で**測り直す。定義・ビン（等頻度 12 分位・自然対数・始点条件付け・タスク内隣接記録点）・閾値（$\beta$ の CI が 1 を含む → `MOBILITY_SCALING`、$|\rho|<0.1$ → `NOISE_DOMINATED`、$|\rho|\ge0.3$ → `DRIFT_DOMINATED_*`）は同 spec §5.2・§11 を逐語継承する。

- 対象は ELU 3 腕・leaky 3 腕。leaky の可動度は定数 0.1 なので **$\beta$ の期待値は 0**（ELU の 1 と対照）。leaky で $\beta$ の CI が 0 を含み ELU で 1 を含めば、可動度の絵が両方で確認される
- **沈下ユニットが seed あたり 3 未満の腕は `INSUFFICIENT_DATA`**。§7 の予測どおり沈下が起きなければ本節は空振りする。それでよい
- `logs/*.npz` に `layer1_zbar` を保存し、ビン集計を `increments.csv` に出す

### 5.5 その他の副次（REPORT_ONLY）

`strict_dead` / `submerged_frac` / `alive` / `eff_rank` / `‖w‖` / `median_M` / `median_B` / `preact_sd_median` の時系列。実測 $\gamma(k)$ と $\cos(\boldsymbol\mu,\boldsymbol\mu|_{\gamma=0})$。`eval_loss_exact` の用量反応。**`‖w‖` は主要共変量**（継続）。

### 5.6 数値発散

[[ELU沼結果_0830]] で `E_none`（2 層・ELU・入力 dose ≈ 12・lr 0.01）が step 281000 に発散した。**本走の ELU 帯内腕は同じ条件に近い。** 扱いを事前登録する。

- 検出・停止・救済なしは Phase 1 §5.7 を逐語継承（seed 除外・再初期化・LR 低下・clipping をしない）
- 発散した腕のセルは **`NUMERIC_DIVERGENCE`** で空になる。§5.1 の verdict はそのセルを欠いたまま判定し、`SOFT_WALL_AT_HIGH_DOSE` / `DOSE_NOT_GATE_MEDIATED` が **`*_1216` セルを要求するのにそれが空なら `INCONCLUSIVE_DIVERGENCE`** とする
- **発散そのものを結果として受ける**。「ELU は高用量の生入力＋lr 0.01 で不安定」は、それ自体が用量と活性化の相互作用の記述になる
- lr を下げた再走は**別 spec**（9 腕すべての lr を揃えて下げる。ReLU 側の柱3 の系譜と切れるので、その spec が必要になった時点で改めて判断する）。**本走から自動起案しない**

## 6. 前段チェック・出力・コスト

- **S0'（必須）**: `R_off` / `R_933` / `R_1216` が `results/dose_const_5m_0830/` の `dose_off` / `dose933` / `dose1216` と seed ごとに **state_hash（1M・5M）・既存全列が完全一致**。**不一致なら本走に進まない**
- **S-pair（必須）**: 9 腕の step 0 で init・教師・入力列・flip が bit 一致（30k 短走）
- **S-dose（必須）**: 各腕の記録点の実測 ‖µ‖ が目標と相対誤差 $10^{-10}$ 以内。**活性化によらず入力側の量なので 9 腕すべてで確認**
- **S-grad（必須）**: leaky の閉形式勾配を有限差分と照合（相対誤差 1e-6・$z=0$ 近傍と $z=-30$ を含む）。ELU は `elu_swamp_0830` の合格を再実行して確認
- **S-leaky-limit（新規）**: $a\to0$ で leaky 経路が ReLU 経路に一致。**S-elu-limit**: $\alpha\to0$ で同様（教訓⑩・サニティ側にも掛ける）
- **S-submerge**: 沈下判定が ReLU 腕で `strict_dead` と完全一致
- **S-taut**: 判定指標（未フィット率）とサニティ側の双方で、活性化の変更が定義上恒真になっていないこと
- **S1**: `OMP_NUM_THREADS=1`。**S6**: 床は継承・再較正しない（`floor_calibration.csv` を参照するだけ）
- **本走前に §5 の集計は一切しない。**

出力: `results/gate_dose_0830/` に `verdict.csv`（腕・活性化・用量・$n_{\rm onset}$ 1M/5M・CP95・$U_k$ 全 10 値・P3・P4・`CI_DEGENERATE`・`NUMERIC_DIVERGENCE`）、`summary.md`、`dose_response.csv`（活性化 × 用量 × 窓）、`increments.csv`、`provenance.json`、`logs/`、`arm_status/`。**数値は `verdict.csv` と `summary.md` からのみ転記。**

コスト: `dose_const_5m_0830`（6 腕・1 層）が 3281 s。9 腕で **約 85 分**、ELU の `exp` を含めて 100 分前後の見込み。

## 7. 事前予測（**2026-08-30 記入・実行前**）

### 7.1 主予測（Issa・Claude 一致）

**「ELU も leaky も全用量で発症せず、strict_dead 相当は 0 近傍」＝ `GATE_LOAD_BEARING`、かつ ELU・leaky の 6 腕すべてで 5M 末尾窓の `submerged_frac` < 0.05。**

- Issa: 2026-08-30 チャットで「同意です」と明示
- Claude: 同日、[[ELU沼結果_0830]] の後に提示

**一致しているので、本 spec の情報価値は外れた場合にある。** §2.3 の 3 つの対立仮説を、外れたときの読みとして事前に固定してある。

### 7.2 副予測（Claude 記入・Issa は未記入）

- **P4 = CI が 0 を含む**（ゲートが無ければ用量は水準にも効かない）。理由: [[ELU沼結果_0830]] で `E_A1` の未フィット率に棘が無く、用量の影響が見えなかった
- **§5.4 は `INSUFFICIENT_DATA`**（沈下が起きないので測れない）。測れた場合は leaky で $\beta$ の CI が 0 を含み、ELU では [[ELU沼結果_0830]] と同じく $\beta$ が負に出る（上流混入が消えても負なら、可動度の絵そのものを疑う）
- **`E_1216` は発散する可能性がある**（`E_none` の前例）。発散したら `INCONCLUSIVE_DIVERGENCE` を受け入れ、lr を下げた spec を別途立てる

**Issa の P4 予測（記入待ち）**: ゲートが無くても用量が学習を遅らせる（Clevert の効率経路）と読むなら「CI が 0 を上に外す」。**ここで割れれば、主予測が当たった場合にも情報が残る。**

### 7.3 外れたときに第一に疑うもの

- **Issa**: ELU の仕様そのもの（[[ELU沼_spec_0830]] §8.1-6 を継承）。leaky が並ぶので、本走では「ELU の仕様」が「$e^{z}$ の減衰」に絞られる
- **Claude**（順）: (i) 発散（§5.6）で帯内セルが空いていないか。(ii) S-dose — オラクル固定が ELU / leaky でも $10^{-10}$ で通っているか（入力側なので通るはずだが、通らなければ用量が違う）。(iii) §2.3 の第 3 行（`DOSE_NOT_GATE_MEDIATED`）が出たら、ノルム成長（`‖w‖` の時系列）とマージン枯渇（[[過剰パラメータ化とスコープ条件_0829]]）を順に見る。柱2 を疑うのはその後

## 8. 引用上の注意（結果が出た後）

- **`GATE_LOAD_BEARING` は「5M までに観測しなかった」の強さ。** 0/10 の片側 95% 上限は $p\le0.2589$
- **活性化の異なる腕のペアリングは init・教師・入力実現まで**（§4.2）
- **`submerged_frac` / `strict_dead` を verdict に使わない**（§4.4）
- **ELU / leaky の結果で ReLU 側の柱1・柱2・柱3 の測定値を書き換えない。** 書き換えるのは柱3 の**書き方**（§1 の改訂案）であり、それも本走が `GATE_LOAD_BEARING` の場合に限る
- Dohare 付録の活性化関数比較は**原典確認まで引かない**
- rsl_rl / PPO への接続は予測。Adam・幅・多層・教師がすべて外
- 過剰パラメータ化比 1:1 のスコープ条件を付す
- スコープ: condA・隠れ 1 層・w100・$T=10^4$・batch=1・lr 0.01・$\alpha=1$・$a=0.1$

## 9. 作業表

| # | 作業 | 走 | 状態 |
|---|---|---|---|
| 1 | §7.2 の P4 予測を Issa が記入（任意だが推奨） | なし | 未着手 |
| 2 | **本 spec と config を repo へ単独 commit・push**（実装より先） | なし | 未着手 |
| 3 | `VecMLPL` に leaky を追加、S0'・S-pair・S-dose・S-grad・S-leaky-limit | なし | 未着手 |
| 4 | 本走 | **あり** | 未着手 |
| 5 | Dohare 付録の活性化関数比較を原典確認 | なし | 未着手（[[ELU沼_spec_0830]] §10-3 と統合） |
| 6 | 結果次第で柱3 の改訂案を v4 に反映 | なし | 未着手（`GATE_LOAD_BEARING` の場合のみ） |
