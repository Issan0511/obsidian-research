# ELU の沼（`E_A1` で第2層の µ は再生されるか、沈下は駆動か拡散か）

親: [[LoP防止アーキテクチャ_0829]] §7 / 状態: **起草（§8 の Issa 予測欄 未記入・repo 側 commit 未了・未実行）** / 作成 2026-08-30 / 出典チャット: `elu_swamp_0830`

関連: [[多層Phase1結果_0829]] / [[µ考察3書き直し案_0829]] / [[中心主張v4草案_0829]] µ 考察 §3・§5 / [[論点マップ]] §3・§8・§14 / [[命題リスト]] Q0・Q13・Q15 / [[機能盲目性_0823]] / [[運用ルール]] §2・§5 / [[引用禁止]]

> **§8 の事前予測欄が未記入のあいだ、本 spec は事前登録として成立しない。** 記入 → 本ファイルと `configs/elu_swamp_0830.yaml`・`specs/spec_elu_swamp_0830.md` の push 確認 → 実行、の順を守る。
>
> **本走は候補 A（層入力の走行平均センタリング）以外の介入を含まない。** 候補 B（発火率ホメオスタシス）は入れない。
>
> **活性化関数を変える走は本プロジェクトで初めて。** 既存の測定値のスコープ（condA・w100・T=10⁴・batch=1・ReLU）は動かさない。ELU 側は**新しいスコープ**として立て、ReLU 側の結論を書き換える材料にしない。

---

## 0. 一行

`L2_A1`（隠れ2層×100・**第1層の入力だけ**を走行平均で中心化した腕）は、強化学習の標準構成（観測正規化あり・隠れ層に正規化なし）と同じ配置で、第2層の壁が立った。**その第1層の活性化関数を ELU に替えて、(Q1) 第2層の入力 µ が抑えられるのか再生されるのか、(Q2) 沈下が µ 方向の駆動によるのか無方向の拡散＋可動度勾配によるのかを測る。**

## 1. なぜ回すか

| 材料 | 出所 | 格 |
|---|---|---|
| `L2_A1` の第2層 dose は early 8.108 → late 7.183（Δ −0.905 [−0.932, −0.874]）でほぼ平坦。第2層 strict_dead 90.68・壁トレンド 0.405 [0.396, 0.421] | `results/mlp2_phase1_0829/summary.md` | **commit 済み出力** |
| 無介入 `L2_none` の第2層は壁トレンドが符号検定を通らない（0.044・p=0.754）。**上流を直すと下流の壁が露出する** | 同 / [[多層Phase1結果_0829]] | **commit 済み出力** |
| ReLU の自己生成比は $\|\boldsymbol\mu\|/\sqrt{\operatorname{tr}\Sigma}=0.684$（零平均ガウス事前活性）。実測 0.707–0.740 | [[µ考察3書き直し案_0829]] §2-1 | 論証＋実測 |
| Clevert et al. 2015（ELU、arXiv:1511.07289）: 入力ユニットの平均活性が非ゼロだと、それに比例した **bias shift** 項が自然勾配と通常勾配の差として現れる。ELU はこれを抑える設計 | 原典 | 先行文献 |
| rsl_rl 系の実装では ReLU ではなく **ELU** が使われることが多い（Issa 聞き取り 2026-08-30） | 未確認 | **伝聞。原典確認を作業表に置く** |

**動機**: v4 µ 考察 §5 の予測（「観測正規化＋ReLU の網では隠れ層に壁が立つ条件がむしろ整う」）は、実用側が ELU を使っているなら**そのままでは届かない**。柱1（µ が壁座標を $\mathbf w_i$ の方向と結合させ、無方向の揺れを壁座標の移動に変える機構）の罠が ELU に無いことは Clevert が先に書いている。**届くかどうかを決める分岐が 2 つあり、どちらも測っていない。**

## 2. 何が理論的に未決か

### 2.1 ELU は正斉次でない（Q1 の根）

ReLU の比が定数なのは $\mathrm{ReLU}(cz)=c\,\mathrm{ReLU}(z)$（正斉次）だから。ELU は負側が $\alpha(e^z-1)$ で $-\alpha$ に床打ちするので正斉次でなく、比が事前活性のスケール $s=\mathrm{sd}(z)$ に依存する。$z\sim\mathcal N(0,s^2)$、$\alpha=1$ のとき

$$\mathbb E[\phi]=\frac{s}{\sqrt{2\pi}}+e^{s^2/2}\Phi(-s)-\tfrac12,\qquad
\mathbb E[\phi^2]=\frac{s^2}{2}+e^{2s^2}\Phi(-2s)-2e^{s^2/2}\Phi(-s)+\tfrac12$$

$$g(s):=\frac{\mathbb E[\phi]}{\mathrm{sd}(\phi)}:\quad g(0.35)=0.08,\;g(1)=0.20,\;g(2)=0.33,\;g(4)=0.45,\;g(\infty)=0.684$$

**ELU が µ を消せるのは $s$ が小さい範囲だけで、$s$ が育つと ReLU に漸近する。** 導出は本チャットの事後計算（未事前登録・引用不可）。数値は §5.2 の判定閾値の根拠としてのみ使う。

### 2.2 沈下は Σ を痩せさせずに µ を作る（Q1 のもう一つのレバー）

ReLU の凍結ユニット（$\hat p_i\equiv0$）は出力恒等 0 で、$\boldsymbol\mu^{(2)}$ からも $\Sigma^{(2)}$ からも同時に消える。**ELU で全 32 パターン $z<0$ に沈んだユニットは出力がほぼ定数 $-\alpha$ になり、$\Sigma^{(2)}$ には効かないまま $\boldsymbol\mu^{(2)}$ に $-\alpha$ を寄与する。** つまり ELU には「沈下そのものが下流の µ を作る」という ReLU に無い経路がある。Clevert の「ELU は平均活性を 0 に寄せる」は**生きているユニットについての言明**で、この経路を排除しない。

> **格**: これは導出であって Clevert の主張ではない。本チャットの静的計算（commit 済み ckpt を反実仮想で ELU に通したもの）で符号は付いたが、**事後・未事前登録・学習なし**なので数値を引かない。

### 2.3 段差の壁 → 粘性の坂（Q2 の根）

1 step の壁座標の変化は活性化関数によらず

$$\Delta z_i=-2\eta\,\delta\,v_i\,\phi'(z_i)\,(\mathbf x\cdot\mathbf x'+1)$$

で、$\phi'(z)$ が**ドリフトにも揺れにも同じ係数で掛かる可動度**として働く。ReLU は $\mathbb 1[z>0]$（閉じると恒等ゼロ＝厳密な吸収）、ELU は $z\le0$ で $\alpha e^{z}$（連続な粘性勾配）。

自己項（税）の符号は逆になる。$\partial_{z_i}\tfrac12\mathbb E[o_i^2]=v_i^2\,\phi(z_i)\phi'(z_i)$ は、ReLU では $\phi\phi'=\phi\ge0$ で常に $z$ を下げるが、**ELU では $z<0$ で $\phi<0,\phi'>0$ なので $z$ を上げる**。ELU の自己項は折れ目への復元力で、沼へ押し込む片符号の力ではない。

一方、駆動ゼロでも可動度が場所依存の拡散 $dz=\phi'(z)\sigma\,dW$ の滞在密度は $p(z)\propto\phi'(z)^{-2}=e^{-2z}$（$z<0$）で低可動度域に溜まる。積分が発散するので厳密には吸収でなく null-recurrent（戻るが期待時間が無限）で、**有限ホライズンでは実効的な壁**。復元力 $f>0$ を加えると平衡深さが有限になり

$$p(z)\propto e^{-2z}\exp\!\Bigl(-\frac{2f}{\sigma^2}e^{-z}\Bigr),\qquad z^{*}=\log\frac{f}{\sigma^{2}}$$

$f\propto v_i^2$ なので、**読み出し重みの小さいユニットほど深く沈む**という含意が付く。

> **格**: §2.3 全体が導出であり未検証。特に $z^*$ の式は 1 次元・$v_i$ 固定・$\sigma$ 一定の近似で、実際には $v_i$ も学習される（$\partial_{v_i}L=2\delta\phi(z_i)\ne0$ は沈下しても消えない）。**登録するのは検定であって結論ではない。**

### 2.4 本走が答えないこと（明示）

- **柱2 の「片道の壁・不可逆吸収」は ELU には literal には適用されない。** ELU に吸収集合は無い。本走が問うのは「機構（無方向の揺れ＋可動度の非対称）が軟らかい版でも成り立つか」であって、柱2 の再確認ではない
- **rsl_rl / PPO への接続は本走の外。** 実用側は Adam（[[命題リスト]] Q15 の成立条件から外れる）・幅 512 級・別の教師。v4 と同じく**予測として書き、測定の主張と混ぜない**
- ELU の $\alpha$ は 1.0 単一値。**掃かない**
- ReLU 側の既存結論を書き換えない

## 3. スコープと前提

condA（m=20・f=15・T=10⁴・batch=1・enc=std）、隠れ 2 層 × 100、素の SGD、lr 0.01、正則化なし（Adam / weight decay を入れない）、`center_alpha` = 0.01、seed 0–9、`total_steps` = 5e6（500 タスク）、device=cpu。**condB・他幅・他教師・3 層以上へ外挿しない。**

## 4. 設計

### 4.1 腕（5 本）

| 腕 | 活性化 | 中心化 | 役割 |
|---|---|---|---|
| `R_none` | ReLU | なし | **`mlp2_phase1_0829` の `L2_none` を bit 再現する**（S0'） |
| `R_A1` | ReLU | 第1層 | **同 `L2_A1` を bit 再現する**（S0'）。Q1 の対照の基準 |
| `E_none` | ELU | なし | ELU で LoP が出るか。入力 µ（dose ≈ 12）下での沈下 |
| `E_A1` | ELU | 第1層 | **本命。** 観測正規化＋ELU に対応する配置 |
| `E_Aall` | ELU | 両層 | **Q2 の判別腕。** 第2層の µ を消しても沈下が残るか |

**`E_Aall` を入れる理由**: 沈下が µ 駆動なら第2層の中心化で止まり、無方向の拡散＋可動度勾配なら止まらない（可動度 $e^z$ は中心化に不変）。Q2 の 2 説を**介入で分ける唯一の腕**。

### 4.2 腕の対応づけ

活性化関数の変更も中心化も**乱数を消費しない**ので、5 腕は init・教師・入力列・flip 軌道が bit 一致する。したがって**全腕が seed 単位で対応がつき、paired bootstrap を使える**。

- **ただし ReLU 腕と ELU 腕は step 1 以降で軌道が分岐する。** ペアリングが落とすのは init・教師・入力実現の分散だけで、「同一個体の追跡」ではない。引用時にこの一行を落とさない
- **S-pair（§6）でこの前提を検査する。** 落ちたら paired 統計は使わない

### 4.3 実装

`src/nets.py` の `VecMLPL` に活性化オプションを足す。**ReLU の実行経路は 1 行も触らない**（S0' が守る）。

- 前向き: `a = where(z>0, z, alpha*expm1(z))`。`expm1` を使う（$z\to0^-$ で桁落ちしない）
- 後ろ向き: $\phi'(z)=1$（$z>0$）／$\phi(z)+\alpha$（$z\le0$）。**前向きの `a` を再利用する形で書く**（`exp` を 2 回呼ばない・数値一致を保つ）
- **勾配は閉形式のまま。autograd に切り替えない**（S0' が不可能になる）
- 中心化は Phase 1 の層別 `centered` フラグと `running_mean` をそのまま使う

### 4.4 ELU での用語（**新規・[[用語と記号]] に 1 行足す**）

ELU にゲートは無いので `strict_dead`（$\hat p_i\equiv0$）は定義できない。代わりに

$$\text{沈下（submerged）}:\quad \max_{x\in\text{32 パターン}} z_i(x)\le 0,\qquad \bar z_i:=\mathbb E_{\text{support}}[z_i]$$

- **`submerged_frac` を verdict に使わない。** 沈下は死ではない（$\phi'=\alpha e^{z}>0$ なので勾配は流れ続ける）。`strict_dead` を verdict に使わない cbp_harm 裁定と同じ理由が、より強く当てはまる
- ReLU 腕では従来どおり `strict_dead` を REPORT_ONLY で記録し、**ELU 腕の `submerged_frac` と同じ列に混ぜない**

## 5. 集計（事前登録）

窓: 早期 = タスク 2–11、末尾 = タスク 451–500（Phase 1 と同じ）。未フィット率の床は **1e-23**（Phase 1 で実測較正済み。本走では再較正せず、床にかかった点の割合を報告する）。bootstrap は B=10000、`rng = np.random.default_rng(20260829)`。**`20260830` は `generator_offset` として既用なので使わない。**

CI は studentized bootstrap。**退化検出と fallback は Phase 0b §5.5 を逐語継承**（resample の SE < 1e-15 が 1% 超、または CI 幅が点推定の絶対値の 100 倍超で `CI_DEGENERATE` → studentized は報告のみ、percentile と符号検定で判定）。

### 5.0 G0 — ELU で LoP が出るか【前段ゲート】

各 ELU 腕について末尾窓の未フィット率の seed 中央値と全 10 値。

| 条件 | verdict |
|---|---|
| 末尾窓 seed 中央値 ≥ 0.05 | `LOP_PRESENT` → Q1・Q2 の集計へ進む |
| < 0.05 かつ `R_A1` との paired Δlog10 の CI が 0 を下に外す | `LOP_REDUCED` — **これも結果**。Q1・Q2 は記述として報告するが、機構の主張には使わない |
| 上記以外 | `INCONCLUSIVE` |

閾値 0.05 は Phase 0/0b/1 から引き継ぎ、変更しない。

### 5.1 Q1 — 第2層の dose は抑えられるか再生されるか【主 endpoint】

対象は `E_A1` の第2層入力 dose $=\|\boldsymbol\mu^{(2)}\|/\sqrt{\operatorname{tr}\Sigma^{(2)}/d_2}$（32 パターン厳密サポート上・タスク末尾記録点のみ・位相を揃える）。

**P1a（水準）**: 末尾窓の seed 中央値。

| 条件 | ラベル | 読み |
|---|---|---|
| < 2.0 | `MU_SUPPRESSED` | Clevert regime。$g(s)\le0.2$ すなわち $s\lesssim1$ に対応（dose $=\sqrt{d_2}\,g(s)=10\,g(s)$） |
| ≥ 9.33 | `MU_REGENERATED` | 柱3 の遷移帯下端（dose 単位 9.33–12.16）に到達 |
| その間 | `MU_INTERMEDIATE` | ReLU の 7.18 と同水準かを併記 |

**閾値 2.0 と 9.33 は結果を見る前に凍結。** 2.0 は §2.1 の閉形式（$d_2=100$ 幅だが i.i.d. 近似で dose $\approx\sqrt{d}\,g$、$\sqrt{100}=10$）、9.33 は [[柱3遷移帯の時間依存_spec_0829]] §1b の換算による。**dose と生の $\|\boldsymbol\mu\|_2$ を混同しない。**

**P1b（傾き）**: 各 seed でタスク末尾 dose 系列（500 点）とタスク番号の Spearman。seed 中央値 ＋ studentized bootstrap。**増加 = CI が 0 を上に外す。**

- ReLU `R_A1` の対応値（Phase 1 committed は Δ median −0.905 [−0.932, −0.874] で減少側）と **paired 差**を取る
- **P1b が Q1 の判別線。** 水準だけでは §2.1（$s$ 依存）と §2.2（沈下レバー）を分けられない。傾きが上向きなら沈下レバーが効いている

**P1c（レバーの分離・事前登録）**: 各記録点で第1層の $\mathrm{sd}(z_i)$ 中央値 $s$ と沈下率を出し、実測 dose を $10\,g(s)$（§2.1 の閉形式）と比べる。

| 署名 | 読み |
|---|---|
| 実測 ≈ $10\,g(s)$ | dose の動きは事前活性スケールで説明できる（§2.1 のみ） |
| 実測 ≫ $10\,g(s)$ かつ 沈下率と正相関 | **沈下が µ を作っている**（§2.2） |

格: P1c は P1a/P1b の verdict を上書きしない。閉形式はガウス近似で、事前活性は 32 パターン離散。**一致・不一致のどちらも近似の妥当性の話に落ちうる。**

### 5.2 Q2 — 沈下は駆動か拡散か【主 endpoint】

**位相の扱い（重要）**: 増分はすべて**タスク内の隣接記録点**（`lop_every=1000`、T=10⁴ なので 1 タスクあたり 9 区間）で取る。タスク境界を跨がない。境界では flip ビットが変わりサポートが変わるので、$\bar z_i$ の跳びは重みの移動を意味しない（教訓⑦・[[引用禁止]] C）。

対象ユニット: 区間**始点**で沈下（$\max_x z_i\le0$）しているもの。**始点で条件付ける**（終点や区間平均で条件付けると平均への回帰が符号に化ける）。

**P2a（可動度スケーリング）**: 始点の $\bar z_i$ でビン分割し、ビンごとに $\mathrm{sd}[\Delta\bar z_i]$ を出す。$\log \mathrm{sd}$ を $\bar z$ に回帰した傾き $\beta$。

| 条件 | ラベル |
|---|---|
| $\beta$ の CI が 1 を含む | `MOBILITY_SCALING` — 拡散が $\phi'=\alpha e^{z}$ に従う |
| CI が 1 を外し 0 を含む | `NO_SCALING` — 可動度勾配で説明できない |
| その他 | `PARTIAL_SCALING` |

**P2b（駆動対雑音比）**: 同じビンで $\rho:=\mathrm{median}[\Delta\bar z_i]\,/\,\mathrm{sd}[\Delta\bar z_i]$（無次元）。沈下領域（$\bar z<0$）のビンをプールした seed 中央値。

| 条件 | verdict |
|---|---|
| $\rho\le-0.3$ | `DRIFT_DOMINATED_DOWNWARD` — **Issa 予測の側**。µ 方向の駆動で沼へ押される |
| $\|\rho\|<0.1$ | `NOISE_DOMINATED` — **Claude 予測の側**。無方向の揺れで沈む |
| $\rho\ge+0.3$ | `DRIFT_DOMINATED_UPWARD` — 自己項の復元力が正味で勝つ |
| その他 | `MIXED` |

閾値 0.3 / 0.1 は結果を見る前に凍結。**符号と大きさを別々に報告する**（符号だけでは §2.3 の 2 説を分けられない）。

**P2c（µ 駆動の介入判別）**: `E_A1` と `E_Aall` の第2層で P2a・P2b を比べる（paired）。

| 署名 | 読み |
|---|---|
| `E_Aall` で $\rho$ が 0 へ寄り、`E_A1` で負 | 沈下は **µ 駆動**（Issa 側） |
| 両腕で $\rho\approx0$ かつ両腕で `MOBILITY_SCALING` | 沈下は **拡散＋可動度**（Claude 側） |
| `E_Aall` で沈下率だけが下がり $\rho$ は変わらない | µ は沈下の**速さ**を変えるが機構は拡散 |

**P2c が Q2 の本命。** 観察的な条件付けではなく介入なので、[[引用禁止]] E の「層別後の観察的関連を因果介入と呼ばない」に抵触しない。

### 5.3 副次（REPORT_ONLY）

- **$z^{*}\propto\log v_i^2$ の検定**: 末尾窓の沈下ユニットについて Spearman($\bar z_i$, $\log v_i^2$)。seed 中央値 ＋ bootstrap。**正なら §2.3 の平衡深さの式と整合**し、選抜が $|v_i|$ という機能量を見ていることになる。[[機能盲目性_0823]] の ELU 版は ReLU（RD = −0.2353・PROTECTIVE）と結論が変わりうる。**$v_i$ は沈下しても学習され続けるので観察的関連であり、因果ではない**
- 層別の `dose` / `mu_norm` / `sigma_rms` / `‖w‖` / `eff_rank` / `eff_rank_W` / `alive`（ReLU 腕）/ `submerged_frac`（ELU 腕）/ `wcos_mean` / `stable_rank_W` / `top1_frac` の時系列
- **`‖w‖` は主要共変量として事前登録**（Phase 0/0b/1 から継続。Chiley §3.4 と Dohare 図 4b の教訓）
- 未フィット率の早期窓と末尾窓の paired Δ（Phase 1 §5.2b の水準シフト判別。**署名表は Phase 1 で欠陥が判明している**［[[多層Phase1結果_0829]]・現在地の穴 4］ので、**署名ラベルは付けず生の Δ と傾きだけ報告する**）

### 5.4 退化・検閲の扱い

- **$\Sigma^{(2)}$ の退化ガード**: $\sqrt{\mathbf w_i^\top\Sigma^{(2)}\mathbf w_i}<10^{-8}$ を `NA` とし、**件数を判定と同格で報告する**。ELU では沈下ユニットの出力が定数に寄るので $\Sigma^{(2)}$ の実効ランクは ReLU より落ちやすい。`NA` が末尾窓で 20% を超えたら、その腕の dose と壁座標を `DEGENERATE` と立てて**判定に使わない**
- 床検閲: 各腕の床（1e-23）にかかった点の割合を列に持つ。20% 超で `CENSORED`、paired 統計を判定に使わず符号検定へ
- **数値発散**: Phase 1 §5.7 の実行追補を逐語継承。probe ごとに重み・bias・読み出し・`running_mean` の NaN/±Inf を検出し、検出した腕は `NUMERIC_DIVERGENCE` に固定して停止。seed 除外・再初期化・LR 低下・clipping による救済をしない。**`L2_A2` の前例（step 141000・task 15・seed 7）があるので、これは形式ではない**

## 6. 前段チェック（本走前。結果は見ない）

- **S0'（必須）**: `R_none` / `R_A1` が `results/mlp2_phase1_0829/` の `L2_none` / `L2_A1` と **state_hash・既存全列が完全一致**すること。**不一致なら本走に進まない**
- **S-pair（必須）**: 5 腕の step 0 で init・教師・入力列・flip_state が bit 一致すること（30k 短走）
- **S-grad（新規・必須）**: ELU の閉形式勾配を有限差分と照合し、相対誤差 1e-6 以内。$z=0$ の近傍（$|z|<10^{-6}$）と深い負側（$z=-30$）を含める
- **S-elu-limit（新規・恒真の逆チェック）**: $\alpha\to0$ で ELU 経路が ReLU 経路に一致すること。**教訓⑩に従いサニティ側にも掛ける**
- **S-taut**: 中心化を入れた層で $M_i$ が定義上ゼロになることを確認する。**したがって M / D は verdict に使えない**（Phase 1 と同じ）
- **S1/S2**: 32 パターン全サポート上の直接列挙と $\mathbf w_i\cdot\boldsymbol\mu+b_i$ / $\sqrt{\mathbf w_i^\top\Sigma\mathbf w_i}$ が相対誤差 1e-10 以内で一致
- **S3**: `OMP_NUM_THREADS=1`
- **S5**: 退化ガードの自己検査（Phase 0b から継承）
- **S-submerge（新規）**: 沈下判定 $\max_x z_i\le0$ が、ReLU 腕に掛けたとき `strict_dead` と完全一致すること（同じ条件の別表現であることの確認）
- **本走前に §5 の集計は一切しない。** スモークで見るのは構造だけ

## 7. 出力とコスト

`results/elu_swamp_0830/` に `verdict.csv`（腕・活性化・対比・endpoint・CI・`NA` 割合・床割合・`DEGENERATE`・`CENSORED`・`CI_DEGENERATE`・`NUMERIC_DIVERGENCE` を列に持つ）、`summary.md`、`layer_stats.csv`（タスク末尾行のみ）、`increments.csv`（§5.2 のタスク内増分・ビン集計）、`provenance.json`、`logs/`、`arm_status/`。

**数値は `verdict.csv` と `summary.md` からのみ転記する。**

コスト: 5M × 10 seed × 5 腕。Phase 1（5 腕）が約 1 時間。ELU は `exp` のぶん重いので **1.5 時間前後**の見込み。**想定を大きく超える場合は本走前に相談する。**

## 8. 事前予測（**実行前に記入すること。未記入のあいだ事前登録は成立しない**）

### 8.1 Issa

1. **Q1 = ?**（`MU_SUPPRESSED` / `MU_INTERMEDIATE` / `MU_REGENERATED`）— 記入待ち
2. **P1b（第2層 dose の傾き）= ?**（増加 / 変化なし / 減少）— 記入待ち
3. **Q2 = ?**（`DRIFT_DOMINATED_DOWNWARD` / `NOISE_DOMINATED` / その他）— 記入待ち。**チャット時点の表明は「µ の射影 x 方向に強い力場がかかり負へ向かう」＝ `DRIFT_DOMINATED_DOWNWARD` 側**
4. **P2c（`E_Aall` で沈下が止まるか）= ?** — 記入待ち
5. **G0（ELU で LoP が出るか）= ?** — 記入待ち
6. **外れたときに第一に疑うもの = ?**（順序も指定）— 記入待ち

### 8.2 Claude（2026-08-30 記入・実行前）

1. **Q1 = `MU_INTERMEDIATE`、P1b は増加**。理由: §2.1 で $s$ が 0.35 → 0.5 級に育つだけでは dose は 1 前後にしかならないが、§2.2 の沈下レバーは沈下率に比例して効く。両者の合成で ReLU の 7.2 に届くかは読めないが、**傾きは上向き**に出ると予測する
2. **Q2 = `NOISE_DOMINATED`、P2a = `MOBILITY_SCALING`**。理由: §2.3 で ELU の自己項は復元力であり、沼へ押し込む片符号の力ではない。沈下は $p(z)\propto e^{-2z}$ の滞在密度で説明できる
3. **P2c = 「両腕で $\rho\approx0$、`E_Aall` で沈下率だけが下がる」**。µ は沈下の速さを変えるが機構は拡散、という署名
4. **外れたときに第一に疑うもの**（順）:
   - (i) **タスク内増分の窓が短すぎる／長すぎる。** 1000 step の区間で $\bar z_i$ が動きすぎると始点条件付けが効かない。区間長の感度は登録していない — **これが最初の容疑者**
   - (ii) **$\Sigma^{(2)}$ の退化**（§5.4）。沈下ユニットの定数出力でランクが落ち、dose と壁座標が偏った部分集合の統計量になる
   - (iii) **F_self / F_rest の構造的相殺**（[[論点マップ]] §14・Q17）。ELU でも自己項と rest 項が鏡像なら、自己項だけで符号を語った §2.3 が誤り。**この分解は本プロジェクトで 5 回以上拾われて 5 回とも落ちている**
   - (iv) 上が全部空振りなら **§2.3 の 1 次元近似そのもの**を疑う。$\sigma$ が $\bar z$ に依存する（沈下ユニットほど $\delta$ の効き方が違う）なら滞在密度の式が変わる

**両者の予測が Q2 で割れている。** どちらが当たっても記録に残す。**割れていること自体を結果の格に反映させる**（後から「そう思っていた」を足さない）。

## 9. 引用上の注意（結果が出た後に守ること）

- **ReLU 腕と ELU 腕のペアリングは init・教師・入力実現までで、同一個体の追跡ではない**（§4.2）
- **`submerged_frac` を verdict に使わない。沈下は死ではない**（§4.4）
- **ELU 腕の結果で ReLU 側の柱1・柱2・柱3 を書き換えない。** ELU は新しいスコープ
- **柱2 の「片道の壁・不可逆吸収」を ELU に literal に適用しない**（§2.4）
- **§2.1 の閉形式・§2.3 の滞在密度と平衡深さは導出であり、本走の測定対象。前提として引かない**
- **rsl_rl / PPO への接続は予測。** Adam・幅・教師がすべて外（§2.4）
- 中心化を入れた層の $M$ / $D$ は恒真。verdict にも機構の主張にも使わない
- 「rsl_rl が ELU を使うことが多い」は**伝聞**。原典（実装リポジトリ）確認までこの格を落とさない
- スコープは condA・T=10⁴・batch=1・隠れ 2 層 × 100・$\alpha=1$

## 10. 作業表

| # | 作業 | 走 | 状態 |
|---|---|---|---|
| 1 | §8.1 の Issa 予測を記入 | なし | **未着手（これが済むまで実行不可）** |
| 2 | rsl_rl の既定活性化・正規化配置を原典で確認 | なし | 未着手（§1 の伝聞の格を上げる） |
| 3 | 本 spec と config を repo へ commit・push | なし | 未着手 |
| 4 | `VecMLPL` の ELU 拡張と S0'・S-grad・S-elu-limit | なし | 未着手 |
| 5 | 本走 | **あり** | 未着手 |
| 6 | [[用語と記号]] に「沈下（submerged）」を 1 行足す | なし | 未着手 |
