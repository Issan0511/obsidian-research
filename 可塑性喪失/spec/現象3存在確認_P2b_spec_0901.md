# 現象3存在確認_P2b_spec_0901: 幅 5 × 活性化（対 Linear ベースラインの seed 別符号検定）

状態: **起草**（未凍結・未実行。**§4 の記入枠 3 行が埋まれば凍結できる**） / 作成: 2026-09-01（チャット `P3_P2_0901` 起草） / run id 案: `width5_gate_b_0901`

前身: **[[現象3存在確認_P2_spec_0901]]（失効）**。repo 正本 `c916acf` 凍結 → 実装 `240b713` → preflight `2ccf6be` で **S-cap FAIL**、登録どおり本走を停止した。
親: [[現象3_ゲート非依存LoP_0831]] §8 P-2 / 参照: [[現象3指標再定義_P3_0901]]・[[容量不足レジームの機構_0830]] §5.1–§5.2・[[ゲート条件付き用量結果_0830]]・[[9月運用_0901]] W3

> **凍結手順**: ① Issa が §4 の記入枠 3 行を埋める → ② repo に **spec 単独で commit** → ③ 実装 commit → ④ 結果 commit。**§8 の裁定 6 点と §4 の carry-over 4 点は前 spec から引き継ぎ、再裁定しない。**

---

## 1. 前 spec が失効した理由と、本 spec が変えるところ

### 1.1 失効理由（**committed 出力**・`results/_preflight_width5_gate_0901/`）

`onset_threshold = 0.05` が**幅 5 の到達床より下**にあった。early 窓（task 2–11）の seed 別最小 `unfit`:

| 腕      | 最小     | 中央値    | 最大     | 0.05 未満  |
| ------ | ------ | ------ | ------ | -------- |
| `R5`   | 0.0609 | 0.1632 | 0.6744 | **0/20** |
| `LIN5` | 0.1549 | 0.3183 | 0.7057 | **0/20** |

**どの緩め方をしても FAIL する**（最良 seed でも 0.061 > 0.05）ので、判定は操作化に依存しない。

### 1.2 ★ これは閾値のずれではなくスコープの発見である

**幅 5 には「フィットした状態」が存在しない。** 走り始めの最良時点ですでに 16%（ReLU）〜 32%（線形）が未フィットである。本プロジェクトが一貫して使ってきた LoP の登録概念 —— **フィット状態から絶対閾値を割り込む** —— は、**幅 5 では定義されない。**

そして **Dohare も同じ regime にいる。** 図 B.10 の縦軸は 0.45 → 1.05 で推移し Linear は 0.79–0.85 なので、原典でも一度もフィットしていない。つまり **Dohare の LoP は絶対水準の割り込みではなく、線形ベースラインに対する相対的な上昇である。** [[容量不足レジームの機構_0830]] §5.1 が「判定規則はここが残る差」と書いていた項目が、実は本質だった。

### 1.3 ★ §5.1 の分解能較正は幅 5 では成り立たない（前 spec の前提が 1 つ壊れた）

前 spec §5.1 は **幅 100 からの外挿**で「腕間ペアリングは分散を減らさない」と結論し、それを根拠に G1 を報告へ降格した。**preflight の実測はこれを否定する。**

|                      | w100（`gate_dose_0830` からの外挿） | **w5（preflight 実測）**                    |
| -------------------- | ---------------------------- | --------------------------------------- |
| 腕間の seed 相関（Pearson） | 0.087                        | **0.735**                               |
| 対応差の SD              | 1.129（各腕の SD **より大きい**）      | **0.0905**（各腕の 0.128 / 0.120 **より小さい**） |

**幅 5 ではペアリングが効く。** 幅 100 では各腕が別々の解に行けるのに対し、幅 5 では両腕が同じ教師・同じタスク実現に押さえつけられ、seed ごとの難易度が腕をまたいで共通に効くためと見られる（**機構は未検証**）。

**留保**: これは early 窓（task 2–11・約 110k step）の値である。5M 末尾では乖離が進んで相関が落ちうる。本 spec は**相関の水準に依存しない符号検定**を主判定に置くことでこの不確かさを回避する。

### 1.4 変更点（1 箇所だけ）

> **G0 を「絶対閾値の発症カウント」から「対 `LIN5` の seed 別符号検定」へ置き換える。**

較正定数が不要になり、**Dohare の判定規則そのもの**になる。設計・腕・乱数・ロガーは前 spec のまま変えない。

---

## 2. 設計（前 spec から変更なし）

- harness・condA・5M 直行・窓（末尾 `task 491–500` / 1M `task 91–100` / early `task 2–11`）・seed 20 は前 spec §2 のまま
- 腕 8 本（案 B）: `R5` / `LR5` / `E5` / `LIN5` / `R100` / `LR100` / `E100` / `LIN100`。`LIN` は leaky($a$=1.0) の実装
- **`generator_offset = 202609011921` を据え置く。** 同じ offset・同じ seed・同じ腕なので、**preflight が測った early 窓の値はそのまま本走の early 窓の値になる**（軌道が一致する）。§1.1 の表は本走の early 窓として引き継げる
- bootstrap seed も **`202609011921` を据え置く**（前 spec では verdict に消費されていないため再利用に問題はない。G0 は Clopper–Pearson で bootstrap を使わない。G1・G3 の水準で使う）
- ロガーは前 spec §2.4 のまま（$\bar z$・`denom`・$s_i$・$m_i=\mathbb E[\varphi']$・$R_c$・可動度重みペア cos）。実装は `240b713` に存在し、S-mob・S-rank・S-lin は **PASS 済み**

---

## 3. 実行前 sanity

前 spec で **PASS した 8 項目（S-omp / S-offset / S-lin / S-mob / S-rank / S-floor / S-initial-pairing / S0′）は再実行する**（実装を触るため）。結果は `2ccf6be` と一致すること。

**S-cap は廃止し、S-floor-char に置き換える。**

- **S-floor-char（較正・ゲートではない）**: `LIN5` と `R5` の early 窓の seed 別最小 `unfit` を測り、**`summary.md` に床として報告する**。**本走を阻止しない。** 前 spec の S-cap は絶対閾値の妥当性を検査するゲートだったが、本 spec の G0 は絶対閾値を使わないので、検査対象そのものが消えた
- **S-lin-ref（新規）**: `LIN5` が全 seed・両窓で有限の `unfit` を返すこと。G0 は `LIN5` を分母ではなく比較相手に使うので、**`LIN5` が発散する seed があると当該 seed が符号検定から落ちる**。落ちた seed 数を `summary.md` に明示し、n を減らして Clopper–Pearson を計算する
- 未フィット率の床は **1 層系 `1e-16` を `gate_dose_0830` から継承**（S-floor で確認済み）

---

## 4. 事前予測

### 4.1 前 spec から引き継ぐ（**再記入不要**）

| #   | 量                            | Issa 記入（凍結済み・`c916acf`） |
| --- | ---------------------------- | ----------------------- |
| 4   | `LIN5` に対する `LR5` の位置        | **上（`LR5` の方が悪い）**      |
| 5   | `LR5` の沈下率（末尾・中央値）           | w100 の 0.63–0.67 と同程度   |
| 6   | `E5` の沈下率（同上）                | w100 の 0.36–0.45 と同程度   |
| 7   | $R_c$ が `LR5` で `LIN5` より低いか | 低い                      |
| 8   | 外れたときに疑う原因                   | （前 spec の記入を引き継ぐ）       |

### 4.2 ★ 新しく記入が要る 3 行（**ここだけ埋めれば凍結できる**）

$k_A$ := **末尾窓（task 491–500）で腕 $A$ の `unfit` が同 seed の `LIN5` より大きい seed 数**（20 中）。$k_A$ が大きいほど「線形より悪い」＝ Dohare の意味で LoP がある。

| #      | 量                         | 起草側候補値                            | **Issa 記入** |
| ------ | ------------------------- | --------------------------------- | ----------- |
| **N1** | $k_{R5}$（ReLU 対 Linear）   | ≥ 15/20                           | ≥ 15/20     |
| **N2** | $k_{LR5}$（leaky 対 Linear） | ≥ 15/20（carry-over の項目 4「上」と同じ向き） | ≥ 15/20     |
| **N3** | $k_{E5}$（ELU 対 Linear）    | 5–15/20                           | 5–15/20     |

**N1 が非自明であることの注意。** preflight は **early 窓で `R5` が `LIN5` より良い（19/20 seed・差の中央値 −0.158）** ことを示している。したがって N1 に「≥ 15/20」と書くことは、**5M の間に符号が反転すると予測すること**を意味する。Dohare 図 B.10 の「ReLU はやがて Linear を上回る」がまさにその主張である。**反転しない可能性も同程度にある**ので、「分からない」は妥当な記入である（[[柱3定用量5M_spec_0830]] §7 の前例）。

**格の限定**（前 spec §4 と同じ）: 候補値は起草側（Claude）が先に書いた値である。採否した場合、当たったときの格は「起草側の予測を Issa が承認し、それが再現した」であって「Issa が独立に立てた予言が当たった」ではない。

---

## 5. 事前登録判定

実験単位は seed（n=20）。**G0 は Clopper–Pearson 95%（bootstrap 不要）。** G1・G3 の水準は percentile bootstrap（B=10,000・seed `202609011921`）＋ studentized fallback と退化ガード（教訓⑬）。

### G0（**単独の主判定**・Dohare の判定規則）

腕 $A \in \{R5,\ LR5,\ E5\}$ ごとに独立に、$k_A/20$ の Clopper–Pearson 95% CI を 0.5 と比較する。

- CI 下限 > 0.5 → **`{A}_ABOVE_LINEAR`**（Dohare の意味で LoP あり）
- CI 上限 < 0.5 → **`{A}_BELOW_LINEAR`**
- CI が 0.5 を含み、かつ **CI ⊆ [0.20, 0.80]** → **`{A}_NOT_SEPARATED_TIGHT`**
- CI が 0.5 を含み、帯からはみ出す → **`{A}_INCONCLUSIVE_WIDE`**

**n=20 での到達可能性（事前計算・[[運用ルール]] §5 教訓⑪の連続版）**: `ABOVE` には $k \ge 15$（CP 下限 0.509）、`BELOW` には $k \le 5$（CP 上限 0.491）が要る。`NOT_SEPARATED_TIGHT` は **$k \in \{9,10,11\}$ でのみ到達可能**（$k$=9 で [0.231, 0.685]、$k$=12 で [0.361, 0.809] は帯からはみ出す）。**帯 [0.20, 0.80] はこの到達可能性から逆算した値であり、科学的な閾値ではない。**

**現象3 の主判定**: `LR5` と `E5` の少なくとも一方が `ABOVE_LINEAR` → **`PHENOMENON3_REPRODUCED`**。両方が `BELOW_LINEAR` または `NOT_SEPARATED_TIGHT` → **`PHENOMENON3_NOT_REPRODUCED`**。それ以外 → **`PHENOMENON3_INCONCLUSIVE`**。

### G0b（**登録された副判定**・図 B.10 の交差そのもの）

腕 $A$・seed $s$ について、**early 窓で `LIN5` より良く、末尾窓で `LIN5` より悪い**（＝符号が反転した）seed 数 $c_A/20$ を Clopper–Pearson で報告する。図 B.10 の曲線形状（線形ベースラインの下から始まり上で終わる）の直接の対応物である。

- **条件つき判定**: early 窓で $A$ が `LIN5` より良い seed が **15/20 以上**の腕についてのみ判定に用いる。それ未満の腕では交差の定義が空振りするので **`CROSSING_UNDEFINED`** とし、early 窓の内訳を `summary.md` に出す
- **preflight により `R5` はこの条件を満たすことが既に分かっている**（19/20）。`LR5` と `E5` の early 窓は未測定

### G1（水準・**報告のみ**）

末尾窓 $\log_{10}U$ の腕別 20 seed 中央値と、対 `LIN5` の対応差の CI。**§1.3 により前 spec の降格根拠（分散が減らない）は幅 5 では成り立たないが、early 窓の相関からの外挿にすぎないので、本 spec では昇格させない。** 実測された末尾窓の相関を `summary.md` に出し、**次の spec の設計材料にする**。

### G2（幅対比・記述のみ）

w5 腕 対 w100 腕。**非対応**（`train.make_gens` の基底に幅が入る）なので腕間の paired 統計は取らない。

### G3（機構・3 軸・報告のみ）

[[現象3指標再定義_P3_0901]] の 3 軸。軸1 `median_s`（`median_M + median_B` ではない）と生の `zbar`・`denom`／軸2 $m_i$（ReLU・leaky では $\hat p$ の関数なので恒真・ELU 腕でのみ独立）／軸3 $R_c$ と $R_c/\sum_i\tilde m_i$。**登録判定に入れない**（§8-3 の裁定を引き継ぐ）。

---

## 6. 窓・引用の規律

- 数値を書くたびに窓ラベルを添える（教訓⑫）
- **`k_A` を「LoP の発症率」と呼ばない。** 測っているのは**線形ベースラインとの相対位置**であって、絶対的な機能劣化ではない
- **`ABOVE_LINEAR` を「絶対閾値を割り込んだ」と読み替えない**（§1.2 のとおり幅 5 に絶対閾値は置けない）
- **「ELU / leaky は LoP を防ぐ」と一般には書かない**（Q22 反論欄・維持）
- `LIN` を「線形ネットワーク」と呼ぶときは leaky($a$=1.0) の実装であることを併記する
- 幅対比を「同一個体の幅違い」と書かない
- **§1.3 の相関 0.735 を末尾窓の値として引かない**（early 窓・task 2–11 の値である）
- スコープは condA・$T=10^4$・batch 1・`lr` 0.01・5M

---

## 7. コスト

前 spec §7 のまま。8 腕・**約 2 時間**の見積り（`gate_dose_0830` の実測 429–468 秒/腕・10 seed・w100 からの外挿）。preflight の 8 項目再実行は数分。

---

## 8. 裁定（**前 spec §8 から引き継ぎ・再裁定しない**）

案 B（w5×4 + w100×4）／`LIN` 腕あり／G3 は報告のみ／`generator_offset` と bootstrap seed は `202609011921`／seed 並列は使わない。**変更は G0 の定義 1 点のみ**（§1.4）で、これは 9/1 の Issa 指示による。

---

## 9. 本 spec が前提にしている未登録の判断

- **§1.2 のスコープ主張**（幅 5 に絶対閾値の LoP 概念は置けない・Dohare も同じ regime）は **Claude の整理**であり、先生の承認も登録判定も無い。[[命題リスト]] への行立ては裁定待ち
- **§1.3 のペアリングの機構説明**（幅 5 では seed 難易度が腕をまたいで共通に効く）は**論証であって測定ではない**
- 図 B.10 の読みは**目視読み**。本走はそれを再現するのではなく**置き換える**ためのもの
