2026-08-21c（前版 2026-08-21b / 2026-08-21 / 2026-08-20b / 2026-08-20 / 2026-08-19b / 2026-08-19 / 2026-08-17）

# 論点マップ（チャット分岐の全体整理）

幹: Step 4のSDE導出崩壊（8/8）→ 理論v2への置換とµスイープ数値検証（8/11）→ 先生がSDE統一モデルへ移行提案・両レポート全面受諾（8/12）: dw = E[g]dt + √C(w)dW、Path A（diffusion＋吸収壁 → dead units）／Path B（drift → 低ランク整列）→ 8/12–15の実験群で「逐次連鎖」から「共有力場からの結合並行パス」へ再編 → **E1（posreset_0819）と condA 厳密力場計算により「整流（ラチェット）モデル」へ再々編（8/19）**: Path A / Path B は「同じ方程式の別々の項」ではなく「**同じ二極場の、罠のある側とない側**」→ **ロギング走（ratchet_log_0819）で時間構造を改訂（8/19b）** → **teachw_0820 で停止条件の需要説を棄却、blindspot pass で盲点6件を洗い出し、[[命題リスト]] を起票（8/20）** → **中和スモークで「税＝駆動」を撤回、駆動を rest へ差し替え（8/20 夕）** → **8/21 対面で中心主張を確定**: 「**力場が dead ユニットと整列を作り、問題の複雑さとは無関係にランクを下げる**」。**これがメインで、これ以上掘り下げない**。一般化（継続学習を超えて機械学習全体）はフレーミングで扱う。実験の続行範囲は未決定 → **dead2path_0821 で死の二経路を分離、P3 の混在懸念が解消（8/21b）**。

> **足場の台帳は [[命題リスト]] に分離した**（Q0–Q16）。本マップは経緯と分岐の地図、命題リストは主張ごとの証拠状態。未検証の思いつきは [[blindspot_0820]]。**記号・略号の定義は [[用語と記号]]**（8/21 新規）。
>
> **実験の選び方（8/20 に変更・8/21 に保留化）**: 実験は [[命題リスト]] の「閉じる作業」列から選ぶ。**どの命題の穴も閉じないものは回さない。** 旧「夏休み検証計画（E1–E6）」は `過去の遺物/` へ移し、**E1 以外は廃止**。本文中に残る E2/E3/E5/E6 の記述は「機構としてまだ埋まっていない穴」を指すだけで、**予定された実験ではない**。**8/21 以降は「これ以上掘り下げない」との線引きが決まるまで、新規実験は起案しない**。
>
> **データの所在（据え置き）**: `lop_analysis` は**実在する自分のリポジトリ**（github.com/Issan0511/lop_analysis）。チャット環境ではMCPが使えないので毎回cloneして参照する。

## 0. 現行フレーム: 整流（ラチェット）モデル

→ [[整流モデル]]（機構の確定形）／ [[ロギング走の結果]]（時間構造の決着）／ [[中和と境界窓]]（8/20 夕の役割訂正）／ [[死の二経路]]（8/21b の分離）／ [[位置リセット判別]]（出典実験）／ [[停止条件]]（8/20 新規）

> **中心主張の確定（8/21 対面）**
> 中心主張は「**力場が dead ユニットと整列を作り、問題の複雑さとは無関係にランクを下げる**」で確定
> （[[命題リスト]] に差し替え済み。旧「整列と dead は同一力場の二極」は取り消し線で保存）。
> **これがメインで、これ以上掘り下げない。**
> ランク低下は dead 経由（§10「殺すことによって」＋ teachw の複雑さ非依存）で立ち、
> 生存者側の eff_rank 低下への橋渡し（下記「未カバーの穴」①）は**非目標になった**（穴として残るが主張の前提ではない）。
> 一般化「継続学習を超えて機械学習全体で」は**論文のフレーミング（イントロ・議論）**で扱う。実験の続行範囲は未決定。

**部品1: 力場は厳密に二極【確定】** — F_i^ungate = −2η·v_i·G（G = E[δx] は全ユニット共通）。condA は全サポート32パターンで期待値を厳密評価でき、サンプリング誤差ゼロ。G ≈ 潮流（|cos(G,µ̂)| 0.966–0.995、µ項/Cov項 median 4.3）。二極性の符号一致率 0.993。極の割り当ては sign(v_i·E[δ])。**留保**: t=500k の単一スナップショット。**極の割り当ては時間平均するとほぼ消える**（部品3）。**8/20 追加**: |cos(G,µ̂)| = 0.97 が「潮流」と「税（自己項）」の混合である可能性（condA は非負入力なので両者が方向として縮退する）→ [[blindspot_0820]] §3、[[命題リスト]] Q12。**8/21b: 判別の spec を起票**（`specs/spec_cell4_0821.md`）。**保存ログでは出ない**（行と列が別対象）ので再計装リプレイが要る。FAIL の場合の処置は「解釈の限定を書く」で確定済み。

**部品2: 移動度は片側消灯【確定】** — 経験ゲート曲線は cos < −0.15 で p̂ = 0.000（ガウスβ近似の −0.46 より遥かに急峻。condA は有界入力なので**文字通りの吸収**が実在）。滑落は p̂<0.05 で停止 — 停止は力の釣り合いではなく**力の消灯**。**8/21b: この吸収はタスクごと**（→ §4、[[死の二経路]] §5）。

**部品3: 駆動の時間構造【8/19b 全面改訂／8/20 に生ログで再検証】** — `ratchet_log_0819`（境界±100 step を毎 step 記録、10 seed × 1M step、厳密期待値）。s(t)=sign(G·µ̂_t) の一致率を**ラグを揃えて**測定:

| ペア | 一致率 |
|---|---|
| バルク 1000 step 隣接（P1） | **0.497** [0.487, 0.507] |
| 1 step 隣接（一般・flip 跨ぎ除外） | **0.797** [0.783, 0.808] |
| **1 step 隣接・flip を跨ぐ**（＝A_boundary） | **0.496** [0.460, 0.532] |

- **「タスク内で安定」は棄却**（P1 FAIL）。相関時間 τ ≈ 5–10 step、タスク周期 10⁴ より 3 桁短い
- **「境界で反転」は成立**。paired 差（generic − straddle）= **+0.301 [+0.268, +0.332]**。flip は 1 step で符号を完全再ランダム化する
- **8/20 に生ログ（`logs/seed*.npz` の G・flip_state）から独立再計算し確認**: 非跨ぎ 0.7973 / flip 跨ぎ 0.4960 / 差 +0.3014。**flip は記録点 B と B+1 の間で起きる**ので、跨ぎペアは「先頭が step ≡ 0 (mod T)」側（オフバイワンに注意）。**8/21b に全 10 seed で 99/99 を再確認**（[[死の二経路]] §1）
- ~~旧・部品3 の 0.567（60遷移、事後解析）~~ → **廃棄**（再現不能）
- **引用禁止**: summary の「A_within 0.503 ≈ A_boundary 0.496 だから境界は特別でない」は**ラグ 1000 と ラグ 1 の比較**という誤り

**部品4: 自己項は構造的に片側【8/19b 確定・定理】／ただし駆動ではない【8/20 夕 訂正】** — 発火中ユニットの µ̂ 射影を self/rest に分解すると、**self は 10 seed × 20,901 記録点で一度も正にならない**（符号安定率 1.000）。解析的に F_self,i = −2η·v_i²·E[a_i·gate_i·(x·µ̂)] で condA は全因子非負 → **構造的に ≤ 0**。rest は 0.806、大きさ比 0.62。**自己項 = ∇½E[o_i²]（自分の出力パワーの勾配）の恒等式は 8/20 に決着**（点毎に成立する強い形）→ [[自己項の恒等式]]、[[命題リスト]] Q2・Q13。**ただし 8/20 夕、この片側性が正味の輸送を担うという含意は撤回**（フィット中 99.5% 中和、死亡個体の沈下に税が占める割合は中央値 24.5%）→ [[中和と境界窓]]、[[命題リスト]] Q2b。**8/21b: self の 1.000 は輸送死・再分類死・生存の 3 群別でも厳密に成立**（[[死の二経路]] D3）。**rest 0.806 は死亡ユニットの降下窓限定**で、群別では 0.787 / 0.858 / 0.838。

**合成【仮説・8/20 夕 改訂】** — ~~構造的に片側しか向かない自己項~~ × 片側だけ吸収的なゲート → −µ̂ 側にのみ堆積。**駆動は自己項ではなく rest**（v_i に線形の二極の揺らぎ）。自己項の役割は**罠の吸収性**の側へ移った（死領域で勾配が開集合上で恒等ゼロ）→ [[中和と境界窓]] §6、[[自己項の恒等式]] §5-bis、[[命題リスト]] Q2b。説明対象は変わらず (i) +µ極の不在（cos>+2SD 0/1000、dead 915中 cos>0 は0）、(ii) posflip の往復再死（+0.325→−0.300 ≒ 原初帯 −0.325）、(iii) sign_clone_frac ≡ 0。**輸送の供給源は「タスク境界に同期した反転」ではなく intrinsic churn が主**で、境界キックは符号ゆらぎ事象の ~1/1000。境界に固有の役割は**µ̂ ジャンプによる凍結ユニット再露出（匍匐）**の側に残る。「整流」の語の役割も変わった: 罠は「対称な揺れを非対称に変換する装置」ではなく「**もともと偏った輸送を不可逆にする装置**」。**ただし rest がなぜ偏るか（穴A）は未同定。**

**帰結** — 唯一の安定終点が dead なので Dohare 現象は普遍的に見え、Joudaki のクローン多様体には到達しない。非対称性は力場でなく**罠（ReLUゲート）**に由来。「力場と状態の乖離」の3例目。傍証: |E[δ]| と生存ユニット数の Spearman −0.890（p=0.0006, n=10）。

**既存結果との照合【verdict.csv 一致確認済】** — 矛盾なし。むしろ説明側に回る: sign_clone≈0／fullbatch の決定論成分（**intrinsic churn が輸送を供給**、diffusion は追加の対称輸送＝増幅因子）／perturb-only の毒（対称輸送の追加＝堆積加速）／coupling の順序非固定（同じ駆動の2観測量）／rank_int の一過性（**状態への介入は場に再輸送される**）／dironly>posonly と svdrec 有害・shuffle 中立の同方向性。A5 も「持続成分 = Cov 側、反転成分 = E[δ]µ 側」として収まる — 部品4 の self/rest 分解と同方向。

**未カバーの穴** — ①**生存者側の症状が出ない**（eff_rank 低下は整流単体から導けない。「eff_rank = m_alive × 生存者多様性」の**第2因子への橋渡しが未検証**。**8/21 対面で非目標化** — 中心主張の確定によりランク低下は dead 経由で立つ）。②condB への外挿は禁止。③b 経路は整流ではなく片符号ドリフト＋罠。④**intrinsic churn の起源が未同定**（`E_delta` 列で一部事後解析可能）。⑤**停止条件**（→ §0c）。⑥**v の力学・多層**（→ [[blindspot_0820]] §2・§6）。⑦**穴A: rest の偏りの機構が未同定**（8/20 夕 追加。→ [[中和と境界窓]] §8）。

**スコープ**: 部品1・2 は単一時点・condA・w100・t=500k。部品3・4 は 10 seed × 1M step 全域。**中和・役割訂正は R=8 スモーク**。**二経路の分離は 10 seed × 1M step 全域**（[[死の二経路]]）。

## 0b. ロギング走の判定サマリ

→ [[ロギング走の結果]]（**下表の P1–P5・E1 は同ノートのローカル ID。[[命題リスト]] の Q番号とは無関係**）

| ID | 予測 | 結果 |
|---|---|---|
| P1 | 駆動符号はタスク内で安定 | **FAIL**（A_within 0.497、基準 ≥0.90） |
| P2 | 境界で反転 | 字義 **FAIL**／**内容は成立**（同ラグ対照 +0.301） |
| P3 | 死亡は単調輸送 | **PASS**（median PE 0.396 > 帰無95%点 0.218、766窓）。**8/21b: 二経路を分離しても PASS**（輸送死のみ・µ̂ 固定増分のみで 0.3754 > 0.2085、596窓）→ [[死の二経路]] |
| P4 | 凍結ユニットの変位は境界集中 | 事前登録指標は**無効**（トートロジー）／**amended で支持**（BC_w 0.315、時間占有 2.01% に対し 15.7×。凍結区間の 76.5% で ‖w‖ が動く）。**‖w‖ 基準なので二経路の混在の影響を受けない** |
| P5 | 帯深は切替回数スケール | 字義 PASS だが**判定保留**（相関 0.9999 で識別不能）→ T スイープ待ち |
| E1 | 駆動分解（探索的） | self は構造的に負（1.000）・rest 0.806・比 0.62。**8/21b: 群別に再計算済**（self は 3 群とも 1.000） |

**P2 の扱い**: 事前登録基準が「within が安定」を対照に要求する形だったため、within が崩れた時点で自動的に落ちる。**予測内容そのものは成立**しているので、「基準の設計が仮説の一方に依存していた」という形で報告する。
**P3 の扱い（8/21b 追加）**: 分離前の 0.3959 は再分類死の窓（170/766）が押し上げていた（同群の PE は全増分 0.4843 / 輸送増分のみ 0.3068）。**以後は分離後の 0.3754 を引く。**
**P4 の扱い**: 事前登録 BC(cos) は p̂=0 のユニットで恒真。機構判断は amended 指標の 2 行のみ。**rank_int の width プールと同じ格**。

**サニティ**: S2（probe 有無で 100k step 後の全 state ハッシュ bit 一致）・S3・S4 全 PASS。恒等式は G 相対誤差 0.0、F_gate 4e-16。**生ログから主要指標を独立再計算し一致確認済**（8/20 の時間構造、8/21b の P3・E1 の全桁再現）。

## 0c. 停止条件 — 需要説の棄却【新規 8/20】

→ [[停止条件]]（詳細）

自己項が普遍かつ片側なら素朴には全滅するが、実測は止まる。「なぜ止まるか」に理論が無い、というのが blindspot pass で顕在化した穴。

`teachw_0820`（教師 LTU 幅 H_T ∈ {1,2,4,8,32,100} × seed 10、入力ストリームは全アームでビット同一 S2 PASS、H_T=100 は ratchet_log と厳密一致 S3 PASS）:

- **事前登録 P1 FAIL**（キルライン）: 傾き −5.755 [−7.615, −3.930]、Spearman ρ 中央値 −0.629。予言と**符号が逆** → **「生存者数＝タスクを表現するのに必要なユニット数」は棄却**
- **事後層別**（低 H の損失スケール交絡を除く。判定外・ラベル必須）: matched 層 H∈{4,8,32,100} の傾き **−0.0181 [−0.379, +0.309]** ＝ ゼロ上のタイトな null
- 同区間で**未フィット率（残差/信号分散、無次元）は 0.0038 → 0.211（約56倍）**、alive の paired 差は **−0.20 [−2.40, +1.60]**
- → 対立側（罠幾何説 / H_marg 系）の予言「入力統計が固定なら生存者数は教師複雑度に非感応」が**積極的に支持**。**需要が増えても動員は起きない**
- **予算不足説は排除**（教師関数は 1M 通じて固定・サポート32パターンを各312回提示／t=100k では全レベル高精度フィット／未フィット率は時間とともに上昇）
- **T4 傍証（別枠・交絡つき）**: H∈{1,2} は alive 46/42 と高く同時に plateau ≈ 0 ＝「堆積には未フィット残差の持続が必要」と整合

**残る候補**: (a) 罠幾何が決める定数、(b) 生存者は稼いでいるが稼ぎ先は H 非依存の低次成分。判別は `spec_surv_hist_0820.md`（再学習なし）の T1×T2 決定行列。

**訂正**: 「生存者は 8.5% で止まる」は誤り。alive は 1M でも下げ止まっていない（700k→1M: H1 −9 / H2 −13 / H4 −6 / 高 H は −1〜−2）。**「停止条件」という枠組み自体が未確定。**

## 0d. 死の二経路 — 分離の決着【新規 8/21b】

→ [[死の二経路]]（詳細。`results/dead2path_0821/`）

死を **再分類死**（境界で µ̂ が跳び、同じ w が新しい入力サポートで再判定されて落ちる。輸送ゼロ）と **輸送死**（タスク内で力に運ばれて滑落）に分ける。**分類は記録構造から排他的で厳密**: 粗い区間は境界を跨がないので、全ペアが「境界ペア」か「µ̂ 固定ペア」のどちらか一方に落ちる。

- **D1 PASS**: 再分類死の当該 1 step の |Δ‖w‖| は**厳密 0**（96.6% が同一窓・非境界ペアの q95 以下）。対照の輸送死は同指標で 92.8% が対照超 → トートロジーではない
- **D2 PASS（主判定）**: P3 の再判定。輸送死のみ・µ̂ 固定増分のみで **0.3754 > 0.2085**
- **D3 PASS**: self 符号安定率は 3 群すべてで厳密 1.000
- **D4**: 混在の量は Σ|Δcos| の **16.1%**、median PE で **−0.020**
- 割合: 全死亡で再分類死 28.2%、恒久死で 31.3%（n=32・CI 広い）、**終端死で 14.3%**（事後追加 S-D0、n=942）

**死は事件ではなく定着**: 生の下方クロスの **91–92% が再点灯**し、終端の死までのクロス回数は中央値 **11 回**、終端 dead の **97.3% が p̂=0**。吸収はタスクごとで、恒久死は全変種の罠の共通部分（[[命題リスト]] Q11）。

**残る混在**: 力の台帳（Q2b）は未組み直し。群ラベルは `results/dead2path_0821/events.csv` に供給済み。

## 1. 位置リセット判別の決着

→ [[位置リセット判別]]（**同ノートの Q1–Q8 / P4–P7 はローカル ID。[[命題リスト]] の Q番号とは無関係**）

- **座標復元説（B1 主成分形）は棄却**: posonly は効く（Δ 0.207 [0.161, 0.252]）が、floor 補正後の寄与は full の 0.502。dironly（0.893）との差はマージンでは説明されない。**B1 は「座標復元は便益の約半分を説明する副成分」に改訂**
- **床の正体候補は消音**: v←0 だけで便益の 40.5% [0.275, 0.539]。treated は ‖w‖ median 2.44 の「稀に発火して大出力を撒く」ユニット。**8/20 追加**: ゲート閉鎖時は v の勾配も凍るので「大きな v を抱えたまま死ぬ」は機構から出るはず（未検証）→ [[blindspot_0820]] §2
- **v符号二極説は棄却**: sign(v) と極の一致率 0.527、dead 率も v 符号で差なし。+µ 極は状態に存在しない
- **µ=0・b経路のレジームは G0 不成立**: 95% を初期化しても clean eval 不動 → 全判定 void。ただしゲート再開率 posonly 0.498 / dironly 0.011 で **β = b/‖w‖ の定式化自体は正確に動作**。「ゲートを開けても性能は買えない」
- B1''（配置仮説、未検証）: 再初期化の価値 = 消音（〜40%）＋ **潮流から外れた方向の供給**。dironly の新方向は |cos| median 0.152 ≈ ランダム床 → dir_angle ダイヤルで判別。**ただし B1 最終形（座標復元＝約半分）とは両立していない**。8/21 に出すのは B1 最終形
- 残交絡: posflip vs dironly の**ノルム不揃い**（1.41 vs 2.51）

## 2. 力場と状態の乖離【中核・3例】

- **P3-7**（`center_selfcov_0814`）: |cos(E[g], e₁Σ)| = 0.907（unit平均 0.81）vs 重み状態側 0.105（ランダム床 0.174 未満）
- **rank_int_0814**: svdrec は悪化、基底保持シャッフルは中立。**介入は一過性で ~100k で再崩壊**＝場への再輸送
- **posreset_0819**: 二極性は力場に厳密に実在し、状態には −側の堆積しか現れない
- µ=0 レジームの但し書き: 軸吸引 0.91 の実体は主に**タスク依存・符号反転する誤差フィードバック**（符号安定率 6.1%）。符号安定な自律成分の優越は 1/‖w‖ 切断後にしか起きず、µ=0 がノルム成長を止めるため**このレジームでは理論内在的に到達不能**。**condA 版はロギング走で確定**
- drift の Cov 項の正体は **Σ 支配方向への軸吸引**（`aniso_perp_0812`: κ=16 で |cos| 0.63–0.74、符号は run 内でも ± に割れる）。「E[g] ∝ Σµ 増幅」は棄却済み。**condA は µ 項支配（4.3×）、condB 異方は Σa 軸吸引が可視**
- **8/20 の未処理 → 8/21b にコストを訂正**: 「向きによる分解（E[δ]µ + Cov）」と「作者による分解（self + rest）」を**一度も交差させていない**。condA では self×µ と rest×µ が方向として縮退する。~~保存ログから追加実験なしで計算可能~~ は**誤り** — 保存されているのは「ゲート抜き共通 G の列分割」と「ゲート後ユニット別 µ̂ 射影の行分割」で**別対象の行と列**。probe 拡張の**再計装リプレイ**（軌道 bit 一致・学習 363 秒）が要る → `specs/spec_cell4_0821.md`、[[blindspot_0820]] §3・§13、[[命題リスト]] Q12
- 宿題: µ=0 導出の **p 符号修正**（task_080）
- H-sign（符号プリミティブ説）は部分支持のまま → [[第二の共通方向]]

## 3. µ経路とb経路【8/14–15決着】

- 標準 regime（µ≠0）の LoP は**全面的に µ 経路**。b 凍結はクリーンなヌル（w100 diff +0.002）
- µ=0＋b 可変では dead 最大 0.99 だが、**機能コストと解離し経路依存**。「適応的スパース化」は棄却
- b 経路の dead は 95% 初期化しても clean eval が動かない。b 経路は整流ではなく**片符号ドリフト＋罠**
- b 経由 dead は K 限定（K=100: 0.99 / K=10³: 0.04 / K=10⁴: 0）。**K = condB の教師再サンプル周期**（→ [[用語と記号]] §4）
- centering は dead 0.964→0.294 だが µ 除去は LoP 自体を消す → **µ=0 で LoP が出る regime 探しは未解決**
- **A2 の正しい読み**: A2 が測っているのは「step 1e4 の駆動符号ラベル・v 符号と 1e6 の dead のクロス表」（差 ≤0.06）。これは「**ラベルが最終位置を予言しない**」であって「両側等しく死ぬ」ではない。実際 dead 915 個中 cos>0 は 0 個で、**死は −µ̂ 側に片側堆積**（median −0.325）。~~「二極クラスタだから両側等しく死ぬので A2 と整合」（v2 §3(c)）~~ は**撤回**

## 4. Path A の性格【diffusion は増幅因子】

- `fullbatch_0812`: **厳密ノイズゼロでも整列と LoP は進行**（w5）。dead は full で減るが消えず（w100 で 0.58）。**8/20 追加の説明候補**: 税（自己項）はノイズ非依存の決定論的ドリフトなので、フルバッチでも死が進むのは当然（[[blindspot_0820]] §1）。**ただし 8/20 夕に税＝駆動は撤回**（[[中和と境界窓]]）ので、この説明も rest 側で立て直しが要る
- w100 は b=32/128 で eval_loss ≈ 0 と LoP 非発生。破綻はオンライン SGD（b=1）のみ（**この `b` はバッチサイズ**。バイアスの `b` と別物 → [[用語と記号]] §4）
- hazard は eval_loss ≈ 0 で最大 → 想定していた因果方向（誤差→死）の逆転
- perturb-only（0.112 → 0.742、Path A に毒）は**対称輸送の追加＝堆積の加速**として直読み
- 吸収性: PB-5 字義 FAIL（ガウスでは Φ(β)>0）だが、**condA の有界入力では文字通りの吸収を実測**（cos<−0.15 で p̂=0）。**8/20 追加**: µ̂ は境界で跳ぶので吸収はタスクごとであり、恒久死は**全タスク変種の罠の共通部分**という幾何的対象（P4-move の 76.5% がその実測）→ [[blindspot_0820]] §5、[[命題リスト]] Q11。**8/21b: これに数値が付いた** — 生の下方クロスの 91–92% が再点灯し、終端死までのクロス回数は中央値 11 回、終端 dead の 97.3% が p̂=0（[[死の二経路]] §5）
- 停止機構の実測は「深さ」より**「力の消灯」**。帯深の切替回数スケーリングは単一 T では識別不能
- **死亡の軌跡は単調輸送**（P3 PASS）。**8/21b: 二経路を分離しても PASS**（0.3754 > 0.2085）。以後は分離後の値を引く

## 5. パス間結合の構造【個別結果は有効】

- `rank_int_0814`: svdrec 悪化・shuffle 回復 → **srank を上流因果ノードとする逐次連鎖モデルは棄却**
- `coupling_ab_0813` / `coupling_fbw_0813`: B→A リード大（9–29 タスク周期）、A→B は限界的。結合方向は条件依存、**対称に提示しないこと**。順序の非固定性は整流では「同じ駆動の2観測量なので固定順序を予言しない」として自然に出る
- 隣接ペアの連鎖から推移的因果を主張するのは無効。**e²→dead リンクは介入未済**（実験は未起案）
- 「共有力場からの結合並行パス」という上位フレームは**整流モデルに吸収**

## 6. ‖w‖・Lyle 接続（不可逆性）

→ [[ノルム増大と不可逆性]]

- 1/‖w‖ 操舵切断描像に**反証寄りの観測が蓄積**: P7（posonly の Δcos が予測と逆符号 −0.033）、Q7（vzero ≈ 0）。前景は「**場が方向を特定の帯へ輸送する**」動力学 — **ロギング走 P3（単調輸送）が直接支持**
- condA での不可逆性の実測上の正体は「壁」でも「等方的減速」でもなく**力の消灯**＋**匍匐**
- **‖w‖ と死の関係は未整理の穴のまま**: β̇ = ḃ/‖w‖ − β·d(ln‖w‖)/dt によりノルム成長は沈んだ β を薄める**復元項**（Path B 節の「ノルム＝悪玉」と逆符号）。E1 でも alive ‖w‖ 3.30 > dead 2.44。ノルム膨張介入なら両 Path の予言を同時判別できる（**未起案**）。**8/21b の傍証**: 死亡時 ‖w‖ の中央値は再分類死 1.525 / 輸送死 1.740（探索・判定なし）
- **8/20 夕: 「ノルム増大 → 境界衝撃の増大」の筋は棄却**（台帳は育たない → [[台帳（実効バイアスの縮退）]] §4）
- 角度力学の導出は一回検算のみ、外部資料前に手で再導出（task_080）。ゲートロック仮説は未検証

## 7. µスイープの数値検証【8/11・凍結】

→ `過去の遺物/入力平均スイープの数値検証`（**凍結**。本番 10⁶ 完走は廃止）

- ノルム完全固定下で β = 0.0258 [0.0182, 0.0361] → **λ_lock はノルム媒介でない実在の力**
- α = 1.036 > ランダム基準 1.0101 → **λ_align > 0**
- 関数形は線形でなく飽和。γ（奇数項）はアーティファクト
- 効き幅 c̄ 0.016→0.089 は「クローン」水準ではない（**c̄ = 平均 inter-unit cos** → [[用語と記号]] §5）

## 8. 介入・既存手法のSDE分解

- `cbp_harm_0815`: CBP の reset は**残差の有無に関わらない固定コスト**。**便益は実容量損失に比例**。**dead_frac は治療の要否を決めない**（+0.349 CI [+0.216, +0.503]）
- **posreset が reset の内訳を初めて分解**: 消音（40.5%）＋座標復元（床抜き約半分）＋座標に還元されない新方向。one-shot ≠ CBP 継続適用（**継続型は未検証**）
- 整流の下では「方向多様性を減らす介入は害、増やすのは益」で svdrec 有害／shuffle 中立／dironly>posonly が同方向に並ぶ
- アンカー: 条件 A では CBP 有効のまま（dead 0.968→0.034、eval 0.937→0.005）。**Dohare 批判ではなく「適用条件の未明示」の指摘**として書く
- `methods_sde_0813`: Leaky 復帰は α に単調（**死は壁でなくゲートを介したドリフトの帰結**）、perturb-only S&P は Path A に毒／Path B に薬、CBP は drift/diffusion 外のジャンプ項
- **8/20 追加の予言（未検証）**: 出力ゼロへの2経路のうち、v→0 は可逆（dv の勾配が残る）だがゲート閉鎖は全勾配消失。**正測度の後者を持つのは ReLU だけ**で、tanh では出力ゼロ近傍がむしろ最も感度が高い（[[blindspot_0820]] §1）

## 9. 計測の信頼性【運用合意】

- **eff_rank が最も信頼できる指標**（LoP 活性 regime で eval_loss と Spearman −0.72〜−0.81）。dead_frac / wcos / srank はノイジーな代理
- dead_frac と機能コストの解離は複数実験で再現
- `OMP_NUM_THREADS` を run 間で一致させる。全 spec の S1 に明記
- n=5 bootstrap の p は非較正 → 境界判定は seed 10 で事前登録
- ゲート曲線はビン集計・誤差棒なし。イベントレベル数値は run 単位集約→seed bootstrap で CI を付けてから「直接証拠」と書く
- **8/19b の教訓**: ①**事前登録の判定基準が仮説の一方に依存していると、その一方が崩れた時に基準ごと無意味になる**（P2）。②**指標が測定対象の一部で恒真になっていないか事前に確認**（P4）。③**ラグの異なる統計量を比較しない**
- **8/20 の教訓**: ④**チャットの言い回しを数値の出所にしない**（「残差2桁増」「8.5%で止まる」の2件が発生）。⑤**絶対値の損失をレベル間で比較しない**（信号分散も動くので無次元化が要る）。⑥**複雑度ダイヤルは f ではなく教師幅**（f はサポートサイズ・µ̂ 構成・境界ジャンプ幾何が同時に動く。**この `f` は flipping bits の本数 =15**。ユニット出力は 8/20 夕に f_i → o_i へ改名済み → [[用語と記号]] §4）
- **8/21b の教訓**: ⑦**指標が依存している基準量（µ̂）が動く点を跨いでいないか確認する**（P3 の cos 増分は境界で物差しごと動いていた）。⑧**事前登録の統計量が痩せたら、事前登録版を残したまま補遺を「事後」ラベルで足す**（D0 → S-D0）。⑨**「再学習不要だから安い」を採否基準にしない**（安い解析ほど追跡需要を生む）

## 10. 新規性の位置づけ

- 符号クローン集合はどの対称性の固定点集合でもない → Chen（Itô ドリフト）・Amari（Fisher 退化）が構造的に適用不可。**Chen 機構の排除は実施済み**（fullbatch）
- **整流の新規性は現象の輸入ではなく対応づけ**（Dead = 吸収端／低ランク整列 = 罠なし側を、単一の厳密二極場で接続）。ratchet 自体は物理の確立概念
- Dohare / Joudaki の関係の書き換え: 「同じ方程式の別々の項」→「**同じ駆動の、吸収端とそれ以外**」
- **8/20 追加 — 暗黙バイアス文献に対する条件節の除去（論証。実験結果ではない）**: 暗黙の低ランクバイアスは通常「フィットの内側での選好」として定式化される（同程度に損失を下げる解の中から単純な方が選ばれる。neural collapse も補間後の終端相）。teachw は**未フィット率 21% を残したまま**実効容量が 4–5 に固定され需要 25 倍でも動員が起きないので、低ランク化は**選好ではなく吸収的な上限**として働いている。Joudaki との違いは反論ではなく**機構の置換**——彼らの不変集合は等式多様体（測度ゼロ・未到達）、こちらはゲート閉鎖（正測度・常に到達）。**ランク低下は「整列によって」ではなく「殺すことによって」達成されている**（→ §0 冒頭の確定注記。8/21 の中心主張はこの形で立っている）
- **8/21 追加 — 一般化のフレーミング**: 「継続学習の文脈を超えて機械学習全体でいえるのでは」（先生）。足場は [[命題リスト]] Q13（恒等式は二乗損失一般・2層＋線形読み出しで成立、継続学習も µ も不要）。**無条件に言えるのは税の存在と吸収性まで**（符号の片側性には入力条件が要る）。イントロ・議論で扱い、追加実験は未定
- **8/21b 追加 — 死の描像の精緻化**: 「一度落ちたら終わり」ではなく「**タスクごとに引き直される罠に対して、輸送が w を少しずつ動かし、やがて全変種の罠の共通部分に入る**」。境界は**状態を変えず判定を変える**装置、タスク内の力場は**判定基準を変えず状態を変える**装置で、恒久死はその積（[[死の二経路]]）
- **宿題**: (i) alive 数ではなく **eff_rank / srank を H 別最終スナップショットで測る**（Joudaki の言語への翻訳。スナップショットは .gitignore のためローカルのみ）、(ii) **Q0 走査**（dying ReLU / dormant neurons / rank collapse / neural collapse との線引き）→ [[命題リスト]] Q0
- Phase 1 の終点は「D→0」ではなく「D 有限のままゲート同期」→ [[第二の共通方向]]
- Lyle「independent」主張は fork 構造を排除できない設計

## 11. 怪文書の帰趨【凍結】

→ `過去の遺物/怪文書の検証`（**凍結**。残っていた確認項目は廃止）

- A1 の測定対象が W でなく E[g]、sign_rand が要素単位、貢献 (b)(d) の要修正、が確定した骨子
- 恒等式 E[g_{W_i}] = 2·vᵢ·E[δ·1[preᵢ>0]·x] を相対誤差 1.5e-6 で確認。**整列は学習の帰結ではなく構造的に最初から存在する**
- **H-cov の下位結論「teacher 側共分散は閾値落ち（0.37）」は誤り。参照禁止**
- **「怪文書」は私的な呼称。外部文書には書かない**

## 12. 仮定の scope condition

- e⊥x 外生性: 生成モデル y = f_t(x) + ξ に置換済み。タスク切替分散が系統誤差を支配する regime でのみ成立
- ‖w‖ 大で最も崩れやすい。Cov(e,x) の ‖w‖ 依存性が測定項目
- アーキ／最適化: **2層 ReLU・MSE・SGD/GD 限定**（Adam・LayerNorm・weight decay アーム不存在）。8/7 資料冒頭「Adam のドリフト」はスコープ修正対象
- rank_int のスコープ限定（condA full-batch w10/20・t_int=150k・amended p）を常時明記
- b経由 dead は K=100 限定
- **多層は未着手**（上流の死と整列が下流の µ・Σ を作り変える。非負入力の前提は ReLU が自己生成している）→ [[blindspot_0820]] §6

## 13. 読解系の未回収枝【優先度低】

- Lyle Appendix F の存在文/全称文問題
- Joudaki: Appendix A.3、Remark 2.2 / 2.4、A.5 の安定性分類

---

## 直近の優先順（8/21b）

**8/21 の決定**: 中心主張を確定（冒頭・§0）。**本線はこれ以上掘り下げない**。**実験の続行範囲は未決定** — 詳細な保留リストは [[命題リスト]] の「現在の優先順」。

1. ~~8/20 先生返信（task_081）~~ — **完了**
2. ~~8/21 ミーティング（task_078）~~ — **実施済み**（結果は冒頭の幹・§0・[[研究索引]] マイルストーン）
3. ~~Q2 の手証明~~ — **完了 8/20**（[[自己項の恒等式]]）
4. ~~Q16（死の二経路の分離）~~ — **完了 8/21b**（[[死の二経路]]・§0d）
5. **フレーミング（書き物・実験なし）**: 一般化の射程をイントロ・議論へ（[[命題リスト]] Q13・Q0、§10 の 8/21 追加）
6. 【保留】Q12 4セル分解（spec 起票済み・再計装リプレイ）／Q2b の本番組み直し（群ラベル供給済み）／穴A／Q10 surv_hist／Q7 T スイープ／Q0 の実験部分 — **続行可否が決まってから**

候補（未起案・保留）: dir_angle ダイヤル／~~生存者側の症状の橋渡し~~（**8/21 に非目標化**）／churn の起源（`E_delta` 列で安い）／posflip・dironly のノルム揃え対照／事後中心化（µ 除去で堆積が止まるか）／ノルム膨張介入。

---

## 8/22 の再解析による更新（[[xr分解と生存機構_0822]]）

**既存データのみ（新規学習走なし）。全て事後計算・未事前登録・spec 未起票・コード未コミット。現時点では引用不可。**

### §0 部品への影響

| 部品 | 影響 |
|---|---|
| **部品1**（力場は厳密に二極） | **無傷**。ただし恒等式は**期待値**の話で、1 step は `Δw ∝ δ_t·gate·x_t`（その瞬間の入力方向）に乗る。ドリフトは µ̂ 方向 1 本、拡散はランダム 5 次元 |
| **部品2**（移動度は片側消灯） | **格上げ**。タスク内 16,475,293 ペアで p̂=0 からの復活 **0 件**、`max\|Δcos\| = max\|Δ‖w‖\| = 0.000e+00`。観察 → **厳密** |
| **部品3**（駆動の時間構造） | 無傷。ただし 0.497 は**共通場の符号** `sign(G·µ̂)` の話で、**ユニット別・ゲート後の実効ドリフト**とは別対象だった |
| **部品4**（自己項） | 無傷。ただし x 水準別に見ると **中和率は位置に依らず平坦**（0.990–0.999）。上へ行くと self 14.68 倍 / rest 14.26 倍とほぼ同率で伸び、差だけが 29.43 倍残る。**「self か rest か」の二択は成立しない** |
| **合成（整流）** | **半分棄却**。拘束の非対称性は厳密に成立、**撹拌の対称性は棄却**（`E[Δx\|alive]` が上ほど強い下向き、最上位帯 −0.02585 対 最下位帯 −0.00039、**66 倍・CI 非重複**、`\|v\|` 統制後も残る） |

**整流という語**: 定義は「対称な撹拌 × 非対称な拘束」。実測は「**爪は完璧、歯を押す力の方が偏っていた**」。「整流ではない」ではなく「**整流に加えて駆動も偏っている**」。フレームを維持するなら**対称性を主張しない形**に直す。→ **[[フレーム前の穴]] 後-2（Q17）は「対称ではなかった」で閉じる**。

**穴A（rest の偏りの機構）**: 数字は付いたが、答えは「self か rest か」ではなく「**沈む `b_eff` と育つ `reach` の競争**」（下記）。問いの立て方ごと書き換えが要る。

### §2「力場と状態の乖離」— 4 例目にして、乖離の正体

`cos = x/√(x²+r²)` を分子分母に割ると、乖離の大部分が**正規化のせい**だと分かる。

- **x = w·µ̂** ≒ **実効バイアス**（`Spearman(b_eff, x) = +0.6155`）。ここに力が乗る
- **r = µ̂ 直交成分** ≒ **入力に応答する重み**（`Spearman(reach, r) = +0.7305`）。ここに拡散が乗る

**正規化量で力学を書くと、分母の成長が力に化ける。** §6 の復元項 `−β·d(ln‖w‖)/dt` はその一例で、力ではなく座標変換の項。ただし分母を育てる `r` が生存と強く相関する（+0.97 SD）ので、**この座標変換には選択がかかっており無害ではない**。

**今後は `x = w·µ̂` の生の座標で測る**（保存 npz の `cos_u_mu × w_norm` で復元可）。

### §6「‖w‖・Lyle 接続」— ‖w‖ は増大している。方向が違う

死者は死亡時点で凍結するので全体平均では見えない。コホートを分けると:

| task | 全体 | 終端死者 | **現 alive** |
|---|---|---|---|
| 0 | 1.418 | 1.420 | 1.415 |
| 100 | 2.956 | 2.860 | **4.221** |

`E[Δ‖w‖ | alive] = +0.009135` [+0.007526, +0.011038]、step 区間別でも全部正で後半も減衰しない。

**ただし成長の 98% は µ̂ 直交方向**（x² の割合は 100 タスク通して 1.5〜2.1%、等方基準 5% より痩せている）。checkpoint 500k でも µ̂ 方向のエネルギー割合は alive 0.0247 / dead 0.1124。

→ 「ノルム増大 → 操舵切断」の 1 次元的描像に対し、実測は「**舵の軸だけ痩せたまま、無関係な 19 次元が膨らむ**」。→ [[ノルム増大と不可逆性]] に詳細

### §10「新規性の位置づけ」— 「整列」が実測で否定された

checkpoint（500k、10 seed、32 パターン厳密評価）の重み行ベクトルのペア cos:

| 群 | 符号付き平均 | 平均\|cos\| | ランダム基準\|cos\| |
|---|---|---|---|
| **alive** | **+0.034** | 0.277 | 0.181 [0.151, 0.214] |
| dead | **+0.180** | 0.259 | 0.181 |

**揃っているのは死者の方。** alive 限定 eff_rank は 7.53（ランダム基準 10.80）で**低ランクだが整列ではない**。しかもその圧縮は罠と別起源で、**入力スパンの狭さ**から出る（alive のエネルギー配分は flip 15 次元 0.223 / ランダム 5 次元 **0.777**、等方は 0.75/0.25）→ [[blindspot_0820]] §15。

**Q0 への含意**: 「ランク低下は選好ではなく吸収的上限」という現行論法に、**第三の候補（入力被覆の粗さ）**が加わる。線引きの文でこれを潰さないと「あなたの低ランクは単に入力が低次元なだけでは」と言われる。

**中心主張への含意**: 8/21 の「力場が dead ユニットと**整列**を作り…」の「整列」は、書き方の問題ではなく**内容の問題**。生存者では起きていない。→ [[フレーム前の穴]] 前-9 / task_097

### §4「Path A の性格」— 死の描像

- タスク内では死者は**完全に静止**（復活も沈下もゼロ）。動きが起きるのは境界だけで、致死率は alive 18,247 件中 **1,226 = 6.72%**
- 恒久死の cos は死んだ瞬間 **−0.2560** → 1M 時点 **−0.3523**（差 −0.0880、86.01% が深くなった）。**沈下ではなく、浅い個体が復活で抜けた選択効果**
- 生存者は「一度も落ちなかった個体」ではない。下方クロス回数の中央値は 早期死 8 / 中期死 10 / 後期死 11 / 最後期死 12 に対し **生存者 10**

### §0c「停止条件」— Q10 は滞留者側

生存者は `r` が大きく（4.161 対 死者 2.632）、`|v|` はほぼ同じ（1.10 対 1.09）、`|x|` はむしろ小さい（0.564 対 1.017）。**「稼げるユニットだから生きている」の予言と逆。**

ただし `r` の成長には**正のフィードバック**があり（太い個体ほど `E[Δ(r²)]` が 7 倍）、単純な「罠の幾何が決める定数」でもない。**寿命は初期値でも需要でもなく `r` の成長競争で決まる**という第三の候補が要る。

### 機構文の候補（未検証）

condA のタスク内では死の条件が閉形式 1 本で書ける（10 seed × 100 unit で 32 パターン列挙と厳密一致）:

```
生きている ⟺ b_eff + reach > 0
   b_eff = flip·w[:15] + b            （閾値のつまみ ← x 座標）
   reach = Σ_{j∈rnd} max(w_j, 0)      （入力に応答する重み ← r 座標）
```

| 群 | margin | b_eff | reach |
|---|---|---|---|
| alive | +1.0758 | −1.2912 | **2.4967** |
| dead | −0.8717 | −2.3083 | 1.3069 |

> 実効バイアスは上ほど強い復元力で沈められ、入力に応答する重みは拡散で育つ。**床の沈下に背の伸びが追いつかなくなった個体から死ぬ。**

死は機能喪失の結果ではなく、**学習と沈下の競争**。死んだユニットも成長分の 84% はランダム側にあり、**学習途中で凍結**されている。

**留保**: 「reach の成長 ＝ 良い学習」は構造からの読みで未確認（`eval_loss` との対応は未測定）。時間発展（`b_eff` の沈下と `reach` の成長のどちらが先か）も未測定。すべて condA・w100・T=1e4・batch=1 で **condB へ外挿しない**。


---

## 14. 力と変位の分離【新設 8/27】

→ 出典: チャット `lop-作業4-xr-recheck-spec`。**枠組みの整理であって測定ではない。** 引用している数値は committed 出力（`results/xr_recheck_0827/window_metrics.csv`）からの読みのみ。

### 一般命題

> **状態でゲートされ、片側に吸収壁がある系では、「力が釣り合う点」と「動きが止まる点」は別の場所にある。**

横軸に `x = w·µ̂` を取り、曲線を二本引く。

- **力の曲線** `E[F_gate | x]` — その場所でゲートが開いているときに掛かる力の平均
- **変位の曲線** `E[Δx | x]` — その場所にいた個体が実際に単位時間あたり動く量の平均

普通の系ではこの二本はほぼ同形でゼロ点も一致する。この系では一致しない。理由は 4 つあり、どれも系の設計から出る。

1. **ゲート**: 力は開いている間しか働かないが、変位は閉じている時間も 0 として積む
2. **吸収**: 落ちた個体は母集団から抜ける（`E[·|alive]` の条件付けが場所依存になる）
3. **符号の打ち消し**: 変位は力の積分。同じ大きさの力でも、向きの揃い方で 1000 歩ぶんにも √1000 ≈ 32 歩ぶんにもなる（**30 倍**）
4. **比例定数の時間変化**: 1 歩の変位 = 誤差 × 脚力（`v_i` と発火率）。`v` は 6.4 倍に育ち、発火率は死に近づくほど下がる

### この構図が現れた箇所

| どこで | 力の側 | 変位の側 |
|---|---|---|
| 整流モデルの骨格（§0 合成） | 二極の駆動（対称と仮定） | 正味の流れは下向きのみ |
| §2 力場と状態の乖離 | 力は Σ 主軸を向く（\|cos\| 0.907） | 状態は整列しない（ランダム床 0.105） |
| [[xr分解と生存機構_0822]] §4 ⚠ | `E[F_gate]` の向き | `E[Δx]` と**符号が逆**（未解決・最優先） |
| `xr_recheck_0827` A5-a | 1 歩は誤差に厳密比例（定理） | 1000 歩の距離は ρ 0.4566（**FAIL**） |

A5-a の内訳（seed 0、committed `window_metrics.csv`）: 誤差と変位の比（＝脚力）がタスク 0–10 で 0.016、50–75 で 0.158、75–100 で 0.083 と **10 倍動く**。誤差自体は 177 倍動くので相関は残るが、順位が入れ替わって単調にならない。

**A5-a FAIL の正しい読み**: 「揺れは残差に比例」（`F_self + F_rest = F_gate ∝ E[δ…]`、[[中和と境界窓]] §1 の代数。本番で残差 3e-8）は**無傷**。落ちたのは、その恒等式から 4 段階離れた**変位側**の命題。

### 遡及的な帰結 — **Q17 は閉じていない**（最重要）

[[命題リスト]] Q17 の主張文は「駆動（rest）の**符号**は陣営（sign v_i）によらず対称で、正味の非対称は罠に由来する」で、これは**力の命題**。測定対象は主張文に合わせて `F_rest` とし、天井 T0 とは独立 spec で閉じる。

ところが 8/22c の「棄却」も 8/22d の「ほぼ対称」も、測ったのは

- 8/22c: `E[Δx | alive]` の水準別ドリフト（**変位**）
- 8/22d: `Δx` の符号割合 49.7 : 50.1（**変位**）

で、**どちらも Q17 の主張文を測っていない**。8/22d が「弱いところ」に挙げた「`E[F_gate]` と `E[Δx]` の符号不一致は未解決」は、測定の瑕疵ではなく**この分離そのもの**。

| | 8/22 の記述 | 8/27 の帰趨 |
|---|---|---|
| 8/22c「撹拌の対称性は棄却」 | 8/22d で「言い過ぎ」と訂正済み | **加えて対象が違う**。変位の非対称は力の非対称を含意しない |
| 8/22d「ほぼ対称・上側に弱いバイアス」 | 変位側の記述としては有効 | **Q17（力）の判定には使えない** |
| 8/22d「後-2 はこれで閉じる」 | — | **閉じていない。撤回する** |

→ **Q17 は「答えが出ているので記録のために回すだけ」ではなく、依然として未実施の判別実験。** フレーム選択（整流を名乗るか）の分岐条件も元に戻る。

### 天井（旧「穴A」の後継）も同じ図で書き直せる

8/22d の仮説 H「天井は壁ではなく平衡点」は、力と変位の平衡を分けていなかった。

- **変位のゼロ点**: `E[Δx/k|x]=0`
- **力のゼロ点**: 符号付き `E[F_gate|x]=0`

旧定義 `|F_self|/|F_rest|=1` は同符号でも成立するため、力のゼロ点として使わない。`F_self≈-F_rest` は補助的な分解にすぎない。二本の下降根の位置差 `g=z_D-z_F` を直接観測量にする。

同じ図の上で、**上端（ゴムの天井）と下端（片道の壁）は同じ曲線の二種類のゼロ点**として同居する — 上は負の傾きで滑らかにゼロを切る（可逆・通れる）、下は壁で切り落とされてゼロ（不可逆・通れない）。§0 部品2 と 8/22d の上下対比表が一枚に畳まれる。

### 8/28 先生提案 — 座標軸回転の適用範囲

一般の有限差分は、旧軸を基準に

\[
x_1-x_0=(w_1-w_0)\cdot\hat\mu_0+w_1\cdot(\hat\mu_1-\hat\mu_0)
\]

と「重みの移動」と「軸の回転」に厳密分解できる。ただし旧 `E[F_gate]=+2.98e-7` 対 `E[Δx]=-1.75e-3` は同一タスク内 Δ1000、本 T0 も境界前の同一タスク内 `k<=40` である。std では `mu_hat` はタスク内固定なので、どちらも回転項は**厳密0**。符号不一致の説明にはならない。T0 には `flip_state_start==flip_state_end` / `axis_rotation_cos=1` の sanity として追加した。

境界の回転自体は別の問いとして [[タスク方向回転M1_spec_0828]] へ分離。先生提示の一般式

\[
c_B=\frac{|f_T\cap f_{T'}|+1.25}{\sqrt{(n_T+1.25)(n_{T'}+1.25)}}
\]

は正しいが、実装は隣接タスクを独立15bitで引き直さず**1bitだけ反転**する。したがって代表値約0.57は使えず、定常1-bit遷移の構造期待は約0.9416。

### 絞り込みの限定

`F_gate` はすでにゲート込みなので、ゲート開放率を再度掛けない。1-step では現在状態に条件付けて `E[Δx|state]=F_gate` だが、k を伸ばすと経路中の状態変化と、未来の生存で条件付けた顔ぶれの変化が入る。

ただし、この二つを因果寄与へ一意に加法分解できるわけではない。後継 T0 は

1. 固定開始コホートで k だけを変え、位置差のホライズン変化を測る
2. 同じコホートを終点 S/L に分け、帯内の厳密な混合恒等式で帳簿を出す

ところまでを行う。S/L は post-treatment 条件なので「選抜の因果寄与」とは呼ばない。

### 閉じる作業（8/28 実行済み）

正本は [[天井プログラムT0_spec_0827]]。境界前窓の共通 60 起点と `k∈{1,2,5,10,20,40}` を使い、同じ開始コホートで次を行う。**新規学習走なし。**

1. `E[F_gate,start|x]` と `E[(x_end-x_start)/k|x]` の下降根と位置差を推定
2. `g_A(40)` を主判定、`g_A(40)-g_A(1)` をホライズン判定とする
3. A/S/L/U の曲線を同一行集合で引き、F・D・H の帯内混合分解を出す
4. Δ1000 は時間領域が排他的なので、交絡を明記した別枠参考値に限定する
5. 全主ペアで軸回転が0であることを sanity に固定する。境界回転と再分類死の相関は M1 で独立判定する

Q17 は本節に相乗りさせない。`F_rest` の陣営内バランス・陣営間差・ゼロ割合を独立 spec で判定する。

**結果**: [[天井T0とタスク回転M1_0828]]。sanity は PASS。`g_A(40) = −0.932492 [−0.983459, +0.562485]`、`P_g = −0.542467 [−0.626400, +0.492470]`で、同時根存在率はどちらも1.0000。判定は `NO_RESOLVED_SEPARATION` / `NO_RESOLVED_HORIZON_SHIFT`。**根は存在するが位置差は未解像**であり、「差なし」とは書かない。

M1 は `INCONCLUSIVE_GUARD`。有効 seed 4/10 で登録ガード 8/10 に届かず、負相関の予言は支持も棄却もできない。

**留保**: condA・w100・T=1e4・batch=1 の外へ出さない。旧 T1a/T1b/T1c は自動開始せず、先生の再判断に戻す。

### 8/28 先生提案 M2 — 決定論的再計装を実行【完了】

`S_i=(1/32)Σ_x relu(z_i(x))` は死後も0で定義できるが、元ログからは復元不能だった。そこで [[直接機能量と開口量M2_spec_0828]] を事前登録し、同じ軌道を読み取り専用で再計装。最終 complete-state hash と既存全 landmark 列は完全一致。

**結果**: `A_deltaS=+0.155695 [+0.146237,+0.180084]`、**HIGH_LESS_PUSHED**。起点 `A_S0=+0.042295 [+0.031337,+0.058090]`は **INCONCLUSIVE_BASELINE**。高 ΔL 群は「起点が遠かっただけ」ではなく、固定幾何セル内で動的変化差が残る方向。正本 [[直接機能量と開口量M2_0828]]、結果 `bd68952`。

### 8/28–29 後継 T0b — 観測量を位置から量へ替えて解像した【完了】

T0 が未解像で終わったのは対象の性質ではなく**推定量の性質**だった。主根は max 型で、帯 [0.6,0.7) の単帯ブリップが転ぶと座標が飛ぶ。そこで [[天井T0b_spec_0828]] を事前登録し、観測量を**位置**（下降根の差 `g`）から**量**（上側窓 `[+0.1,+0.9)` の `H := D_k − F_0` の平均 `M(k)`）へ替えた。腕E は既存 std の探索、腕C は新規 seed の事前登録。

**結果**: 腕C（事前登録）**`H_POSITIVE`**、`M(40) = +6.381574e-05 [+1.361767e-05, +1.060230e-04]`、寄与 seed 10/10。腕E も同符号で有意（`EXPLORATORY_H_POSITIVE`、`+9.861240e-05 [+3.906183e-05, +2.505427e-04]`）なので §8.4 により腕C を主として報告する。ベースラインゲート `M(1)` は両腕とも CI が 0 を含み PASS。正本 [[天井T0b上側窓_0829]]、結果 `be0cf35`。

読み: **上側窓では、40 歩で実際に動いた量が始点の期待力の予言より上向きにずれる**（押し戻しが予言より弱い）。経路分解は両腕とも `F_path − F_0` のみが 0 を外し、`D − F_path` は 0 を含む。窓内 strict off は全 k で 0 なので、**未解像の残りを選抜で説明することはできない**。

**「二種類のゼロ点」の図はこの走では支えられない。** 上側窓の帯別 `F`・`D` に isotonic をかけると、前身で `z_F = 0.6630` を支配していた単帯ブリップは吸収され、**窓内に下降零点が立たない**（腕E は `F`・`D` とも全帯負、腕C は `z̃_F = +0.155` のみで `z̃_D` が無い）。したがって `g̃` は定義されない。本節の 8/27 の記述「二本の下降根の位置差 `g = z_D − z_F` を直接観測量にする」は、**量の観測量に置き換えられた**。位置差は依然として測れていない。上端と下端が同じ曲線の二種類のゼロ点として同居する図は、**まだ数字で支えられていない**。

手続き側で 2 つ欠陥が出て、どちらも spec §12 に記録して直した。判定基準（窓・k・ガード・閾値）は動かしていない。

- **percentile CI は seed クラスタ 10 本では反保守的**。P2（符号ランダム化ヌル）が棄却率 0.085（閾値 0.07）で FAIL したため、実データの `H` を計算する前に studentized (bootstrap-t) へ差し替えた（0.035）。改訂 `7f6b7d7`
- **seed 番号を変えても新しい seed 群にならない**。`src/train.py` の `make_gens` は乱数を `SEED_BASE[exp] + width + generator_offset` からのみ作る。初版 §8.2 のコマンドで作った腕C は seed 0–9 と bit 一致していた。独立系列のノブは `generator_offset`（先例: `function_blind_direct_0823_confirm`）。B9 に軌道列指紋の非一致を追加し、腕C を再生成した。改訂 `83b4a16`。**`--seeds` だけで「新しい seed 群」と呼んでいる走が他にあれば同じ衝突を起こしている**

**留保**: condA・w100・T=1e4・batch=1・std・境界前窓・k ≤ 40 の外へ出さない。centered は `mu_norm` 中央値 0.117 対 std 3.041 で B7/B8 の閾値により対象外（µ 軸が退化しており別の対象）。

---

## 15. 作業6 の r 差し替え — 代理変数への還元は否定【新設 8/27】

→ 正本: [[直接機能量による選抜確認_0823]] §7 ／ spec: [[r差し替え解析spec_0824]]（事前登録 `3e22b6b`）／ 結果 `results/r_swap_0824/`（`d357779`・push 済み）。**既存 `exposures.csv`（`ac29d87`）の再解析のみ、新規学習走ゼロ。**

**防衛タスク**（査読で必ず出る反論を既存データで閉じる）。深掘りではない。

### 判定

| # | 群 / 層 | RD | 判定 |
|---|---|---:|---|
| **R1（主）** | `ΔL` 三分位 high−low ／ `cell_id × r_half` | **−0.3114 [−0.3577, −0.2895]** | **SPECIFIC** |
| **R2（副）** | `r` 三分位 high−low ／ `cell_id × u_half` | **+0.0311 [−0.0003, +0.0721]** | **INCONCLUSIVE** |
| R3（記述） | seed 別 `Spearman(ΔL, r)` | 中央値 **+0.1981**（+0.1380〜+0.2604） | — |
| R3（記述） | `r` 三分位の周辺 RD（`cell_id` のみ） | +0.0165 [−0.0022, +0.0449] | — |

事前登録の検出力ガードは**両方とも不発**（G1 保持率 0.5254 / 0.5726 > 0.40、G2 CI 半幅 0.0341 < 0.10）。R2 が有意でないので §8 の 2×2 分岐は発火させていない。

### §10・柱3 への影響

- **「作業6 の `ΔL` 効果は `r`（＝出力を作っている成分）の言い換えだ」という反論は閉じた。** `r` で条件付けても SPECIFIC。**`ΔL` と `r` の相関はそもそも弱い**（+0.198）ので、[[xr分解と生存機構_0822]] §17 の `Spearman(reach, r) = +0.73` から素朴に予想される交絡は実際には成立していなかった
- **`r` 自体の保護効果は見えない**（点推定は保護と**逆符号**の +0.0311、周辺 RD も +0.0165）。ただし INCONCLUSIVE なので null とも書かない。§0c の「生存者は `r` が大きい」（横断的な群間差）と、per-task ハザードで `r` が保護的に働くか（縦断的な効果）は**別の量**であることが可視化された形
- 作業2 H の null を説明し切ったとは書かない。H の天井（追跡窓 30 倍）は別要因のまま

### 引用禁止・要注意

- **「`r` で条件付けると効果が強まる（−0.2353 → −0.3114）」を機構の主張に使わない。** 層別を細かくすると有効セル集合と曝露集合が変わる（2,839 セル・5,023/4,431 曝露 → 1,664 層・2,639/2,537 曝露）ので、増分に (a) `r` 統制 と (b) 母集団シフト が**未分解のまま混ざっている**。事前登録に like-for-like の対照は無い。**SPECIFIC の判定はこの比較に依存しない**
- スコープは作業6 と同一。**層別後の観察的関連であり因果介入ではない**

### §9 の教訓に追加 ⑩

**⑩ 「指標が測定対象の一部で恒真になっていないか」の確認は、判定基準だけでなくサニティ側にも掛ける。** 本走では事前登録サニティ 6 件のうち 2 件が実行後に**空虚**と判明した。

- **S4**（`p̂ == 0 ⇔ pre_max <= 0`）: リスク集合が `p̂ >= 0.05` なので両辺とも 0 件。定義から成立する
- **S5**（中央値分割の群サイズ偏り）: 同値を上位側へ送る中央値分割では上位側サイズが常に `ceil(n/2)` になるので、少数側シェアは**セルの大きさ `n` だけで決まりどの変数で切っても同一**。実測でも `r_half` と `u_half` の分布が全 6,002 セルで完全一致した。**分割の質を何も検査していない**

`ratchet_log_0819` P4（BC(cos) が p̂=0 で恒真）・`cell4_0821` P1（非負性から sign が v_i に従う）と同型で、これで 4 例目。教訓②は**判定指標に限定して書かれていた**が、サニティ・S 系にも同じ罠がある。どちらも判定に使っていないので本走の結論には影響しない。


---

## 16. 作業4 の再現登録 — 8/22 の主要測定は事前登録で残った【新設 8/27】

→ 正本: [[xr分解と生存機構_0822]] §21 ／ spec `specs/spec_xr_recheck_0827.md`（事前登録 `d7ecd8d`）＋追補・実装 `11619e7` ／ 結果 `results/xr_recheck_0827/`（`04e43e1`・push 済み）。**新規学習走ゼロ。** 実施チャット `lop-作業4-xr-recheck-spec`、転記 `claude-復習-0824-0827`。

[[中心主張v3作業リスト_0823]] **作業4**（8/22 系の spec 化・回し直し）の実施。対象の数値は 8/22 に既に見ているため真の事前登録は不可能で、`repro` / `prereg` / `report` の三層で格を分けてある。**引用時に tier を落とさない。**

### 通ったもの / 落ちたもの

| | 結果 | tier |
|---|---|---|
| 片側吸収の厳密性（柱2 の核） | 非境界 **16,475,293 ペアで復活 0**・`max\|Δcos\| = 0` | prereg PASS |
| 復活は境界でのみ起きる | 境界 80,753 ペア中 9,318 = **11.54%** | repro PASS |
| ほぼ対称な撹拌 | p₊ = **0.4956 [0.4909, 0.5009]**（pooled 0.4972 / 0.5008、n=149,492） | prereg PASS |
| 生存者の非整列 | 符号付きペア cos **+0.0335 [−0.0467, +0.0966]**。死亡側 +0.1798 と**分離**（差 −0.1464 [−0.2247, −0.0855]） | prereg PASS |
| 境界あたりハザード | **0.1478 [0.1349, 0.1605]**（seed クラスタ・新規に CI） | prereg |
| **揺れと変位の比例** | Spearman **0.4566 [0.3767, 0.5342]**、基準 CI 下限 > 0.5 | **FAIL** |
| 天井（A6） | 比の窓中央値 0.9845 [0.9793, 0.9897] | **判定を置かない** |

### §14・柱2 への影響

- **A5-a FAIL が §14「力と変位の分離」の起点。** 落ちたのは代数（`F_self + F_rest = F_gate ∝ E[δ…]`）ではなく、そこから 4 段階離れた変位側の命題（→ §14）
- **A6 に判定を置かない決定に伴い、v3 柱2 から [[xr分解と生存機構_0822]] §20 由来の天井の記述を降ろし、A1-d（復活は境界でのみ・11.54%）に置き換える**
- **§7 の「n=12 で弱い」という留保は誤りだった。** 12.0 は seed あたりの平均本数で、10 seed の seed クラスタ CI が付く。旧「前-9（整列の語）は弱い証拠」の格付けはこの点で更新される
- 母集団は **`p̂ > 0`**（k ≥ 1）が正。`p̂ ≥ 0.05`（k ≥ 2）で切ると 149,492 → 119,046 で **30,446 件ずれる**。作業1 の `strict_dead` / `near_off` 裁定がここにも効いている

### 引用禁止・要注意

- **[[xr分解と生存機構_0822]] 冒頭の「すべて事後計算・未事前登録」は、§21 の表に載った行についてのみ解除される。** 載っていない節は事後計算のまま
- **A5 の旧値 ≈0.96 は出所不明・撤回。** repro tier を与えていない
- **A3（整列）は snapshot 走、A1/A2/A4/A5/A6 は logs 走で別 run。** 同一個体についての主張として読まない
- **A4-a は初回実行後に集約粒度を訂正した**（判定基準ではない）。境界ごと（99 点）なら 0.0323–0.2647 で FAIL する
- **A1-d は prereg から repro へ格下げ済み**（[[命題リスト]] Q3 に旧値があったため）


---

## 17. 動的修復走 — O は畳まない。ただし合唱団は半分しか当たらない【新設 8/27】

→ 正本: [[機能盲目性_0823]] §7（O-1・C-1・F-1）／[[停止条件]] 追記節（Ch-1）／ spec [[動的修復spec_0826]]（事前登録 `9f86ee8`）／ 結果 `results/dynrepair_0826/`（`1e9dabd`・push 済み）。転記チャット `claude-復習-0824-0827`。

**防衛タスク**。[[機能盲目性_0823]] §5「言わない」に自分で書いた穴（「オラクル修復が再開後も効果が持続する」）を、査読で突かれる前に閉じにいった。**新規の学習走あり**（snapshot からの継続 200,000 step × 6 アーム）。等価幅 δ = 0.0134（静的 O の効果量 0.1343 の 10%）。

### 判定

| 判定ID | 窓 | 推定 | 判定 |
|---|---|---:|---|
| **O-1（主）** | 短 | **−0.1375 [−0.2599, −0.0479]** | **PERSISTENT**（精度 INSUFFICIENT） |
| O-1 | 長 | **−0.1152 [−0.2089, −0.0308]** | **PERSISTENT**（精度 OK） |
| C-1 | 短 | +0.0124 [+0.0003, +0.0335] | INCONCLUSIVE |
| C-1 | 長 | +0.0155 [−0.0646, +0.0688] | INCONCLUSIVE |
| F-1 | 短 | +0.2762 [+0.1370, +0.4000] | **DESCRIPTIVE_ONLY**（科学判定 INCONCLUSIVE） |
| Ch-a | 長 | 再凍結率 **0.9720**、KM median τ = **200 step** | DESCRIPTIVE_ONLY（科学判定 PASS） |
| Ch-b | 長 | dead −0.0410 [−0.0640, −0.0210]／frozen −0.0580 [−0.0900, −0.0300] | INCONCLUSIVE |
| **Ch-1（総合）** | 長 | Ch-a PASS ＋ Ch-b INCONCLUSIVE | **PARTIAL**（記述扱い） |

**ガードは 3 本立った。** G1 CLEAR、**G2**（CI 半幅 > 0.10）、**G3**（A1 の実用復活率 0.390 < 0.5）、**G4**（A3 起因の致死率が 10 seed 中 6 seed で 0.5 以上）。G3 により F-1 と Ch-a は記述扱い。G4 により **A3 は「同大の無方向摂動では改善しない」の対照として使えない**（破壊対照）。

### §7.5 の 2×2 のどこに落ちたか

事前に凍結した表では「O-1 = PERSISTENT × Ch-1 = PASS」は**一過性**の枠（`rank_int_0814` の ~100k 再崩壊と同型、→ §2）。実際は Ch-1 が PARTIAL なので、**この枠に半分だけ入っている**。

- 復活ユニットの **97.2% が再凍結**し、KM の中央値は **200 step**（記録間隔 100 step の粗さに近い。時定数として引かない）
- それなのに `U` の改善は長窓終端でも残る（**−0.1152**）。**改善の出所は「復活したユニットが働き続けること」ではない**
- spec §7.2 の分岐どおり、O-1 = PERSISTENT は柱3 を強くする代わりに新しい穴を開ける: **なぜ通常の学習はそれを見つけないのか**。運用ルールに従い、穴の起票までを本走の成果とし、追跡実験を自動起案しない
- §8「介入・既存手法のSDE分解」の CBP 論点には**決着を与えなかった**（C-1 = INCONCLUSIVE、しかも one-shot 限定）

### 引用禁止・要注意

- **A1 はオラクルであって学習手法ではない**（現タスクの 32 パターン厳密損失を使う）。PERSISTENT でも「手法」と書かない
- **短窓 O-1 は精度不足**（G2）。数値を引くときは長窓（−0.1152、精度 OK）を主にする
- **F-1 の点推定は作業6 と逆符号**（+0.2762 対 −0.2353）。**作業6 の PROTECTIVE を疑う根拠として引かない** — G3 で母集団が痩せた記述値であり、介入したのは `p̂` であって `ΔL` ではない
- **A2 は one-shot であり CBP の継続適用ではない。** S7 で `cbp_harm_0815` のアンカーとは同一土俵でないことを確認済み（A2 直後の mean U = 0.2341）
- 記述専用アーム A1_hi（k=1.0）は非有限値 30,900 件で発散した。判定アーム（A0/A1/A2/A3）に非有限値はない
- **S2 は「元軌道との state hash 一致」ではない**（元軌道の step 700,000 state が未保存）。保存済み posreset S2・A0 probe 有無の replay hash・既存 none-resume ログ照合の合成確認である


## 18. 収束速度と可塑性のトレードオフ【論点・未検証 8/29】

→ [[収束速度と可塑性のトレードオフ_0829]]（詳細）／ [[中心主張v4草案_0829]] µ 考察 §7

**仮説（Issa・8/29、チャット `v4_init`）**: ReLU + µ/Σ 大の regime では、**可塑性と引き換えに学習の収束が速くなる**のではないか。立てば「µ 大は病理」が「µ 大は速さと引き換えの病理」になり、正規化・中心化が何を捨てて可塑性を買っているかまで言える。

- **先行研究とは逆向きの予測**: Clevert et al. 2015（ELU）は bias shift を学習を**遅らせる**要因として扱った（深い網・前層活性由来の µ・効率の議論）。本系は 1 隠れ層・固定入力平均・ReLU のみで、条件が違う。「矛盾」ではなく「効率が支配する regime と壁＋速度が支配する regime は別かもしれない」として置く（[[命題リスト]] Q0 の線引きに速度の軸が増える）
- **言えること**: µ を変えても関数クラスは変わらない（シフトは `b_i ← b_i − w_i·m_t` に吸収できる）ので、µ 水準間の差は**最適化力学の差**。std でも t = 100k では全 H 水準が高精度フィット（§0c）なので「µ 大だと最初から学べない」ではない。可塑性側の代償は柱3 の表（作業5）
- **言えないこと**: µ 水準ごとの初期収束速度そのもの。作業5 の 8 arm には `online_loss_A_w100.csv` があり入力列は arm 間でビット同一なので pilot は**新規走なし**で計算できるが、**spec を書く前に見ない**
- **候補機構（Claude 案・未検証）**: µ/Σ 大では初期化時点でユニットが「全入力で消灯／全入力で点灯（線形）／壁近傍の薄い殻（入力依存ゲート）」に割れる。点灯群が多いほど網はほぼ線形で速く、非線形容量を担う殻は壁に吸収される — 速さと可塑性喪失が同じ幾何の二つの面、という絵。予言: 教師の非線形性（`teachw_0820` の H）が強いほど速度の利得は消える
- **交絡**: スケール（`E‖x‖² = ‖µ‖² + tr Σ` で実効学習率が動く）、初期の生存数、教師の非線形性、EMA 中心化の過渡。速度比較ではこれを切らないと結論にならない
- **昇格条件**: pilot で速度指標が µ に単調かつ柱3 の最終 eval_loss と逆向き／confirmation でスケールを揃えても差が残る／H 層別で候補機構の予言が当たる。三つ揃うまで論点に留める


## 19. 過剰パラメータ化とスコープ条件【条件・穴あり 8/29】

→ [[過剰パラメータ化とスコープ条件_0829]]（詳細）／ [[中心主張v4草案_0829]] 位置づけ・柱3・支持節B

**条件（Issa・8/29・必須）**: 主張は「**学習器が教師よりある程度複雑**」な場合に限る。そうでないと「死ぬ＝容量不足＝そもそも学習できない」が自明に成り立ち、主張の中身が消える。考えたいのは実用系の状況（1 兆パラメータの LLM は生きているノルムが 1 万分の 1 でも表現能力は十分）。

**容量は二層ある**:

| 層 | 現状（w100・std・H_T=100） | 根拠 |
|---|---|---|
| パラメータ上の容量 | **足りている** | オラクル修復で NMSE 0.143367 → 0.009148（93.619%、[[機能盲目性_0823]]） |
| 活性部分網の容量 | **足りていない** | `alive_final` 中央値 5（教師 LTU 幅 100）、`strict_dead` 0.926 |

**v4 の主張は後者が尽きた regime に立っている。Issa が考えたいのは「活性部分網も十分なのに壊れる」regime で、そこは未測定。**

**実測の位置（committed・`teachw_0820`、学習器 w100 固定）**: `alive_final` 中央値は H_T=1: 46 / 2: 42 / 4: 4 / 8: 4 / 32: 2.5 / 100: 5、`dead_frac_final` 中央値は 0.54 / 0.58 / 0.96 / 0.96 / 0.975 / 0.95。**H ≥ 4 では alive はほぼ定数 4–5**（スケール整合層の傾き −0.0181 [−0.379, +0.309]）で、損失が壊れるかは「残った 4–5 個で教師が張れるか」で決まっているように見える。H ∈ {1,2} では死が半分で止まり損失もほぼ 0 — **残差が消えると駆動が消えて死が止まる**（§0c の T4 傍証、交絡つき）。

**したがって条件は二枚要る**: (A) 学習器 ≫ 教師、(B) それでも残差が持続。興味があるのは **A ∧ B** だが、**現状その測定点はゼロ**（H1/H2 は A ∧ ¬B、H4–100 は終端で実質 ¬A）。

**跳ね返り**: 柱3 の閾値 µ* は過剰パラメータ化比 1:1 で測った値なので、幅依存なら「µ の閾値」ではなく「マージン枯渇の閾値」を測っていることになる。支持節B の「全体は壊滅する」も regime 2 の観測である。

**決定的な問い**: `alive_final` は幅に対して (i) 絶対定数（罠幾何説の予言、§0c 残る候補 (a)）か (ii) 比例か。(i) なら過剰パラメータ化は保護にならず主張は強くなる。(ii) なら LoP はマージン消費の現象になり、実用系で効く量は消費速度になる。**どちらでも v4 の書き方が変わる。**

**判別**: 第一手は新規走なし（`coupling_fbw_0813` に w ∈ {5,10,20,50,100} × H_T=100 の committed ログあり。ただし全アームが教師以下の幅なので A ∧ B は作れない）。第二手は教師固定（H_T=4 か 8）× 学習器幅 w ∈ {100,300,1000} の新規走。**どちらも spec を書く前に数値を見ない。**
