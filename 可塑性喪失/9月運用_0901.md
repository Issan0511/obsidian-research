# 9月運用（現象2→3 直列集中）

親: [[現在地]] / 状態: **検証中**（今月の取り決め。主張ではない） / 更新: 2026-09-01 / 決定: Issa（8/31–9/1 チャット `論点マップ整理_0831`）

> **このノートの役割。** 9 月の進め方の正本。[[運用ルール]]（恒久のプロセス）とは別に、**今月だけの優先順と資源配分**をここに固定する。週次の枠は目安であり、判定が想定外の分岐に落ちたら枠ごと組み替える（**追跡実験の自動起案はしない** — [[運用ルール]] §3 は変えない）。

---

## 0. 決定事項

1. **今月は 現象2 → 現象3 の直列集中**（[[現象2_b経由の死_0831]] → [[現象3_ゲート非依存LoP_0831]]）。計算機の主キューはこの 2 つが専有する
2. **現象1（v4.2 の詰め）は「走なし＋15 分級の小走まで」で並行。** M 級の新規走は現象1 では回さない。残件の大半（N2 Dohare・Q26 確認・命題リスト転記・patch 適用・Phase 0b 再解析）はもともと走なし
3. **N1 は一行を「吸収域 × 到達（2 チャネル）」に書き直して今週提出**（先生レビュー優先 1 の採用。従来の「N2 の目処後」を差し替え）
4. 先生ブランチ（kubo_review）は 9/1 マージ済み（`8fbe7ae`）。v5計画の編集も commit 済み（`249529e`）
5. **現象3 の理論は先生が検討中。** 理論が届いたら、**予言を spec に固定してから**結果と照合する（逆順にしない）

## 1. 前提の更新（9/1 未明）

**P-1（condA・centered × `freeze_bias`）は既に走った** — `centered_freeze_0901`（repo `f402f80`・5M・10 seed・training 1655.9 s）。P1 **`BIAS_ROUTE_DECISIVE`**（late 窓 `strict_dead` 0.4639 → **0.0000**・10/10 seed）、P2 **`FROZEN_WORSE`**（厳密 unfit 0.0040 → **0.2281**）。vault 転記済み → [[centered_bias凍結結果_0901]]、[[論点マップ]] §23。

したがって **W1 は「回す」ではなく「畳む」から始まる**: Dead 側は決着、残る裁定は (a) 現象2 を柱1 の書き直しに畳むか（v5 未決 2）、(b) `FROZEN_WORSE` をどう書くか（`BIAS_ROUTE_DECISIVE` ≠ `LOP_CURED`）、(c) [[命題リスト]] への起票。

## 2. レーン構造（原則）

| レーン | 中身 | 律速 |
|---|---|---|
| **計算機**（現象2→3 専有） | M 級の走。回すと決めた走だけ | 機械 |
| **走なし解析** | 現象1 の詰め・登録作業・保存ログの再解析 | 人 |
| **裁定・書き物** | Issa 確認・先生やり取り・草案 | 人＋先生 |

**原則: 計算機が止まっている時間と、走を待って人が止まっている時間の両方をゼロに近づける。** 走行中は必ず走なしレーンの先頭タスクへ移る。

## 3. 数値計算の効率規則（ローカル 1 台・ほぼ直列）

**実測の規模感**: 5M × 10 seed × 1 腕 ≈ **28 分**（`centered_freeze_0901` provenance 1655.9 s・w100 1 層）。`LR_A1` 級 ≈ 15 分。P-2（幅 5 × 3 活性化 × seed 20 × 5M）は粗く 1–3 時間級。**つまり律速は計算機の速さではなく、再走事故と段取りである。**

1. **★ 再走を潰す spec 前チェックリスト**（今月の全 spec で毎回通す。効けば [[運用ルール]] §5 へ昇格）
	- [ ] 新 seed 群は `generator_offset` で作る（`--seeds` だけでは bit 一致の同一系列 — T0b の事故）
	- [ ] 吸収・凍結のある系は**総和と率の両方**を endpoint に書く（教訓⑰）
	- [ ] **等価限界 Δ を事前登録**する（「CI が 0 を含む」を「タイトな null」と「判定不能」に分ける — 保持測定の教訓）
	- [ ] 二値 endpoint の飽和を事前検討（教訓⑪。幅 5 は `dead_frac` 分解能 0.2 → `eval_loss` 主体・seed 20+）
	- [ ] studentized fallback と退化ガード（教訓⑬）／窓ラベル（⑫）／マスク個数の検算（⑭）／step 0 過渡（⑮）
	- [ ] 未フィット率の床の継承元を明示（2 層系 1e-23・1 層系 1e-16）
	- [ ] ロガー相乗り（下記 2）を spec 起草時に検討したか
2. **ロガーを盛ってから回す。** `M`・`B`・`denom`・`submerged`・per-layer 統計・境界スナップを新規走の既定にする。再計装リプレイ 1 回 ＝ 新走 1 本ぶんの時間。8/31–9/1 の走なし解析ラッシュ（`layer_stats.csv` 直読み等）が効いたのはログが厚かったから。**ただし主判定は spec の 1 つ**（相乗り列は事後ラベルで使う）
3. **3 commit 構成を厳守**: spec 単独 commit → 実装 → 結果（`gate_dose_0830` 型）。`centered_freeze_0901` は 1 commit に畳まれた（`elu_swamp` と同型・チャットログで時系列は追える）— **次からは崩さない**
4. **seed のプロセス並列（任意・効けば数時間級バッチが 30 分級になる）。** per-seed 独立プロセスなら決定論は壊れない。`OMP_NUM_THREADS=1` を全プロセス共通に固定したまま、runner の seed ループの外側に並列を足すだけ。**導入条件: 既存走 1 本（例: `centered_freeze_0901` frozen 腕）の bit 一致再現を先に確認する。** P-2（60 seed-run 級）の前に入れると一番効く
5. **キュー運用**: 回すと決めた走は前夜までに spec を凍結し、朝一・離席前・就寝前に投入する。1M パイロットは「パイロット」と spec に明記（**確証走は 5M 直行** — 1M の見かけ事故 2 回の教訓）

## 4. 週次の目安

**W1（9/1–9/7）: 現象2 を畳む ＋ N1 提出**

- 裁定: P-1 の帰結 —— 柱1 への畳み込み（v5 未決 2）・`FROZEN_WORSE` の書き方・[[命題リスト]] 起票
- 書き物: **一行を「吸収域 × 到達」へ書き直して N1 提出**（[[中心主張v4.2草案_0830]]。0 バイト問題の復旧を含む）
- 走なし小物: 第2層 `wcos_mean` の 10 seed 中央値（数分・先生レビュー優先 2）／Q26 の Issa 確認 4 点／`n5_strict_dead.patch` の適用
- 小走枠（15 分級）: なし or `LR_A1`（W2 から前倒し可）

**W2（9/8–9/14）: 現象3 の道具づくり**

- **P-3: 整列指標の再定義**（設計・走なし。候補: 活性化に依らず定義できる $s_i=M_i+B_i$ の分布 — 先生レビュー §2.3）
- **P-2b: `gate_dose_0830` の 1M→5M 水準対比の事後登録**（走なし。現象3 の自前 signal 候補 — `E_off`/`LR_off` だけが悪化）
- P-2 の spec 起草（チェックリスト全通し・P-3 の指標ロガーを積む）
- 小走枠: `LR_A1`（N3・性質 A/B の多層分離。現象1 と 3 の両方に効く）

**W3（9/15–9/21): 現象3 の存在確認**

- **P-2 投入: 幅 5 × 活性化 3 種 × 5M・seed 20+**（今月最大の走・週初に投入）。指標なしで回すと二値しか取れず再走になるので **P-3 が先**
- 走行中: 現象1 の残り（命題リスト転記・Phase 0b 再解析・Q17 rest 帳簿化の設計）

**W4（9/22–9/30）: 判定と分岐**

- P-2 判定 → **現象3 の主張形式の裁定**（「整列由来」か「獲得的有効容量の崩壊」か — v5 未決 3）
- 先生理論との照合（予言を spec に固定してから）／バッファ

## 5. 今月やらないこと

- 現象1 の M 級新規走（`L2_A2` 再走の発散対処を含む — 10 月以降）
- P-5 Σ ダイヤル（[[現象3_ゲート非依存LoP_0831]] §6.2 の縮退割り）— 設計メモまで
- 用量分散の判別走（kubo_review §3.3）— P-2 の後
- **v5 を名乗るかの決定**（v4.2 の帰趨が先）

## 6. 依存・待ち

- 先生: N1 への応答／現象3 の理論
- Issa: Q26 の 4 点／P-1 帰結の裁定／seed 並列を入れるかの判断（§3-4）
