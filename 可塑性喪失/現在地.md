# 現在地

状態: **常に最新**（区切りごとに上書きする。履歴は残さない — 履歴は [[研究索引]] と [[論点マップ]]）/ 更新: 2026-09-01

> **新しいチャット・Codex への引き継ぎはこの 3 ファイルだけで足りる。**
> [[現在地]]（今どこ）→ [[引用禁止]]（絶対に踏んではいけない線）→ [[運用ルール]]（進め方）
> 個別の論点に入るときだけ [[研究索引]] の論点ツリーから辿る。

## 一行

**8/30 に主軸が動いた。** v4 の柱3「µ/Σ に閾値がある」は、用量を厳密に固定して 5M まで走らせると**消えた**（帯内の全用量が発症）。代わりに、用量を帯内に固定したまま活性化関数だけを替えると **ReLU 10/10 に対し ELU・leaky ReLU は 0/10** で、**壁を病理にしているのはゲートの片道性**であることが介入で示された。**v4.2 は二本柱**: 柱1 = µ が壁を作り ReLU が µ を作る（性質 A・尺度不変で逃げられない）、柱2 = 壁は片道で片道が病理を作る（性質 B）。一行は「**LoP は、ReLU が作り、ReLU が閉じる罠である**」。µ/Σ だけでは無害、ゲートだけでは弱い、病理は積。

## 正本

- 主張: **[[中心主張v4.2草案_0830]]（二本柱・先生未承認）**。前版 [[中心主張v4.1草案_0830]]（三本柱・同日）／[[中心主張v4草案_0829]]／[[中心主張v3草案_0823]]（8/23 承認済み）
- 作業台帳: **[[中心主張v4.2作業リスト_0830]]**（v3 の残件も §B に引き継ぎ済み）
- 用語: [[用語と記号]]。`strict_dead := p̂=0` / `near_off := 0<p̂<0.05` / 旧互換 `dead_0.05 := p̂<0.05` / **沈下 `submerged := max_x z_i(x) ≤ 0`**（8/30 追加・非 ReLU 腕用）
- 証拠台帳: [[命題リスト]]（**8/30 に v3・v4.2 を転記済み** = Q19–Q26 新設・Q18 欠番。**Q26 の Issa 確認 4 点は 9/1 決着**（転記承認・字義修正採用・task_080 接続・`w_free` 統一）。残るのは task_101 / task_102）
- **三分割（8/31・久保先生の口頭指示）**: 棚卸しは [[中心主張v5計画_0831]]（計画であって主張ではない）。**1 論点 1 ファイルへの分離を 8/31 に実施** → **[[現象2_b経由の死_0831]]**（[[論点マップ]] §23）・**[[現象3_ゲート非依存LoP_0831]]**（同 §24）。あわせて [[論点マップ]] に**全節の索引**を新設し、無番号だった 2 節に §13b・§22b を後付けした（**既存の節番号は動かしていない**）

## v4.2 の構成（8/30・二本柱）

**v4.1 の柱3 は柱1 と柱2 の論理積の言い換えで、単独で反証できる命題を含まない**（Issa 指摘 8/30）。二本柱に畳んだ。

- **柱1【機構・性質 A】** µ が壁を作り、ReLU が µ を作る。1a 単層（閉形式・用量反応）／1b **用量は率で閾値ではない**（`dose_const_5m`・v4 柱3 の撤回）／1c 多層（**尺度不変性** 0.683 = $1/\sqrt{\pi-1}$、重みの大小では逃げられない・`L2_A1` で第2層 dose 7 が残る・$\lambda$ 訂正）
- **柱2【動力学・性質 B】** 壁は片道で、片道が病理を作る。2a 吸収（復活 0・ハザード一定）／2b **片道性だけを外す介入**（`gate_dose`: ReLU 10/10・ELU/leaky 0/10・P4 で用量効果なし）／2c 多層（`E_A1`: µ は大きいのに落ちない）
- **積の構造**（新設）: 2×2 で LoP が出るのは「µ/Σ あり × 片道ゲートあり」だけ。**µ/Σ だけでは無害、ゲートだけでは弱い。ReLU は両方を持つ。** 要因計画ではない（別配置の 4 セル）
- 支持節 A・B（v4 から変更なし）、µ 考察 §1–§9
- 一行: **「LoP は、ReLU が作り、ReLU が閉じる罠である」**（先生未承認・未決 2）

## 8/30 の 3 走（すべて事前登録・committed）

| 走 | 結果 | ノート |
|---|---|---|
| `dose_const_5m_0830`（用量をオラクル固定・6 腕・5M） | **帯は 1M の見かけだった。** 5M で全用量 10/10、跳び $J$ 0.457 → 0.029、$g^*$ 10.04 → 9.33（`BAND_MOVES_DOWN_BY_5M`）。1M の発症数は用量に単調 → **用量が決めるのは発症までの時間** | [[柱3定用量5M_spec_0830]] |
| `elu_swamp_0830`（2 層・ELU・5 腕・5M） | **G0 = `LOP_REDUCED`。沼は空だった**（第2層沈下 1.5%）。`E_A1` は第2層入力の ‖µ‖ が ReLU より大きい（2.00 対 1.45）のに未フィット率 0.0028（ReLU 0.0219）。**Issa・Claude の機構予測はどちらも外れ** | [[ELU沼結果_0830]] |
| `gate_dose_0830`（用量固定 × 活性化 3 種・9 腕・1 層・5M） | **`GATE_LOAD_BEARING`。** ReLU 10/10、ELU・leaky は 1M・5M とも 0/10。P3 は全 6 対比で Δlog₁₀ −1.8 〜 −2.6（10 seed 同符号）。**P4 は 0 を含む＝ゲート無しでは用量が水準にも効かない** | [[ゲート条件付き用量結果_0830]] |

**最大の機構的発見（8/30）**: **沈下率は下がっていない**（ReLU 0.98–0.99・ELU 0.36–0.45・leaky 0.63–0.67）のに LoP だけ消える。**病理は「負に落ちること」ではなく「落ちた先で勾配が消えること」。** leaky が ELU と同等なので**滑らかさは要らず、片道でないことだけが効く**。副予測「沈下率 0 近傍」が外れたことで、沈下と LoP が交絡せずに分離された。

**ReLU の二性質（v4.1 柱3c）**: A = 正斉次な整流が隠れ層で用量を作り直す（入力を正規化しても第2層に dose 7）。B = 負側の勾配が厳密に 0 で壁を片道にする。**A は B が無ければ無害**（leaky も A を持つが死なない）。ReLU が特殊なのは両方を同時に持つ点。

## 開いている穴

1. **⚠ Dohare 付録 B との緊張 — 9/1 に再裁定（`TENSION_REFRAMED` 案・Issa 裁定待ち）** → [[原典条件照合_0901]]
   - **幅軸は閉じた。** `width5_gate_b_0901`（repo `2a62803`）で幅 5 を自前で回し、原典と一致させた。8/31 の `TENSION_LOCALIZED`（軸 = 学習器幅）はここまで
   - **しかし §12 の条件差の棚卸しが不完全だった。** 9/1 に Dohare の公開実装を直接読んで 3 本の差が出た: (i) **原典の Linear ベースラインは隠れ層ゼロの単層線形回帰**（`lop/nets/linear.py`）で当方の `LIN5`（leaky $a$=1.0・隠れ 5）と別物、(ii) **図の x 軸は 1M まで**（走は 3M）で当方の 5M 末尾窓は描画範囲の 5 倍先、(iii) **ビン幅はコードでは 20k**（論文本文は 40k で原典内不一致）
   - **★ §5.2 の目視読み「leaky は Linear より上」は原典本文に対応物を持たない。** 本文が名指しするのは ReLU・tanh（線形水準に**達する**）と ELU（less severe）だけで、**leaky-ReLU は一度も言及されていない**
   - **したがって「Dohare の再現に失敗した」と書かない。** 原典**本文**に対しては当方の結果は整合する（`R5` が Linear と分離しない = 「線形ベースラインの水準に達する」）。ただし比較相手が別物なので **`LIN0` 腕が入るまで暫定**（[[HANDOFF_LIN0_と1M窓_0901]]）
   - **「ELU / leaky は LoP を防ぐ」と一般には書かない**は維持
2. ~~**性質 A と B の多層での分離が未測定**~~ → **9/1 決着**（`lr_a1_0901`・repo `1970f3b`・[[LR_A1結果_0901]]）。P2 **`A_WITHOUT_B_HARMLESS_MULTILAYER`**: 2 層 leaky・第1層中心化で第2層に dose **6.38** [6.04, 6.55] が立つのに Δlog₁₀U は **−0.974** [−1.201, −0.669]（`E_A1` 帯の内側・沈下率 0.84 でも無害）。**「A は B が無ければ無害」が多層で介入により閉じた。** P1（閉形式帯 [5.35, 6.35] との一致）は **`INCONCLUSIVE_WIDE`**（λ補正帯なら一致だが report-only・[[引用禁止]] B）。沈下率の事前予測（0.60）は外れ（実測 0.84） → v4.1 未決 4 は解消
3. **`L2_A2`（第2層のみ中心化）が数値発散で未検証**（8/29 から継続）。「第2層単独では効かない」は言えない
4. **eff_rank は「死」と「平均支配」を区別しない**（8/29 から継続）。平均除去した活性での再定義が要る → **9/1 に格上げと設計**（[[現象3指標再定義_P3_0901]]・**事後・未登録**）。`mlp2_phase1_0829/layer_stats.csv`・step 5M・layer 1 で `L2_Aall`（alive 98.5）の `eff_rank` **3.097** [2.713, 3.944] 対 `L2_none`（alive 18.5）の **9.408** [5.255, 16.879]。**生きているユニットが 5 倍多い腕のほうが 3 倍低く、seed 範囲は非重複・対応差 10/10 同符号。** 置き換え案は平均除去実効ランク $R_c$ だが、**活性化行列が committed に無いため新規ロガーが要る**（穴は閉じていない）
5. **Phase 1 の署名表が「介入が早期を悪化させる」型を覆っていない**（8/29 から継続）。「LoP を防いだ」とは書けない状態が続く
6. **Q17 の系統的な rest 駆動の帳簿化**（8/28 から継続）。`gate_dose` の Q2 は全腕 `NOISE_DOMINATED` だが**非 ReLU 腕での測定**で、ReLU の駆動は Q17 のまま。一行に「意味のない揺れで」を戻すかはこれ次第 → v4.1 未決 3
7. ~~**`gate_dose_0830` の `strict_dead` 列が非 ReLU 腕で `submerged_frac` と恒等的に同値**（`p̂=0 ⟺ max z ≤ 0`）。verdict には未使用だが、**先生向け summary で「ELU でも strict_dead 36%」と誤読される**~~ → **8/30 解決**（Issa 裁定「列を空にする」）。`verdict.csv` の当該列を非 ReLU 6 腕で空にし、`src/gate_dose.py` も ReLU 腕でのみ出すよう修正、`summary.md` に注記。**数値の変更はゼロ。repo 反映も 9/1 に確認済み**（HEAD `74a7baf` に修正入り。[[v5作業リスト_現象1_0901]] §2）
8. **可動度の絵が合わない。** `gate_dose` の Q2 は ELU β 0.4–0.6（期待 1）・leaky β 有意に負（期待 0）で両方 `SCALING_MISMATCH`。**主張には使わない**（ELU 沼プログラムは打ち切り）
9. **`E_Aall` で第2層を中心化すると第1層の沈下が 6% → 39% に跳ねる**（機構未解明・[[ELU沼結果_0830]] §4.4）
10. 柱3 の枠づけ（8/27 部分裁定・先生の最終裁定待ち）／作業3(a) の Issa 確認と [[命題リスト]] 転記、v3 転記
11. ~~**centered 腕（`L1w100_A1`・5M）の死因が未確定**~~ → **8/31 決着（[[centered死因分解結果_0831]]・事後解析の登録）。** E1 `WALL_INVARIANT`（onset $\beta$ は3腕で −2.23〜−2.26）、E3 `BOUNDARY_CARRIES_DESCENT`（`L1w100_A1`: 境界 −4.297、内部 +1.933）、E4 `CENTERING_REDUCES_BUT_NOT_REMOVES`、E5 `ABSORPTION_BROKEN_BY_EMA`（タスク内復活 25,723、`L2_none` L1 は 0）。**訂正:** 「降下は全部 $B$」は登録 E2 では言えない。step 0 の EMA 初期過渡を含む字義どおりの E2 は `MU_CHANNEL_ALIVE`、step 10k 起点の $B$ 優勢は REPORT_ONLY 感度分析。
    - **追跡2本も8/31完了。** [[境界窓細粒度記録結果_0831]] は $\tau_{\rm fit}=230$ step で `TIMESCALES_NOT_SEPARABLE`、段階2未実行。[[オラクル中心化結果_0831]] は真の切替499点で P1 `BOTH_CONTRIBUTE`（R=0.5167 [0.4858, 0.5805]）、P2 `ORACLE_INCREASES_DEATH`（Aexact strict-dead率10 seedすべて1.0、A1との差 +0.50 [+0.46, +0.595]）。**Aexactはタスク可識別性も消す交絡がある。**
12. **⚠ µ の用量反応が非単調である（8/31 新設・最優先）。** `L2_none` 0.809 / `L1w100_A1`（EMA 中心化）0.469 / `L1w100_Aexact`（$\mu\equiv0$）**1.000**。Aexact は `unfit` も 1.000、**復活 0 件**（支持が一度も引き直されないので [[吸収不等式の命題化_0824]] 系1 が厳密に成立する唯一の腕）、全滅到達 task 154–454。これは `mu_titration_0823` の低用量反騰の $\mu\to0$ 端であり、§6 の予言「µ 非依存の**床**が残る」の**床が天井だった**ことになる。**[[中心主張v4.2草案_0830]] 柱1 の書き方に触る**（改訂は Issa 判断・未着手）
13. **P1 の登録値 $R_{499}=0.5167$ は打ち切りを受けている（8/31 新設）。** Aexact は中央値 **36 境界**で凍結し（A1 は 387）、残りは $\Delta\beta=0$ を積む。生存中 1 境界あたりに直すと **Aexact −0.0509 対 A1 −0.0211 で符号関係が反転**（生存本数を揃えても −0.0489 対 −0.0279）。よって **B1（EMA の遅れが原因）は支持されず、遅れはむしろ保護的**という読みになる。登録判定 `BOTH_CONTRIBUTE` は残すが単独で引かない → [[オラクル中心化_spec_0831_追補2]]（事後解析の登録・**実装と repo 反映は未了**）、[[運用ルール]] §5 教訓⑰
14. ~~**現象2の condA・centered × `freeze_bias` が未走。**~~ → **9/1完了**（[[centered_bias凍結結果_0901]]）。終盤 `strict_dead` は free 0.46394 → frozen 0で `BIAS_ROUTE_DECISIVE`。一方、厳密 `unfit` は 0.00397 → 0.22811で `FROZEN_WORSE`。**b 経路の Dead は決着したが、b 凍結は治療ではなく、Dead と機能は逆向きに解離した。**
15. **チャネル遮断 2×2 は9/1完了**（[[チャネル遮断2x2結果_0901]]、repo `bb5fd09`）。主判定 `TWO_CHANNELS_BOTH_NECESSARY`、S-floor PASS。一方で B02 差 8.980 dex の `CEILING_CONTAMINATED` と `LADDER_INVERTS` が立つため、**E-drift 単独では引かない**。EMA 中心化の可識別性交絡は未解決。交互作用は spec §6.3 の字義式で `SUPERADDITIVE` だが、§2.1/§8.1 と符号規約が逆だったため機構の証拠にしない。
16. **現象2 の主張を一枚化**（9/1 夜・[[現象2主張v1_0901]]）。C1–C5（b の双方向自走・b 限定 WD による治療・2 チャネル必要性・残差の純化）＋先行境界（[[先行精読_bias暴走_0901]]・repo `54a7e3e`）＋引用制限。**裁定待ち 2 件**: P-1 の恒真性降格、命題リストへの行立て。
    - **スコープ拡張の計画を分離**（9/1・[[現象2スコープ拡張計画_0901]]）。8 軸の棚卸し＋**走なしの平衡式 $\lambda b^\ast=-(\tau+R)$ の導出**（lr・batch・$T$ を予言つきスポットチェックへ格下げする根拠）。**最優先は棚卸しに無かった第 9 軸＝教師難度（残差水準）× b-WD**（C4「復元力はタダ」の本丸）。**登録判定なし・裁定待ち 5 件**（順序／既存 `REPORT_ONLY` 腕の格／[[9月運用_0901]] への配置／窓の定義式と等価マージン／PMNIST の移植対象）
17. **§20 の可識別性交絡を割る 2×2 の spec を起草**（9/1・[[可識別性2x2_spec_0901]]・run id `ident_mu_2x2_0901`）。**Option B（4 セル統一ハーネス）で Issa 決定**: µ を EMA でもオラクルでもなく**可視入力の構成**で置くので、EMA 由来の副次交絡 4 つ（追随ノイズ・遅れ窓・`ABSORPTION_BROKEN_BY_EMA`・step 0 過渡）が同時に落ちる。代償は既存 committed 腕との bit 一致の放棄。**設計上の補題**: タスク内定数かつタスク間で識別的なチャネルは必ずタスク依存平均を持つので、**純セル (µ無・可識別有) は前活性の経路では作れない**。よってバイパス案が割るのは §20 の鋭化版であり、**per-unit 可識別性と「境界ごとの µ の引き直し」は閉じない**。**未了: D0（[[9月運用_0901]] §0-2「現象1 の M 級新規走は今月回さない」との衝突）、§8 の事前予測（Issa）、repo 側 spec 単独 commit。**
    - **D0 は同日決着（Issa）: [[9月運用_0901]] を改訂して W1 に置いた。** 理由は「長い実験だからこそ早くに終わらせる必要がある」。改訂は 3 箇所 —— §0-2 に本走限定の例外、**§3-6 に新規則「長い走ほど早く投入する」**（M 級は判定・書き物より先にキューへ。走なしが先に片付くことを理由に後ろへ送らない）、§4 W1 に走枠。
    - **★ v2 へ改稿（同日・純化 2×2）。** committed ログ（`center_oracle_0831`・`74a7baf`/`72a0fd8`）の事後再計算で **`Aexact`（= 旧 `im` セルと同分布）の死が全数 b の自走（現象2）**と判明（$M\equiv0$ ゆえ $\hat p=0\iff b\le-\kappa\sigma$ が代数的に強制。初回消灯時 b の seed 中央値 −0.999、+b 走行も 18.5%）。旧設計の M ダイヤルは「µ の壁」と「b への荷重移転（Q19 C2）」を同時に切り替えており不公平だった。**v2 は要因 4 セル全部に b-WD（λ=1e-3・C4）を敷いた純化 2×2**（Issa 裁定 D8）。主判定の読みは「b 拘束下」限定。副産物として登録副判定 **R-ext（b-WD は `Aexact` 型全滅を止めるか）**を `im` vs `im_nowd` で持つ。**残るのは §8 の事前予測（Issa）と repo 側 spec 単独 commit の 2 点のみ。**
    - **9/1 夜: §8 の事前予測 5 項を Issa が記入。vault 側は凍結可。** `MU_DOMINANT` ／ E-drift 順位は「µ が強い」型（M+ 2 セル > M− 2 セル）／ R-ext `BWD_PREVENTS_EXTINCTION` ／ S-op 通る ／ 書き直し先は「わからない」。**予測 2＋3 の含意（§8.1 に明示）: 「µ→0 端の全滅は µ の話ではなく丸ごと b の話だった」に賭けている** —— 当たれば穴12 の非単調性が整理され、外れれば C4「復元力はタダ」への初の反例になる。**残るのは repo 側 `specs/spec_ident_mu_2x2_0901.md` の単独 commit（＝事前登録の成立点）のみ。**

## 次に決めること

> **9 月の進め方は [[9月運用_0901]] が正**（9/1 決定・Issa）: **現象2 → 現象3 の直列集中**。現象1 は走なし＋15 分級の小走まで／確証走は 5M 直行／spec 前チェックリスト／seed プロセス並列は bit 一致確認後に導入。以下の個別項目は同ノートの週次枠に配置済み。

- ~~**⚠ 最優先は [[中心主張v4.2作業リスト_0830]] N2**~~ → **8/31 に `TENSION_LOCALIZED` まで到達**（[[容量不足レジームの機構_0830]] §5・緊張は学習器幅の一軸に局在。N1 未決リストからも削除済み `96795cf`）。規模差の機構説明は現象3 の P-2 と同じ問いに畳まれた
- **[[中心主張v4.2草案_0830]] を先生に出す**（N1）。~~**N2 の見通しが付いてから**のほうが安全~~ → **9/1 差し替え（Issa 裁定）**: 一行を「**吸収域 × 到達（2 チャネル）**」（[[kubo_review_v4.2_0831]] §1）へ書き直して**今週提出**（[[9月運用_0901]] W1）。**改稿 7 点は 9/1 に草案へ反映済み**（[[v5作業リスト_現象1_0901]] §1a・§7）。**残るのは §1b の未決 9 点を添えた提出そのもの**。なお kubo_review §2.2 の独立支持（`wcos_mean` 単点）は**再現せず引用禁止**（[[wcos_mean中央値_0901]]・提案 2 本体は無傷）
- ~~**N3 `LR_A1`** を回すか~~ → **9/1 完了**（[[LR_A1結果_0901]]・穴2 決着。実行時間 37.3 分）
- ~~今すぐ片付く: N5（`strict_dead` 列の誤読）・N12＋B4（秘書 stale の整理）~~ → **8/30 完了**
- ~~N6＋B3＋B1: 命題リストへの転記の塊~~ → **8/30 完了**（[[命題リスト]] Q19–Q26）。**残るのは Q26（吸収不等式）の Issa 確認 4 点**と、`n5_strict_dead.patch` の repo への適用・commit
- ~~**centered死因分解と追跡2本を実行**~~ → **8/31 完了**。[[centered死因分解結果_0831]]（`d38a391`）→ [[境界窓細粒度記録結果_0831]]（段階1で失効、`3d8ca58`）→ [[オラクル中心化結果_0831]]（P1 `BOTH_CONTRIBUTE` / P2 `ORACLE_INCREASES_DEATH`、`74a7baf`）。追加追跡は自動起案しない
- 候補 B（発火率ホメオスタシス）の spec を起こすか（[[LoP防止アーキテクチャ_0829]] §4）
- **9/2 新規: [[逆伝播漏れ2x2_spec_0902]]**（run id `bwd_leak_0902`・下書き v2）。**v6 前半の★穴 1（「勾配が消える」が「出力が消える」から分離されていない）を巻き込んだ。** forward × backward の片道性を別々に外す 2×2 —— `BL`（順 ReLU／逆 leaky＝勾配のみ）と `FL`（順 leaky／逆 ReLU＝出力のみ）—— で担い手を判定（V1: `GRADIENT_CARRIES` / `OUTPUT_CARRIES` / `EITHER_SUFFICES` / `BOTH_REQUIRED`）し、その下に b-WD の折衷（V2）を置く。1 層 5M・`gate_dose_0830` の判定基準を逐語継承・対照 `R`/`LR` は committed 値。**段 1 最小 = `BL_1216`+`FL_1216` の 2 腕・15 分**で [[9月運用_0901]] §0-2 の 15 分級に収まる。先行 SUGAR（arXiv:2505.22074・原典未精読）。**未了: §7.1 の V1（`FL`）予測（Issa）、段の切り方（作業表 #0）、S-log 分岐調査、repo 側 spec 単独 commit。** なお Issa の V2 予測（`RESTORING_FORCE_REQUIRED`）は v6 の予言（`BL` 0/10）と既に割れている
- `L2_A2` の再走（発散 seed 除外・n=9 の規則へ）／Phase 0b の腕効果の再解析（**新規走 不要**）

## 運用メモ（8/30 更新）

- **repo 側の事前登録が 8/30 に正しい形になった**: `gate_dose_0830` は spec＋config（`d66da2d`）→ 実装（`e15e081`）→ 結果（`c4fe8ed`）の 3 commit。**その 1 走前の `elu_swamp_0830` は 1 commit（`c60335c`）で、repo 履歴だけでは順序を示せなかった**（凍結の証拠は vault 側）。**spec は必ず単独で先に commit する**
- [[運用ルール]] §5 の教訓⑪〜⑬（二値 endpoint の飽和 / regime ラベルの窓ずれ / studentized bootstrap の退化）。**studentized は Phase 0b 以降ほぼ全行退化し、percentile が主**というのが既定と思ってよい
- 未フィット率の床は**系ごとに違う**。2 層系（Phase 1・`elu_swamp`）は `1e-23`、1 層系（`dose_const_5m`・`gate_dose`）は `1e-16`。**決め打ちにせず、継承元を明示する**
- `20260830` は `generator_offset` として既用の値。**日付として流用しない**。bootstrap seed は `20260829` を継承
- **dose = $\|\boldsymbol\mu\|/\sqrt{\operatorname{tr}\Sigma/d}$ と生の $\|\boldsymbol\mu\|_2$ を混同しない。** condA 入力では dose = 4‖µ‖
- **第2層入力の µ/Σ 比を引くときは窓を書く**（8/30）。0.740 / 0.707 は `final_t500`（task 500 単点）、late 窓 451–500 では 0.848 / 0.718。**`L2_none` は窓で 1.39 → 0.74 と動くので、「閉形式 0.683 と整合」は無介入腕について無条件には書けない**（`L2_A1` は全窓 0.707–0.811 で安定）→ [[引用禁止]] B、[[運用ルール]] §5 教訓⑫
- **活性化の異なる腕のペアリングは init・教師・入力実現まで**（step 1 以降は軌道が分岐）。「同一個体の追跡」ではない
- **0/10 は「観測しなかった」の強さ**（片側 95% 上限 $p\le0.2589$）。「起きない」と書かない
- **多層化は解禁済み**（8/29）。[[論点マップ]] §12 の「2層 ReLU 限定」は測定範囲の記述であって禁止ではない
