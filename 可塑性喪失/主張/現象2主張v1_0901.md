# 現象2 主張 v1（2026-09-01）

親: [[現象2_b経由の死_0831]] / 関連: [[centered_bias凍結結果_0901]]・[[チャネル遮断2x2結果_0901]]・[[先行精読_bias暴走_0901]]・[[命題リスト]] Q14/Q26・[[論点マップ]] §20/§23
状態: **主張の骨格完成・裁定待ち 2 件（§6）** / 作成: 2026-09-01（チャット `P1結果監査_0901`）

> **このノートの格。** 9/1 の一連の登録判定・committed 出力・事後計算・先行精読を、現象2 の主張として一枚に統合したもの。各行に格を付す。[[命題リスト]] の証拠列を書き換える権限は持たない。

---

## 0. 一行

**現象2（b 経由の死）の正体は LoP の一種ではなく、centered regime で拘束を失った bias の双方向の自走である。** 下に流れれば痩せて死に、上に流れれば飽和して線形化しランクが潰れる。b に微小な復元力（b 限定 weight decay）を与えると両方向とも止まり、機能はむしろ改善する。この病気は std でも劣化の過半を運んでおり、b 成分を除去した残差が、初めて中心主張（µ/Σ 駆動の壁）どおりに振る舞う。

---

## 1. 主張の分解（C1–C5・格つき）

| # | 主張 | 格 |
|---|---|---|
| **C1** | centered では task 末で $\mu$ 残差が rnd 座標のジッタだけになり（実測 $\lVert\mu_{\rm res}\rVert$ 中央値 ≈0.075）、$s=w\cdot\mu+b$ を動かせるのは実質 $b$ のみ。台帳（flip×b 縮退）の逃げ道も塞がる | 恒等式（Q26）＋committed 実測（`centered_freeze_0901` / `channel_2x2_0901`） |
| **C2** | **下向き自走**: 税（Q14・$-2\eta v_i^2\mathbb E[a_i\,\mathrm{gate}_i]\le0$・入力分布に無条件・自己消灯性あり）が b を沈め、alive の発火が痩せ、壁を割って死ぬ。centered 腕で dead 0→0.46〜0.48 が単調に蓄積 | 定理（Q14）＋committed 軌跡 |
| **C3** | **上向き自走**: 第2層を centering された第1層では b が損失をほぼ動かさない方向に暴走（alive 中央 +0.17→**+12.85**、新系列で Δ**+13.91** [+10.37, +17.75]）、常時発火 68% → 線形化 → 活性 `eff_rank` 10.9→3.1 | committed（`channel_2x2_0901` ledger・独立系列で再現）。暴走の**機構**（+b 方向の平坦化仮説）は未検証 |
| **C4** | **治療**: b 限定 WD（λ=1e-3）で dead 0.464→**0.010**、`mean(log10 unfit)` **−0.239** [−0.269, −0.208]（改善）、劣化 −0.132、第1層の飽和・ランク崩壊も防止。b≡0 凍結（機能 50 倍悪化）とは別物 — **自由度の除去ではなく深さの上限** | **登録判定** `BIAS_WD_PROTECTS`（3 条件連言・`bias_wd_0901`） |
| **C5** | **regime 横断と残差の純化**: std でも b 遮断で劣化が 0.335 [0.249, 0.425] に減る（登録・`INCONCLUSIVE_PARTIAL` = 事前の箱の中間）。b と µ の遮断 2×2 は **`TWO_CHANNELS_BOTH_NECESSARY`**（`both` E-drift −0.026 [−0.075, +0.021] ⊂ ±0.15）。b 成分除去後の残差では M（$w\cdot\mu$）が沈み・b≈0 のまま死が積み、**この計画で初めて dead↔機能劣化の seed 間共変**（ΔL2_dead ρ=+0.73, p=0.025） | 登録判定 2 件＋事後計算（共変は n=9・未登録） |

---

## 2. 中心主張への帰結

**脅威ではなく純化。** 「centering は治癒でなく遅延」の行はこう読み替わる: *遅延。ただし残る死と劣化は µ の病気ではなく b の自走で、微小な復元力で除去可能。除去後の残差が µ/Σ 駆動の壁の純粋なサンプルになる。* 先生側提案の「吸収域 × 2 チャネル」の書き直しは、同日中の登録 2×2 で裏打ちされた。

---

## 3. 先行との境界（→ [[先行精読_bias暴走_0901]]）

| 要素 | 先行 |
|---|---|
| b 下方ドリフト → 死 | **経験的先行あり**: Cornacchia et al.（arXiv:2111.02154）が定常・純ラベルノイズ条件で負の bias drift と "ReLU death" を報告（**定理でなく本文＋Figure 4c**・ノイズ切り対照なし・治療なし）。本計画の差分: 無条件の定理（Q14）・閉形式の壁（Q26）・ノイズゼロの継続学習設定・治療 |
| b 上方暴走 → 線形化・ランク崩壊 | Lyle et al.（arXiv:2402.18762）が preactivation 正側シフト→線形化を現象論として扱うが、**hidden b への帰属なし**（不在検索ログ付き） |
| パラメータ帰属の 2×2 / b 限定 WD / 復元力治療 | **両論文に無し**。慣行はむしろ逆（bias を WD から除外するのが標準レシピ） |

注意: Lyle の "additive benefit" は介入パッケージの本文表現で factorial 対比ではなく、**`SUPERADDITIVE`（劣加法）判定と矛盾しない**。Lyle の WD の掛け先（bias 含むか）は**未確定** — 全パラメータ L2 とコードで確定しているのは Dohare のみ。

---

## 4. 書けないこと（引用制限）

1. `BIAS_ROUTE_DECISIVE` / dead 低下を `LOP_CURED` と読み替えない（frozen で dead 0 と機能 57 倍悪化が両立した前例）
2. 「µ を消した」と書かない — EMA 中心化はタスク可識別性も同時に消す（[[論点マップ]] §20・唯一の実質的な穴）
3. 「2/3 : 1/3」の比を主張に載せない（劣加法・床効果・指標依存）
4. E-drift 単独で引かない（`CEILING_CONTAMINATED`・`LADDER_INVERTS`）。到達水準では `bwd` 単独が最良（−5.20）で順位が入れ替わる
5. 「b-WD が LoP を治す」と一般化しない。スコープ: condA・幅100・$T=10^4$・batch=1・lr=0.01・plain SGD・5M・λ=1e-3。Dohare の Permuted MNIST 等への外挿は未知 → **このスコープをどう狭める／広げるかの計画は [[現象2スコープ拡張計画_0901]]**（計画であって主張ではない。**本行の制限は計画の完了までそのまま有効**）
6. C3 の暴走機構（+b 方向の平坦化）と、深さ2オンライン SGD の稀な発散（WD 起因説はほぼ棄却・慢性/急性未判別）は未解決として引く

---

## 5. 開いている穴

1. **§20 の可識別性交絡**（科学の穴はここに集約）: 可識別性 × µ の 2×2 が未設計・未走
2. frozen 腕の unfit≈0.20 の床の正体（$c$ が動くのに残る理由）
3. C3 の上方暴走の機構
4. std での b-WD 最適 λ（centered の 1e-3 を流用しただけ・非単調の兆候あり）
5. **教師難度（残差水準）が C4 の「復元力はタダ」を壊すか**（9/1 追加）。棚卸ししたスコープ軸に入っていないが、機構上いちばん壊れやすい。$b^\ast=-(\tau+R)/\lambda$ の rest 項 $R$ が残差水準を運ぶため

> **スコープ軸の棚卸しと閉じ方**（$\lambda$ 用量反応・教師難度 $H_T$ × b-WD・lr/batch/$T$ の予言つきスポットチェック・幅 5 相乗り・Permuted MNIST）は **[[現象2スコープ拡張計画_0901]]** に分離した（9/1・計画であって主張ではない）。同ノートに **$b$ の平衡式 $\lambda b^\ast = -(\tau+R)$ の導出**（走なし）が入っており、lr・batch・$T$ の 3 軸を新規測定から予言の検定へ格下げする根拠になっている。**登録判定は含まない。**

## 6. 裁定待ち（Issa）

1. **P-1 の格の降格**: frozen の dead=0 は $b\equiv0$＋centering の下で代数的に強制（$\lVert\mu_{\rm res}\rVert\le0.167$ 対 壁 $0.5\lVert w_{\rm free}\rVert_1$・最悪 3 倍不足）。「介入による決着」から「Q26 の系＋harness のサニティ」への降格案。降格するなら [[現在地]] 穴14・[[論点マップ]] §23・[[centered_bias凍結結果_0901]] の 3 か所を同時修正
2. **[[命題リスト]] への行立て**: 本ノート C1–C5 を Q として立てるか、既存 Q14/Q26 に系として付すか
