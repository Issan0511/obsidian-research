# 中心主張 V10 — 主張の穴と閉じる作業

親: [[中心主張v10草案_0916]] / 索引: [[論点マップ]] §45 / 証拠台帳: [[命題リスト]]
状態: **検証中（A1修正済み。H1の個体配分を計測済み、A2の実変位接続と他の作業は未完）** / 作成・更新: 2026-09-16 / 起草: Codex
依頼: Issa「今の主張の穴、作業リストを洗い出してください」。
関連する希望: [[0916 V10でやりたいこと(全くやってない)]]

> **2026-09-16・A1完了（Issa依頼・Codex）**: [[駆動源問題_0909]] §11へ非斉次・中心化・層間の補正、Adamの実更新式、μの輸送式を反映。H1の独立検証とA2以降は未完。追加学習・再集計・実験起票は行っていない。

> **同日・個体別の十分条件を導出（学習走なし）**: [[駆動源問題_0909]] §12。ラベル切替の局所感度と旧残差の上界から抑制要求を保証し、通常SGD・Adamの平均前活性下降の条件まで接続した。切替前の旧確信度だけでは足りず、局所寄与・入力内積・Adam履歴を含める。条件の成立範囲と時間的な持続は未測で、A2の実データ検証は未完。

> **同日・H1/A2の個体配分を計測（Issa依頼、実行・検算済み）**: [[個体へのラベル切替要求の配分_結果_0916]]。RLの既知軌道の10タスクprefixを再現し、両隠れ層で下流Jacobian・旧残差・P/B/V別要求を測定。切替直後の各seed内集計ではGELU/SiLUの全群で旧支持・抑制が総量で優勢。個体・画像への要求は一律でなく、時間途中で反転する群もある。A2のうち配分と条件被覆の記述が進み、Adam実変位の時間収支への接続は未完。repo結果 `301f3d5`。独立の未使用軌道による確認とは区別する。

> **同日夜・H3（A3）を µ₂ 壁の座標で検証（Claude・lop_analysis `l2_wall_0916`）**: [[第2層のµ₂壁_崩壊層の釘付けと切替衝撃_事後_0916]]・[[第2層のµ₂壁にεとLayerNormを当てる_結果_0916]]。第2層の沈みは t5 までは W₂ 自身と共変が担い、その後は上流（‖µ₂‖ の伸び）が 7 割以上を運ぶ。崩壊は切替ごとの一時吸収から ELU だけが回復できなくなる形。affine なしの入力 LN は伸びを止めて崩壊を防いだ（登録判定 `RESCUED`）。第2層の cap1/cap2 は未着手。

## 0. 結論

**V8からの骨格は使える。最大の穴は、W増大 → 負側への輸送 → 応答低下 → 再学習の低下を、同じ条件でつなぐ部分にある。** 各矢印の証拠は既にあるが、主な証拠が異なる入力・活性化・層・更新則に分かれている。

特に、最新の「マージン衝撃の整流」は駆動源の有力候補になっている。ただし、**重み付きの和の恒等式、実際の更新の符号、W増大が輸送を強める因果効果は、それぞれ別の段階**である。

ここでの RL-MNIST は **Random Label MNIST（乱数ラベル）**。強化学習の実験ではない。強化学習への接続は§5の発展課題に置く。

### 監査の前提

- 正側の侵食はW増大を必要としない。**ReLUを全部救えないことを、W増大による深い輸送の反証にはしない。**
- μの内積は幾何の土台として採用済み。μの存在だけから負の更新を導かない。
- 適応αSnakeのスケール対照としての説明は採用済み。残るのは式・実装・適用条件の明記。
- 以下は既存ノートと一部実装を照合した監査であり、新しい実験判定ではない。登録値の再集計、追加学習、実験の起票は行っていない。

## 1. 優先順位付きの穴

P0 = 中心の因果説明を立てるために必要。P1 = 条件・比較・提示を固める作業。

| 穴                     | 優先     | 既にある足場                               | まだ飛んでいる接続                                 | 閉じる作業                                          |
| --------------------- | ------ | ------------------------------------ | ----------------------------------------- | ---------------------------------------------- |
| **H1 駆動源の式と実更新**      | **P0** | マージンの恒等式、正側・帯・谷の奥へのAdam実変位の分解        | 式は修正済み。和の制約からユニットの沈降は出ず、実更新への予測は未検証 | A1完了。補正込みの式と群・時間帯別の実変位を照合する（A2）           |
| **H2 W増大が担う輸送**       | **P0** | 幅の一回介入後に深さが追随。condAの幅固定で後期沈降停止の登録判定  | ノルム成長・μへの射影・分布幅の変化を同一視できない。飽和型での因果接続が薄い   | 成分・層・入力を特定し、既存の侵食のもとで成長抑制が深さをどう変えるか検査する（A2・A4） |
| **H3 多層で誰が運ぶか**       | **P0** | 層別キメラ、第1層の実変位分解、入力置換の比較              | 第2層自身のW更新と、上流変化によるμ・入力分布の変化が混ざる           | 同じ軌道で自己更新・入力変化・biasを帳簿化する（A3）                  |
| **H4 飽和が学習能力を失わせる接続** | **P0** | ELUの初期出力を揃えた応答回復で再学習が改善              | その自然な崩壊の原因がW増大だったこと、どの応答消失が効いたかは未完        | H2・H3と同じ設定で輸送・応答・再学習を測り、下流の応答介入と接続する（A4・A5）    |
| **H5 成り立つ条件と分岐**      | **P1** | PM/RL差、GELU/SiLUの時間帯・seed差、V9の幅対深さ介入 | 活性化の名前だけでは運命が決まらない。どの状態でこの経路が効くかが未整理      | 入力・層・更新予算・応答が残る範囲で条件表を作る（A6）                   |
| **H6 スケール対照の射程**      | **P1** | 適応αSnakeの関数式・実装、leakyの斉次性            | 理想的な倍率変更と、EMA・clip付きの実軌道を同一視できない          | スケール共変性を式で示し、実装の追随・clip・位相を確認する（A7）            |

## 2. P0の具体的な中身

### H1. マージンの恒等式から、何がまだ出ていないか

正本は [[駆動源問題_0909]] §11、最新の実変位は [[第1層の押し込みを正側・帯・谷の奥に割る_結果_0915]]。

**A1で修正済み（2026-09-16）**: 導出の正本は [[駆動源問題_0909]] §11.1。対象の一層、一標本、通常SGD、正則化なしで、$u_j=\partial\ell/\partial a_j$、$q_j=u_j\phi'(z_j)$ とすると、

$$
\begin{aligned}
S_l&=\sum_ja_ju_j,\quad
H_l=\sum_ju_j\{z_j\phi'(z_j)-\phi(z_j)\},\quad
B_l=\sum_jb_jq_j,\\
\langle W,\Delta W\rangle_F&=-\eta(S_l+H_l-B_l),\\
\langle\widetilde W,\Delta\widetilde W\rangle_F
&=-\eta(S_l+H_l-B_l-C_l),\quad
C_l=(\mathbf1^\top x)\sum_jm_jq_j.
\end{aligned}
$$

ここで $m_j$ は入力座標に関する行平均、$\widetilde W$ はそれを各行から除いた重み。バッチ平均損失では、同時点の一標本量を平均する。

- 非斉次補正 $h(z)=z\phi'(z)-\phi(z)$ をELU・GELU・SiLU・通常Snakeまで具体化した。正側というだけでは省略しない。
- 層間は $S_{l-1}=S_l+H_l-B_l$。出力CEの和を下層へ移す際にも補正が要る。condAのMSEは実装の $1/2$ なし規約に合わせ、$S_K=2\delta(\hat y-c)$ とした。
- 成長には $2\langle W,\Delta W\rangle_F$ に加え $\|\Delta W\|_F^2$ がある。タスク正味増分の整列項と、更新ごとの整列項の和も分けた。
- ~~Adamでは符号まで対応~~という一般保証を撤回。正本§11.1cに実更新 $\Delta\theta=-\eta D\widehat M$ と、初回からSGDと符号が逆になる反例を記載。更新則は [PyTorchの一次資料](https://docs.pytorch.org/docs/2.14/generated/torch.optim.Adam.html) と照合した。
- μへの輸送は $\mu^\top\Delta w_i+\Delta b_i$ であり、ノルムへの整列とは別。多層の有限差分は下のH3へ接続した。
- CEの恒等式が決めるのは全ユニットの重み付きの和。個々の $u_j$ やP/B/V群への配分、Adamを通した変位の符号を直接導くという記述を修正した。

**検算**: 6活性化の二層の合成例をfloat64自動微分と照合し、層間・通常SGD・中心化の式を確認。μの四項、タスク収支、Adamの符号反例と共通分母での加法閉包も確認した。これは式の実装検算であり、学習軌道の新たな実験証拠ではない。

**A2に向けた十分条件（導出済み・RL切替時の配分と被覆を計測、実変位への接続は未）**: [[駆動源問題_0909]] §12。旧ラベル$o$・新ラベル$n$と下流Jacobian $J_{ci}=\partial r_c/\partial a_i$ を使い、
$\operatorname{sign}(a_i)(J_{oi}-J_{ni})>(1-p_o)\max_{c\ne o}|J_{ci}-J_{oi}|$
なら個体の活性を局所的に縮める要求が保証される。通常SGDのバッチではラベル切替による押し $T_i$ が残差上界 $R_i$ を超えると下降、Adamでは実分母と履歴を含む $Q_i^D>0$ が十分条件。記号と証明は正本参照。条件を満たすときの一歩の符号一致は定理の帰結であり、研究上の検証対象は**条件の成立範囲・寄与の大きさ・その後の回復との収支**に置く。

**既存解析を使う場所**: 正側・帯・谷の奥の実変位は、実際のAdam分母を共通に使った加法分解として既に閉じている。これを再発明する必要はない。ただし、その群を除いた学習の反実仮想ではなく、分母の変化を介した効果までは分離していない。

**重要な例外**: 最新結果の切替直後と残りの時間分解は事後解析。全活性化・全seedで同じ符号ではない。登録した時間平均ではGELUの谷の奥は押し上げ側だった。「谷がいつも沈める」に戻さない。

**閉じたと数える条件**: 補正込みの式と計測量が対応し、マージン候補が予測する群・時間帯・符号について、未使用データまたは独立した確認走で判別できる。全部の速度・全活性化の指数を先に導く必要はない。

### H2. 大きくなったWの、何が負側への輸送を増やすのか

**2026-09-16・設計改訂**: [[H2_W成長抑制と負側輸送_設計案_0916]] §10が現行案（実装・実行なし）。段階0の訂正70e9ed8・再訂正008c515を反映し、RL・ELU→leakyの第1層でμ平行/直交成分を2×2に抑制する案へ優先順位を変更。ρの適用範囲、m/W̃/biasの収支、尺度と正側の裾、下流停止を分ける。旧ELU→ELUの層別案は第2層へ戻る別段階として保持。本走の採否と事前登録は未確定。

μを固定すれば、平均位置の変化は $\Delta\bar z_i=(\Delta w_i)^\top\mu+\Delta b_i$。これはノルム成長の恒等式とは別である。

**問うべきこと**は「侵食がどう生まれるか」に加えて、**その侵食があるとき、重みの尺度を大きくすると応答を失う深さまで運ばれやすくなるか**。W成長を止めても浅い位置でReLUが消灯することは、この問いと両立する。

既存の強い足場を本文へ戻す:

- [[幅と沈下の介入_結果_0909]]: leaky/Snakeで中心化Wの幅を一回変更し、その後の深さが追随した。登録判定は倍率で割れるため、完全な再平衡やユニットごとの普遍則とはしない。
- [[幅を止めた着座点をSGDとfull-batchで比べる_結果_0915]]: condA・leakyでは登録集約で後期沈降の停止。ただし集約順の感度が残る。
- [[前活性分散の厳密分解_事後_0910]]: 置換平均の分散分解は既にある。固定画像や学習中の隠れ表現へ、その係数をそのまま移せない。

**閉じたと数える条件**: 少なくとも一つの飽和型の条件で、指定したW成分への介入が実際に成長を抑え、それに応じて負側輸送・低応答化が変わる。初期性能の直接変化、bias・行平均・他層への規模の移り方も測る。

### H3. μを入れたことで、多層の帳簿が書ける

同じ評価分布で旧状態と新状態を比較すると、

$$
\Delta\bar z_i
=(\Delta w_i)^\top\mu
+w_i^\top\Delta\mu
+(\Delta w_i)^\top\Delta\mu
+\Delta b_i
$$

が厳密に成り立つ（右辺の $w_i,\mu$ は旧状態）。自身の更新、上流・入力の変化、両者の交差、biasを分けられる。交差項をどちらへ含めるかを変える場合は規約を記す。

- 第1層の固定入力の分解は既にある。優先する不足は**第2層への拡張**。
- 平均μだけでは入力ごとの広がりは決まらない。分布幅と、どの標本で応答が残るかを併記する。
- タスク切替による入力変化と、タスク内で上流層が学習した変化を別に記録する。
- [[蹴りのダイヤル_RLラベルのまま画素を部分置換_結果_0915]] はμだけの介入ではない。入力全体の構造も変わる。
- 帳簿が閉じることと、その項が原因として必要なことは別。優勢な経路が分かってから対象層を絞る。

**閉じたと数える条件**: 崩壊に向かう軌道で第2層の変位を再構成でき、「自分のWが育って沈んだ」と「上流に運ばれた」を区別できる。全深さのネットワークへ広げる必要はまだない。

### H4. 深さ・応答・学習能力のどこまでつながったか

[[初期出力を揃えてELUの応答を戻す_結果_0913]] は、初期特徴・予測・W・Adam履歴を揃えた応答変更によって再学習が改善したという重要な足場。ただし固定補正を持つ介入であり、自然軌道の媒介割合を測ったものではない。

残る作業は二つ:

1. **同じ設定で上流へ戻る**。その低応答状態へW成長が運んだかをH2・H3で示す。condA・leakyの上流実験とRL・ELUの救済だけをつないで証明済みにしない。
2. **機能的な応答を定義する**。ReLUの支持上の厳密ゼロ、滑らかな活性化の微小な微分、ネットワークの性能の床を分ける。GELU/SiLUでは微分の符号と大きさを別々に読む。平均前活性だけで死亡判定しない。

Adamのεを原因として本文に入れるなら、微分が小さいという観察に加え、勾配・一次/二次モーメント・実更新を対応づける。**$\phi'$ とεを直接比較しただけでは、停止機構の検証にならない。** εの介入まで行うかは、この帰属を中心文に必要とするかで決める。

**閉じたと数える条件**: 有限の更新予算・固定した対象入力のもとで、輸送に伴う低応答化と再学習の低下が対応し、応答への介入が低下を変える。全LoPの説明や、自然な媒介割合の完全同定は要求しない。

## 3. P1で固める範囲

### H5. 「どこで効くか」を条件表にする

- [[長い地平線で沈下と幅を分ける_結果_0910]] のPM・Adamでの深さ固定の弱い効果を残す。深く沈めば常に性能が落ちるとは書けない。
- [[層別キメラでELU崩壊の所在を決める_結果_0914]] は二層MLPで第2層の活性化選択が崩壊を分ける証拠。前向きと逆向きが同時に変わるため、微分の床だけの単独効果とはしない。
- [[GELUとSiLU_s2は150タスクで死ぬか_結果_0915]] と最新の第1層分解では、層の深さ・谷越え・ネットワークの生存が単純には並ばない。
- [[SGD橋_箱Bのoptimizerだけを替える_結果_0914]] は既にある。ただし更新回数も変わるため、「optimizerだけの効果」の一般証明には使わない。
- 精度の初期水準、freshとの差、時間劣化、床到達を別々に提示する。集約順で判定が変わる箇所は、主集約と感度を併記する。

### H6. 適応αSnakeは短い式と実装条件で説明する

実装の通常Snakeは $\phi_\alpha(z)=z+\sin^2(\alpha z)/\alpha$、$\phi'_\alpha(z)=1+\sin(2\alpha z)$。正の倍率 $s$ に対して、

$$
\phi_{\alpha/s}(sz)=s\phi_\alpha(z),\qquad
\phi'_{\alpha/s}(sz)=\phi'_\alpha(z)
$$

となる。**前活性の倍率にαが逆比例して動けば、傾きの形が保たれる**というIssaの説明をそのまま式にできる。

現行実装は前活性分散のEMAからαを決め、上下限でclipする。コードの幅変数 `W_i` は前活性の幅で、重みベクトルそのものではない。→ 実験repo `src/pmnist_boundary_host_0908.py:97–135`（`AdaptiveSnake`）、位相拡張は `src/snake_phase_acts_0914.py`。

残作業は、本文で使う腕について、EMAの追随とclipが上の理想条件をどこまで満たすか確認すること。**幅を保ったままの平行移動は、この倍率不変性とは別**なので位相も見る。全入力で正の微分の下限があることや、LoPが全部消えることをこの式からは主張しない。新しい大規模な保護機構探索は不要。

## 4. 実行順の作業リスト

**A1は2026-09-16に完了。A2以降は作業候補。** A4・A5の追加実験は、先行する解析で不足を絞ってから設計する。

| 作業                  | 種類・順序           | やること                                               | 成果物・完了条件                            |
| ------------------- | --------------- | -------------------------------------------------- | ----------------------------------- |
| **A1 式を直して主張を限定** | **完了・09/16** | 非斉次・中心化・層間補正、Adamの実更新、μへの輸送、1更新とタスク収支を修正 | [[駆動源問題_0909]] §11、V10本文§1.1–1.2へ反映。独立検証はA2に残す |
| **A2 既存の駆動源データを接続** | A1後・既存解析        | 第1層の群別実変位とマージン候補を照合。切替直後・残り・タスク正味を揃え、成長/侵食の収支と並べる  | 何の符号を説明でき、何が残るかの表。足りない列だけ短い再生候補へ    |
| **A3 第2層のμ帳簿**      | A2と並行可能・既存データ優先 | 同じ崩壊軌道のW・μ・bias・入力幅を比較。checkpoint不足なら対象タスクだけの再生を設計 | 自己更新/上流移動の閉包と優勢な経路。時間順序だけで因果としない    |
| **A4 Wを縛った後を確認**    | A2・A3後・追加実験候補   | 同じ飽和型の設定で成長抑制を入れ、成長・負側輸送・低応答化を追う。対象層・開始点・減衰方式を固定   | 操作が効いたかと、輸送が止まる/続く条件を判別。性能だけの合否にしない |
| **A5 同じ条件で下流へ接続**   | A4と共通設計・追加実験候補  | 応答回復の既存介入を接続し、再学習を比較。対象課題・更新予算・初期出力差を明示            | 「W介入が救うが応答経路は不明」と「応答経路を支持」を区別できる比較  |
| **A6 条件表と確認対象を固定**  | 今から着手可能・整理      | H5の環境・層・活性化・時間帯・集約を一表へ。本文に使う事後発見だけ確認計画へ            | 適用範囲と反証になる条件を各中心命題に一行ずつ記す           |
| **A7 スケール対照を完成**    | 小・式/実装/保存ログ     | H6の式、α則、EMA・clip・位相を本文の腕に対応づける                     | 短い説明と実装条件。適応αSwishの着想への接続も書ける       |
| **A8 論文用に証拠を整える**   | 提示前・文献/記録       | V10の新規性を既存文献と再照合。結果commit・主集約・事後区分を確認しQ27–Q32を更新   | 主張ごとに出所と証拠の格を辿れる図表・台帳               |

### A4の「Wを縛る」は未着手からの出発ではない

[[Wの蓄積を止めれば時間劣化は消えるか_結果_0914]] と [[Lの分解で救命の出どころを分ける_結果_0914]] は既にある。特にleakyでは中心化Wのsoftな減衰だけで時間劣化が大きく減る一方、ReLUは同じ半分だけでは救われない。

**次に必要なのは、これを飽和型での輸送の問いへ接続すること。** 結合型L2、AdamWの減衰、硬いノルム上限を同じ操作と呼ばない。重みノルムの頭打ちが確認できても、方向・bias・上流入力が変われば前活性は動く。頭打ち後の観察ではそこも追う。

### 結果が出たときの主張の分け方

- 成長抑制 → 負側輸送減少 → 応答維持 → 再学習改善: 指定条件でV10の経路を支持する。
- 成長抑制 → 輸送減少、再学習は改善しない: 輸送の上流性は残り、学習能力への接続をその条件では限定する。
- 成長抑制 → 再学習改善、輸送・応答は変わらない: 幅による別経路を支持し、V9側へ置く。
- 操作した成長は止まったが深い輸送が続く: μ・向き・bias・他層への経路を確認し、**指定した成長が輸送に必要という主張**を再検討する。
- 操作自体が学習を壊す: 経路の反証と即断せず、介入として判別不能とする。

## 5. Issaの「やりたいこと」との対応

| 希望                | V10での位置         | 次の一歩                                |
| ----------------- | --------------- | ----------------------------------- |
| **多層での検証**        | **主張の穴を閉じる作業**  | A3。二層の第2層から始める。層数を増やす前に自己更新と上流を分ける  |
| **WDでWが収束した後を見る** | **主張の穴を閉じる作業**  | A4。既存R/LRの結果を再利用し、飽和型で残る輸送を問う       |
| **kune-kuneを作る**  | 活性化設計への発展       | 保ちたい応答・滑らかさ・位相挙動を仕様化し、まず同じ設定で比較する   |
| **適応αSwish**      | H6から自然につながる比較候補 | αの規則と倍率変更時の応答を式で確認。初期関数・適応則を揃える     |
| **強化学習などの実用環境**   | 外的妥当性と応用        | A4・A5の測定が動いた後、ELUを使う一つの実用設定で同じ経路を測る |

**最短の順番は、式の接続 → 既存の第1層データ → 第2層のμ帳簿 → 飽和型でWを縛った継続 → 応答介入との接続。** 活性化の新設や実用環境への展開を全部終えなくても、限定したV10の機構主張は立てられる。

## 6. 証拠・記述の維持作業

- V10本文の証拠表に、既存の幅の一回介入と最新の第1層分解を加える候補とする。登録判定と事後の時間分解を混ぜない。
- [[命題リスト]] のQ27–Q32は9/9時点の記述が多い。たとえば「SGD対照が無い」「push未確認」を9/16の現状とそのまま読まない。証拠を再照合して更新する。
- [[文献照合_W増大メカニズム_0909]] は既存の照合の入口。ノルム成長の恒等式やμの内積を新規性とせず、**継続学習でどの条件の輸送と故障を説明・介入できたか**を比較の単位にする。先行研究との差の確定は未了。
- ReLUの非救済、PMでの深さ固定の弱い効果、GELU/SiLUの生存例を落とさない。それぞれ、中心文に課す条件を決める証拠になる。


## 7. 一般化の候補への接続（同日・Issa発案）

正本: [[固定尺度のReLU型活性化の飽和不可避性_仮説_0916]]。固定尺度のReLU型活性化のクラス全体について、飽和が避けられなくなる学習条件を問う。関数の極限は条件付きで導出できる一方、学習軌道の輸送と機能的LoPは未証明。

- **A1–A4**: 侵食と回復の時間非対称を足場に、成長・輸送が続く条件と止まる条件を明確にする。既存の幅介入の因果証拠を継承する。
- **A2–A4・A6**: 平均の深さに加え、応答を担う正側の裾の消失を調べる。W発散と平均沈降だけでは全入力の飽和は出ない。
- **A5・A7・A8**: 有限予算の機能低下、leaky・固定/適応Snakeの対照、クラスとしての新規性を接続する。

本件を全条件での普遍的な死亡の証明として採用したわけではない。無限時間版と有限時間版の目標を分け、追加実験の採否は未決。
