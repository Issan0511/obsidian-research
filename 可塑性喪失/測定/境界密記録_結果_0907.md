# 境界密記録の結果 — 沈下はバイアスではなく「タスク向きの重みの整列」が運んでいる

親: [[境界密記録_spec_0907]]（実行済みへ移動）／[[vとwの成長_0907]] §8（候補機構の棄却） / 状態: **決着（2026-09-07 14:04 完走・lab・登録判定 `results/boundary_dense_0907/verdict.json`）** / 作成 2026-09-07 / 発端: Issa「上端に下向きの力がかかって見えるのはなぜ？」
関連: [[命題1-5_上端則結果_0905]]（上端則）／[[引用禁止]]

> **run id: `boundary_dense_0907`**。spec+config `c6c13a7`（実装前）→ 実装・検査 11 本・前検査 `5227f60` → 本走 13:46–14:04（5 腕・10 seed・2M・並列 5）。
> **S-null: 記録格子を変えても軌道は 1 bit も動かない**——`BDref_1216` は `offset_grid_0906` の `LRoff0_1216` と `state_hash_final` が 10 seed すべて一致、共有 step の全列バイト一致（変異体の対は 393 列で不一致）。
> **`layer1_dzbar` は境界密格子では定義されない**ので読んでいない（spec §8-2）。判定は z̄ と b の 2 点差だけ。

---

## 0. 一行

**1 タスクの沈下 −0.0116 のうち、バイアス b が運ぶのは 17% だけ**（`GEOMETRY_CARRIED`・f_b = 0.173 [0.163, 0.183]）。**主役は A = タスク内での w_flip の整列（−0.0174/タスク）** で、反転はそれを **+0.0078 しか戻さない**。しかも **b の変化は反転後 1 step で全部済む**（`TRANSIENT`・τ½ = 1）。**「ユニットはバイアスを下げて沈む」という素朴な描像は棄却。** 沈下は**タスクに合わせて重みを整列させた残りかす**である。

## 1. 登録判定

| 判定 | ラベル | 数値 |
|---|---|---|
| 主 `SINK_CHANNEL` = E[B]/E[N] | **`GEOMETRY_CARRIED`** | f_b = **0.173** [0.163, 0.183]・E[N] = −0.01163 [−0.01268, −0.01060]（床 0.005 を通過） |
| 第 2 `SINK_TIMING` = τ½ | **`TRANSIENT`** | **τ½ = 1 step**・E[B(10000)] = −0.00288 [−0.00356, −0.00226] |

## 2. 1 タスクあたりの収支（seed ブートストラップ 2000・恒等式残差は全腕ちょうど 0）

| 腕 | a | c | **J（幾何の跳び）** | **B（バイアス）** | **A（w_flip の整列）** | **N（正味）** | 跨ぎ率 | zmax(t0) |
|---|---|---|---|---|---|---|---|---|
| `BDref_1216` | 0.1 | 0 | **+0.00778** | −0.00201 | **−0.01740** | **−0.01163** | 0.30 | −0.127 |
| `BDa0p5_1216` | 0.5 | 0 | +0.00077 | −0.00015 | −0.00094 | −0.00031 | 0.98 | +0.883 |
| `BDoffm0p5_1216` | 0.1 | −0.5 | +0.00438 | −0.00115 | −0.00908 | −0.00585 | 0.48 | +0.490 |
| `BDvf1_1216` | 0.1 | 0（v 凍結） | +0.00337 | −0.00085 | −0.00764 | −0.00512 | 0.55 | +2.715 |
| `BDFB_1216` | 0.1 | 0（full-batch） | +0.00472 | −0.00182 | −0.01480 | −0.01190 | 0.37 | −0.146 |

定義（すべて 2 点の差・t0 は反転**前**の行）:
J = [z̄−b](t0+1) − [z̄−b](t0)（**x が変わる・w は変わらない**）／B = b(t0+10⁴) − b(t0)／A = [z̄−b](t0+10⁴) − [z̄−b](t0+1)（**w が変わる・x は変わらない**）／N = J+B+A。

## 3. 読み（事後）

### 3-1. 沈下は「学習して下げ、反転で半分だけ戻る」

タスク中、ユニットは**いま来ているパターンに対する前活性を下げる**ように w_flip を動かす（A = −0.0174）。パターンが入れ替わると z̄ は**上へ戻る**（J = +0.0078）が、**戻りは 45% だけ**。差し引き −0.0116 が毎タスク積もる。これが沈下の正体で、**バイアスはほとんど関与していない**（−0.0020 = 17%）。

独立の裏づけ: 5M 時点で b の中央値は |b| ≈ 0.61 なのに z̄ = −3.73。**前活性の沈下の 84% は w_flip·x_flip の側にあり、バイアスの側ではない**——f_b = 0.17 と整合する。

### 3-2. バイアスは反転直後の 1 step で動き切る

b の累積プロファイル（τ = 1 → 10,000）は **−0.00279 → −0.00285 でほぼ平ら**（τ=1 の時点で総量の 98%）。残り 9,999 step は行って戻るだけ。**b は「反転直後の巨大な残差に 1 回蹴られる」以上のことをしていない。**

### 3-3. 雑音は要らない・v の適応も要らない

full-batch（雑音ゼロ）の収支は online とほぼ同じ（N = −0.0119 対 −0.0116・A = −0.0148 対 −0.0174）。**沈下は一次の力**という既存の読みを、収支の内訳のレベルで再確認した。v 凍結でも N = −0.0051 と沈む（ただしこの腕は二峰なので平均の意味が違う・§4-4）。

### 3-4. a=0.5 では収支そのものが消える

N = −0.00031（a=0.1 の 1/37）。J も A も 1/10〜1/18。跨ぎ率は 0.98 と高いのに沈まない。**跨いでいることは沈下の必要条件であって十分条件ではない。**

### 3-5. c<0 は収支を半分にする

`BDoffm0p5` は A・J・N がすべて c=0 のおよそ半分（−0.0091 / +0.0044 / −0.0059）。[[vとwの成長_0907]] §7-2 の「c<0 の坂は位置でなく人数を決める」に対し、**1 タスクあたりの収支そのものも半減している**。坂は毎タスクの整列を弱める向きに効いている。

## 4. 引用上の注意

1. J と A の分け方は**「x が変わったか w が変わったか」**で定義してある。A を「幾何」と呼ばない（A は学習）。J を「力」と呼ばない（J は同じ w での期待値の入れ替え）。
2. **`layer1_dzbar` はこの走では定義されない**（spec §8-2）。読まない。
3. 2M で止めた走なので 5M 走と水準比較しない。
4. `BDvf1_1216` は上下二峰（±12）に割れた世界で、平均の収支は 2 群の混合。単独で読まない。
5. 「反転が z̄ を上げる（J>0）」は**力ではない**。整列していたパターンが無くなって期待値へ戻るだけ。
6. w_flip をベクトルとして記録していないので、**「Δw_flip がいまのパターンに沿って下がる」以上の分解はできていない**（§6-1）。

## 5. 予測の採点

- Claude: 主判定 `BIAS_CARRIED` 45% / `MIXED` 35% / **`GEOMETRY_CARRIED` 20%** → **20% の枝が当たり＝外れ**。理由の誤り: 「ゲート (1−a) が乗るのは ∂L/∂b だから b が主役」と読んだが、**同じゲートは ∂L/∂w にも乗る**。w は 15 次元ぶん受け取るので、b の 1 次元より桁で大きく動く（fan-in の非対称——[[vとwの成長_0907]] §1 で ‖w‖ が育つ理由として書いたのと同じ構造を、ここで自分で見落とした）。
- Claude: 第 2 判定 `TRANSIENT` 50% → **当たり**（τ½ = 1 と予想より極端）。
- Claude: S-null 一致 95% → **当たり**。
- Issa: 未記入。

## 6. 次

1. **w_flip をベクトルで記録する**（ユニットあたり 15 個・タスク終端だけで十分）。そうすれば Δw_flip を「いまのパターン方向」と「直交方向」に分けられ、**なぜ戻りが 45% しかないのか**（＝ 何が across-task に残るのか）が測れる。これが沈下の機構の核心。
2. **a=0.5 で収支が消える理由**。跨ぎ率は高いのに A が 1/18。ゲート E_支持[φ′] の差（0.12 → 0.63）で説明が付くかを、A/E_支持[φ′] が腕をまたいで一定かで検定する。
3. 上端則との接続: A と J の差し引きが「上端 zmax を一定に保つ」ようにフィードバックしているかを、zmax についても同じ J/B/A 分解で見る（記録はすでにある）。

## Log

- 2026-09-07 13:46 投入 → 14:04 全腕 COMPLETE。解析 `src/boundary_dense_analyze_0907.py` は 15:0x に実装・実行。生ログはギガファイル（未アップロード）。
