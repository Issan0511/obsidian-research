# 可識別性 2×2（純化版）結果 — 縮小走

状態: **決着（縮小走）** / 作成: 2026-09-01 / 親: [[可識別性2x2_spec_0901]]・[[可識別性2x2_spec_0901_追補1]]
run id: `ident_mu_2x2_0901` / repo: 事前登録 `0d7a746`（spec 単独）→ 実装 `0333eb4` → S-op FAIL 記録＋追補1 `ced2b85` → 結果 `ac2727c`（すべて origin/main 済み・生ログ npz 40 本込み）

---

## 1. 一行

**b を縛ると、µ=0 端の全滅は完全に消え、µ が立っているセルだけが死ぬ。** 登録副判定 R-ext は `BWD_PREVENTS_EXTINCTION`（`im_nowd` 10/10 全滅 対 `im` 0/10）。ただし **S-op が FAIL したため I+ セルは無効**で、要因計画の主判定は出していない（[[可識別性2x2_spec_0901_追補1]]）。

## 2. 走った腕と終端

10 seed・5M step・除外 seed 0・S3 は 4 腕とも PASS・S-floor PASS。1 腕 7.4–7.7 分。

| 腕            | µ                | b-WD   | E-level B10 | E-drift | 終端 `strict_dead` | 全ユニット中央値 $b$ (B10) | E-onset 中央値 |
| ------------ | ---------------- | ------ | ----------- | ------- | ---------------- | ------------------ | ----------- |
| `im`         | 0                | λ=1e-3 | **−5.688**  | +0.038  | **0.0002**       | −0.078             | 打ち切り（10/10） |
| `iM`         | flip$_0$（静的）     | λ=1e-3 | 0.000       | +0.318  | **1.000**        | **−0.000**         | **22**      |
| `im_nowd`    | 0                | 0      | 0.000       | +3.744  | 1.000            | −0.969             | 99.5        |
| `std_anchor` | flip$_t$（引き直しあり） | 0      | −0.315      | +1.323  | 0.980            | −0.948             | 64          |

## 3. 登録副判定 R-ext = `BWD_PREVENTS_EXTINCTION`（予測 3 的中）

|                       | 全滅到達      | Clopper–Pearson 95% | 到達 task |
| --------------------- | --------- | ------------------- | ------- |
| `im_nowd`（λ=0）        | **10/10** | [0.692, 1.000]      | 112–249 |
| `im`（λ=1e-3）          | **0/10**  | [0.000, 0.308]      | —       |
| 参照 committed `Aexact` | 10/10     | —                   | 154–454 |

`strict_dead_frac = 1.0` と `alive = 0` の食い違いは **0 件**。λ=0 側で committed `Aexact` の全滅を新系列で再現したうえで、**λ=1e-3 が完全に止めた**。併記（REPORT）: `im − im_nowd` の E-level 差は **−5.688 dex**、これが「v1 設計で現象2 が運んでいた量」の見積もりになる。

**[[現象2主張v1_0901]] §8.1 の賭けは当たった側。** µ→0 端の全滅（`ORACLE_INCREASES_DEATH`・非単調用量反応の右端）は µ の話ではなく b の話だった。[[現在地]] 穴12 の非単調性は、少なくとも b 拘束下では整理される。

## 4. 登録済み M 主効果 (i) `iM − im`（**天井汚染あり**）

| endpoint | 点推定     | 95% CI             | 帯 ±0.15 dex |
| -------- | ------- | ------------------ | ----------- |
| E-drift  | +0.2806 | [+0.2139, +0.3312] | **OUT_POS** |
| E-level  | +5.6879 | [+5.6619, +5.7158] | **OUT_POS** |

向きは §8 予測 2（「µ が強い」型）と一致する。ただし **S-ceiling が `CEILING_CONTAMINATED`**（B02 の 2 セル差 **5.407 dex** > 3）。`iM` は **task 22** で死んでおり早期窓 B02 の時点で既に天井なので、**E-drift は単独で読まない**（登録どおり）。差を運んでいるのは E-level 側である。

**要因計画の主判定は出していない。** 対比 (ii) `IM − Im` と I 主効果 2 本・交互作用はセルが無効で計算していないので、`MU_DOMINANT` のラベルは付かない（予測 1 は検証されない）。

## 5. ★ 台帳が死因を分けた

終端の全ユニット中央値 $b$ が、同じ「全滅」を 2 種類に割る。

- `im_nowd` −0.969 / `std_anchor` −0.948 …… **b の自走**（現象2）で死んでいる
- `iM` **−0.000** …… b は WD で実質ゼロに抑えられている。**それでも 1.000 全滅した**

つまり **b を縛った地面の上で残る殺し屋は µ だけ**である。`iM` の alive 条件付き $B$・$M$ は B10 で `nan`（生存ユニットが 0）で、B02 では $B=+0.023$・$M=-0.059$ と b 側は静かなまま。

**言えないこと**: `iM` の λ=0 対照は走っていないので、「b-WD が `iM` を救えなかった」とは言えない。言えるのは「**b が抑えられていても µ があれば死ぬ**」まで。C4「復元力はタダ」にスコープ条件が付くかどうかは、M+ 側の λ=0 腕を足さないと決まらない。

## 6. S-op の帰結（[[可識別性2x2_spec_0901_追補1]] §1・§4）

改2 の切替プローブで **`Im` が対照より境界直後 loss を +0.11254 [+0.09046, +0.13520] 悪化**（10/10 seed 正）。同定は完了済み（事前反転 平均 8.486 回・初見 bit 0%）なので「$u$ が同定されていない」では説明できない。**A2 のバイパスは境界で反保護的**であり、本体 §3.3 の「1 自由度しか運べない」は弱すぎた —— その 1 自由度が境界で誤った向きに動く。§8 予測 4「通る」は**外れ**。

## 7. 引いてはいけない線

1. **`I_CELLS_INVALID_S_OP` を「I ダイヤルに効果が無かった」と読まない。** 効果を測っていない。[[論点マップ]] §20 の可識別性交絡は本走では割れていない
2. **E-drift を単独で引かない**（`CEILING_CONTAMINATED`・§4）
3. **`BWD_PREVENTS_EXTINCTION` を `LOP_CURED` と読み替えない。** R-ext は死の主張である。ただし本走では `im` の機能側も E-level −5.688 と健全なので、**この腕に限っては死と機能が同方向**である（過去 2 件の解離とは違う）ことを併記してよい
4. **`iM` の死を「b-WD が効かない」と読まない**（§5 の「言えないこと」）
5. すべて **b 拘束下（λ=1e-3）・condA・幅100・hidden [100]・$T=10^4$・batch=1・lr=0.01・5M** 限定。λ は移送値で、結果を見て選び直していない
6. **`im` の E-onset は 10/10 打ち切り**（一度も閾値を上抜けしない）。「到達が遅い」ではなく「到達していない」

## 8. 閉じた／閉じない

**閉じた**

- **R-ext**: b-WD は `Aexact` 型全滅を止める（[[現象2主張v1_0901]] §5 穴5 系の新材料）
- **µ→0 端の全滅の帰属**: 丸ごと b（§3・§5）
- **A2 バイパスの評価**: 境界で反保護的（§6）。D2 の再裁定材料

**閉じない**

- **I 主効果**（S-op FAIL でセルごと無効）。§20 の交絡は未解決のまま
- **M+ 側の b-WD の効き**（λ=0 対照が無い・§5）
- 「A2 が境界で悪いのか常時悪いのか」（追補1 §4・未測定）
- per-unit の可識別性・境界ごとの µ の引き直し・µ の用量非単調性の一般形・層2 の µ 再生成
