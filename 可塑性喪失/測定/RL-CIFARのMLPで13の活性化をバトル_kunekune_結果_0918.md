# RL-CIFAR の MLP で 13 の活性化をバトル（kunekune を含む）：結果 0918

親: [[適応αSwishをSnakeとバトルさせる_結果_0917]]（同じ MLP 箱で SNA が固定 Snake に勝った参照）/ [[現在地]]
事前登録: repo `specs/spec_rlcifar_mlp_battle_0918.md`（本文 `c7b3890`・追補 1 `36af4c6`・Issa の予測 `fd0c4d4`。Claude の 19 予測は 02:20 に実装前、Issa の 4 ラベルは 13:30 に本走中、**どちらも結果を読む前**）
run id: `rlcifar_mlp_battle_0918` / 状態: **実行済み（03:25 → 14:59）・結果 commit `3dc101e`（ブランチ `claude/rlcifar_mlp_battle_0918`、main 統合と片付けはこれから）。独立監査なし**
起案の出所: Issa「leaky（傾きも数点）、Smooth-Leaky（Lillo & Cheney）、ELU、Swish・GELU とバトルさせるか」＋ Issa が新活性化 **kunekune** を起案（Snake の 1 周期だけ残して外を直線に）。「当然適応 α が kunekune」「縦に 2/3 に潰した版も」。裾の傾き 1 の版は Claude が切り分け用に足した。

> **転記の格**: 数値は走自身の出力 `results/rlcifar_mlp_battle_0918/summary.md` と `verdict.json` から転記した。独立の再計算はしていない。

## 0. 何を測ったか

箱は `pmnist_rlcifar_0907` そのまま: 3072-100-100-10 の MLP、seed ごとに CIFAR-10 の 1200 枚を固定し、**タスクごとに一様乱数ラベルを引き直す**（教師網は無い。純粋な暗記）。1 タスク 400 epoch × 75 step（batch 16）= 30,000 step、50 タスク、Adam 1e−3（moment 持ち越し）、WD なし、正規化層なし、float32。
入力は `raw`（u8/255）と `std`（チャネルごとに L&C の定数で標準化）の 2 条件、seed 0–9。**13 腕 × 2 条件 × 10 seed = 260 走、発散 0**。

腕（全部既定値のまま、この実験のための調整なし）:

| 記号 | 活性化 |
|---|---|
| `SNA` | 適応 α Snake（c=0.6、β=0.01、α ∈ [0.005, 3]） |
| `KKA` | **kunekune**: θ=2αz が [−3π/2, π/2] では Snake、外は傾き 2 の直線（継ぎ目 C²）。α は SNA と同じ適応則 |
| `KKA23` | KKA の 2/3 倍 |
| `KKT1` | θ ∈ [−2π, π] で Snake、外は恒等（裾の傾き 1、継ぎ目 C¹） |
| `R` / `LK001` / `LR` / `LK03` | ReLU / leaky 0.01 / leaky 0.1 / leaky 0.3 |
| `SL` / `RSL` | Smooth-Leaky（α 0.1・p 3・c 5）/ Randomized Smooth-Leaky（学習時 r ∼ U(0.125, 0.333)） |
| `ELU` / `SILU` / `GELU` | ELU(1) / SiLU / GELU |

読み出しは登録どおり: 窓 = t31–50 の online 正解率の平均（更新前の argmax）、低下 = (t1–10) − (t41–50)、崩壊 = 窓 < 0.5、腕対腕は seed 対応の符号検定（勝ち = 相手が勝つ seed が 1 以下かつ p < 0.05）。

## 1. 窓（seed 中央値、* = 崩壊）

| 腕 | raw | std | std − raw | raw の memo 最小 | 崩壊 raw/std |
|---|---|---|---|---|---|
| `KKT1` | **0.9565** | 0.9871 | +0.031 | 0.954 | 0/0 |
| `SNA` | 0.9549 | 0.9870 | +0.032 | 0.919 | 0/0 |
| `KKA` | 0.9535 | 0.9871 | +0.034 | 0.858 | 0/0 |
| `KKA23` | 0.9484 | **0.9878** | +0.039 | 0.813 | 0/0 |
| `RSL` | 0.8653 | 0.9404 | +0.075 | 0.945 | 0/0 |
| `LK03` | 0.8447 | 0.9789 | +0.134 | 0.745 | 0/0 |
| `LR` | 0.7264 | 0.9655 | +0.239 | 0.767 | 0/0 |
| `SL` | 0.7229 | 0.9645 | +0.242 | 0.738 | 0/0 |
| `LK001` | 0.3881* | 0.8967 | +0.512 | 0.572 | 10/0 |
| `SILU` | 0.1233* | 0.1132* | −0.010 | 0.116 | 10/10 |
| `GELU` | 0.1160* | 0.1132* | −0.003 | 0.109 | 10/10 |
| `R` | 0.1132* | 0.1132* | 0 | 0.107 | 10/10 |
| `ELU` | 0.1011* | 0.1005* | 0 | 0.090 | 10/10 |

seed の範囲（raw）: SNA 0.9527–0.9570、KKT1 0.9530–0.9590、KKA 0.9477–0.9619、KKA23 0.9462–0.9531、RSL 0.8625–0.8697。std では snake 系 4 本が 0.9865–0.9884 に収まる。

## 2. 登録判定（`verdict.json` の `labels`）

| 判定 | raw | std |
|---|---|---|
| **A**（SNA は誰にも負けないか） | **`SNA_BEATEN`** — KKT1 に −0.0018（1/10 seed・p 0.022）。KKA は TIE（+0.0021・6/10）、KKA23 には勝ち（+0.0058・10/10） | **`SNA_BEATEN`** — KKA に −0.0002（0/10）、KKA23 に −0.0009（0/10）。KKT1 は TIE |
| **main** | `NOT_BOTH` | |
| **K1**（周期は要るか: KKA − SNA） | **`PERIOD_FREE`**（−0.0021・4/10・p 0.75） | **`PERIOD_HURTS`**（+0.0002・10/10・p 0.002） |
| **K2**（縦の潰し: KKA23 − KKA） | `SCALE_MATTERS_DOWN`（−0.0051・1/10） | `SCALE_MATTERS_UP`（+0.0007・10/10） |
| **K3**（裾の傾き: KKT1 − KKA） | `TAIL_FREE`（+0.0031・8/10・p 0.11） | `TAIL_FREE`（−0.0001・3/10） |

腕どうしの差の seed 範囲: raw の KKT1 − SNA は −0.0023〜+0.0049、std の KKA − SNA は +0.0000〜+0.0005。**snake 系 4 本の差はすべて 0.6 pt 以下**で、std では 0.1 pt 以下。

## 3. 読み

1. **kunekune は Snake と同等以上。Snake の周期は要らない。** KKA は raw で SNA と TIE、std では 10/10 seed で上（差 0.02 pt）。Issa の起案「1 周期でいい」はこの箱で成立した。位相を見ると、θ の座席は t50 で第 1 層 −1.1（raw）/ −0.4（std）、第 2 層 −1.3〜−1.4 / −1.0〜−1.1 で、kunekune の帯 [−3π/2, π/2] の外に出ている質量は第 1 層 3–7%・第 2 層 1.5% しかない。**周期の 2 つ目以降には最初から誰も居ない**ので、そこを直線にしても何も変わらないのは道理。
2. **裾の傾きは 2 でも 1 でも同じ**（K3 両条件 TAIL_FREE）。KKT1 は raw で 4 本中トップ（SNA に 9/10 seed で勝ち、p 0.022 でぎりぎり「勝ち」）。裾に質量が無いので裾の形は効かない。
3. **縦に潰すと raw では下がり std では上がる**（K2 が条件で符号反転）。Adam で活性化を k 倍するのは次層の init と lr を k 倍するのと厳密に同じなので、これは**形でなく歩幅の効果**。raw の memo 最小が 0.81 まで落ちるのも同じ理由（実効 lr が 2/3）。
4. **SNA の A は両条件 `SNA_BEATEN` だが、負けた相手は自分の派生 3 本だけ。** 5 位の RSL とは raw で 9 pt 差。「SNA は誰にも負けない」は破れたが「Snake 系が他を寄せつけない」は残る。
5. **ReLU・ELU・SiLU・GELU は raw/std どちらでも 10/10 seed で t4 までに床**（0.10–0.12）。leaky は傾き順に生き（0.01 は raw で崩壊）、Smooth-Leaky は leaky 0.1 と同じ水準（0.72 / 0.96）。RSL は raw では SL より 14 pt 上だが std では逆に 2.4 pt 下。
6. **std は崩壊しない全腕を押し上げる**（LK001 +51 pt、LR/SL +24 pt、snake 系 +3 pt）が、崩壊する 4 腕は std でも救えない。RL-CIFAR × MLP の順位を論じるなら入力条件を明記しないと比べられない。

## 4. 予測の採点

- **Issa（ラベルのみ）1/4**: K1「`PERIOD_FREE` か `PERIOD_HURTS`」的中。`SNA_TIED_TOP`（0.02–0.2 pt の一貫した微差で `SNA_BEATEN`）・`SCALE_FREE`・`TAIL2_BETTER` は外れ。
- **Claude 8/19（Brier 0.230）**: 崩壊の予測（R・ELU・SiLU・GELU・LK001）と「leaky は全部 STD_HELPS」「K3 は TAIL_FREE」が当たり。LR・LK03・SL・RSL を崩壊と踏んだ 4 つ、A 系 3 つ（P1・P11・P13）、K1・K2、「SNA は raw/std で TIE」が外れ。
- 二人とも外したのは「SNA が派生に負ける」こと。差が微小なので符号検定の 10/10 は「一貫した微差」の意味で読む。

## 5. 先行研究との位置（[[ELUとLoPの文献照合_rsl_rl_0914]] と 0917 の照合から）

- **RL-CIFAR × MLP で非 ReLU 活性化の数値表を出した論文は 0917 の照合では見つからなかった。** Rohani ら（CoLLAs 2025、2×100 MLP・1200 枚・100 タスク・SGD 1e−2）は tanh/GELU を図だけで「severe plasticity loss」、Dynamical Isometry（2026）は 4×512 MLP で ReLU と GroupSort を図だけ。
- 高い数値（Leaky 98.3・Smooth-Leaky 98.4・RReLU 98.0・Deep Fourier 96.2）は **Lillo & Cheney の Table 2 で、Kumar の CNN**。指標も全タスク平均の online で当方の窓 t31–50 と違う。当方の MLP の leaky 0.1 は raw 0.73・std 0.97 で、std ならほぼ彼らの CNN の水準に届く。
- Snake・kunekune をこの箱で回した例は無い。Lewandowski ら（Deep Fourier）は「sin 1 つでは足りない」と書いているが、MLP で乱数ラベルを回したのは MNIST 系だけ。
- したがって「素の MLP で RL-CIFAR を窓 0.95（raw）/ 0.99（std）」は**照合した範囲では数値としての前例が無い**。ただし SNA/kunekune の α は EMA 統計で動くので「素の活性化」ではなく「正規化層・WD・リセットなしの適応活性化」と言うべき。std 条件では leaky 0.3 も 0.979 に届くので、独自性は raw 条件の差（9 pt）と、周期が要らないという構造の知見にある。

## 6. 実装の反省（Issa「なんでこんなに手間取ったのか」への答え）

実作業 2 時間（spec 50 分・実装と検査 65 分）のあと事故で 4 時間失った。内訳は (a) 宿主を 20 走束ねる CUDA グラフのエンジンに書き換えたので「宿主と同じ計算」を示す検査 11 本がエンジンと同じ分量になった（作法の代価・1 h）、(b) 書き上げてから速度を測って 14.5 ms/step と分かり CUDA グラフに書き直した（**書く前に測れば不要だった**・50 分）、(c) S-eval の相対 1e−6 と S-reuse の較正標本の誤り（40 分・[[検査の閾値は算術から導く]] の 8・9）、(d) **前の実験の §4 退避が `data/` の symlink を辿って CIFAR 本体を移し、後発 7 腕が 4 時間死んだ**。教訓は memory `impl-cost-and-cleanup-lessons-0918`。

## 7. 限界と残り

- 1 箱・10 seed・c=0.6 固定。c や β のダイヤルは振っていない。leaky の傾きは 3 点、Smooth-Leaky は原典の既定値だけ。
- 50 タスクで snake 系はまだ上がり続けている（低下 −0.02）。もっと長い地平で差が開くかは未測定（ckpt から延長できる）。
- 独立監査なし。スナップショット（各タスク終わりの W・b・V と z1・z2、23 GB）は片付け時に `obsidian-research-data/rlcifar_mlp_battle_0918/` へ退避する。
