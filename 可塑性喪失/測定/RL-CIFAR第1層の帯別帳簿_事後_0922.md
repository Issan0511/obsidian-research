# RL-CIFAR 第1層の帯別帳簿 — √t の中身を「どこが消され、どこが残るか」で分ける（事後・走ゼロ・0922）

Claude, 2026-09-22。親: [[W増大メカニズム_0909]]・[[RL-CIFARのMLPで13の活性化をバトル_kunekune_結果_0918]] §8.3–8.4・[[第1層の成長の中身_事後_0910]]。データ: `rlcifar_mlp_battle_0918` の課題別スナップショット（W1・t00–t50・seed 0–9・raw/std）。**格: 事後（走ゼロの再解析）。予測 P1–P7 は計算前に固定**（`obsidian-research-data/rl_ledger_posthoc_0922/PREDICTIONS.md`）。数値は seed 0–9 の中央値、必要なところだけ [最小, 最大]。理論の検算は Codex（gpt-6-astra・xhigh）に独立に出した（同フォルダ `theory_check_codex.md`）。

## 0. 問い

Issa（0922）: V11 で RL を主にして「各課題の更新には、損失が二度と戻しに来ない成分が含まれる。WD が無ければその成分は消えず、Adam の正規化はその成分に関係の薄さに見合わない η の歩幅を与える。その成分の二乗が足し算になって √t が出る」と「RL（入力固定・ラベル乱数）では網は毎回考え直している（上位方向の前の解を打ち消してから書く）」を検証したい。

## 1. 道具

入力 1200 枚（cond ごとの前処理後）を中心化した共分散の固有ベクトル 1199 本＋平均画像の直交残り μ⊥（1 本）＋補空間 comp（3072 − 1200 = 1872 次元）。帯は §8.4 と同じ top = PC1–10 / mid1 = 11–100 / mid2 = 101–439 / low = 440–1199。課題ごとの増分 ΔW_t = W_t − W_{t−1} を帯に射影し、帯 b ごとに

- 堆積 d_b(t) = ‖P_b ΔW_t‖²、侵食 e_b(t) = 2⟨P_b W_{t−1}, P_b ΔW_t⟩、恒等式 N_b(t) − N_b(t−1) = d_b + e_b（残差 max 5.5e−11・相対 2e−15。N と d, e は別々の射影から出るので実の検査）
- 累積比 r_b = ‖Σ_{s=6}^{50} ΔW^b_s‖² / Σ_s ‖ΔW^b_s‖²（1 = 無相関のランダムウォーク、≪1 = 打ち消し、>1 = 整列）
- 侵食率 Σe_b / Σd_b（t6–50。0 = 積んだものが一切消されない、−1 = 全部消える）
- q_b(t) = ⟨W^b_50 − W^b_t, W^b_t⟩ / ‖W^b_t‖²（時刻 t までに書いた成分が以後どれだけ消されるか）

基底が走の入力空間であることは、保存された z1 と X·W1ᵀ + b1 の一致（float16 丸め内）で確認した。

## 2. 予測と判定

| 予測 | LR std（登録腕） | LR raw | SNA std | 判定 |
|---|---|---|---|---|
| P1 ‖W1‖² 対 t が線形（R² > 0.95） | 0.998 | 0.9995 | 0.990 | **成立**（指数 p は 0.496 / 0.423 / 0.355 で §8.3 を再現） |
| P1 堆積 d(41–50)/d(5–14) が 0.7 以上 | **1.56** | 1.12 | 1.03 | 成立。ただし予測の向きと逆で**堆積は増える** |
| P2 r_top < 0.3 | 0.105 | 0.089 | 0.054 | 成立 |
| P2 r_low ∈ [0.7, 1.3] | 0.812 | 0.677 | 1.097 | std 成立・raw は僅かに外 |
| P3 q_low(10/20/30) の絶対値 < 0.1 | −0.03 / −0.03 / −0.02 | −0.10 / −0.07 / −0.05 | +0.12 / +0.09 / +0.06 | LR std 成立・raw と SNA は部分 |
| P3 q_top < −0.5 | −0.73 / −0.77 / −0.69 | −0.80 | −0.92 / −0.90 / −0.89 | 成立 |
| P4 low 帯がノルムの過半 | 0.112 | 0.318 | 0.116 | **不成立**（過半は mid2。切り方の誤り。下記） |
| P4 low 帯の前活性分散寄与 < 10% | 0.0009 | 0.008 | 0.002 | 成立 |
| P5 cos(W_top,t−1, ΔW_top,t) < −0.5 | −0.26 | −0.34 | −0.43 | **不成立**（道具の誤り。積分量では成立、下記） |
| P6 補空間は一切動かない | N_comp 20 → 221 | → 343 | → 169 | **不成立**。Adam は勾配の張る空間の外へ出る（§4） |
| P7 SNA の低指数の理由 | — | — | 堆積 ×1.03・r_low 1.10 | (b) 堆積が増えない。低帯の侵食ではない（むしろ +0.14 で補強） |

SL std は LR std とほぼ同じ（p 0.502・r_low 0.80・q_low −0.03）。ELU std は t20 前後で凍る（後期堆積 ~1e−12・r_top 1.00・上位 10 方向にノルムの 41%・前活性分散の 97%）ので P1–P5 は記述だけ。

## 3. 帯別の帳簿（LR std・t6–50）

| 帯 | ノルム比 t50 | 前活性分散の比 | 侵食率 Σe/Σd | 累積比 r_b | q_b(20) | 指数 p_b |
|---|---|---|---|---|---|---|
| top 1–10 | 0.035 | 0.551 | **−0.925** | 0.105 | −0.77 | 0.32 |
| mid1 11–100 | 0.396 | 0.404 | −0.683 | 0.363 | −0.45 | 0.46 |
| mid2 101–439 | 0.455 | 0.044 | −0.403 | 0.623 | −0.17 | 0.55 |
| low 440–1199 | 0.112 | 0.0009 | **−0.194** | 0.812 | −0.03 | 0.56 |
| μ⊥ | 0.0002 | 0（構成上） | **+8.9** | 7.9 | **+1.29** | 0.84 |
| comp（画像の外） | 0.003 | 0（構成上） | **−0.053** | — | — | 0.39 |
| all | 1 | 1 | −0.607 | 0.428 | −0.32 | 0.50 |

LR raw は同じ順で top −0.991 / mid1 −0.895 / mid2 −0.729 / low −0.339 / comp −0.504。SNA std は top −0.989 / mid1 −0.773 / mid2 −0.108 / low **+0.136** / comp −0.008。

全帯の課題あたり収支（LR std、早期 t5–14 → 後期 t41–50）: 堆積 2912 → 4543、侵食 −1210 → −3160、**正味 1680 → 1400**。SNA std: 堆積 1190 → 1222、侵食 −508 → −847、正味 691 → 380。LR raw: 12860 → 14480、−8760 → −11000、正味 4100 → 3500。

## 4. 読み（二つの主張の判定）

1. **「毎回考え直している」は支持。** 上位 10 方向に積まれた堆積の 92〜99% が後の課題で消される（r_top 0.05〜0.11。時刻 t に書いた上位成分は t50 までに 7〜9 割消える）。登録した P5 の課題ごとの cos が −0.26〜−0.43 で閾値に届かないのは、増分に「前の解の除去」と「新しい解」が同居し、cos が薄まるからで、積分量（侵食率・累積比・q）が正しい道具だった。Codex の一次の定理（線形化 f = Jθ・独立ラベルで E⟨θ, δθ⟩ = −η‖Jθ‖² ≤ 0）は課題開始時の unlearning を与え、「毎課題が同じ解写像で置き換わる」極限では連続増分の相関 −1/2・累積比 1/T。実測は相関 −0.16〜−0.26・r_top 0.05〜0.11（1/T = 0.022）で、**部分的な置き換え**。
2. **「損失が二度と戻しに来ない成分」は支持。ただし 0/1 ではなく連続。** 侵食率は top −0.93 → mid1 −0.68 → mid2 −0.40 → low −0.19 → comp −0.05 と、その方向が出力に効く度合いの順に並ぶ。LR std の low は q −0.03 で「以後ほぼ触られない」。raw の low は −0.34 で触られ、SNA の low は逆に +0.14 で補強される。
3. **「Adam の正規化が、関係の薄さに見合わない η の歩幅を与える」は本走では識別できない。** Codex の線形代用モデル: 1 歩の SGD は帯の係数変化が ∝ λ_j、課題を完全に当てはめる増分は ∝ 1/λ_j（低分散方向を通して出力を動かすには大きな係数が要る）。つまり低帯にノルムが溜まるのは「当てはめ」自体からも出るので、スナップショットだけでは Adam の再重み付けと区別できない（画素座標の Adam は PCA 座標の Adam ではない）。同日の [[第1層の増幅器はAdamか_ε介入_0922]] も LE の伸びで「Adam は速さを決め、存在は決めない」。**Adam 固有と言えるのは補空間への堆積だけ**（§5）。
4. **「二乗が足し算になって √t」は帳簿としては成立、機構としての「定数堆積 × 直交」は不成立。** 堆積は t5–14 → t41–50 で ×1.56（‖W‖ ≈ 3 倍に対し D² ∝ N^0.4、PM の D ∝ N^0.2 と同じ）、侵食も同じ速さで増え、**差の正味がほぼ一定**（−17%）なので ‖W‖² が t に線形に見える。0910 の PM（「堆積も侵食も t100 で大きくなるが差は変わらない」）と同じ形。Codex の検算では、定数 D・c の現象論（dN²/dt = D² − 2cDN、局所指数 1/2 − x/6、x = N/N*）に PM の c = 0.05・N(120)/N* = 0.67・指数 0.46–0.48 を同時に入れると矛盾する（x = 0.67 なら局所指数 0.32）。**[[W増大メカニズム_0909]] §C の固定点 N* = D/2c は D 一定を仮定しており、書き直しが要る。**
5. **Snake の指数 0.35 は、堆積が増えないのに侵食が増えて正味が落ちる（691 → 380）から。** leaky が 0.5 を保つのは堆積が ×1.56 に増えて侵食の増加を埋めるから。「Snake は堆積が少ない」（PM の SNA 0.465 対 LR 1.005）と同じ向き。なぜ leaky の堆積が N とともに増えるかは未解明（候補: 課題を学ぶのに要る更新数が伸びる＝LoP の帰還。Codex も「学習曲線の長さ・活性座標数・時間的整合の増加」を挙げる）。

## 5. 予測を外した 2 点は機構の情報

- **P6: Adam は勾配の張る空間の外へ重みを出す。** 勾配 Σ_k e_k φ′ x_k は必ず画像の張る空間にあるが、Adam の m̂/(√v̂ + ε) は座標ごとの非線形なので更新はその外へ出る（Codex の反例: 張る空間 span(1,2)・勾配 (1,2) → Adam の 1 歩 ∝ (1,1)）。実測: comp のノルム 20 → 221（LR std）・343（raw）・769（ELU std）、堆積の 1e−3（等方の 250〜800 分の 1）だが float 丸め（1e−14）の 11 桁上。**comp は 1200 枚すべてで前活性に厳密に 0 しか足さない＝損失が厳密に見えない方向**で、侵食率 −0.05 の純粋なランダムウォーク（p 0.39）。主張の「戻しに来ない成分」の最も純粋な例で、かつ Adam 固有（SGD・momentum は張る空間を保つ）。ノルムの 0.3% なので成長則の担い手ではない。凍った ELU では後期堆積の 44% が comp（勾配が微小になると画素ごとの正規化の不均一が向きを支配する）。
- **μ⊥ 方向は弾道的。** μ⊥ は中心化データに直交するので w·μ⊥ は 1200 枚すべてに同じ定数を足す（bias と同じ働き）。この方向は r 7.9（std）〜18.6（SNA std）、指数 0.84〜0.94、後の課題が積み増す（q +1.3）。勾配のこの成分は bias 勾配 × ‖μ⊥‖² に等しいので、bias の沈みと同じ符号で毎課題積む。ノルム比は 2e−4 で成長則には効かないが、**輸送（Δz̄ = ΔW·μ）の第 1 層側の担い手の一部**として記録する。

**0922 夕 追記:** 補空間の指数 0.39 の初期値オフセットの説明と帯別の堆積の伸び（top 1.82 → 補空間 0.95）、および「損失が見ない方向への漏れが主因か」の検査（漏れは定常には無く、Adam は当てはめの太り・ε 域の drift・古い v̂ の一撃の三つの歩幅で 2/3 を担う。補空間の堆積が毎課題一定なのは一撃が毎課題 1 回起きるから）は [[RL-CIFAR第1層の漏れ検査_Adamの三つの歩幅_事後_0922]] §8・§5。

## 6. 引用の訂正（0922 昼のチャットの発言を直す）

- 「WD なし Adam の √t は Kosson 2024 §5.1 にある」は誤り。Kosson et al. 2024（arXiv:2305.17212）§5.1・Fig. 14 は WD を切るとノルムが増える**観測**と「WD を切るのは η_r に指数減衰を掛けるのと同じ」の一文だけ。同論文の Eq. 15（Q_{t+1} = (1 − ηλ)² Q_t + η² C）は λ = 0 で線形成長を含意するが本文では述べない。
- **明記した先行は Kosson, Welborn, Liu, Jaggi & Chen 2025, "Weight Decay may matter more than µP for LR Transfer in Practice"（OPT 2025 workshop）付録 F.2 式 (37): ρ_t² = ρ_0² + t·η²**（定数 η・λ = 0・「simplified model」＝毎歩の更新ノルム bη で重みに直交、の仮定）。相対更新が t^−0.5 で減る、と書いている。原文 PDF で確認済み（`opt-ml.org/papers/2025/paper41.pdf` p.28）。
- Merrill et al. 2021 の √t（付録 E.2）は ‖δ_t‖ ∝ η_t‖θ_t‖ と T5 の 1/√t スケジュールから出るもので、定数 η では指数成長を与える別の機構。
- したがって V11 が「定数堆積 × 直交 → √t」を自分の導出として書く余地は無い。書けるのは、帯別の帳簿（どこが消され、どこが残るか）と、定数堆積の簡約モデルが実測（堆積が増え、侵食も増える）と合わないこと。

## 7. 未検査

- 堆積 D² ∝ N^0.4 の出どころ（課題内の更新数・整合の時間変化。課題内スナップショットが要る）。
- Adam の帯再重み付けと当てはめ（∝ 1/λ_j）の切り分け（RL-CIFAR に SGD 腕が無い。ε 掃引か SGD 腕）。
- WD 腕（本走に無い。「WD が無ければ消えず」は PM の L2 結果に依存）。
- 5+1（実ラベル）の同じ帳簿。転用できる知識があるなら上位帯の累積比が 1 を超えるはず（未測）。

## 8. 所在

`~/Projects/obsidian-research-data/rl_ledger_posthoc_0922/`: `PREDICTIONS.md`（計算前の予測）・`band_ledger.py`（解析・62 秒・CPU）・`per_task_bands.csv`（21,420 行）・`summary.csv`・`summary.md`（全表）・`basis_diag.json`・`theory_check_codex.md`・`theory_check_prompt.txt`・`SHA256SUMS`。スナップショット本体は `obsidian-research-data/rlcifar_mlp_battle_0918/`。repo は触っていない。

**0922 夜 追記:** 帯別の侵食率を PC ごとに分けると入力分散に対して 5 桁で滑らかに減る（PC 1 −0.96 → PC 1001–1199 −0.09、境目なし）。λ 重みの帳簿では幅（Var z1）の侵食率は −0.90 で、堆積 ∝ V^0.44・侵食 ∝ V^0.61 から σ* ≈ 92 の定常状態が見える → [[RL-CIFAR第1層の幅を広げる者_消し残りと収束後の増幅_事後_0922]] §3・§4。
