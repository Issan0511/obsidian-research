# Random Label CIFAR（CNN）結果 — 中間（seed 6/10）

spec: [[RandomLabelCIFAR_CNN_spec_0908]]（予測 §7・**走らせる前に記入**） / 状態: **中間・6 seed・走行中（完走は 09/09 06:00 頃）** / 作成 2026-09-08 18:26 / Claude
関連: [[RandomLabelMNIST結果_0906]]（比較基準 +0.0233）／[[RandomLabelCIFAR_spec_0907]] §8（MLP 版の中止）／[[引用禁止]]

> **run: `rlcifar_cnn_0908`**・λ=1e−4（段 0 が選択・§3.5）・Adam lr 0.001・50 タスク・400 epoch・1200 枚・Kumar A.1.3 の CNN。発散 0。
> **n=6 では符号検定の最小 p = 2/64 = 0.031。** 登録の「反対 1 本以下かつ p<0.05」は 6/6 でのみ満たせる。

## 1. 登録判定（中間）

| 判定 | ラベル | 根拠 |
|---|---|---|
| **Q1** | **`GAP_WIDENS`** | `SNA+l2` − `R+l2` = **+0.3231 ± 0.1176**・6/6・p=0.031（基準 +0.0233） |
| **Q2** | **`C_PARTIAL`** | 帯 [0.50,0.85] に入るのは `SNA` が c2/f1/f2、`SNA+l2` は c2 のみ。clip 85.0% |
| **Q3** | **`SNAKE_SPECIFIC`** | `SNA+l2` − `LR+l2` = **+0.0094 ± 0.0007**・6/6 |
| vs l2init | `B_ABOVE_C` | `SNA+l2` − `R+l2init` = +0.0292 ± 0.0047・6/6 |

## 2. 腕（窓 31–50・6 seed 中央値）

| arm | online | memo | 崩落 | mob c1 | c2 | f1 | f2 | clip% | ‖w‖比 c1 |
|---|---|---|---|---|---|---|---|---|---|
| `R` | 0.1129 | 0.114 | **task 4** | 0.005 | 0.000 | 0.000 | 0.000 | | 3.90 |
| `LR` | 0.8275 | 0.998 | — | 0.102 | 0.105 | 0.113 | 0.210 | | 3.36 |
| `SNA` | 0.9463 | **1.0000** | — | 0.471 | 0.512 | 0.670 | 0.589 | 50.6 | 1.57 |
| `R+l2` | **0.7209** | 0.962 | — | 0.215 | 0.003 | 0.304 | 0.000 | | 2.04 |
| `LR+l2` | 0.9717 | 1.000 | — | 0.100 | 0.229 | 0.340 | 0.683 | | 0.14 |
| **`SNA+l2`** | **0.9813** | 0.999 | — | 0.281 | 0.605 | 0.902 | **1.233** | **85.0** | 0.12 |
| `R+l2init` | 0.9498 | 0.998 | — | 0.185 | 0.038 | 0.245 | 0.591 | | 0.70 |

## 3. Q1 の +0.3231 を額面で読まない（重要）

**分母が暴れている。** `R+l2` は 0.7209 で **SE 0.1176**、seed 間で崩れる/崩れないが割れる（seed 2 本時点では 0.4056 だった）。**差の SE は差の 36%。** 一方 `SNA+l2` − `LR+l2` = +0.0094 は **SE が差の 7%** で桁違いに安定している。

**したがって「CIFAR で差が 14 倍に開いた」とは書かない。** 正しくは「**`R+l2` が seed によって崩落し、その不安定さが差の大半を作っている**。Snake 固有の優位は `LR+l2` 比の +0.0094（MNIST の +0.0149 より**小さい**）で測るべき」。

## 4. 予測の採点（[[RandomLabelCIFAR_CNN_spec_0908]] §7・中間）

| # | 予測 | 結果 |
|---|---|---|
| 1 | 段 0 で λ が見つかる | **当たり**（1e−4 で online 0.8765） |
| 2 | Q1 = `GAP_WIDENS`（≥ +0.03） | **当たり**（ただし §3 の限定つき） |
| 3 | Q2 = `C_FC_ONLY`（conv が外れ fc は帯内） | **外れ**。実際は逆向きで、`SNA+l2` は **f2 が 1.233 で上に外れ**、c2 だけが帯内。`SNA` は c1 のみ外れ |
| 4 | `mob` > `mob_pool`（ReLU で差最大） | **外れ**（probe 段階で `mob_pool` 1.05 > `mob` 0.82・確定待ち） |
| 5 | 素の `R` は崩落 | **当たり**（task 4） |
| 6 | 層間膨張は conv で累積し conv→fc で切れる | 未集計 |
| 7 | 素の `SNA` が正則化腕より上 | **外れ**。`SNA+l2` − `SNA` = **+0.0343・6/6**（WD が助ける） |

## 5. WD の符号は 3 箱で ＋ − ＋（REPORT）

`SNA+l2` − `SNA`:

| 箱 | 差 | 向き |
|---|---|---|
| Permuted MNIST | **+0.0185**（10/10） | WD が助ける |
| Random Label MNIST | **−0.0217**（10/10） | WD が害する |
| **Random Label CIFAR CNN** | **+0.0343**（6/6・中間） | **WD が助ける** |

**[[理論が説明すべき事実_0906]] 項目 13 の「記憶課題では WD が容量税」という読みは CIFAR では成り立たない。** 同じ乱数ラベル記憶なのに符号が違う。残る差は入力次元（784 対 3072）・重み共有・λ（1e−3 対 1e−4）。**理論への問い T10 を書き換える必要がある。**

## 6. 未確定・注意

- **固定 α 腕（`SN3`・`SN06`）は seed 1 本**で `SNA` を上回る（0.9636・0.9597 対 0.9463）。`SN3` は深いサイトの mobility が 1.02/0.99/0.97 と**ゲートが消えている**のに勝っており、[[RandomLabelCIFAR_CNN_spec_0908]] とは別に書いた予測（`SNA` > `SN3`・9/10）に反する。**seed 1 本なので何も決まらないが、当たれば「効くゲートは conv1 だけ」になり適応 α の主張は CNN で弱くなる**
- 完走は 09/09 06:00 頃（10 seed）。**n=6 の判定は n=10 で覆りうる**
- Kumar Fig.2 の該当値（Baseline 0.26・L2 0.75・L2 Init 0.96）と当方の数値を**直接比べない**（padding/pool の構成・online の定義・λ 格子が一致している保証がない。spec §7-8）

## 引用制限

- **中間（n=6）。** 登録の格は完走まで暫定
- **Q1 の +0.3231 を単独で引かない**（§3）。Snake 固有の量は `LR+l2` 比の +0.0094
- 「CIFAR で検証した」と書かない — 乱数ラベルの記憶であり視覚構造を使わない
- Q2 が `C_PARTIAL` なので「c=0.6 は箱に依らない」は**この箱で否定された**
