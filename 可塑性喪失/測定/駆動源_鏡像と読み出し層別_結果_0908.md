# 駆動源の鏡像と読み出し層別 — 結果（drive_mirror_0908）

spec: [[駆動源_鏡像と読み出し層別_spec_0908]] / 親: [[殻極と角検出項_検証計画_0908]] / 状態: **完了（事後解析）** / 2026-09-08 / 実行: Issan/Claude
格: **既存データの事後解析**。走は無い。source は `act_offset_0906` / `LRoff0_1216` / 10 seed / T=10,000 / 窓 t=200–499。

## 0. 判定

| endpoint | 値 | ラベル |
| --- | --- | --- |
| **主 D31**（K31 群の seed 等重み平均 N） | — | **`NOT_DETERMINED_OCCUPANCY`** |

**K31 の個体が事実上いない。** 窓の条件付け時刻 (t−2)T において、10 seed 合計で K31 のイベントは **5 件**（seed 2 が 2 件、seed 4/6/9 が各 1 件、他 6 seed は 0 件）。適格 seed（ユニーク 5 units・10 tasks・50 events 以上）は **0**、要求は 8。K32 も 1 件、K30 も 8 件しかない。この母集団では鏡像側の主判定は作れない。

在庫確認は通っている（`availability.json`）: 必須列は 10 seed すべてに揃い、`zmin` もある、`p_hat`×32 は整数と厳密一致（誤差 0）、`zmin ≤ zbar ≤ zmax`、タスク終端行 501 本が連続。checkpoint を float64 で再構成した `zbar` は log の float32 列と最大 2.4e-7 で一致した（支持・`running_mean` の復元検査）。

## 1. 副（REPORT_ONLY・δ_A=1e-4/task・95% CI・seed bootstrap 10,000）

| 群 | v 層 | events | 平均 N | 95% CI | ラベル |
| --- | --- | --- | --- | --- | --- |
| K1 | 全体 | 49,517 | −0.0441 | [−0.0478, −0.0402] | K1_DOWN |
| K1 | large_v | 47,344 | −0.0481 | [−0.0519, −0.0438] | K1_DOWN |
| K1 | **small_v (|v|≤0.05)** | 2,173 | **+0.0423** | [+0.0234, +0.0605] | **K1_UP** |
| K2 | 全体 | 16,376 | −0.0966 | [−0.1078, −0.0848] | K2_DOWN |
| K2 | small_v | 489 | −0.0127 | [−0.0440, +0.0232] | NOT_DETERMINED |

読める範囲: **上端に張り付いた群（K1）の平均変位は、読み出しが小さいユニットだけ符号が逆になる。** 問い合わせ §1 の「小さい v で下降」という候補予測とは逆向き。ただしこれは主判定ではなく、(t−2)T で条件付けた群の平均運動であって瞬時の力ではない。K2 の方が K1 より大きく沈むことも同じ格で読む。

## 2. checkpoint の残差成分（§7・学習なし）

1M の 32 点で δ=f−y を分解（`mode_energy.csv`）。恒等式 mean δ² = E0+EP+ER は 10 seed とも成立（残差 ≤1.4e-17）。

- 平均成分 E0 と parity 成分 EP の比は seed で 4 桁動く（seed 0: E0=2.2e-7・EP=1.5e-7、seed 5: E0=1.0e-4・EP=1.1e-2、seed 9: E0=2.0e-2・EP=6.1e-3）。**「教師の parity エネルギーが平均成分と同じ」は前提にできない。**
- 残り成分 ER が 10 seed すべてで最大（全体の 55–99%）。
- 初期の mean(φ q) は 1e-5–4e-3 の桁で、ユニットは parity をほとんど表現していない。
- 教師の flip 応答（15 bit を 1 個ずつ仮想反転・`teacher_flip.csv`）も保存した。

## 3. 引用上の注意

1. 主判定は `NOT_DETERMINED_OCCUPANCY`。**「K31 で上がらなかった」ではなく「K31 がいない」。**
2. §1 は全て REPORT_ONLY。CI は seed 単位 percentile bootstrap で、event 数の多さは精度を上げない。
3. §5-3 の対応付け（`matching.csv`）は K31 が 5 件しかないため 5 組しか作れず、実質 `NO_COMMON_SUPPORT`。
4. 回帰（`regression.csv`）は K1 群のみ成立、K31 は標本不足。係数の符号を平均運動の符号と混同しない。

## 4. 予測の採点

- Kubo（候補予測・確率は転記しない）: K31 で上昇 → **測れず**。小さい v で下降 → **符号は逆**（K1_UP・REPORT_ONLY）。
- Codex: 「K31 の個体不足で判定不能になりうる」→ **的中**。

## 5. 次

- 鏡像側は自然軌道に居ないので、B/C の**作った** K31 でしか調べられない。これは spec 通りの設計（親 §4-1）。
- 自然な K1 の small_v / large_v の符号反転は、B の v 層別（§6-6）と突き合わせる価値がある。

## Log

- 2026-09-08: 実装 `ef4d900`（検査 18 本 PASS）→ 実行。出力 `results/drive_mirror_0908/`。
