# ELU の幅成長と沈下 結果 0909

状態: 検証中 / 更新: 2026-09-09
親: [[沈下の機構_事後解析_0909]] / spec: [[ELUの幅成長と沈下_spec_0909]]
run: `elu_growth_0909`（repo `lop_analysis` / branch `collective_kick_0908` / 結果 commit `07998e4`）
出所: `results/elu_growth_0909/summary.md`・`verdict.csv`・`seed_verdict.csv`・`validation.csv`

> 0909 夜に同定した「**尺度方向が損失の平坦方向であること**が leaky の √t を作る」という読みの検定。leaky は正斉次で h(z)=zφ′−φ≡0、ELU は h=α[e^z(z−1)+1]≥0。**h が活性化のすべてを決める**なら、h の大きい活性化ほど指数が小さくなるはず。

scope: 120 タスクを新規学習（CPU）。同じ seed の全腕が同一の初期値・置換列・層化抽出・バッチ順を共有（S-init）。LR/SNA は既存 committed checkpoint の再現であって独立な再現ではない。

## 0. 進捗ゲート G1: PASS
LR・SNA の全 seed が既存 checkpoint **42 点**（task1・21–40 終端・task100・101–120 終端）を **params maxabs 0.0** で再現。変異対照（初期値に +1e−3）は 1.47–1.85。

## 1. 中心的な結果: 斉次性の欠損が大きいほど指数が小さい

`p_late`（`ln‖W̃‖` を `ln t` に回帰、t60–120、3 seed）:

| 腕 | h(z) の大きさ | p_late | p_early (t5–30) |
|---|---|---|---|
| **LR** | **h ≡ 0**（厳密） | **0.465 / 0.471 / 0.476** | 0.407 / 0.404 / 0.404 |
| **ELU03** | 0 ≤ h ≤ 0.3 | **0.443 / 0.436 / 0.450** | 0.403 / 0.405 / 0.406 |
| **ELU1** | 0 ≤ h ≤ 1.0 | **0.437 / 0.430 / 0.419** | 0.430 / 0.433 / 0.426 |
| **SNA** | 振動、深部で −7 まで | **0.372 / 0.379 / 0.386** | 0.374 / 0.366 / 0.372 |
| ELU1+l2 | — | −0.005 / −0.001 / 0.007 | — |

**4 腕の順序 LR > ELU03 > ELU1 > SNA が 3 seed すべてで重なりなく成立し、h の大きさの順序と一致する。** `E = Σ(∂L/∂a1)h(z)` という一般形（本走で相対 1e−9、変異対照 3e−3〜4e−2）が指数を決めるという読みを支持する。ただし個々のラベルは §2 のとおり出ていない。

## 2. 事前登録の判定（そのまま記録）

| 判定 | LR | SNA | ELU1 | ELU03 | ELU1+l2 |
|---|---|---|---|---|---|
| V1 指数 | `EXPONENT_HALF` | `EXPONENT_SUB` | **`EXPONENT_MIXED`** | **`EXPONENT_MIXED`** | `EXPONENT_SUB` |
| V2 ユニット | `UNIT_UNIMODAL` | `UNIT_UNIMODAL` | `UNIT_UNIMODAL` | `UNIT_UNIMODAL` | `UNIT_PARTIAL`※ |
| V3 飽和 | `SATURATION_FLAT`※ | `SATURATION_FLAT` | `SATURATION_PARTIAL` | `SATURATION_PARTIAL` | `SATURATION_FLAT` |
| V4 沈下 | `SINK_SQRT_LIKE` | `SINK_PARTIAL` | `SINK_PARTIAL` | `SINK_PARTIAL` | `SINK_PARTIAL` |
| V5 欠損項 | `DEFECT_MIXED`(13/18) | `DEFECT_MIXED`(5/18) | **`DEFECT_MIXED`(11/18)** | **`DEFECT_MIXED`(13/18)** | `DEFECT_MIXED`(8/18) |
| V6 WD | — | — | — | — | **`WD_PINS_ELU`** |

※ LR の `sat` は φ′<0.05 の割合だが leaky の負側ゲートは 0.1 で常に閾値より上なので**構成上ゼロ**。LR の V3 は情報を持たない。
※ ELU1+l2 の「凍結 90–92 ユニット」は WD がノルムを固定して全ユニットの指数が ≈0 になったことの定義上の帰結で、飽和による凍結ではない。

**ELU の V1 が `MIXED` なのは、値 0.419–0.450 が `HALF` の帯 [0.44, 0.52] の**すぐ下**に落ちたため**（6 seed 中 4 つが帯の下側）。`SUPER`（>0.56）からは遠い。

### 予測の採点

| | Claude (§4) | Issa (追補1・2) | 結果 |
|---|---|---|---|
| V1 | `HALF` **または** `SUPER`（選言） | **`HALF`**（`SUPER` でない） | `MIXED`（0.419–0.450）|
| V5 | `DEFECT_OUTWARD` | `DEFECT_OUTWARD`（弱い・**復元なし**） | `DEFECT_MIXED` |
| V4 | `SINK_SQRT_LIKE` | 同じ | `SINK_PARTIAL` |
| V2 | `UNIT_UNIMODAL` | — | **`UNIT_UNIMODAL` ✓** |
| V6 | `WD_PINS_ELU` | — | **`WD_PINS_ELU` ✓** |

- **分岐点（`SUPER` か否か）は Issa の勝ち。** Claude の「やや外向きに強い」＝超拡散は明確に否定された（指数はむしろ 1/2 の下）。
- **Issa の「復元力がない」も支持される。** どの腕も `DEFECT_RESTORING` に落ちず、ELU の E の中央値は 6 seed 中 4 つが負。**復元力として働く systematic な内向きの力は無い。**
- ただし **Issa の `HALF` も厳密には出ていない**（帯のすぐ下）。両者ともラベルとしては外れ、方向は Issa が正しい。
- **欠損項は無視できる大きさではない**: |E|/|斉次項| の中央値は ELU1 で 0.43–0.94、ELU03 で 0.50–0.98。**大きいが符号が安定しない**（11/18・13/18）。これが「大きな欠損があるのに復元力にならない」理由。

## 3. 実質的な発見: ELU は leaky と同じ線上を、より遠くまで行く

| t120 | ‖W̃‖ | σ_inv | z̄_inv | z̄/σ | 飽和 | 精度低下 (t2–6→t116–120) |
|---|---:|---:|---:|---:|---:|---:|
| LR | **8.40** | 2.71 | **−4.26** | −1.57 | 0.00 | −2.05/−1.67/−2.57 pt |
| ELU1 | **8.27** | 4.48 | **−6.92** | −1.54 | 0.59 | −2.09/−1.33/−2.09 pt |
| ELU03 | 8.11 | 3.40 | −4.86 | −1.43 | 0.66 | −2.85/−1.79/−2.10 pt |
| SNA | 6.17 | 1.86 | −1.42 | −0.76 | 0.15 | −0.74/−0.84/−1.28 pt |
| ELU1+l2 | 1.02 | 0.31 | +0.14 | +0.45 | 0.00 | **+1.91/+2.34/+1.69 pt** |

- **ELU と leaky は幅がほぼ同一（8.27 対 8.40）なのに、ELU は 62% 深く沈む**（−6.92 対 −4.26）。
- **z̄ 対 σ は leaky・ELU1・ELU03 が同一直線に乗る**（k = −1.43〜−1.64、R² 0.97–1.00）。SNA だけ別の浅い直線（k −0.35〜−0.69）。**ELU は leaky と同じ線をより遠くまで進んでいる。**
- **ELU は LoP を防がない。** 精度低下は leaky と同等（−1.8 対 −2.1 pt 中央値）。ELU03 はやや悪い。防ぐのは **l2 だけ**（+1.9 pt 改善）。
- 飽和は ELU1 で 0.31→0.59、ELU03 で 0.51→0.66 と増えるが、`sat120 > 2·sat20` の基準は満たさない（t20 で既に高い）。**予測した順序 ELU03 > ELU1 は成立**（閾値 z<ln(0.05/α) が −1.79 対 −3.0 と浅いため）。
- **ユニットの凍結は起きない**（指数 IQR 0.04–0.05、指数<0.10 のユニットは 0–1）。撤回した初稿の予測の根拠が実測でも否定された。

## 4. σ が幅で決まらない（未解明）
ランダムな参照置換の下では σ²_i = m_i²·(1ᵀΣ1≈1310) + c‖W̃_i‖² + 交差項 で、c は入力データだけで決まる定数のはず。ところが「残り/E‖W̃‖²」は **LR 0.075 / ELU03 0.133 / ELU1 0.206** と 2.7 倍違う。ノルムの Jensen ギャップは無い（E‖W̃‖²/(E‖W̃‖)² = 1.00–1.04）。
最有力の説明は **行平均 m_i の分散**（本走では m_i の per-unit 値を保存していない）。z̄_i ≈ 102.5·m_i + b_i は確認済み（LR: 102.5×(−0.0393)−0.192 = −4.22 対 実測 −4.26）。**m_i を per-unit で保存すれば決着する。**

**同日の事後解析「z̄ = −1.7σ」への訂正**: 行平均 m は z̄ に線形（×102.5）、σ² に二乗（×1310）で入るので、**z̄–σ の相関の一部は機械的**である。オフセット項は LR・ELU1 とも σ² の約 28% を占める。オフセットを除いた幅だけで見ると LR の比は −1.85 になり関係自体は残るが、**「幅が深さを決める」を σ の全体で語らないこと。**

## 5. 検算（15 腕seed）
| 検査 | 実測 | 変異対照 |
|---|---:|---:|
| G1: LR/SNA が既存 42 checkpoint を再現 | **0.0**（厳密） | ≥1.47（初期値 +1e−3） |
| 恒等式 E = Σ(∂L/∂a1)h(z)（分母は内積の自然スケール） | ≤3.3e−9 | ≥3.3e−3（別の活性化の h） |
| leaky で E ≡ 0 | ≤1.2e−9 | 同上 |
| ELU の φ′ = autograd | ≤1.1e−16 | ≥2.8e−3（α を 1.01 倍） |
| 測定が学習を変えない | **0.0**（厳密） | ≥0.18 |

## 6. 限界
- 120 タスクは leaky の推定天井（N*≈12.5・task ~290）より短い。ELU・leaky とも天井の有無はこの走では決まらない。
- V1 の帯 [0.44, 0.52] は leaky の実測 0.458–0.461 を中心に事前に置いたもので、ELU の 0.419–0.450 はその外だが `SUB`（<0.40）でもない。**帯の設計が結果の解像度を決めている**ことを、指数の順序（§1）と分けて読むこと。
- 精度低下と幅・沈下の因果は検定していない（対応の記述のみ）。
- 元 GPU 軌道ではなく CPU 学習。
